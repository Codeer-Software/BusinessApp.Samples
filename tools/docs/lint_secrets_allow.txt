# lint_secrets.py の誤検知抑制リスト
#
# 書式: <ルールID> <パスの glob>       （ルールID が * なら全ルール）
# ルールIDの一覧: python tools/docs/lint_secrets.py --list-rules
#
# ここに登録するのは「検出されたが公開して問題ないと確認したもの」だけ。
# 本物の秘密情報・ローカルパスを黙らせるために使わない。

# リンタ自身のパターン定義（キーの接頭辞などが自己マッチするため）
*	tools/docs/lint_secrets.py

# 雛形ファイル。プレースホルダのみで実値は含まない（実体は Git 追跡外）
SEC-001	.claude/settings.local.json.sample
SEC-001	Designer/.claude/settings.local.json.sample

# ベンダー同梱の静的アセット（Bootstrap / open-iconic 等）。自作コードではない
*	BusinessApp/BusinessApp.Client/wwwroot/css/**
*	BusinessApp/BusinessApp.Client/wwwroot/lib/**

# インメモリ SQLite の接続文字列。秘密情報を含まず、外部にも繋がらない
# （スキーマテストが使い捨ての DB を作るために必要なリテラル）
SEC-011	BusinessApp/BusinessApp.TestSupport/TestDatabase.cs
SEC-011	BusinessApp/BusinessApp.Schema.Tests/SchemaSnapshotTests.cs
