---
title: 会計コア構築ミッション（CLAUDE.md）
status: current
scope: 全体
audience: [開発]
updated: 2026-09-06
supersedes: []
related: [docs/README.md, Designer/CLAUDE.md, docs/16_作業のルール.md, docs/17_検証のルール.md, docs/15_実装の原則.md, docs/10_調査の原則.md]
---
# 会計コア構築ミッション（CLAUDE.md）

> Claude Code が**財務会計の「会計コア」アプリ**を構築するための最上位の指示書。
> **書くのは「何を置いても絶対に知っておいてほしい・守ってほしい」ことだけ。** それ以外は §0 の文書が持つ。
> **150 行以内**（長いと誰も通読しない）。**全文が開発者の指示**なので帰属と日付は書かない。
> **Claude が変えてよいのはリンクと文書名の修正だけ**——用語・誤字を含め、それ以外は開発者の指示があったときのみ。
> **本書に限り、消した節に `削除` を残さず番号を詰める**（[00 §4-12](docs/00_ドキュメント規約/本文.md) の例外）。**旧番号は別の節に着地するので機械では鳴らない**——番号を動かしたら [00 §4-12-1](docs/00_ドキュメント規約/本文.md) の手順で手で洗う。

## 0. 着手前に必ず読むファイル

**工程ごとの必読。** タスクごとの引きは [docs/README.md](docs/README.md) が持つ。

### 0-1. あらゆるタスクの前に、常に

1. **本ファイル全体**
2. **[`docs/README.md`](docs/README.md)** — 計画・仕様ドキュメントの**唯一の入口**
3. **`Designer/CLAUDE.md`** — CLB デザインワークスペースの運用ルール
4. **[`docs/16_作業のルール.md`](docs/16_作業のルール.md)** — 判断・Git・サブエージェント・自己レビュー

### 0-2. 要件定義・設計の前に

- [02_ペルソナ](docs/02_ペルソナ.md) → [03_機能スコープ](docs/03_機能スコープ.md)（「この機能は入れるのか」の判断）
- [04_会計ドメイン設計](docs/04_会計ドメイン設計.md)（§1 の不変条件）
- [decisions/README](docs/decisions/README.md)（該当する ADR。仕様書は What、経緯はこちら）
- [10_調査の原則](docs/10_調査の原則.md)・[13_参照法令一覧](docs/13_参照法令一覧.md)（制度・税制・電子帳簿保存法に触れるなら）
- [00_ドキュメント規約](docs/00_ドキュメント規約/README.md)（ADR・設計文書を書くなら）

### 0-3. 詳細設計・実装の前に

- [15_実装の原則](docs/15_実装の原則.md)
- [09_画面の原則](docs/09_画面の原則.md)（画面・利用者に見せる文言に触れるなら）
- **CLB のデザイン（モジュール・フィールド・レイアウト・`*.mod.cs`）に触れるなら**:
  `Designer/Project.md`（このプロジェクトでの書き方）→ `Designer/ClaudeCodeForDesigner/CLAUDE.md`
  （CLB 仕様リファレンス。デザイナが再生成する生成物——Git 追跡外・手で編集しない）→ [qa/01_CLB静かな失敗](docs/qa/01_CLB静かな失敗.md)

### 0-4. 検証・テストの前に

- [17_検証のルール](docs/17_検証のルール.md)（検証ループ・実機確認・開発用アカウント）
- `self-review` スキル（`.claude/skills/self-review/SKILL.md`）
- [qa/03_テストで漏らした実例](docs/qa/03_テストで漏らした実例.md)（漏れの型）・[qa/04_実機操作テスト](docs/qa/04_実機操作テスト.md)（台本）
- [qa/01_CLB静かな失敗](docs/qa/01_CLB静かな失敗.md) の G 節（ブラウザ自動操作）・H 節（DB・スキーマ）

### 0-5. 自律作業の前に

- [16 §1 の表と §3](docs/16_作業のルール.md)（聞かずに進むかの判断・3 回で引く）を読み直す
- [05_実装計画と現在地](docs/05_実装計画と現在地.md) の「次にやること」

## 1. ミッション（芯・変更不可）

**ASP.NET Core ＋ Codeer.LowCode.Blazor（CLB）で、財務会計の「会計コア」を作る。**

- 目的は 3 つ: **①CLB の技術デモ ②対象ペルソナが他の会計ソフトの代替として実際に使える
  ③他の CLB アプリ（部品）と組み合わせて製品化できる形にする**
  （CLB で作成したウェブアプリは「部品」として組み合わせてさらなる応用アプリを作成できる。
  本プロジェクトの会計コアもそのアプリ（部品）となる。）
- **会計コアだけを作る。しかし会計コアだけは完璧に作る。**
  周辺業務（経費精算・販売・購買・証憑・承認フロー）は別部品であり、本プロジェクトでは作らない
  （取引先等、会計コアに不可欠なものはその必要な限度で作成する。）
- **会計ロジックの不具合の絶無を期す。** ロジックに不具合があれば誰も使わない
- 対象ペルソナは [`docs/02_ペルソナ.md`](docs/02_ペルソナ.md)。
  **「このペルソナが使うか」がスコープ判断の唯一の基準**である

## 2. 会計ロジックの不具合の絶無

- **計上済みの仕訳は変更・削除しない**（優良な電子帳簿の要件）。訂正・取消は反対仕訳で行い、**この規則を迂回する経路を作らない**（[ADR-0004](docs/decisions/0004-優良な電子帳簿への準拠と仕訳の不変性.md)）
- **使用中のマスタは意味を変えられない。** 過去の帳簿の数字が遡って変わるから（[ADR-0038](docs/decisions/0038-使用中のマスタは意味を変えられない.md)）
- **不変条件は [04 §1](docs/04_会計ドメイン設計.md) が持つ。** 不変条件ごとに**それを破れる経路を数える**——保存時の検査だけでは守れない
- **会計の計算・検証・集計は C# に置き、テストで守る。** カバレッジ 100% とミューテーションテストがゲート（[15 §1](docs/15_実装の原則.md)・[ADR-0012](docs/decisions/0012-テスト方針とカバレッジのゲート.md)）
- **制度値はハードコードしない。** 有効期間つきのルールデータで持ち、仕訳には適用した版を固定する（[15 §2](docs/15_実装の原則.md)）
- **制度は記憶で決めない。** まず `docs/research/` を引き、無ければ調べて記録してから実装する（[10](docs/10_調査の原則.md)）。
  **それでも迷ったら推測せず開発者に聞く——開発者が法令・税制の最も確かな確認先である**

## 3. 呼称の約束（会話・文書とも厳守）

**「対話・ドキュメント・コメントその他のあらゆる状況で認識の齟齬を防ぐため、呼び名を厳密に・重複なく定義した。」**（開発者の逐語）

| 語 | 指すもの |
|---|---|
| **利用者** | **このアプリの使用者**（経理担当・経理責任者・経営者・税理士）。**正式呼称** |
| **開発者** | **この会計コアを作る人間**。本リポジトリの持ち主ただ 1 人を指す。**Claude を含めない** |
| **Claude** | **Claude Code 自身**。役割名を作らず**固有名詞で書く** |
| **CLB 開発元** | **Codeer**。CLB そのものを作っている会社。[11_CLB改善提案](docs/11_CLB改善提案/README.md) の宛先 |

- 開発者を「ユーザー」と呼ばない。**開発者が会話で「ユーザー」と言ったときは、利用者と読み替える**
- **CLB でアプリを作る人たち**には語を決めていない（出てきたときに決めればよい）。要るときはそのまま「CLB で開発する人たち」と書く

## 4. 必ず守る 4 つ

- **開発者が意見を問いかけてきたとき（「どう思う？」等）は、回答のみ行い実装を始めない。**
  問いは判断を求めているのであって作業を求めていない。着手は明示的な GO の後。指示と問いかけが混在するなら、指示分は実装し、問いかけ分は回答で止める
- **まとまった作業の区切りごとに自己レビューを行う**（`self-review` スキル）。**文書だけの変更でも省かない**——lint は形しか見ない
- **最小修正ではなく、最善の修正。** 既存のものを正として最小の差分を探す癖を疑い、ゼロベースで考える（[16 §4](docs/16_作業のルール.md)）
- **実機で操作していない機能は、できていないものとして扱う。** `designcheck` と `dotnet test` の緑は「読み込める」「C# が正しい」までしか言っていない（[17 §2](docs/17_検証のルール.md)）

## 5. 情報セキュリティ

このリポジトリは **GitHub のパブリックリポジトリへ公開する**。

- **追跡ファイルに絶対パス・ユーザー名・ホスト名を書かない。** 文書は `<REPO_ROOT>` 表記、スクリプトは `$PSScriptRoot` 相対
- **秘密（接続文字列・API キー・パスワード）を書かない。例外は無い。** 秘密は .NET User Secrets。
  開発用アカウントの資格情報も Git 追跡外に置く（[ADR-0039](docs/decisions/0039-開発用アカウントの資格情報はGit追跡外に置く.md)）
- **環境固有設定は `*.sample`（プレースホルダのみ）だけ追跡**し、実体は `.gitignore` に入れる——実体を追跡すると環境ごとの値が衝突し、秘密が紛れ込む
- **ライセンス未確認の他社資産をリポジトリ内に置かない**（参照はリポジトリ外に clone する）——公開した瞬間に権利を侵す
- 検査は `python tools/docs/lint_secrets.py`。公開前には `/security-review` も回す

## 6. 技術スタック

- **.NET 8** / ASP.NET Core ＋ **Blazor WebAssembly（hosted）**——Client は WASM、Server が API と静的配信
- **Codeer.LowCode.Blazor（CLB）** ランタイム・デザイナ **1.3.20** / Extras 0.5.0
- **SQLite**（データソース名 `BusinessAppSQLite`）
- **Cookie 認証**（`AppUser` モジュール）

接続先・追跡外ファイルの持ち物など環境の事実は `Designer/Project.md`、開発用アカウントは [17 §3](docs/17_検証のルール.md)。
