---
title: 会計コア構築ミッション（CLAUDE.md）
status: current
scope: 全体
audience: [開発]
updated: 2026-08-30
supersedes: []
related: [docs/README.md, docs/00_ドキュメント規約/README.md, docs/09_画面の原則.md, docs/10_調査の原則.md, Designer/CLAUDE.md, Designer/Project.md]
---
# 会計コア構築ミッション（CLAUDE.md）

> 本リポジトリで Claude Code が自律的に**財務会計の「会計コア」アプリ**を構築するための最上位の指示書。
> CLB の具体的な作り方（JSON / フィールド / レイアウト / 検証 CLI）は**ここには書かない**。
> それらは `Designer/` 配下に完備されており、本書はそこへ誘導する。
> **方針が固まったら Claude 自身が本ファイルを更新してよい**（ただしミッションの芯＝§1 は変えない）。

## 0. 着手前に必ず読む（順番厳守）

1. **本ファイル全体**
2. **[`docs/README.md`](docs/README.md)** — 計画・仕様ドキュメントの**唯一の入口**。
   タスク起点の索引。個別の文書名を本書に列挙しない（腐るため）
3. **`Designer/CLAUDE.md`** — CLB デザインワークスペースの運用ルール
4. **`Designer/ClaudeCodeForDesigner/CLAUDE.md`** — **CLB 仕様リファレンス（通読）**。
   このフォルダ全体はデザイナが `ai-refresh` で再生成する生成物（Git 追跡外・手で編集しない）
5. **[`docs/qa/01_CLB静かな失敗.md`](docs/qa/01_CLB静かな失敗.md)** — CLB で踏むと静かに壊れる挙動の一覧

## 0-1. 呼称の約束（会話・文書とも厳守）

| 語               | 指すもの                                                       |
| ---------------- | -------------------------------------------------------------- |
| **利用者**       | **このアプリの使用者**（経理担当・経理責任者・経営者・税理士）。**正式呼称**（2026-08-25 開発者決定） |
| **開発者**       | **この会計コアを作る人間**。本リポジトリの持ち主ただ 1 人を指す。**Claude を含めない**（2026-08-28 開発者決定） |
| **Claude**       | **Claude Code 自身**。役割名を作らず**固有名詞で書く**         |
| **CLB 開発元**   | **Codeer**。CLB そのものを作っている会社。[`docs/11_CLB改善提案/`](docs/11_CLB改善提案/README.md) の宛先 |

> **「対話・ドキュメント・コメントその他のあらゆる状況で認識の齟齬を防ぐため、
> 呼び名を厳密に・重複なく定義した。」**——**開発者の逐語**（2026-08-29。上の 2 つの決定に、
> あとから共通の理由として与えられたもの）。

- 開発者を「ユーザー」と呼ばない。開発者が会話で「ユーザー」と言ったときは、**利用者**と読み替える
- **CLB でアプリを作る人たち**には語を決めていない。言及が要るときは
  そのまま「CLB で開発する人たち」と書く（開発者の判断。2026-08-28。出てきたときに決めればよいから）
- **「開発者の指示」「開発者の決定」と書くときは、その理由も一緒に残す**（開発者の指示。2026-08-28。
  **理由のない指示・決定はしない主義であり、後から最も知りたいのは理由だから**）。既存分は遡らなくてよい

## 1. ミッション（芯・変更不可）

**ASP.NET Core ＋ Codeer.LowCode.Blazor（CLB）で、財務会計の「会計コア」を作る。**

- 目的は 3 つ: **①CLB の技術デモ ②対象ペルソナが他の会計ソフトの代替として実際に使える
  ③他の CLB アプリ（部品）と組み合わせて製品化できる形にする**
  （CLBで作成したウェブアプリは「部品」として組み合わせてさらなる応用アプリを作成できる。
  本プロジェクトの会計コアもそのアプリ（部品）となる。）
- **会計コアだけを作る。しかし会計コアだけは完璧に作る。**
  周辺業務（経費精算・販売・購買・証憑・承認フロー）は別部品であり、本プロジェクトでは作らない
  （取引先等、会計コアに不可欠なものはその必要な限度で作成する。）
- **会計ロジックの不具合の絶無を期す。** ロジックに不具合があれば誰も使わない
- 対象ペルソナは [`docs/02_ペルソナ.md`](docs/02_ペルソナ.md)。
  **「このペルソナが使うか」がスコープ判断の唯一の基準**である

## 2. 実装方針の原則

**アプリは 2026-08-23 時点で施行済みの制度で動作し、将来の変更は成立済みで未施行のものまで考慮する**
（開発者の決定。2026-08-30。**後の改正は先の改正の上に乗るので、先を外すと積み上がらないから**）——
**令和 7 年度改正**（本体は**デジタルシームレス保存の新設**。2027-01-01 施行）と、
**令和 8 年法律第 12 号**（令和 8 年度税制改正。2026-03-31 成立・公布。**「大綱」ではなく成立済みの法律**）。
**この 2 つは独立していない**（後者の青色申告特別控除 75 万円が前者を要件の選択肢に含む）。
**これで全部ではない。** 一覧・条番号の扱い・基準日を動かすときの洗い直しは
[電帳法リサーチ](docs/research/2026-08-23_電子帳簿保存法.md)・[消費税リサーチ](docs/research/2026-08-23_消費税インボイス制度.md)。

**原則の本体は別の文書が持つ。ここには芯だけを置く。**

| 何をするとき | 読むもの |
|---|---|
| **画面を作る・利用者に見せる文言を書く** | [09_画面の原則](docs/09_画面の原則.md) |
| **制度・税制・電子帳簿保存法に触れる実装をする** | [10_調査の原則](docs/10_調査の原則.md)。**記憶で決めない。調べて `docs/research/` に記録してから実装する** |
| **どこに置くか・どこで実行するかを決める** | **5 本の ADR に散っている**（[0008](docs/decisions/0008-CLBとCSharpライブラリの責務分担.md)・[0013](docs/decisions/0013-機能単位のモジュール分割と依存方向.md)・[0016](docs/decisions/0016-画面から呼ぶサーバ処理はWebAPIに置く.md)・[0022](docs/decisions/0022-明細を並べる帳簿はクエリモジュールで作る.md)・[0027](docs/decisions/0027-入力の一覧はラップモジュールとクエリモジュールで作る.md)）。**3 冊目の原則文書に集約する**——[05 §1](docs/05_実装計画と現在地.md) |
| **文書を書く** | [00_ドキュメント規約](docs/00_ドキュメント規約/README.md) |

### 2-1. ロジックは C#、CLB は薄いプレゼンテーション層

CLB スクリプト（`*.mod.cs`）はツリーウォーク型インタプリタで、静的検査がほぼ効かない。
**会計の計算・検証・集計は、外部依存ゼロの C# ライブラリ（`AccountingCore`）に置き、
ユニットテストで守る。** スクリプトに書いてよいのは、画面上の値の出し入れと C# サービスの呼び出しだけ。

構成と実行場所の決め方は [ADR-0008](docs/decisions/0008-CLBとCSharpライブラリの責務分担.md)。

### 2-2. 計上済みの仕訳は不変

優良な電子帳簿の要件を満たすため、**計上済み仕訳は変更・削除しない**。
訂正・取消は反対仕訳で行う。**この規則を迂回する経路を作らない**（[ADR-0004](docs/decisions/0004-優良な電子帳簿への準拠と仕訳の不変性.md)）。

### 2-3. 制度値をハードコードしない

税率・控除割合・閾値・耐用年数は、**有効期間つきのルールデータ**として持つ
（各マスタへの当てはめは [08_マスタ台帳](docs/08_マスタ台帳.md)）。仕訳には適用したルールの版を固定して保存し、
後日マスタを更新しても過去を再計算しない。

### 2-4. 削除（→ [10_調査の原則](docs/10_調査の原則.md)）

### 2-5. スコープ拡大への既定の答えは「別部品」

[ADR-0029](docs/decisions/0029-所有は相手抜きで存在するかで導き粒度は決断で決める.md) の基準に当てて判断する。
例外を認めるときは ADR を起こす。

### 2-6. 削除（→ [10_調査の原則](docs/10_調査の原則.md)）

### 2-7. 削除（→ [09_画面の原則](docs/09_画面の原則.md)）

## 3. 自律運用ルール

### 3-1. 自律と判断

- **原則として自律的に動き、仕様の意思決定まで自分で行う。** 判断に迷ったら、止めるより**根拠を ADR に残して前進する**
- ただし**後戻りコストの大きい根本設計・不可逆な操作・要件の重大な曖昧さ**は遠慮なく質問する
- **開発者が意見を問いかけてきたとき（「どう思う？」等）は、回答のみ行い実装を始めない。**
  着手するのは明示的に GO が出てから。同じ依頼に指示と問いかけが混在するときは、
  指示分は実装し、問いかけ分は回答で止める

**選択肢が複数あるときの決め方**（実装・テスト・レビューのいずれでも同じ）

| 状況 | どうするか |
|---|---|
| 明らかにベストプラクティスが 1 つに定まる | **判断理由を残して選び、進む。** 聞かない |
| 正解が 1 つではない／開発者の意見を聞いたほうがよい | 聞く。ただし**聞く時期を選ぶ**（下 2 行） |
| ↳ 後から聞いても支障がない | **文書に保留として残し、まとめて聞く**（[00 §4-4](docs/00_ドキュメント規約/本文.md) の保留リスト） |
| ↳ 後からだと手戻りが大きい／すぐ聞きたい | **手を止めてすぐ聞く**

### 3-2. サイクル

1. **状態を読む** — [`docs/05_実装計画と現在地.md`](docs/05_実装計画と現在地.md) の「次にやること」
2. **設計する** — 該当する ADR とリサーチを確認する。CLB は `Docs/AppPatterns/` と `_samples/` を正典に複製する
3. **作る**
4. **検証する** — §4
5. **自己レビューする** — §3-2-1。**まとまった実装の区切りごとに必ず入れる**
6. **記録する** — 現在地・ADR・仕様書を更新する。CLB の改善に気づいたら
   [`docs/11_CLB改善提案/`](docs/11_CLB改善提案/README.md) にその場で追記する（発見日と CLB バージョン必須）
7. **コミットする** — §3-4

### 3-2-0. 3 回で引く

**同じ問題の修正に 3 回以上失敗し、かつ新しい手がかりが何も得られていないなら、そこで追うのをやめる。**
分かっていることを文書に残し、残りのタスクへ移り、後で開発者に報告する（開発者の指示。2026-08-24）。

- 判断の基準は回数ではなく**進展の有無**である。試すたびに新しい情報が出ているなら続けてよい。
  「同じところで同じように失敗している」なら止める
- **とくに CLB とブラウザ操作は、開発者が自分で触ると解決することがかなりある**
  （前回プロジェクトでも、ブラウザの拡張機能では動かないが手で操作すると動いた事例があった）。
  環境側の問題を延々と追わない
- 残すのは「何をしようとして、何が起きて、何を試したか」。次に誰が見ても同じ場所から始められる形にする

### 3-2-1. 自己レビュー（工程として省かない）

**手順は [`self-review` スキル](.claude/skills/self-review/SKILL.md) が持つ**（依頼文の雛形つき）。
まとまった実装の区切りで、そのスキルを起動する。

**「別の目で見る」ことには予想以上の価値がある**（開発者の指示。2026-08-24）。
自分が書いた直後のコードは、書いた本人には正しく見える。

- **観点を明示して、別々のサブエージェントに読ませる。** 1 人に「全部見て」と頼まない。
  観点の一覧と依頼文はスキルが持つ
- **文書だけの変更もレビューを通す。** ADR・設計文書・リサーチの追加と変更は、コードと同じく
  **別の目を通してからマージする**。`designcheck` も `lint_docs.py` も
  **書いてある内容の矛盾は見ない**（見るのはフロントマター・リンク・日付という「形」だけ）。
  文書の 5 観点はスキルの §5
- **レビュー役は書き込めないエージェントを使う**（読むだけ。修正は自分で行う）
- 指摘は**自分で取捨選択する**。指摘をそのまま全部反映しない（レビューは意見であって仕様ではない）
- 記録に残すのは「**後から理由を聞かれるもの**」だけ。**直さないと決めたもの**と**後回しにしたもの**を
  [`docs/qa/02_自己レビュー記録`](docs/qa/02_自己レビュー記録.md) に書く。
  直した軽微な指摘はコミットが記録しているので書かない
- **作り込んだ不具合と、それをテストが見逃した理由**は
  [`docs/qa/03_テストで漏らした実例`](docs/qa/03_テストで漏らした実例.md) に書く。
  **1 件増えたら、機械で守れないかを毎回考える**（守れたらカタログから消す）
- 直したら §4 の検証をやり直す

### 3-2-2. 一時ファイル・一時フォルダを作らない

**作ると必ず消すことになり、削除のたびに開発者の確認で作業が止まる。** 作らない方法を先に探す。
- **DB の検査・投入**: `sql` CLI に `--query` を渡し、`--out` を省いて標準出力で受ける
  （PowerShell からは `ProcessStartInfo.ArgumentList`。`Start-Process -ArgumentList` は引用符が壊れる）
- **スキーマのテスト**: インメモリ SQLite（`:memory:`）。接続を閉じれば消える
- **DDL の適用**: `Designer/ddl/` の追跡ファイルをそのまま `--file` に渡す

やむを得ず作るならスクラッチパッドの**同じパスを上書きし続ける**（増やさない・消さない）。**削除処理をスクリプトに紛れ込ませて確認を迂回しない。**

### 3-3. サブエージェント

- **同時 2 人まで**（トークン上限のため。開発者の許可があるときだけ増やす）。
  3 人以上に見せたいときは**波に分けて 2 人ずつ**流す
- ブラウザ操作・大量ファイルの精読・SQL 結果の大量閲覧は積極的に委譲する
- 各サブエージェントには「自分が作ったタブ以外を触るな」を明記する
- ブラウザを 2 並列にするときは**同じ利用者アカウントで動かす台本を組み合わせる**（クッキー壺が 1 つのため）

### 3-4. Git

- **ブランチを切って作業する。** `main` に直接コミットしない
- ブランチ名は一般的な慣行に従う（`feat/...` `fix/...` `docs/...` `chore/...` `refactor/...`）
- コミットメッセージは **Conventional Commits**。意味のある単位で
- マージは **`--no-ff`**（Fast-Forward しない）
- **`git push` は行わない**（開発者が行う。`.claude/settings.json` の deny で機械的にも禁止している）

## 4. 検証ループ

| 対象                  | 手段                                                                     |
| --------------------- | ------------------------------------------------------------------------ |
| デザイン読込妥当性    | `designcheck` CLI。`findingCount` が 0 になるまで直す                    |
| C# ロジック           | `dotnet test`。テスト・カバレッジ 100%・配置規約を一度に検査（ADR-0012） |
| DB（DDL・投入・確認） | `sql` CLI（自前で DB 接続しない）                                        |
| 稼働アプリへの反映    | `pwsh -NoProfile -File tools/clb/deploy.ps1` → FileWatcher が hot-reload |
| 画面・挙動            | サーバ起動（`http://localhost:5085`）→ ブラウザで操作・スクショ          |
| ドキュメント規約      | `python tools/docs/lint_docs.py`                                         |
| テストの穴            | `dotnet stryker`（コミット前フックが毎回実行。ADR-0012 §8）            |
| **公開前チェック**    | `python tools/docs/lint_secrets.py`（**コミット前に必ず流す**）          |
| 上記の自動実行        | `git config core.hooksPath tools/git-hooks`（clone 後に 1 回。ADR-0012） |

`designcheck` の緑は「読み込める」までの保証でしかない。計算・状態遷移・見た目は実機で確認する。
**`*.mod.cs` の変更と DB スキーマの変更は deploy だけでは反映されない。サーバ再起動が必要。**

### 4-1. 最終確認はブラウザで行う（開発者に確認を取らない）

**CLB 実装 → deploy → サーバ再起動 → ブラウザで操作、までを自律的に回す。** 開発者の許可を都度求めない。

前回プロジェクトの実測で、**コード上は正しく見えてもブラウザで操作すると動かない**ことが繰り返し起きた
（[qa/01_CLB静かな失敗](docs/qa/01_CLB静かな失敗.md)）。`designcheck` と `dotnet test` が緑でも、
それは「読み込める」「C# のロジックが正しい」までしか言っていない。**実機で操作していない機能は、
できていないものとして扱う。**

### 4-2. 検証そのものを育てる（結果を鵜呑みにしない）

**検証を流したら、結果を使うだけで終わらせない。毎回、検証自体を振り返る**（開発者の指示。2026-08-28。
**検証も成長させるものであり、盲目的に従うと関門が形骸化するから**）。**①今回の結果は有用で適切だったか**
（本当の問題を指したか、意図どおりの記述を毎回叩いているだけか）**②かかった時間に見合っているか**
**③見合わないならどう良くできるか**（対象を絞る・警告に落とす・置き換える・やめる）。
判断が要ったものは [`docs/qa/02_自己レビュー記録`](docs/qa/02_自己レビュー記録.md) に残す。

**関門は足したときが完成ではない。** 実例: superseded リンクの検査は鳴った 6 件すべてが
「経緯として正しい」もので、しかも**既定の直し方が `decisions/` では誤り**だった（2026-08-28。ラウンド 19）。

## 5. 公開リポジトリの鉄則

このリポジトリは **GitHub のパブリックリポジトリへ公開する**。

- **追跡ファイルに絶対パス・ユーザー名・ホスト名を書かない。** 文書では `<REPO_ROOT>` 表記、
  スクリプトでは `$PSScriptRoot` からの相対で解決する
- **秘密（接続文字列・API キー・パスワード）を書かない。** 秘密は .NET User Secrets。
  **例外は [ADR-0031](docs/decisions/0031-デモ用の利用者アカウントは開発用のseedに置く.md) だけ**（広げるなら新しい ADR）
- **環境固有設定は `*.sample`（プレースホルダのみ）だけ追跡**し、実体は `.gitignore` に入れる
- **ライセンス未確認の他社資産をリポジトリ内に置かない**（参照はリポジトリ外に clone する）
- 検査は `python tools/docs/lint_secrets.py`。公開前には `/security-review` も回す

## 6. この環境の既知事実

- **ランタイム**: .NET 8。Server は作業フォルダを直読みせず、`LocalData/designs/App.zip` からデザインを読む
- **DB**: SQLite（`LocalData/db/`）。データソース名 `BusinessAppSQLite`。
  `AllowCliSqlAccess: true` のデータソースだけが `sql` CLI の対象（安全境界）
- **認証**: Cookie 認証（`AppUser` モジュール・初期アカウント admin/admin。**CLB がユーザーテーブルの空のときに自動生成するもので、§5 の対象外**）
- **CLB バージョン**: ランタイム・デザイナ **1.3.20** / Extras 0.5.0
- **`Designer/LocalEnvironment.md`（Git 追跡外）が持つもの**: デザイナ exe のパスと、**参照用リポジトリ**
  （リポジトリ外）——Codeer.LowCode.Blazor.Extras（拡張の実装見本）・BusinessApp_old（前回プロジェクト）
