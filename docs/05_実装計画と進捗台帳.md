# 実装計画と進捗台帳

> **毎回の実行はこのファイルを起点に再開する。**「次にやること」を最新に保つこと。
> 更新ルール: タスク完了ごとにチェック＋日付。フェーズ完了時は「検証済みの根拠」（designcheck 0件・実機確認・検算クエリ）を一言添える。
> セッションの記録は末尾の**「セッションログ」節に日付昇順で追記**する（本欄にログを積まない。2026-07-19 整理）。

## 次にやること（ポインタ）

- **現在地（2026-08-09）**: 2026-08-06 レビュー第9弾（ADR-0046: 通知一本化・申請UX・予実既定検索・年度ラベル・
  案件の部長開放）まで完了。2026-08-09 は**全ドキュメントの現行化**（docs 01〜09/README/Project.md の改訂・
  11_E2E 本文への読み替え織り込み＝8/5 からの持ち越し解消）と**体験シナリオ・ライブラリ新設
  （docs/tests/20_体験シナリオ/。1ファイル=実務の一本道×ライブラリ全体で分岐網羅）**を実施中。
  並行してユーザーが各画面のレイアウト調整（デザインのみ・ロジック無変更）を実施中
- **現在地（2026-08-12 夜）**: 改善候補 A-1〜A-4（ADR-0049/0050/0051）と **B-5・B-6（ADR-0052:
  税区分未設定の廃止・消費税集計表の作り直し・期間指定・科目別税区分表の新設）**まで完了。
  実機検証済み・デモ DB はクリーン（仕訳 201 行・貸借とも 27,302,555）
- **現在地（2026-08-13）**: 改善候補 **A-1〜A-4・B-1〜B-7 まで完了**
  （ADR-0049〜0053）。実機検証済み・デモ DB はクリーン
- **現在地（2026-08-14）**: 改善候補 **C-2 完了**（ADR-0054: 定型仕訳の「有効」→「一覧への表示」フラグ）。
  実機検証済み・デモ DB はクリーン
- **現在地（2026-08-14 深夜）**: **ADR-0055 / ADR-0056 を実装・実機検証まで完了**。
  入出金起票は「下書き＋一括起票」方式になり、`journal_lines.department_id` は **NOT NULL**（部門なし 0 行）。
  【仕訳】**部門・案件の修正**を新設（確定済み伝票でも部門・案件だけは直せる）。
  デモ DB はクリーン（仕訳 202 行・76 伝票・下書き 0 件）
- **現在地（2026-08-16）**: ユーザー指摘 12 件を一括対応（**ADR-0059〜0062**）。実バグ 5・UX 改善 5・
  設計判断 3。パスワードポリシーのマスタ化と自己パスワード変更（サーバ拡張）、ポータルの未生成件数の是正、
  SES 請求のガード（完了案件・工数未入力）、請求書一覧の税込列、不可逆操作の確認ダイアログ規約。
  designcheck 0 件・build 0 エラー・実機検証済み。**検証で触ったデータは全て復旧済み**
- **次**: ①**自己パスワード変更の成功パスをユーザーが実機確認**（本人のパスワードが要るため未検証）
  ②docs/tests/20 の体験シナリオを使ったユーザーの実機 UX レビュー → 出てきた改善点の実装。
  未着手の改善候補は **C-3〜C-7・D 群**（`docs/issues/2026-08-09_*.md`。C-1/C-8/C-9 は 8/14 に完了）
- **別セッションに依頼するタスク**: `docs/tasks/01_市販ソフト比較レビュー.md`・
  `docs/tasks/02_ドキュメントルール整備.md`（作業指示書として独立。ユーザーが別セッションで提示する）
- **完了（2026-08-16）**: 固定資産の「資産計上科目」に現金・売掛金まで出ていた件は
  **ADR-0063 で解決**（`accounts.is_fixed_asset_account`。候補 18→4 件、償却仕訳生成に関門つき）。
  **マスタを触るときの注意**: この科目を使っている資産があるのに「固定資産科目」を外すと、
  その資産の償却仕訳生成がエラーで止まる（静かに壊さないための意図的な設計）
- **導線の変更（2026-08-16）**: サイドバーの「パスワードの変更」は **`Settings`（設定）フレーム配下**へ移動。
  トップ階層に生で置かない（ユーザー指摘）。今後の利用者ごとの設定はこのフレームに足す
- **残バックログ**: 予算明細 Excel 一括アップロードの実測（ユーザー手動確認推奨）／FK制約エラーの親切表示（生 SQLite メッセージのまま）／有効フラグの無いマスタ（科目表示区分・承認テンプレ・会計年度等）の削除ガード／新規未保存時の LinkField 生ID表示（低）／一覧 Excel ダウンロードの数値列が文字列セルになる件（FB-032・CLB 側制約）／ポータル「未読の通知」0件時の空テーブル表示の見栄え改善

## フェーズ 0 — 立ち上げ（2026-07-05）

- [x] 税制・会計制度リサーチ（→ `research/2026-07_税制・会計制度リサーチ.md`）2026-07-05
- [x] 企画概要・ペルソナ・機能スコープ（→ `01`〜`03`）2026-07-05
- [x] 会計ドメイン設計（→ `04`）＋ ADR 0001〜0005 2026-07-05
- [x] 実装計画・進捗台帳（本書）・`Designer/Project.md` 整備 2026-07-05
- [x] 会計用 DB／データソース新設（AccountingSQLite、sql CLI 疎通・designcheck 0件確認）2026-07-05
- [x] zip デプロイ packing の実測確立（tools/deploy.ps1。起動時読込＋稼働中 hot-reload をブラウザ実機確認。手順は Project.md）2026-07-05
- [x] Phase 0 一式をコミット（3e4261b / 9786a74 / 36effd1 / c41d5aa）2026-07-05

## フェーズ A — 会計コア

- [x] **A-0 認証移設**: ddl/000_auth.sql 適用、AppUser を AccountingSQLite へ切替、admin 自動作成→ログイン・一覧表示を実機確認、SampleSQLite/PatternsSQLite をワークスペース＋サーバ設定から撤去 2026-07-05
- [x] **A-1 マスタ基盤**: DDL+プリセット71科目 seed（ddl/020）、Account/SubAccount/AccountCategory 画面。実機検証済み 2026-07-05
- [x] **A-2 税マスタ**: DDL+seed（ddl/010、税区分11・経過措置4段階・閾値5）、TaxRate/TaxCategory/InvoiceTransitionRate/SystemThreshold の全画面完了。実機検証済み 2026-07-05
- 磨きバックログ: 一覧の Boolean 列が True/False 表示（TrueText/FalseText で「○/—」等にする）／科目一覧の既定 LimitCount 50 で71科目が2ページに割れる（科目一覧だけ LimitCount 増を検討）／Account 一覧の税区分列追加検討
- [x] **A-3 会計期間**: ddl/030（DATE列・17期/18期seed・18期は4-5月締め済み）、FiscalYear/FiscalPeriod 画面、12ヶ月自動生成スクリプト（初の .mod.cs。うるう年・期末日自動補完も検証）、DeleteTogether カスケード削除検証。**重要な学び: SQLite 日付列は DATE 宣言必須（TEXT だと MM/dd/yyyy 文字列化で比較破綻）— 全 DDL 是正済み** 2026-07-05
- [x] **A-4 仕訳入力（コア）**: ddl/040、JournalEntry/JournalLine、振替伝票画面。実機検証済み: 税行自動生成（内税11,000→本体10,000+仮払消費税1,000、input_amount保持、parent_line_no紐付け）・確定採番（年度内連番）・年度自動解決・締め済み期間ガード・科目→既定税区分/内税の自動セット・貸借合計リアルタイム・確定後ViewOnly・creator/created_at自動。磨き残しは「次にやること」参照 2026-07-05
- [x] **A-5 帳簿**: 仕訳帳・総勘定元帳（window関数残高・諸口判定）完成・実機確認済み。補助元帳は元帳の補助科目版としてバックログ 2026-07-05
- [x] **A-6 試算表**: 貸借検算行付きで完成・実機確認済み（11,000=11,000）2026-07-05
- [x] **A-7 BS/PL**: 区分集計＋当期純利益差込＋検算行／段階利益5種。SQL事前検証＋実機確認済み 2026-07-05
- [x] **A-9 消費税集計表**: 税区分別 本体/税額集計。完成・実機確認済み 2026-07-05
- [x] **B-1 マスタ**: 取引先/部門/プロジェクト画面（fork生成→親が統合検証、取引先C001登録実測）2026-07-05
- [x] **A-8 株主資本等変動計算書**（簡易）: 74f2e82・BS 純資産 1,544,999 と厳密一致 2026-07-06
- [x] **A-10 締め・繰越**: opening_balances＋期首残高インライン編集（Σ=0検証・赤表示）＋試算表繰越残高列＋BS/元帳の年度スコープ化＋翌期繰越（Update+フラグno-op方式、SQL実測Σ=0）。月次締め=会計年度画面の期間ステータス変更で運用（A-4ガード実測済み）2026-07-05
- [x] **A-11 固定資産**: FixedAsset台帳・少額判定ヒント（閾値マスタ期間解決、R8改正40万円を実機確認）・年度別償却仕訳自動生成（定額月割45,000円をE2E検証、部門引継ぎ・二重生成ガード付き）。JournalLineに部門/プロジェクト列も追加 2026-07-05
- [x] **A-検証**: 総合検算オールグリーン（Σ借方=Σ貸方=3,008,000／伝票番号重複0／17・18期 期首Σ=0／売掛金元帳=未回収請求残 110,000 一致）2026-07-06

## フェーズ B — 周辺業務

- [x] B-1 マスタ: 取引先/部門/プロジェクト 2026-07-05
- [x] B-8 権限3ロール（general/approver/accounting、読取ゲート＋ボタン出し分け 187a7d3）2026-07-05（旧ロール。ADR-0041 で廃止 → ADR-0043 で直接フラグ化）
- [x] B-2/B-3 経費精算（B2-1〜B2-5 全完了: 承認ルート自動決定・自動仕訳・固定資産連携・実費超過再承認）2026-07-05
- [x] B-4 請求・入金（B4-1〜B4-5: 見積→受注→検収→請求→入金消込→売掛残高。PDF は Excel 出力で代替）2026-07-06
- [x] B-5 定期請求（SaaS 月額・冪等生成）2026-07-06
- [x] B-6 出納帳（経費支払連動込み 410de0a）2026-07-06
- [x] B-7 予算管理（予実・超過警告・部門引継ぎ 258ecfa。Excel 一括登録は UL 実機確認のみ未）2026-07-06
- [x] B-9 通知（アプリ内E2E済み・Slack/メールは口だけ 0029f51）2026-07-06
- [x] **総合テスト用シナリオ集（docs/tests/06_利用シナリオ.md）**: S-01〜S-10＋検算クエリ 納品（7630025）2026-07-06

## フェーズ B' — IT 受託特化の管理会計

- [x] B'-1 工数入力 → B'-2 人件費配賦 → B'-6 案件別損益（管理会計レイヤ、ADR-0009）2026-07-06
- [x] B'-3 検収基準売上（B-4 の検収確定→売上仕訳で実装済み、ADR-0008）／B'-4 繰延収益（B-5 の月次計上で v1 代替）
- [x] **B'-5 SES 精算幅計算→請求連動** 2026-07-07: 案件マスタに契約条件5項目（月額基本/精算下限h/上限h/控除単価/超過単価）＋「SES精算・請求」画面（計算プレビュー→請求書+SES売上高仕訳の一括生成・冪等・全行 project_id）。**実機E2E: 実績190h vs 幅140-180h → 基本800,000+超過10h×4,500=845,000 → INV-26-004（税込929,500）+仕訳 No.28。計算内訳が摘要に残る**。売上は請求時計上（B-5 SaaS と同方式・ADR-0008 整合）。**これでフェーズ B' も完全完了**

## フェーズ C — 差別化

- [x] C-1 AI 領収書読み取り（オプトイン・Provider=AzureOpenAI 実稼働。**テキスト・画像両経路を実機実証＋ユーザーも本物レシートで成功確認** 2026-07-06。Claude/Mock 経路も温存）
- [x] C-2 資金繰り予測ダッシュボード（ApexChart・4ヶ月予測・資金ショート可視化 688e5c2）2026-07-06
- [x] C-3 インボイス番号実在チェック（実装済み・モック稼働 da80705。**実照合には国税庁 ApplicationId の届出が必要**＝appsettings に設定するだけ）2026-07-06

## フェーズ D — 「全部入り」の完成（2026-07-06 棚卸しで新設 → ADR-0011・03 §8）

> Fable 期限 7/7 までの集中実装枠。ISSUE-0001 より優先（ユーザー指示）。

- [x] **D-1 月次推移表** 2026-07-06 夜: MonthlyTrend（QueryField・PL/BS を検索 Select 切替・年度 Select・Excel DL）。**検証: PL 合計列=損益計算書と13行一致／BS 期末列=貸借対照表と20行一致／BS 検算行が全12ヶ月バランス（sql CLI 突合＋実機）**。月列は「第n月」＋先頭に暦月行（決算期変更に自動追随・ハードコード回避）。知見: 集約 SELECT の statement ゲートは WHERE でなく HAVING（集約は WHERE 全滅でも1行返る）
- [x] **D-2 銀行・クレカ明細 CSV 取込＋自動仕訳＋残高照合** 2026-07-06 夜（設計= ADR-0012/0013）: 貼り付け取込（引用符内カンマ対応パーサ・ヘッダ自動スキップ）→ ステージング（重複キーで再取込スキップを実測）→ ルール10種マスタ（AWS→通信費/家賃→地代家賃/手数料→支払手数料 の自動判定を実測）→ **AI 科目推定は実 Azure OpenAI で稼働実証**（ﾘｿｸ→雑収入と推定→手修正 UI で受取利息に上書き）→ 一括起票4件（税行方式: 55,000→50,000+税5,000 等・締めガード・source_type='bank'・仕訳リンク）→ **残高照合オールグリーン（全5日付で明細残高=帳簿残高・差額0・○一致）**・全仕訳 Σ借方=Σ貸方=3,228,452。画面5: 明細取込/明細一覧/残高照合/口座マスタ/仕訳ルール（サイドバー「銀行」グループ・経理専用）。fork 分業（周辺4モジュール）
- [x] **D-3 キャッシュ・フロー計算書**（間接法）2026-07-06 夜: 営業/投資/財務 の21行構成。**検算: 増減額=現預金仕訳直接集計（17期 -22,000/18期 +1,189,000）・期末残高=BS現預金合計（1,667,000）の両方一致**。全BS科目を区分割当し「営業+投資+財務=Δ現預金」が構造的に成立する設計（詳細= temporary/cfs_report.md → 実装コメント）。fork 分業で作成・親が統合検証
- [x] **D-4 定型仕訳** 2026-07-06 深夜: JournalTemplate/JournalTemplateLine（ヘッダ+明細 CRUD・seed 3種=家賃/給与/社保）＋「この定型から伝票を起票」→ draft 振替伝票生成→自動遷移→確定（**E2E実測: 給与テンプレ→No.17 採番・3,000,000 貸借一致**）。税行はテンプレに持たず伝票確定時の RegenerateTaxLines 任せ（家賃テンプレは PUR_10 内税で確定時に税行が切られる設計）
- [x] **D-5 仕訳 CSV インポート** 2026-07-06 深夜: JournalImport（貼り付け式・形式=伝票グループ,日付,借貸,科目コード,金額,税区分コード,摘要）。**行は無加工取込＝移行元と1円もズレない**（税再計算なし。E2E実測: 家賃複合3行 No.18 が 150,000+15,000/165,000 のまま取込）。グループ単位検証（科目実在/貸借一致/締めガード/日付混在）で NG 伝票は理由付きスキップ（**E2E実測: 貸借不一致 G003 をスキップ・理由表示**）。借貸は D/C・借/貸・借方/貸方 受理。「下書きとして取り込む」オプション。**制約: 取込税行の is_tax_line は立たない→税区分コードは本体行のみに付ける運用**（06 シナリオ S-15・ji_report 参照）。給与ソフト取込・弥生移行はこの汎用形式で吸収（ADR-0011）
- [x] **D-6 買掛・支払管理** 2026-07-06 深夜（余力枠まで到達）: VendorInvoice（仕入先請求書 CRUD・received→accrued→paid の一方向遷移・状態×経理ロールでボタン出し分け）＋支払予定表（期限順・合計行・⚠超過/まもなく警告=SQL両分岐実測）。**E2E実測: AL-7005 未払計上→No.20（D 消耗品費30,000+仮払消費税3,000/C 買掛金33,000・請求日起票=発生主義）→支払登録→No.21（D 買掛金/C 普通預金）→予定表から消滅→買掛金残高0**。会計判断: 仕入先債務=買掛金2000（経費精算の未払金2020と使い分け）。seed: 仕入先ベガソフト（外注費5000・550,000）。全体Σ=6,468,252 一致
- [ ] （D-6 の後続・将来）振込 FB データ生成（全銀フォーマット。実銀行検証不能のため様式検証まで → ADR-0011）

## 磨きバックログ（残タスク。2026-07-06 対話モードで整理）

**すべて任意の改善。優先おすすめ順: 承認待ち「開く」→ 消込差額 → 経費への案件選択**（デモで目立つ順・実務で困る順）

| 項目 | 具体的な内容 | 重さ |
|---|---|---|
| ~~承認待ち「開く」調査~~ | **解決済み（2026-07-07）**: ユーザー実操作では正常に開くことを確認。無反応に見えたのは Claude のブラウザ自動操作の座標クリック空振り（D-2 実装中に同根現象を実測・「JS の btn.click() が確実」と知見化済み）で、アプリ側の不具合ではない | — |
| ~~Boolean 列 ○— 表示~~ | **完了（2026-07-07）**: 13モジュールに TrueText ○/FalseText — を適用（取引先一覧で実機確認） | — |
| ~~補助元帳~~ | **完了（2026-07-07）**: 総勘定元帳に補助科目・部門・案件の任意絞り込みを追加（1画面で補助/部門/案件元帳を兼ねる。絞り込み時は「期中発生分のみの累計」＝期首を含めない設計。**実機: 旅費交通費×案件で経費直課1件・7,273 を確認**）。※補助科目のデモ用 seed（sub_accounts が0件）はバックログ | — |
| ~~月末日付起票の境界実測~~ | **実測→バグ発見→修正完了（2026-07-07）**: 7/31 起票が「対応する月次期間がありません」で失敗するのを実測（境界日の辞書順比較の罠が発現）。全スクリプトの年度・期間・経過措置解決を月初日方式に統一（JournalEntry/ExpenseRequest/Acceptance/Receipt を修正・他は対策済みを確認）。**修正後 7/31 起票 No.23 成功・税行分解も正常** | — |
| ~~経費申請への案件選択~~ | **完了（2026-07-07）**: 案件（任意）欄追加→仕訳全行に project_id 引継ぎ（**実機E2E: 申請→承認→仕訳 No.25 全行 project_id=1・案件損益に経費直課**） | — |
| ~~approval_history.attempt_no~~ | **完了（2026-07-07）**: AttemptNo を DB 取り直しで解決（ResolveDbAttemptNo。今後の書込みが正しくなる。既存データは修正せず） | — |
| ~~年払い前受の繰延~~ | **完了（2026-07-07）**: 定期請求に課金サイクル（月額/年額）追加。年額は起点月に請求書+前受計上（D 売掛金/C 前受収益2110+仮受消費税）・毎月按分振替（D 前受収益/C SaaS売上高・端数最終月調整）。**実機E2E: 7月実行→年額1+振替1（No.26: 1,320,000 前受計上・No.27: 100,000 振替）・再実行全スキップ=冪等**。既存月額は無改変=後方互換 | — |
| ~~SES 精算幅（B'-5）~~ | **完了（2026-07-07）**→ フェーズ B' の B'-5 欄参照 | — |
| ~~消込差額処理~~ | **完了（2026-07-07）**: 差額が閾値（RECEIPT_DIFF_MAX=1,000円・マスタ）以内なら D 預金+D 支払手数料(内税分解)/C 売掛金 の4行で消込→paid（**実機E2E: INV-26-002 に 109,560 入金→No.24 で差額440を自動処理・売掛残0・全体Σ差0**）。閾値超は従来どおり partial | — |
| 予算 Excel UL 実測 | 予算明細の Excel 一括アップロードの実機確認（機能は実装済み・実測のみ未） | 小 |
| その他小粒 | **完了分（2026-07-07）**: ~~科目一覧 LimitCount 100 化~~・~~Account 一覧に既定税区分列~~・~~月次締めの draft 残存警告~~（FiscalPeriod.Status 変更時に件数警告・非ブロック）・~~.ide/ gitignore~~。**残**: 新規未保存時の LinkField 生ID表示（低優先）。~~並列承認の IsOrderCompleted DB化~~は 2026-07-08 の ADR-0016（複数課長 OR 承認の DB ベース化）で回収済み | 小 |

## 未解決・外部待ち（新セッションはここも見る）

- ~~ISSUE-0001（翌期繰越ボタンの ExecuteSqlField 不発火）~~: **解決済み（2026-07-08）**。真因=ExecuteSqlField のパラメータはフィールド名でなく DB 列名で解決される。詳細 `docs/issues/ISSUE-0001`
- C-3 実照合: 国税庁 ApplicationId 取得待ち（ユーザー側）
- **APIキーの Git コミット絶対禁止**: 実キーは **.NET User Secrets**（`dotnet user-secrets --project BusinessApp/BusinessApp.Server`、実体は `%APPDATA%\Microsoft\UserSecrets\dfae2e03-01ce-4d9a-a011-556c5eed75d0\secrets.json`）に置く（ADR-0024。skip-worktree 運用は廃止・全 Git 履歴にキー不存在確認済み）。`appsettings.Development.json`／`designer.settings.Development.json` は**環境固有パスのため Git 追跡外**＝同じ場所の `*.sample` から作る（ADR-0025、手順は `LocalData/README.md`）。新環境ではキーを user-secrets に再登録する
- 作業モード: **完全自律**（2026-07-06 夜にユーザー指示で対話モード終了。コミットも任される。仕様で悩んだら自分で検討して ADR に残して前進、本当にユーザー確認が必要な時だけ停止。トークンは気にせず品質最優先・記録は細やかに）

## 実装の型（毎タスク共通）

1. `Docs/AppPatterns/` の該当パターン＋ `Samples/` の正典モジュールを確認（ゼロから JSON を書かない）
2. DDL → `Designer/ddl/`、sql CLI で適用 → モジュール/スクリプト作成 → designcheck 0件
3. 必要に応じ zip デプロイ→実機確認（3回詰まったらユーザーに相談）
4. Conventional Commits でコミット → 本書のチェック更新＋「次にやること」更新

## リスク・宿題メモ

- 認証まわりの実配線（PasswordCheckUserTableInfo がどのデータソースに向くか）は A-0 で必ず実測（→ ADR 0001 影響欄）
- 食料品消費税1%の法案動向（2027-04 施行案）: 確定したら tax_rates への seed 追加を検討
- ~~フェーズ C 着手時: Claude API キーをユーザーに依頼~~ → **Azure OpenAI キーで実稼働済み**（2026-07-06。会社で使えるのは Claude でなく Azure OpenAI＋Document Intelligence だった）
- Slack/メール送信は実環境未検証である旨をユーザー向けドキュメントに明記（作業合意)

## セッションログ（時系列・追記専用）

> 各セッションの記録。**日付昇順**で追記する。過去の記述は当時のまま残す（編集しない）。2026-07-19 に旧「次にやること」欄から本節へ移設・整列した（文言は無改変。旧欄は時期により先頭追記/末尾追記が混在し順序が崩れていたため）。

磨きバックログ: JournalLine への部門/プロジェクト列追加（SelectField 2本）／補助元帳／経過措置・外税・貸借不一致の実測テスト／.ide/ を gitignore（70ef403 で誤コミット）／月次締めの draft 残存警告／**LinkField をスクリプトで設定すると DisplayText 未解決で生ID表示**（新規未保存時のみ。保存後の再表示は表示名が解決されることを実測確認済み。新規時の見栄え改善は低優先）／多段承認で同一 Order 内の複数メンバー（並列承認）の完了判定 IsOrderCompleted はメモリ Rows 依存のため遅延ロードの罠が残る（現テンプレは全段1名なので影響なし。並列承認を使う時に DB ベース化）
【B2-1 完了 2026-07-05 深夜】承認E2E成功（申請→SimpleExpense自動選択→花子承認→承認済）。**FK NOT NULL は禁止**（承認ランタイム系で実測: CLB の多段ネスト生成は FK 後埋め。66a0ef5 で修正）。既知課題: 承認待ち一覧「開く」ボタン無反応（直URL /Main/ExpenseRequest/{id} で迂回可。ApprovalFlow.mod.cs の遷移スクリプト要調査）。テストユーザー: hanako/hanako（承認 花子, id=2）。B2-4以降・B-8・B-4→B-6→B-7→B-9→B' は docs/07 と旧台帳どおり
【B2-2 完了 2026-07-05 夜】経費申請フォーム2軸拡張が実機OK（97ef115）: 既定値（立替/社員へ精算/対象者=本人）・事前申請→見込み額・取引先→支払取引先・交際費→接待3項目・備品15万→固定資産自動ON＋トースト（閾値は system_thresholds 参照）・費目マスタ画面。**IsRequired はスクリプト設定不可**（CommonMistakes #5、SetError で代替）。諸会費の勘定科目 6260 新設（通常会費=不課税）で費目 DUE を是正
【B2-3 完了 2026-07-05 深夜】承認ルート自動決定 E2E 成功（db60202）: テンプレ4種（課長のみ/部長のみ/課長＋総務/部長＋総務）× approver_role 動的解決（→ decisions/0007）。4パターン実測: 旅費1万→花子のみ／消耗品5万→次郎のみ／備品25万→次郎→三郎＋固定資産自動ON／交際費1万→花子→三郎＋未入力ブロック。2段承認の完走（#5）と中間状態（#4: 次郎承認→三郎待ち）を DB 検証。**重大バグ修正: AdvanceToNextOrder がメモリ Rows の遅延ロード空値でフロー全体を誤 Approved**（DB検索ベースに修正。RecalculateCurrentApproverDisplay も同補強）。閾値 EXP_APPROVAL_MID(3万)/HIGH(20万) は system_thresholds。テストユーザー: jiro/jiro（部長 次郎, id=3, 開発1部部長）・soumu/soumu（総務 三郎, id=4, 管理部）。admin/hanako/jiro=開発1部所属、開発1部: 課長=花子/部長=次郎。admin 表示名=「システム 管理者」に補正済み
【B2-4 完了 2026-07-05 深夜】経理処理フェーズ E2E 成功（aeceddc）: フロー連動（申請→applying、承認完了→approved を NotifyParentStatusChanged/OnApprovalFlowStatusChanged 契約で実現。却下/キャンセル→draft）、仕訳生成（#4 備品25万→工具器具備品227,273+仮払消費税22,727/未払金250,000、EXP-4 固定資産自動登録・取得価額=税抜。#5 交際費1万→9,091/909/10,000）、accounting→settled→completed 遷移、全 posted 仕訳 Σ借方=Σ貸方=316,000。税額はレシート記載(TaxAmount)優先→内税計算。**経理ボタンは現状全ユーザー可視（B-8 でゲート必須）**
バックログ追加: approval_history.attempt_no が承認行で NULL・再申請行で古い値（AttemptNo の ChildModule 遅延ロード。DB ベース解決に直す）
【B2-5 完了 2026-07-05 深夜】実費超過の再承認 E2E 成功: 事前申請（見込み25,000→花子承認）→実費40,000確定→**区分跨ぎ＋110%超を検知して自動再承認（ルートが部長 次郎に再解決）**→次郎承認→仕訳生成（旅費36,364+税3,636/未払金40,000）。閾値 EXP_OVERRUN_RATE=110 マスタ化。履歴に超過理由記録。Σ借方=Σ貸方=356,000。**B-2 経費精算はこれで全ステップ完了**（docs/07 冒頭に実装結果サマリ）
【B-8 完了 2026-07-05 深夜】権限3ロール実装・実機確認済み（187a7d3）: role 列（general/approver/accounting）、A=会計コア18モジュールに UserReadCondition(accounting)→リンク自動非表示＋直URL「不正なアクセス」ブロック、共有マスタ/テンプレ/ユーザー管理/制度閾値は読み全員・書き経理、経理ボタンはロールで出し分け。**設計原則: 一般ユーザーのスクリプトが読むモジュール（テンプレ3種・SystemThreshold・AppUser・ExpenseCategory・共有マスタ）は読取ゲート禁止**（申請フローが壊れる）。ロール: admin/soumu=accounting、hanako/jiro=approver
➡ **B4-1〜B4-5 請求・入金の実装**（設計= docs/08 確定済み・DDL= 110_billing.sql 適用済み e7412a5）: B4-1 Quote/QuoteLine・SalesOrder/SalesOrderLine 画面（header_detail=ListField、正典 Order/OrderDetail。見積→受注ボタンで明細コピー・SO-{yy}-{seq} 採番）→ B4-2 Acceptance 画面＋検収確定→売上仕訳（D 1100 売掛金/C projects.project_type で 4000/4010/4020＋2200 仮受消費税、source_type='acceptance'、締め・二重生成ガード、**確定は経理ロール専用**）→ B4-3 Invoice/InvoiceLine（検収から生成/手動、INV 採番、due_date）→ B4-4 Receipt→消込仕訳（D 1020/C 1100、source_type='receipt'）＋invoice.status(partial/paid)＋売掛残 QueryField → B4-5 E2E（見積→…→入金→試算表/BSで売掛・売上・預金検証）→コミット。サイドバーは「販売」グループ新設（見積/受注/検収/請求書/入金/売掛残高）
【B4-1 完了 2026-07-05 深夜】見積・受注 実機E2E済み（3b64b4c）: Q-26-001 作成（明細2行・数量×単価自動計算・合計1,100,000）→「受注にする」で SO-26-001（明細コピー・見積 accepted 化・受注詳細へ自動遷移）。fork 生成→親検証の分業。B4-2/B4-3（検収→売上仕訳・請求書）は fork 継続中
【B4-2/B4-3 完了 2026-07-06 未明】検収→売上仕訳・請求書 実機E2E済み（c7bf6c9）: A-26-001 検収確定（経理専用）→仕訳 D売掛金1,210,000/C受託開発売上高1,100,000/C仮受消費税110,000（source_type='acceptance'）→「請求書を作成」で INV-26-001（明細コピー・支払期限=翌月末）。**採番バグ修正: OrderByDescending のラムダに .Value が無いとソート無効で伝票番号重複**（4箇所修正・データ振り直し・Project.md 知見化）。B4-4（入金消込・売掛残高）fork 継続中
【B4-4/B4-5 完了 2026-07-06 未明】入金消込・売掛残高 実機E2E済み（403f09d）: INV-26-001 に入金1,210,000→消込仕訳 No.7→paid→売掛残一覧 残0/入金済。検算 Σ2,776,000 一致・伝票番号重複0。**検索インスタンス直接 Submit で他モジュール（Invoice.status）更新が動くことを実証**（新知見）。採番 .Value 監査で fork 分6箇所も修正。**B-4 請求・入金コンプリート**
【B-5 完了 2026-07-06 未明】定期請求 実機E2E済み: RecurringRun で 2026年7月分一括生成→INV-26-002（10万+税1万・7/31発行・8/31期限・billing_month 冪等キー）+売上仕訳 No.8（D売掛金11万/C SaaS売上高10万/C仮受消費税1万）→**再実行で生成0/スキップ1（冪等）**。Σ2,886,000一致・伝票番号重複0。**新知見: 期間解決は境界日（月末=end_date）の >= 比較が失敗する→月初日で解決**（Project.md 記録。月末手動起票の実測はバックログ）。SaaS 売上は請求書生成と同時計上（検収レコード無し。ADR-0008 の運用具体化）
【B-6 完了 2026-07-06 未明】出納帳・入出金起票・経費支払連動 実機E2E済み（410de0a）: 出納帳（普通預金）に No.7 入金 1,210,000／No.9 経費支払 10,000／No.10 小口補充 30,000・残高 1,170,000。経費 Settle は支払仕訳（D2020/C1020, source_type='expense_payment'）込みに拡張。CashEntry は税行なし2行仕訳（税取引は振替伝票へ誘導）。Σ2,926,000 一致・重複0
【B-7 完了 2026-07-06 未明】予算管理 実機E2E済み（258ecfa）: 予算登録（12ヶ月均等・端数期末月）→予実対比（開発1部×減価償却費 予算5万/実績4.5万/消化率90%/⚠超過警告=BUDGET_ALERT_RATE マスタ参照）・部門未設定実績の可視化。経費仕訳に申請者部門を引継ぎ（**Creator も遅延ロード null の罠→DB取り直しで修正、No.12 全行「開発1部」実証**）。予算閲覧は全員・登録/編集は経理設計方針: budgets テーブル（fiscal_year_id×department_id×account_id×月次12列 or 行持ち。行持ち推奨: budget_lines(fiscal_year_id, department_id, account_id, period_no, amount)）＋予算入力画面（部門×科目で12ヶ月入力）＋予実対比 QueryField（部門×科目: 予算/実績（posted 仕訳の department 集計）/差異/消化率）＋超過警告（閾値=system_thresholds に BUDGET_ALERT_RATE 追加、ホーム or 一覧で消化率>閾値を強調）。仕訳側の department は JournalLine.Department（A-11 で列追加済み・経費仕訳への部門引継ぎは未実装=経費申請に部門が無い→**申請者の所属部門を仕訳の Department に引き継ぐ改修**を B-7 に含める）。その後 B-9 通知 → B' → A-8/docs/tests/06
【B-9 完了 2026-07-06 未明】通知 実機E2E済み（0029f51）: 申請→花子宛「承認依頼」（Home 未読・開くで遷移+既読）→承認→申請者宛「承認されました」。DataReadCondition で自分宛のみ（他人宛は URL 直でも不可視）。Slack はモックログ（口だけ・作業合意どおり）設計方針: notifications テーブル（id/recipient_user/title/body/link_module/link_id/is_read/created_at）＋通知一覧（自分宛のみ DataReadCondition or 検索初期値）＋ホームに未読件数/最新表示。発火点: (1) 承認依頼（ApprovalFlow.LoadFromTemplate/AdvanceToNextOrder で次の承認者へ） (2) 承認完了/却下（申請者へ） (3) 支払期限リマインド（invoices due_date 接近=RecurringRun 型の手動実行 or ホーム表示で代替）。**Slack/メールは「口だけ」**: NotificationService 拡張の呼び出し箇所だけ作り、実装はログ出力（作業合意）。CLB の Mail 拡張は使わない。その後 B'-1 工数入力（time_entries: user×project×date×hours、月次一括入力画面）→ B'-2 人件費配賦（月次給与マスタ×工数比率→案件別原価の QueryField。仕訳は作らず管理会計レイヤ）→ B'-6 案件損益（売上=journal_lines.project_id 集計、原価=配賦+外注費等の直課、粗利/粗利率）→ A-8 → docs/tests/06 シナリオ集
【B'-1/2/6 完了 詳細】TimeEntry 全社員・MonthlySalary/CostAllocation/ProjectProfit 経理専用。過去売上仕訳に PRJ-001/002 紐付け済み。工数実データ: 花子 7/6 PRJ-001 600分・PRJ-002 200分設計方針: time_entries(id, user_id, project_id, work_date DATE, hours(小数=REAL か 分単位 INTEGER→**分単位 INTEGER 推奨**・0.25h=15分の丸め問題を回避), note, creator...)＋月次一括入力画面（TimesheetEntry: 対象月×自分の案件別日別グリッドは重いので、v1 は「日付・案件・時間」の行を素朴に入力する TimeEntry CRUD＋自分の入力のみ DataReadCondition＋月次合計表示）→ B'-2 人件費配賦: monthly_salaries(user_id, fiscal_year_id, period_no, cost INTEGER=月次人件費コスト)マスタ＋配賦 QueryField（ユーザー月次コスト×案件工数比率→案件別人件費。仕訳は作らない管理会計レイヤ、ADR 化）→ B'-6 案件損益: 売上(journal_lines.project_id)−直課費用(同)−配賦人件費 の QueryField（粗利・粗利率）。B'-3/B'-4/B'-5 は v1 簡易（検収基準は B-4 実装済み・繰延収益は B-5 の月次計上で代替済み・SES 精算幅はバックログ）と整理する ADR を書く → その後 A-8 株主資本等変動（簡易）→ **docs/tests/06_利用シナリオ.md**（ユーザー明示要望・総合テスト用）→ 磨きバックログ消化
🎉 **フェーズ B 完了（2026-07-06 未明）**: B-1 マスタ／B-2 経費精算（B2-1〜B2-5）／B-4 請求・入金／B-5 定期請求／B-6 出納帳／B-7 予算／B-8 権限3ロール／B-9 通知 — 全機能実機E2E済み。
🎉🎉 **フェーズ B' 完了（2026-07-06 未明・50ac9cc）**: 工数入力（分単位）→人件費配賦（月次コスト×工数比率・配賦合計=コスト厳密一致を実測）→案件別損益（受託 粗利52%／SaaS -75% を手計算照合）。配賦は管理会計レイヤ・機微情報は経理専用（ADR-0009）。**これで B/B' の全機能が実装・実機検証済み**（ユーザー指示「B/B' 完了まで」達成）。
🏁 **フェーズ A / B / B' 全機能達成（2026-07-06 未明）**: A-8 完了（74f2e82・株主資本等変動、BS 純資産 1,544,999 と厳密一致）＋ docs/tests/06 利用シナリオ集 納品（7630025・S-01〜S-10＋検算クエリ）。**残るはフェーズ C（AI 領収書=要ユーザーAPIキー／資金繰り予測／インボイス照合）と磨きバックログのみ**
【2026-07-06 朝のユーザー指示】①10時間自律続行 ②**AI 読み取りはオプトイン必須**（ボタン式・キー無しユーザー前提。キーは今晩→ Provider=Claude 切替） ③帳票は Excel/PDF 出力＋年度検索 ④金額 UI（一覧右詰め・カンマ統一・フォーム入力は左のまま） ⑤予算の一括登録（Excel で対応） ⑥全機能後は自律テスト（年度跨ぎ・複数人の本格データ）→改良 ⑦トークン効率に配慮
【仕上げ第1弾 完了 792d798】Excel DL 23画面（予算明細は一括 UL も。**UL の実機検証は未**=ユーザーデモ時の確認ポイント）・BS/PL/消費税を年度 Select 化（BS 検算 2,014,999 一致）・金額右詰め26モジュール+カンマ統一（**新知見: 一覧セル内 DIV は flex → justify-content で右寄せ**。app.css 新設・deploy.ps1 は対応済みだった）
【C-1 完了（モック） c0482f3+80d29d7】AI 領収書=AITextAnalyzerField をオプトイン配置（領収書アップロードの隣。使ったときだけ AI）。サーバは Provider 切替（Mock/Claude/AzureOpenAI）。**Claude 経路は画像/PDF 直読み（DI 不要）で実装済み・今晩 appsettings に ClaudeApiKey+Provider=Claude を設定するだけ**。モック E2E: 費目=交際費の候補解決・接待3項目の出現+充填まで動作
🎊 **フェーズ C 完了（2026-07-06 朝）**: C-1 AI 領収書（オプトイン・モック稼働、今晩 Provider=Claude+キーで実稼働 c0482f3/80d29d7）／C-2 資金繰り予測（ApexChart+4ヶ月予測・⚠資金ショート可視化・手計算照合 688e5c2）／C-3 インボイス照合（モック稼働、国税庁アプリID 取得後に appsettings 設定で実照合 da80705）。**全フェーズ A/B/B'/C の計画機能が実装済み**
【自律総合テスト 第1ラウンド 2026-07-06 朝】**発見3件・修正2件**: ①自己承認バグ（課長申請→承認者=本人）→ ADR-0010 で自動格上げ実装・3経路実測（88fd369） ②**繰越の事故パターン**: 移行年度（期首も仕訳も無い17期）から繰越すると18期期首が空データで破壊される → 空年度ガード実装＋17期に開始残高を正置。年度跨ぎ本体は実証済み: 締めガード→期間再オープン→決算修正仕訳(17期No.1)→繰越で期首 478,000/-500,000/+22,000（Σ=0）→18期BS 1,992,999 完全一致 ③**繰越ボタンの ExecuteSqlField が差分ゼロ Submit で不発火**（NextYearId 実列バインド化で対処したが実機での発火確認は未完=**バックログ**。SQL 手動実行は完全動作） ④FiscalYear の OrderBy .Value 欠落も修正
【総合検算 2026-07-06 朝・オールグリーン】Σ借方=Σ貸方=3,008,000（全年度）／伝票番号重複0／17期・18期の期首Σ=0／**売掛金元帳残高110,000=未回収請求残110,000（総勘定と補助簿の照合一致）**
⚠ **未解決（ユーザー報告済み・要調査）**: 翌期繰越ボタンの ExecuteSqlField が Submit 経由で発火しない → **不具合票 `docs/issues/ISSUE-0001` に再現手順・切り分け・回避策を集約**（回避策=sql CLI で繰越 SQL 手動実行は完全動作）
【2026-07-06 夜・対話モード】**C-1 実AI稼働を両経路で実証**: Provider=AzureOpenAI（gpt-5.4-mini）テキスト経路（件名生成・交際費解決・接待3項目充填）＋画像経路（DI prebuilt-layout OCR→抽出→費目=消耗品費・領収書欄へ画像添付連動）。ユーザーも本物レシート写真で成功確認。**キーは appsettings.Development.json（skip-worktree）のみ・全 Git 履歴にキー無しを `-S` 検索で確定**（api-keys.json は一度も未コミット・ユーザーが原本削除済み）。UI改善: 経費精算の「金額」→「金額（税込）」・「消費税額」→「うち消費税」（ユーザー指摘）
【2026-07-06 夜・スコープ拡張】ユーザーと「全部入り」棚卸しを実施→**フェーズ D 新設（ADR-0011/0012、03 §8）**。Fable 期限=7/7 のため ISSUE-0001 調査より新機能を優先（ISSUE はユーザーが後日対応）。採用: D-1 月次推移表／D-2 銀行明細CSV取込＋自動仕訳＋残高照合（ステージング＋仕訳リンク方式）／D-3 C/F計算書（間接法）／D-4 定型仕訳／D-5 仕訳CSVインポート（給与取込・移行導線を兼ねる）／D-6 買掛・支払管理（余力枠）。不採用/後回し: 銀行API連携・給与専用取込（D-5に吸収）・振込FBデータ（D-6後続へ）
【2026-07-06 深夜】**D-1/D-2/D-3 完了**（月次推移表・銀行明細取込＋AI仕訳＋残高照合・C/F計算書。全て実機E2E＋検算グリーン。fork 2本の並列分業を活用）。新知見: ①表示専用モジュール（DataSourceName ""）の this.Submit() は機能しない→行単位 row.Submit() で保存 ②DateField への代入は DateOnly.FromDateTime 必須（DateTime だと保存データに乗らず NOT NULL違反） ③スクリプト作成のモジュールは CanCreate:true 必須（false だとスクリプト経由の生成もサーバで拒否） ④ブラウザ自動操作の Blazor ボタンは座標クリックが空振りすることがある→JS の btn.click() が確実
【2026-07-06 深夜②】**D-4/D-5 完了**（定型仕訳ワンタッチ起票・仕訳CSVインポート。E2E実測グリーン。fork 分業: モジュール量産は fork・スクリプト/統合/検証は親）。**これで D-1〜D-5 の必須5機能が全完了**
🏆 **フェーズ D 全完了（2026-07-06 深夜・D-1〜D-6 の6機能すべて実装・実機E2E・検算グリーン）**: 月次推移表／銀行明細取込+AI仕訳+残高照合／C/F計算書／定型仕訳／仕訳CSVインポート／買掛・支払管理。**ADR-0011 の「全部入り」採用スコープを1日で完遂**（fork 最大3本の並列分業）
【2026-07-07 未明〜早朝・磨きバックログ集中消化】ユーザー指示（バックログ最優先→6-7時まで改善継続→余力でホーム画面）に基づき2ラウンド実施。**ラウンド1**: 資金繰り予測に未払連動＋デモデータ資金補正（長期借入5,000,000投入で当月健全化→8月⚠のデモ最適状態）／**月末起票の境界バグを実測で発見→全スクリプト横断修正**（月初日解決に統一・7/31起票 No.23 で実証）／消込差額の自動仕訳（No.24・440円）／経費の案件選択→案件損益直課（No.25）／補助元帳（元帳に補助・部門・案件絞り込み）／小粒5件（○—表示・科目一覧・attempt_no・締め警告・.gitignore）。**ラウンド2**: 年払い前受の繰延（No.26/27・冪等実証）／SES精算幅 B'-5 完了（INV-26-004・No.28）／**ホーム画面全面刷新**（挨拶・やることサマリ・ショートカット役割別出し分け・未読通知維持・経理KPI 2行・未処理件数・⚠資金繰りアラート。admin/hanako 両視点で実機確認）。全 posted 仕訳 Σ=13,936,752 一致。新知見: 検索セレクトは JS イベント不達がありうる（実キーボードなら確実・アプリは正常＝元帳0件騒動の真相）
【2026-07-07 早朝・FBデータ完成】**振込データ作成（全銀フォーマット総合振込）を実装**（ADR-0011 の「後回し」を回収）: 取引先に振込先口座5項目＋自社 company_profile＋「購買/振込データ作成」画面。未払計上済の請求書→ヘッダ/データ/トレーラ/エンドの120桁固定長テキスト生成（コピーしてバンキングへ＝貼り付け思想と対）。**様式自己検証を画面に表示（全レコード120桁・必須項目・使用可能文字・トレーラ件数/合計整合）**。仕様は最新リサーチで確定（出典は temporary/fb_report.md）。**実機E2E: VG 未払計上→生成→様式検証OK・データレコード 550,000・トレーラ整合を確認**。制約: 実銀行への取込は未検証（ADR-0011 どおり様式検証まで）
【2026-07-07 朝・ユーザーQ&A由来の改修3点＋総合テスト第2R修正】①**請求書の1枚ずつ帳票出力（Q4）**: Invoice 詳細に「請求書を出力（Excel/PDF）」ボタン。Resources/invoice_template.xlsx（宛先・番号・日付・件名・明細10行・税計算・自社/振込先）へ FindCellByText 差込→Excel DL 実測OK・**PDF は生成物の日本語描画まで目視検証済み**（￥2,750,000・明細・振込先すべて正しい）。**PDF の環境要件を発見: `C:\Codeer.LowCode.Blazor.Local\Font\NotoSansJP.ttf` が必須**（無いと CustomFontResolver が Object reference エラー→全PDF失敗。NotoSansJP-VF.ttf のコピーで解決・Project.md 知見化。**新環境セットアップ時の必須手順**） ②**取引先口座の分離（機微情報）**: 銀行5項目を Partner から経理専用 PartnerBank モジュール（UserRead/Write=accounting）へ移設。購買/取引先口座メニュー・FBデータ生成の回帰OK（550,000円・様式検証OK） ③経費一覧に案件列。**総合テスト第2R の残修正**: EquityChange 年度自動解決（初期表示が空だった）＋合計行コード ''、TrialBalance 合計行同様（SQLite の compound SELECT+ORDER BY 式はサブクエリで包む=新知見）。**Chrome の連続自動ダウンロードブロックを発見**（2回目以降は成功トーストでもファイル不着=ブラウザ仕様・アプリ正常。Project.md 知見化）
【2026-07-07 朝・見積書出力の横展開】請求書と同方式で見積詳細に「見積書を出力（Excel/PDF）」を追加（quote_template.xlsx: 見積番号/有効期限/お見積金額・消費税は SALES_10 で計算・有効期限/別途見積の注記）。実機トースト成功＋ハーネスで差し込み→PDF 日本語描画まで目視検証（Q-26-002・￥2,750,000）。テンプレ生成スクリプトは temporary/make_quote_template.ps1
【2026-07-07 午前・レビューエージェント監査＋社員役探索の修正一括】fork によるモジュール机上監査（27 .mod.cs + 19 Query.sql、重大度高ゼロ）と ichiro（一般社員）役のブラウザ探索を並列実施し、以下を修正:
- **TimeEntry に DataWriteCondition（担当者=本人のみ書き込み可）**: 一般社員が他人の工数を編集・削除できた（原価配賦の基礎データ）。修正後を実機確認——他人の行は削除ボタンが自動非表示・詳細も読み取り専用化、本人の行は作成OK（ID 110）。CLB の DataWriteCondition はサーバ強制と同時に UI にも自動反映される（デモ訴求ポイント）
- **PL / 消費税集計の年度未選択（初期表示）を現在年度に自動解決**（監査指摘・EquityChange と同型。消費税は申告基礎資料に前期混入の実害）
- **固定資産の償却仕訳に月次期間の締めガード**（年度 closed のみ見ていた。決算作業中の締め済み3月期間への起票を防止）
- **入金消込に過入金ガード**（入金額>請求残額で売掛金がマイナスになるのを防止。前受金への誘導メッセージ）
- **支払予定表の sort_key NULL 対策**（due_date NULL の行が無警告で変な位置に並ぶ）
- 監査の残メモ（低・将来課題）: 伝票番号999超のロールオーバー／BudgetVsActual 等の初期表示0件／元帳・出納帳の date_to 未指定時の翌年度混入（現データ未発生）／int オーバーフロー（経費2,147万超）／年払い期中解約の前受残留（運用注記）／経理専用操作の実行時ロール再チェックの不揃い
- **新知見（Project.md 記録済み）**: API 経由 logout 後に login.html 直行するとログイン不能（antiforgery トークンがユーザー束縛・静的ファイルはトークン更新されない。`/` を一度踏めば回復。通常フローは影響なし）
【2026-07-07 午前・全ロール横断 E2E 完走＋総合検算】一郎の申請2件を実フローで完走: モニター5万（部長のみルート・次郎承認→承認済）／ワークステーション25万（部長→総務の2段・次郎→三郎承認）→ 経理処理（soumu）で仕訳 No.38〜41 生成（経費計上＋社員への支払まで）・固定資産 EXP-15 自動登録（税抜227,273・確認促しメモ付き）→ 精算済→完了。**総合検算オールグリーン: Σ借方=Σ貸方=18,981,552／伝票番号重複0**。帳票類も新データで実機確認: PL 初期表示（今年度のみ・売上高計 5,645,000）／月次推移表（月別展開の合計が PL と一致）／資金繰り予測（9月・10月⚠資金ショート表示＝デモの見せ場が機能）。**追加改善: 固定資産の自動登録に申請者部門を引き継ぎ**（仕訳の部門引継ぎと同方式・EXP-15 の既存データも補正）。磨きメモ（小）: 承認待ち一覧の申請種別が「ExpenseRequest」と生名表示（CLB フロー枠の文字列。表示名マップは将来）
【2026-07-07 昼前・取込税行の is_tax_line 対応（バックログ回収）】仕訳CSVインポートが IsTaxLine を設定せず、**輸入した消費税行（1900/2200）が消費税集計表の課税標準に混入する**問題を修正（科目コードで自動判定）。既存データも import 由来の該当1行を補正済み。BankImport は当初から対応済みを確認。company_profile のマスタ画面化は「FB専用テーブル＋請求書自社情報はテンプレ直書き」という現設計が整合しているため**見送り**（画面新設は二重管理を生む。将来 自社マスタを作るならテンプレとの関係を ADR で整理してから）
【2026-07-07 夜・ユーザーレビューからの残タスク2件（次回実装）】
- **工数管理（代理修正）画面**: TimeEntryAdmin モジュール（同一 time_entries・UserRead/Write=accounting・行制限なし・メニュー 案件/工数管理（経理））。PartnerBank と同じ手口。権限は「まず経理のみ」を推奨（工数=人件費配賦=案件損益の原価データであり、部長は自部門案件の利害当事者のため、内部統制上は中立な経理に修正窓口を一本化。上長の関与は将来「月次工数確定（承認/差し戻し）」ワークフローで設計する）— ユーザーと合意形成中
- **銀行明細取込の2段階化**: 現状は「明細を取り込む」で即 bank_statement_lines に保存（ステージング設計自体は仕訳と分離済み＝帳簿は汚れない）。ただし UX として「貼り付け→プレビュー表（未保存）→内容確認→登録ボタンで確定」のワンクッションが欲しい（ユーザー要望）。実装スケッチ: パース結果を保存せず表示専用 ListField に AddRows で並べ、登録ボタンで row.Submit() ループ＋dedup 判定。あわせて取込済み一覧の説明改善（下の表は「未起票の作業一覧」で、**一括起票で status=journalized になると消える**／行の🗑でも消せる——が画面に書かれていない）
- 予算明細の Excel 一括アップロードは**ユーザー実測で検証完了**（金額修正・行追加とも成功。行追加は Id/Creator/CreatedAt/Updater/UpdatedAt を空欄で OK）
【2026-07-07 深夜・セッション終了時点の状態】デモリハーサルはセクション0〜1まで実施済み・**セクション2以降は新CSV（bank_sample.csv 7/22〜28版）で未実施**。DB はクリーンな状態（リハーサルの二重計上7本は削除済み・残高照合は全10日付○一致を SQL 検算済み・Σ借方=Σ貸方=18,981,552）。**本番デモはそのまま実施可能**。次回の実装タスクは上記2件（工数管理（経理の代理修正・経理のみで合意済み）画面／銀行明細取込の2段階化）から。
➡➡ 現在の起点: **残バックログは僅少**——支払の一部消込・差額／銀行明細のファイルUL・列マッピング／取込税行の is_tax_line／補助科目のデモ用 seed／予算 Excel UL 実測（ユーザー手動確認推奨）／自社情報（company_profile）のマスタ画面化／新規未保存 LinkField 生ID（低）／並列承認 DB化（低）。ISSUE-0001 はユーザー対応予定。**最終総合検算（2026-07-07 早朝）: Σ借方=Σ貸方=14,486,752・伝票番号重複0・両年度期首Σ=0**磨きバックログは D の後: 繰越ボタン発火の CLB コア調査（ISSUE-0001・ユーザー対応予定）／承認待ち「開く」調査／Boolean 列 ○—表示／補助元帳／月末日付起票の境界実測／経費申請への案件選択追加／approval_history.attempt_no DB解決／年払い前受の繰延／SES 精算幅／消込差額処理／予算 Excel UL 実測
- 【残タスク追加 2026-07-08 リハーサル発見・ユーザーと設計合意】**「申請中」「承認待ち」の2モジュール分割**（1画面のロール出し分け案は却下——検索条件を触ると画面の意味が崩れるリスク）。申請中=申請者が自分・未完了（全員見える・一般社員の入口）／承認待ち=現在の承認者が自分（承認者・経理のみ表示）。同一 approval_flows テーブル・別モジュールの PartnerBank 方式で実装。**あわせてメニュー再編**: 親「経費精算」＞子「申請（一覧・新規）」「申請中」「承認待ち」「通知」（現状の親「申請」構成は全部経費精算の話なので。将来ワークフローが増えたら親を再検討）
- 【残タスク追加 2026-07-08 ユーザー要望】①**経費精算一覧に検索フォーム**（件名・状態・申請者・利用日期間・費目。元帳と同じ宣言的検索）②**「この申請を複製」ボタン**（過去の申請を基に新規作成。Quote→SalesOrder のコピーパターン。件名/金額/費目/案件/区分をコピーし、日付=今日・status=draft・承認履歴/添付/実費は引き継がない）
- 【残タスク追加 2026-07-08 リハーサル発見】①**経費仕訳の締め済みフォールバック**: 利用日の期間が締め済みだと仕訳生成がエラーで止まる（ガード自体は正当）。実務の定石どおり**仕訳日を処理日（今日）に自動フォールバック**して起票し、摘要に元の利用日を明記＋トーストで通知（重要性の原則。年度跨ぎのみ決算修正へ誘導する文言に。**スコープ注意: 手動起票では精算ステータスを進める手段がない**——Settle は status=accounting 必須で、accounting になるのは自動仕訳成功時のみ。フォールバック実装で同時に解消すること）②**通知の情報量改善**: 「承認されました/承認をお願いします」に**対象の件名（＋金額）を埋め込む**（複数申請時にどれか分からない。Notification 生成箇所すべて）
- 【残タスク差し替え 2026-07-08 ユーザーレビューで方針転換】仕入先請求書に有効なのは AI 読み取りではなく**「過去の請求書を複製して新規作成」**（仕入先との取引は反復的＝家賃・外注月次・保守料。取引先/科目/口座はマスタ選択なので AI の埋められる項目が少なく投資対効果が薄い、というユーザー指摘が正）。経費精算の複製ボタンと同一パターンで実装。AI は「読み取ったインボイス番号とマスタ登録番号の突合チェック」案のみ将来の慎重検討枠に降格
🎉 **【2026-07-08 深夜】ISSUE-0001（翌期繰越ボタン）解決！** ユーザーの実機クリックで新しい症状（`Must add values for the following parameters: @NextYearId`）を観測 → 「@id は通る・@NextYearId だけ落ちる」の差分から**真因＝ExecuteSqlField のパラメータはフィールド名でなく DB 列名で解決される**ことを特定。SQL を `@next_year_id` に修正して実機で完治を確認（row_id 洗い替え・Σ=0・リセット Submit も成功）。詳細は docs/issues/ISSUE-0001（解決済みに更新）。回避策運用（sql CLI 手動繰越）は不要になった
【2026-07-08 深夜・残タスク第1弾（4本）実装・実機検証済み】
- **⑥締め済みフォールバック**: 利用日の期間が締め済みなら処理日（今日）で自動起票（摘要に利用日明記・年度跨ぎは警告・本日も締め済みならエラー）。詰まっていた #16 USBメモリ申請で実証（仕訳 No.46・案内トースト・ステータス正常進行）
- **⑦通知への件名埋め込み**: TargetSummary ヘルパー（件名＋金額を DB 解決）を承認依頼・最終承認・却下の全通知に適用。実測: 「経費申請「USBメモリ購入…」（1,100円）の承認をお願いします」
- **③経費「この申請を複製」**: 詳細画面のボタンで件名/金額/費目/案件等をコピーした下書きを作成→遷移→編集→申請可能。**重要な発見と対処**: 親に承認フロー子行が無いと CLB は子モジュールを実体化しない（申請ボタンが出ない・親からの子フィールド参照は「操作が存在しません」）→ 複製時に Draft フロー行を作成し実列バインドの ApprovalFlowIdRaw（approval_flow_id）で FK リンク。フロー側は Status "Draft" を「初回申請」として扱う改修（SubmitButton 可視条件・wasNew 判定・Pending 化）
- **⑧仕入先請求書「この請求書を複製」**: 取引先/科目/税区分/金額/摘要/口座をコピー、番号・日付は実物から入力（反復取引の入力を大幅短縮）
【2026-07-08 未明・残タスク第2弾（3本）実装・実機検証済み】
- **①申請中/承認待ちの2モジュール分割＋メニュー再編**: approval_flow の読み取り専用ビュー2本を新設——「申請中」（MyApplication・DataReadCondition 申請者=自分・全ロール）／「承認待ち」（ApprovalInbox・DataReadCondition 現在の承認者=自分・表示は承認者/経理のみ=UserReadCondition でメニューも自動非表示）。旧 ApprovalFlow 一覧リンクは撤去。メニューは親「経費精算」＞申請/申請中/承認待ち/通知 に再編。**申請種別は SelectField の Candidates（経費精算,ExpenseRequest）で日本語表示**——「ExpenseRequest 生名表示」の磨き項目も同時解消。実機: 申請中=自分の9件のみ・承認待ち=受信箱1件（ホームのカウントと一致）
- **②経費精算一覧の検索フォーム**: 件名/状態/費目/利用日＋精算対象者/案件の検索条件（エキスパンダ式）
- **④工数管理（経理）画面**: TimeEntryAdmin（同一 time_entries・経理専用・行制限なし）を 案件/工数管理（経理）に追加。病気・事故等の代理入力・修正の窓口（経理のみで合意済み）。あわせて**ユーザーのテストで残っていた他人名義工数（480分・総務三郎）を削除**（配賦原価の歪み除去）
【2026-07-08 未明・残タスク第3弾＝⑤銀行明細取込の2段階化 実装・実機E2E済み】「明細を取り込む」は **status='preview' のプレビュー**に読み込むだけになり、プレビュー表を確認して「**この内容で登録**」で pending（未起票の明細）へ確定、「**プレビューを取り消す**」で物理削除（重複キーも解放＝同じ CSV を貼り直せる）。未起票一覧に「一括起票で仕訳になるとこの一覧から消えます」の説明ラベルを追加（いつ消えるか問題の解消）。残高照合クエリは preview 行を除外。E2E: プレビュー1件→取消→再取込（重複解放の実証）→登録→pending 移動まで確認。既存デモ CSV は重複スキップ4件と正しく判定（リハーサルで登録済みのため）
【2026-07-08 明け方・仕上げ】
- **Modules を機能別サブフォルダへ整理**（会計コア/経費精算/販売/購買/銀行/案件/予算/マスタ/ホーム。125ファイル git mv・日本語フォルダ名の zip デプロイ＋実機ロードまで確認済み。deploy.ps1 は当初から再帰対応）
- **却下ルートのシミュレーション**: 次郎のテスト申請（5万）を admin が却下→フロー Rejected・親は draft へ差し戻し・申請者へ「経費申請「…」（50,000円）が却下されました（コメント: …）」の通知——却下経路の件名埋め込みも実証。差し戻し後は本人が編集して再申請できる状態
- **BS/PL の勘定式（左右並び）は設計スケッチを残して見送り**（ユーザー「難しいなら無理にしなくてよい」）: 技術的には可能——表示専用モジュールに年度 SelectField＋左右2つの ListField（借方側クエリ/貸方側クエリ）を1行2列で並べる構成。必要工数はクエリ分割2本＋年度パラメータの引き回し＋レイアウト調整で半日弱。報告式（現状）は税務・実務では標準的なので優先度は低と判断
- **最終検算オールグリーン: Σ借方=Σ貸方=19,012,792／伝票番号重複0／preview 残0**
【2026-07-08 夜〜・複数課長/部長タスク（ユーザー正式要望・〜7/9 6:30 自律セッション）】**ADR-0016**: 1部門に複数の課長・部長。department_managers 中間テーブル（旧単一列は使用停止・データ移行済み）／部門マスタに役職者子リスト（DepartmentManager）／ルート解決は役職全員を同一 Order に並列展開（2名以上=approval_type 'any' の OR 承認・1人承認で段完了・残りは Skipped）／完了判定を DB ベース化（**磨きバックログ「並列承認 DB 化」を回収**）／承認可否は Active Order ゲート（stale 画面の二重承認防御）／自己承認禁止を候補除外→格上げ→経理代替に一般化／受信箱を approval_inbox_view（メンバーベース）化＝2人目の課長にも表示・ホーム件数も同基準。テストケースは専任サブエージェント設計（docs/tests/10・25ケース）、コードレビューは3視点並列サブエージェント。実装コミット c75a862。designcheck 0件。
【同夜・完了】レビュー指摘反映（43ec89c: 申請者基準の解決＝代行実費確定の自己承認穴を閉鎖／却下経路の DB ベース化／IsSameId 統一／実在チェック／UNIQUE 制約／Role≠general 候補絞り／ホームのロールゲート。サーバー強制ゲート不在と同時承認レースは ADR-0016 に既知の限界として明記）→ デプロイ・実機テスト **27ケース中26 Pass・1件は仕様確認**（詳細 docs/tests/10 §5。S-01〜03 完全回帰・並列承認・二重承認防御・格上げ・経理代替・スナップショット・受信箱網羅性まで全部実測）。テスト中に既存不具合2件を発見し修正・実測確認（b2f4e5d ほか: 通知の誤警告トースト＝他ユーザー宛 Submit の戻り値は成否判定不能／temporary Order Id の FormatException）。テストフィクスチャとして kenta（課長2人目）・goro（部長2人目）・テスト部（E-05用・役職=健太のみ）を追加（デモデータとして残置）。
【2026-07-09 昼・E2Eテストシナリオ兼業務理解ドキュメント納品（ユーザー要望）】**docs/tests/11_E2Eテストシナリオ/ 全11ファイル**（README＋01初期設定〜10帳票検証）。まっさらDB（DDL+seed直後）→期首残高→販売/購買/経費/仕訳/銀行/管理会計/特殊取引→決算・翌期繰越の1周ストーリーで全約80モジュールを網羅。**全41仕訳の期待値台帳付き**（Σ借方=Σ貸方=11,852,675・BS 5,868,985 一致・C/F恒等式・19期繰越Σ=0 を PowerShell シミュレーション sim.ps1 で機械検算）。作り方: 6並列サブエージェントで全 .mod.json/.mod.cs から機能仕様を抽出→数値計画をスクリプト検算→執筆→レビューエージェントが全数値を再計算＋実コード突合（2ラウンド・計16指摘を全反映）。**レビューで発見した重要事実2件**: ①まっさらDBでは「＋総務」テンプレの2段目承認者が NULL（DDL適用時に soumu 不在→seed の解決が空振り。文書では01章1-10で手動補正する手順に）②残高照合の期待値は支払系操作日が7/25以降の前提（早い日に実行する場合の代替CSV残高も文書に記載）。注意: 期待値は「新規DBにDDL適用直後＋所定のユーザー登録順」前提。既存デモDBでは手順は使えるが金額・番号は一致しない
【2026-07-09 深夜・夜間タスク進行中（ユーザー指示: 質問発生か全完了まで自律継続）】タスク=①アプリ改修（ページフレーム分割/sysadmin/メニュー精査/検索ラベル/予算画面エラー修正）→②E2E全領域拡充→③CLB改善提案。**完了分**: ③初版（docs/12・6410264）／**LocalData 移設**（C:\Codeer.LowCode.Blazor.Local→リポジトリ配下。ADR-0017）／**組織・ユーザー全面再編**（5部×5名+admin=26名を DDL seed 化・部_役職 命名規約・総務部=全員経理・admin=sysadmin。PBKDF2 ハッシュを PowerShell 生成、方式は旧DBで実測検証。ADR-0018）／**DB 新規構築**（旧DBは LocalData/backup とユーザー側に退避。26ユーザー/役職者20行/＋総務テンプレの承認者解決を検証済み。**seed 順序の是正で「まっさらDBの総務段NULL」問題を根治**）。**次**: ①-2 sysadmin 権限スイープ（ADR-0018 の仕分け表が正）→デプロイ→ログイン実機検証→①-1 ページフレーム→①-3 メニュー→①-4 検索ラベル→②
【7/10 未明・①-2 完了 e3818bc】57モジュール＋スクリプト12箇所を一括再編・実機スモーク3ロール分グリーン（admin=sysadminでマスタ編集可／経理=マスタ読取専用化を実測／RecurringBillingの書込未ゲートを発見・是正）
【7/10 未明・①-1＋①-3 完了】**6ページフレーム分割**（ADR-0019: Main=ホーム+経費+工数+業務メニュー入口／販売管理（全員）／購買管理・会計（経理+sys）／経営管理（承認者+）／システム管理（sysadmin・AdminHome新設））。**予算画面の「No permission to read module」根治**（原因=検索セレクトが権限ゲート付き FiscalYear/Account を参照 → 非機微列だけの FiscalYearLookup/AccountLookup を新設して差し替え。予実対比・予算明細は承認者以上に変更=旧「全員閲覧」方針の転換）。実機検証: 一般=業務メニューが販売のみ・経営管理直URL「ページフレームへのアクセス権限がありません」／承認者=経営管理で予実対比+予算明細のみ・年度セレクト候補取得OK／sysadmin=入口5種+AdminFrame。**新発見の CLB 落とし穴**: フレーム跨ぎリンクは遷移先フレームに Module×セグメント登録が無いと真っ白（designcheck 未検出）→ FB-023 として docs/12 に記録・OtherPageModuleDesigns 登録で解消。**メニューパスが全面変更**（/Main/... → /AccountingFrame/... 等）＝docs/tests/06・09・11 は②で追随必須
【7/10 未明・①-4 完了】**検索条件ラベルを全画面一括付与**（32モジュール・78ラベル。DisplayName+「：」を各検索フィールドの前に挿入するスクリプト方式・冪等）。CLB は検索レイアウトへの LabelField 配置で実現可能（正典 AllFieldsSearch のパターン）＝FB-011 は機能提案からマニュアル提案（DOC-015）へ転換。経費・元帳で実機確認済み。**これでタスク①（アプリ改修）は全完了**。残タスク=②E2E全面改訂＋拡充（新フレーム構成・新ユーザー・新権限に追随→経費精算の分岐網羅→販売→…）。E2E実行がデモデータ再構築を兼ねる
【7/10 未明〜朝・② 第1弾完了】docs/tests/11 を**フォルダ構成に再編**（00_初期設定〜09_帳票検証・基本シナリオ.md＋全仕訳台帳.md 分離）し、**①の全変更に追随改訂**（【フレーム】表記・新配役=申請者kaihatsu1_ippan/経理soumu_bucho/総務段soumu_kacho1・章番号振り直し・sysadmin科目登録・予実対比=承認者以上・fork4本で分業+残存grepゼロ確認）。**経費精算の分岐網羅を新規執筆**: README（ルート決定マトリクス・再承認条件表・後片付け原則）＋分岐3ファイル=EXP-R01〜R08（金額境界±1円×費目）/S01〜S03（自己承認回避・総務部内承認・経理代替）/P01〜P05（実費3パターン・取引先支払）/A01〜A04（資産境界±1円・不課税・税額手入力）/X01〜X04（ブロック・ロック網羅チェックリスト）=計20本。**残（②続き）**: 02〜08の分岐網羅（販売→銀行/購買→仕訳/決算の順）／実機実行によるデモデータ再構築（=シナリオ検証）／docs/tests/06・09の追随更新
【7/10 未明〜・② 第2弾=実機実行によるデモデータ再構築（進行中）】docs/tests/11 を新DBで頭から実機実行し「シナリオ検証＝デモデータ再構築」を兼ねる。**完了: 00_初期設定（C003/C004登録・期首残高Σ=0・予算24行DB検証）→02_販売（仕訳No.1〜5・二重受注/二重請求ガード・経理専用ガード・定期請求の冪等スキップ3件・章末Σ=4,170,000一致）→03_購買（No.6〜7・FB全銀4レコード×120桁様式検証OK・複製→削除・章末Σ=5,270,000/買掛0一致）→01_経費精算（E-1〜E-4完走: 並列OR承認・交際費ブロック→却下→再申請→総務2段・固定資産自動判定EXP-3・実費超過の自動再承認・経理処理No.8〜15・検索3条件・キャンセル/複製の後片付け。章末Σ=5,741,600一致）**。発見・是正2件: ①C001 が seed に無く新規DBで参照NULL（040へseed追加＋稼働DB修復・209ffe3）②年額プラン seed がシナリオ2-9のUI登録と二重（210から撤去・8599655）。**1-10 は実キー環境（Provider=AzureOpenAI）で実AI読み取りを実証**——生成した領収書画像から件名/金額12,800/税1,163/日付/店名/費目=交際費/人数4まで自動充填を確認（モック期待値の代わり）。**残: なし（②の計画分は完遂。次セッションは新規指示 or 磨きバックログから）**
🏁 **【7/10 朝・②タスク完遂】** (1) docs/09 デモ台本を全面改訂（ddb7f5e・新体制/実測数値/銀行明細残高のNo.37調整で照合全○復元込み） (2) docs/tests/06 利用シナリオ集を4ロール/6フレームに追随（dababcd） (3) **02〜08 の分岐網羅を fork 4本並列で執筆完了**: 02_販売 SLS/RCV/RCR/SES 計31（うち新規25）／03_購買 PUR/FBX 計10／05_銀行 BNK 計8+チェックリスト／04_会計コア JRN/CSH/IMP 計28／07_特殊取引 DIF/COR 計11／08_決算 CLS 計10 —— **全分岐は各 mod.cs の実ガード・実文言から起こし、「本流の41仕訳・Σを汚さない後片付け原則」を全ファイルで徹底**（実行すると仕訳が生まれる分岐は（未実測）明記で解説に留める設計）。**執筆中に fork が潜在バグを発見→実測で確定→修正**: CSVインポートの下書き取込（source_type='import'）を振替伝票で開くと税行が畳まれ確定時に税再計算される＝「移行元と1円もズレない」保証が崩れる → import 伝票は生データのまま扱う分岐を追加（読込時の復元スキップ・確定時の税行再生成スキップ）。実測: 9,091/909(税行維持)/10,000 の3行が無加工で No.42 確定→検証後に削除し原状復帰（41仕訳・Σ=11,852,675・全期間締め済みを確認）
🎉 **【7/10 午前・② 第2弾=デモデータ再構築 完走！】09_帳票検証 完了で 00〜09 全章を新DBで実機完走**。9-1 試算表 37行＋検算行 11,852,675／9-2 BS 5,868,985／9-3 PL △1,797,226／9-4 株主資本等変動／9-5 月次推移（PL・BS両方、BS検算行が全12ヶ月一致）／9-6 C/F（期末 3,810,610=BS現預金）——**すべて文書の期待値と1円違わず一致**。9-9 資金繰り（7月出金 1,288,000=AL-7005+未払+人件費）・9-10 ホームKPI（当月利益 △1,215,601 赤表示）も一致。9-12 総合検算4項目オールグリーン（Σ=11,852,675・重複0・売掛元帳1,320,000・19期繰越Σ=0）。**実バグ発見・修正2件（09章の突合が検出）**: ①Receipt確定の保存が新規時のみ→過入金ガード後の再確定で金額修正が未保存（仕訳929,500 vs 入金レコード1,000,000の不整合。ガードを保存前に移動＋常時保存化）②売掛残高一覧が差額自動処理済み(paid)を「一部入金・残440」表示（status=paid を残0扱いに）——c605caa。**文書側の訂正**: 9-7 消費税集計表は自動仕訳（按分振替・入出金起票・償却）にも科目既定税区分が付く実装のため本体列を実測値に更新（税額列は全一致・納税額計算に影響なし）。9-11 Excel DLはChromeの連続自動DL制限（既知）により今夜取得済みの見積書Excelで代替確認
【7/10 午前・② 第2弾続き2】**06_案件・経営管理 完走**（工数7件・行レベル権限のUI反映実証・人件費コスト2件・配賦525,000/175,000/500,000完全一致・SES精算845,000文言一致・No.28+冪等・案件損益・予実対比⚠83%）。**07_特殊取引 完走**（No.29〜35: 赤黒訂正・税行逆転・貸倒損失科目のsysadmin追加・補助科目C003付き貸倒処理・INV-26-005取消・入金差額440自動処理4行・過入金ガード文言一致。章末Σ=11,386,950全一致）。**08_決算 完走**（下書き警告→6月締め→E-5フォールバック（本日日付起票・摘要に利用日）→償却No.38 28,125+二重ガード→前払振替→消費税振替（残高344,500/103,289が文書と一致）→法人税等→全期間締め→第19期作成→**翌期繰越: 期首14行が文書と1円違わず一致・Σ=0**→年度クローズ+締め済み年度ガード）。**総合検算: 41仕訳・Σ借方=Σ貸方=11,852,675・番号重複0 = 全仕訳台帳の機械検算値と完全一致**。発見・修正: ①月額契約seedのproject_id NULL（99fa94b・案件損益の売上漏れ根治）②定期請求の採番順が文書と逆（99c1eab・docs訂正: No.3=年額前受/No.5=月額、INV-26-002=年額）③JournalEntry二重税抜化バグ（526145d）とその修正が生んだ下書きline_no未採番回帰（4866f78）——**いずれもE2E実機実行が発見**
【7/10 朝・② 第2弾続き】**04_会計コア 完走**（No.16〜24: 貸借不一致ブロック・確定後閲覧専用・締め済み/年度外3ガード・定型2種・非課税・入出金起票2件・経過措置80%（10,200+800）・外税・CSVインポート（G002貸借不一致スキップの文言完全一致）・元帳「諸口」表示と残高2,708,700一致・章末Σ=9,091,800/現金81,300/小口80,000/未払金55,000/預り金450,000 全一致）。**実バグ発見・修正（526145d）**: RegenerateTaxLines が非冪等で「下書き保存→確定」「確定失敗→再確定」の順路が二重税抜化で必ず貸借不一致になる → 税行生成を確定時のみに変更・失敗時 RestoreInputState で巻き戻し・旧仕様下書きは読込時復元（mod.cs 変更のためサーバ再起動を伴うデプロイ手順も実施）。**05_銀行 完走**（No.25〜27: プレビュー→取消→再取込→登録→重複スキップ5件・対象外2件・実AI推定が「ｻｰﾊﾞｰﾎｽﾃｨﾝｸﾞ」に通信費を正答・一括起票3件・**残高照合 全4日付○一致**（実行日7/10のため文書の代替残高値を使用）・仕訳ルール育成・明細一覧トレーサビリティ。章末Σ=9,180,350/普通預金2,620,150 一致）
【7/11・ユーザーレビュー5点のUI改善（ADR-0020）】①**サイドバー=フレーム入口のみ**（業務メニュー階層廃止・経費/工数はホームのショートカットへ移設。URL は OtherPageModuleDesigns 登録で不変） ②ショートカット9個（承認待ちは承認者+のみ・ロール依存は右寄せ配置） ③**経費申請の検索条件レイアウト**（件名=1行占有・利用日=年月日ピッタリ340px・ラベル折返し解消） ④**マスタの確認ページ分離**（ユーザー方針「マスタを直接見せない」: 編集画面=会計フレーム マスタ/取引先・案件へ移設＝他ロールは画面到達不能／販売管理には閲覧専用の取引先一覧・案件一覧=PartnerView/ProjectView 新設。データ読取は全員のまま=セレクト参照維持。予算明細の新規作成/削除ボタンも撤去） ⑤**Logout 分離＋確認ダイアログ**（app.css で sticky 下端固定=CLB本体CSSに!important必要・index.html の capture リスナで自前オーバーレイ=ネイティブconfirm不使用）。**4ロール実機検証済み**（admin/経理/承認者/営業一般: メニュー・ショートカット出し分け・一覧ボタン非表示・編集画面ゲート・キャンセル/OK両動作）。docs/tests/11・06・09 を fork で追随（14ファイル・残存表記ゼロ）・docs/12 に FB-010追補+FB-024新設
【7/12・ユーザーレビュー第2弾8点=フレーム再編第2弾（ADR-0021）】①**フレーム名から Frame 除去**（URL=/Sales/Quote 等・旧URL非互換はユーザー了承） ②**経営管理を予算管理（Budget）と案件管理（Projects）に分割**（工数入力は Projects の TOP に昇格） ③**経費精算フレーム（Expense）新設**（申請/申請中/承認待ち/通知。Main に入口・承認待ちリンクは読取ゲートで承認者+のみ） ④**ホームショートカット再編**（全員=経費申請・工数入力、経理+=振替伝票・銀行明細取込・月次推移表・資金繰り予測。申請中/承認待ち/通知ボタン廃止。ショートカットは PageFrame 指定で「左メニューごと遷移」） ⑤**ロール可視性の全面精査**: 定期請求/実行/SES/請求書/入金に経理読取ゲート（営業の販売管理から自動消滅）・検収の確定/請求書作成ボタンを経理限定（営業=下書きまで） ⑥**検索条件幅の全モジュール規約化**（ExpenseRequest 原器の型別幅を38モジュールへ一括適用。ラベル幅は係数15→18px/字に実測補正して折返し根治。Project/ProjectView は検索新設） ⑦ホームショートカットのユーザーカスタマイズ要望は FB-025 として CLB へ提案。**4ロール実機検証オールグリーン**（admin=7入口/経理=6入口+SC6/承認者=4入口+SC2・販売5リンク消滅・検収ボタン非表示/一般=3入口・承認待ちなし。詳細= ADR-0021 の検証表）。docs/tests/06・09・11 は fork で追随
【7/13・ユーザーレビュー第3弾（6点）＋全状態データ投入＋全数チェック】
- **実バグ修正**: 非経理で見積/受注詳細を開くと "No permission to read module" → 真因=明細の税区分セレクト＆見積書出力スクリプトが参照する TaxCategory/TaxRate の経理読取ゲート。**参照マスタ（一般のセレクト・スクリプトが読むもの）は読取全員・書込経理の原則**に戻して根治（9461782）
- UI改善4点: 案件管理に案件一覧を追加／**検索条件エキスパンダを全65モジュールで初期展開**（IsExpanderDefaultOpened）／**ユーザー表示名を個別化**（25名・「部門 役職 姓名」形式。DDL seed・DB・摘要/通知等の埋込テキスト・docs/tests/06/09/10/11 を追随）／**全画面先頭に説明文**（一覧=ListPageDesign.HeaderTitle を説明文スロットとする規約＋app.css で補足テキスト風に降格。Detail 型9画面は DescLabel。ホームショートカットのユーザーカスタマイズ要望は FB-025）
- **全状態データ投入**（投入前 DB は LocalData\backup\accounting_v1_demo_20260713_prepopulation.db に退避。第18期を再オープン=4〜6月のみ締め）: 取引先8（免税・取引終了含む）・案件7（SES2本目・internal・completed）・**補助科目 seed（バックログ回収）**・工数24行・人件費9名分／経費申請の全状態（承認待ち2種・**2段ルート中間**・却下差し戻し・**精算済み settled**）／見積 draft→受注→検収draft→経理確定→請求書→**一部入金 partial**／仕入先請求書 受領（期限超過⚠）・未払計上・支払済／銀行明細 **preview・未起票・起票済**＋手修正フロー／8月分定期請求（月額+年額按分）／SES **下限割れ控除**パターン（INV-26-007）／外税振替伝票・入出金起票・定型起票下書き・CSVインポート下書き／固定資産新規+償却仕訳／営業部予算12ヶ月。仕訳 No.42〜55 前後を生成
- **全メニュー×全ロール巡回チェック（108チェック）**: 経理40+・admin13・一般10（+会計直URL=GATE 正常）・承認者6 — **エラーゼロ**。唯一の発見=承認者が経理専用 Detail 型（/Budget/BudgetEntry）へ直URLで入ると「不正なアクセスです」ブロックに加え Blazor 未処理例外バー（通常操作で到達不能・FB-026 として docs/12 に記録。アプリ対処は原則に反するため見送り）
- **注意**: 現 DB は docs/09/11 の帳票期待値とは一致しない（データ投入のため）。E2E 直後状態はバックアップから復元可能（docs/09 冒頭に手順）
【7/13 夜・ユーザーレビュー第4弾（3点）＝フレーム再編第3弾（ADR-0022）】①**財務帳票フレーム（Reports）新設**——会計から帳票8本（試算表/BS/PL/月次推移/C/F/株主資本等変動/消費税集計/資金繰り予測）を分離・「帳票/」接頭辞廃止・会計は仕訳/帳簿/銀行/設定の15リンクに縮小（帳簿=起票中の道具なので会計に残す線引き） ②**マスタ管理（経理）フレーム（Masters）新設**——取引先・案件（←会計）＋取引先口座（←購買管理）を集約。**「システム管理」は表示名を「マスタ管理（システム）」に改名**（フレーム名/URL=Admin のまま）。会計年度（締め操作）・銀行口座マスタ/仕訳ルール（取込WFと密結合）は移設しない ③**取引先口座の新規作成不可はバグではなく設計**（partners テーブルの別ビュー・作成は取引先マスタ側）→ HeaderTitle に説明追記で解消。追随: ホームSC（月次推移/資金繰り）を Reports へ・Home.mod.cs/FbExport.mod.cs の文言・AdminHome・docs/05/06/09/11 一括更新（fork 分業・52箇所+）・ADR インデックス 0016〜0022 補完。designcheck 0件・実機確認=admin 5フレーム巡回＋一般で入口非表示。フレームは10枚に。**同日追記**: 追加レビューで**口座マスタ→「自社口座」に改名して Masters へ移設**（自社口座の編集頻度は低くマスタ扱いが妥当・ユーザー指摘）・取引先口座/承認待ち/検収の説明文修正（ユーザー指定文言）
【7/14 未明〜朝・初見エージェントUXテスト（ユーザー発案の新手法）】**アプリを知らないAIエージェント7体**（一般・承認者・経理×4・sysadmin）に社員ペルソナと業務ミッションだけを与えて実操作させ、詰まり・迷い・質問=説明不足箇所を収集（docs/tests/13 に全記録）。**35タスク中34完了**・唯一の詰まり（検収の請求書作成無反応）も実機再調査で**アプリ正常**（自動操作の座標クリック空振り）と判定。発見33件→**同夜3ラウンドで16件即応**（有効フラグ既定ON・帳票の当年度既定値=普通預金マイナス見え問題の根治・一括起票の伝票No.表示・文言/凡例系多数）。ハイライト: ①全ペルソナが日付入力の6桁問題を再現（CLBへのFB筆頭候補） ②未配賦人件費480,004円がどこにも出ない（U6-4・改善最優先バックログ） ③sysadmin役がADR-0023の画面説明だけで承認ルートの仕組みを完全に読み解き・消費税12%改正手順も正しく導出（内税計算の税率マスタ連動もコードで裏取り） ④テストが作った業務データ（販売チェーンQ/SO/A-26-004+INV-26-009・仕訳No.56〜66・費目「印刷製本費」・ユーザー高橋健・第19期予算等）は**ユーザー確認用に残置**。バックログはU番号で docs/tests/13 が正
【7/14 未明・承認ルート判定のマスタ化（ADR-0023・ユーザー要望「絶対に不具合を入れてはならない場所」）】「費目×金額→テンプレート」の対応が SelectTemplateName() のテンプレート名直書きだった問題を根治——**approval_route_rules マスタ新設**（優先度 first-match・費目 NULL=全費目・金額下限/上限〔両端含む・上限NULL=無制限〕・**テンプレートID参照=改名安全**）＋管理画面「マスタ管理（システム）＞承認ルート判定」（読取=全員/書込=sysadmin）。**EXP_APPROVAL_MID/HIGH は制度閾値から廃止**（seed 移行済み・OVERRUN_RATE は存続）。承認エンジン（ApprovalFlow.mod.cs）は無変更＝名前返却契約を維持。**検証16ケース全PASS**: 境界±1円×4境界（29,999/30,000/199,999/200,000）・1円/500万・交際費優先・事前申請見込み判定・実費超過のルート跨ぎ再承認（attempt2・部長のみ再解決）・**UI新規作成テンプレート＋ルールへの誘導/フォールスルー/無効化/削除の1周**。テストデータは全削除し全件数とΣ=14,505,495 がテスト前と完全一致。発見: 使用済みテンプレート削除は FK で正しく拒否されるが生 SQLite エラー表示（改善候補）
【7/16〜17・UXテストバックログ全消化（ユーザー回答に基づく自律実装・レビュー第4弾続き）】docs/tests/13 の相談事項にユーザーが全回答→自律走行で実装。**Q1/U1-2**: 一覧の削除ボタン全面撤去＋詳細画面に「自分が起案かつ精算=下書き」限定の削除ボタン（Creator は DataOnlyFields 登録が必須という新知見。API直叩き対策はドキュメント記録のみ=合意）。**Q1'/U3-3**: 経理専用「精算処理待ち」ビュー新設＋申請一覧に部門列/部門検索（申請時スナップショット方式・ddl/270）。**Q2/U2-6**: 領収書未添付は警告トーストのみ（申請は通す）。**Q3/U6-4**: 配賦一覧に⚠未配賦（個人別）/（端数）/【合計】行（480,000+4円+5,220,000 の検算一致を実測）。**Q4/U7-1**: 退職者無効化——is_active＋認証を active_app_users ビュー経由（無効→Login failed→再有効化→成功を実測。admin seed は INSTEAD OF トリガーで担保・承認者解決3箇所からも除外）。**Q5/U4-5**: 請求書を下書き始まりに（「発行する」ボタン・明細ガード・売掛/入金は draft 除外）。**U4-3**: 参照ドロップダウン4箇所を「番号 取引先 件名」表示（select_label 列＋DBトリガー保守=全INSERT経路と取引先改名に自動追随・ddl/280）。**U4-2**: 請求書に検収選択（confirmed絞り）。ほか一括GO分: U2-2凡例/U2-4・5遷移説明/U4-1ボタン色/U4-4税区分絞り/U5-1プレビュー残置バナー/U5-4伝票No列/U6-1暦月併記/U6-3年度累計明記/U7-6決め方ラベル/U7-8ログアウト日本語化（index.html方式・CLBローカライズ要望は FB-027 起票）。全項目実機検証済み・docs/tests/13 の対応欄を✅更新
【2026-07-19・ユーザーレビュー第5弾＝全9タスク自律完遂】①総勘定元帳「絞り込むと0件」の真因解明（絞り込み機構は正常。補助科目セレクトの科目カスケード新設・入金消込仕訳に案件引継ぎ・補助科目マスタ重複2件削除・単独日付検索欄 340→170px）②**状態遷移の一元化（ADR-0026）**: 全文書の状態フィールドを表示専用化＋必須化・遷移はボタンのみ（見積: 送付済にする/失注にする、受注: 完了にする 新設）・逆遷移を全機能に設計実装（見積/請求書の下書きに戻す・検収の確定取消・入金取消・未払計上/支払取消=該当仕訳を削除、締め期間・下流ドキュメント存在ガード付き）・一覧の行削除を文書7種から全面撤去し詳細画面の条件付き赤ボタンへ。**新知見: 子持ちモジュールの検索インスタンス Delete() は静かに失敗する→明細→親の順に削除し全戻り値を検証。削除対象を FK 参照する列（vendor_invoices.accrual_entry_id 等）は先に null にして Submit** ③検収の詰み解消（請求書作成済みなら「INV-xx 作成済み」ラベル・確定後は保存非表示）④**ボタン色2色規約（ADR-0027）**: 青=前進/赤=取消・削除の83ボタン一括適用（Info/Success/Warning/Secondary 廃止）・無効ボタンは元色の半透明 ⑤ログアウトの index.html 自前JS（確認ダイアログ・日本語化）撤去=CLBネイティブ化（英語 Logout 受容・FB-027 は存続）⑥**1ロール1ページフレーム再編（ADR-0028）**: General/Approver/Accounting/Sysadmin の4フレーム＋ルート Main は RoleDispatch による振り分け専用・折りたたみ階層メニューで段階的開示・Modules を11フォルダに再分類・固定パス NavigateTo 8箇所を GetModuleUrl 化 ⑦貸借対照表を勘定式（左右対照）化=Excel も同形式（資産合計=負債・純資産合計 8,262,573 の一致を実測）⑧マスタの一覧削除→無効化統一（有効フラグ持ち10種）＋科目参照セレクトの候補絞り込み補完 ⑨docs 総点検（本ファイルのセッションログ整列化・ADR インデックス補完・Project.md 知見追記）。4ロール実機検証・designcheck 0件
【7/20〜21・ユーザーレビュー第6弾＝対話レビュー→キュー消化の自律走行（全12タスク）】
- **CLB 1.3 追随**: デザイナ自動修正の差分精査で HorizontalAlignment 旧値の静かな左寄せ化を発見→全箇所を新列挙値へ移行（4251148・FB-031）。閲覧専用画面のボタン無反応（this.IsViewOnly がボタンクリックも抑止）を per-button IsViewOnly=false で解消（b88d36e・FB-030）。一覧 Excel の数値列文字列化は CLB 制約として記録のみ（4e962bd・FB-032・ユーザー了承）
- **販売の変更契約対応（ISSUE-0002・5661d15）**: P1=検収発生後の受注ロック・P2=受注に対する検収累計と超過警告・P3=運用規約文書化・合算請求（acceptances.billed_invoice_id + InvoiceLookup。隠れ SelectField の権限例外も InvoiceLookup 化で根治）。見積は下書き以外編集不可（14c4bc1）
- **UX改善バッチ（a3c23a7 ほか）**: 通知の未読戻し・参照/名称系入力欄の1行化スイープ・検収の general/approver 読取エラー修正（eb7c580）・検収からの請求書作成を下書き開始に・入金一覧のクエリ導出化（消込状態列＝LEFT JOIN 導出・DB非正規化なし）・振替伝票一覧の軽量クエリ一覧化（4.0s→1.0s・金額列追加）
- **銀行明細 v3 全面再実装（ISSUE-0003・1df3cf0）**: 仕様検討 v1→v3 をユーザーと往復（367bc35〜71062df）後に実装。①明細取込=プレビュー専用テーブル bank_statement_preview（ddl/310）＋スナップショット差分同期（編集・行削除は保存まで DB に書かない）＋登録時ルール適用 ②一括起票（BankPosting 新設）=対象外チェック・ルール/AI はメモリのみ・候補科目で一括起票 ③明細一覧=表示専用の通帳ビュー＋詳細画面（対象外化⇄差し戻し・起票の取り消し=仕訳ごと削除・締め済みは赤伝誘導・明細削除）。preview 状態は廃止（3値化）。一巡をブラウザ実機検証しテストデータ完全ロールバック（明細14件・仕訳72件 No.70 のベースライン一致を確認）。docs/tests/11 05_銀行・06 S-13 を全面追随（BNK-02b/09 新設）
- **残高照合の修正**: 差異の真因＝bank_statement_lines.balance が 7/13 データ投入時の帳簿前提のまま、後続の入金仕訳（+500,000/+880,000/+2,750,000 等）で帳簿が先行したこと。帳簿を正とし、日付ごとの帳簿残高に一致するよう明細残高を再計算（同日内は通帳連鎖で逆算）→ **全11日付 ○ 一致・差額0** を実機確認（データ修正のみ・コード無変更）
- **通知の仕上げ（21c3743）**: ホーム未読リストから「未読に戻す」を除去（専用レイアウト HomeUnread）。行状態によるボタン出し分けは CLB では不可能と実測（ListLayout 初期化イベントは行に配られない）→ Project.md 知見化
- **新知見**: 一覧→詳細のフレームリンクは ModulePageType "Auto"（"List" だと詳細が真っ白）／ListField 自動ロードの OnDataChanged は描画通知を伴わない（初期サマリは OnAfterInitialization で明示 Reload）／docs 再編（テスト類→docs/tests・検討文書→docs/issues=ISSUE-0002/0003）・SaaS/SES 一巡シナリオ追加（bb6419a）
【7/22〜23・ユーザーレビュー第7弾＝販売改修パッケージ（テスト計画先行の自律走行・docs/tests/16）】
- **小粒改善（7/22）**: 受注ロック文言から内部ドキュメント参照を除去（9c221a8・UI文言に docs パスを出さない）／売掛残高に検索条件新設=取引先（後にマスタ参照ドロップダウン化 685a303）・状態（既定「入金済を除く」=OnSearchInitialization）・支払期限（f56e146）／銀行明細一覧の未起票行を黄色ハイライト（9af94a5・**新知見: ListLayouts[""].OnAfterInitialization は行モジュールごとに発火**し、ClassName付与＋app.css の tr:has() で行全体に背景を効かせられる）
- **明細の単位列（4860b16）**: 見積/受注/請求書明細に unit TEXT（ddl/320）・全コピー経路引継ぎ・定期/SES 生成は「式」・帳票テンプレ2種へ列挿入（Excel COM で編集・PDF は同テンプレ経由で自動反映）・実出力検証済み
- **販売伝票への部門付与（ADR-0029・30149f1）**: 部門は案件でなく伝票が持つ（仕訳明細の部門×案件独立2軸と相似・発行時点スナップショット=ddl/270 と同思想）。quotes/sales_orders/invoices/recurring_billings に department_id（ddl/330・作成者部門でバックフィル）・新規時は作成者所属を初期値・売上仕訳（検収確定/定期実行）へ伝搬→総勘定元帳の部門絞りで部門別売上が成立。一気通貫実機検証（見積→受注→検収確定→請求書・仕訳全行部門付き）は**バックアップ→復元方式**でデモデータ無汚染
- **定期請求契約の確定フロー（ADR-0030・fda44de）**: status draft/confirmed 新設（ddl/340）・実行対象=「確定済かつ有効」・権限マトリクス=一般/承認者は閲覧+下書き起票・経理のみ確定/差戻し/有効切替（確定済は非経理に読取専用+🔒注記）・「定期請求契約」に改名・課金サイクルで月額/年額欄出し分け・保存下に運用注記4点（年額=開始月+応当月・1/12按分の補足含む）・一般/承認者フレームへ定期請求契約・売掛残高リンク追加。3ロール実機検証（下書き実行対象外→確定後生成・金額未入力の確定拒否・差戻し）
- **見積→定期請求契約の変換（89779e7）**: 「定期請求契約にする」ボタン（下書き契約を生成し詳細へ遷移・月額=税抜合計仮置き・開始月=翌月・見積は accepted・二重変換ガード）。**実測知見: LoadingService を MessageBox の前に開始するとダイアログが塞がれ操作不能**（Project.md 記録）
- **検索条件の拡充（1b81043）**: 見積/受注/請求書に取引先・案件・部門、売掛残高に案件・部門を追加（負のテスト含め DB 分布と一致を実機確認）。**検収の受注経由（リンク先参照）検索は CLB 非対応と実測判明**——designcheck 通過・UI 描画までされるが検索コンテキストで候補がロードされず空（静かな失敗・FB-033 起票）。ユーザー指示により非正規化はせず検収は現状維持

## 2026-07-25 ユーザーレビュー第8弾（販売・財務帳票）

全6項目を実装・designcheck 緑・デプロイ＋サーバ再起動・実機検証済み（経理=soumu_bucho／一般=eigyo_ippan の2ロール）:

- **帳票ファイル名の改善**: 見積書・請求書の Excel/PDF を `見積書_{発行日yyyyMMdd}_{番号}_{相手方}_{件名}.xlsx` 形式に（Windows 禁止文字は「-」へサニタイズ。実ダウンロードでファイル名確認済み）
- **部門の必須化＋経理限定編集（ADR-0031）**: 4伝票の DepartmentRef を IsRequired 化・非経理は IsViewOnly（自部門固定）・SES 生成分は「全社共通」既定。EmptyCandidateType は Null のまま（検索の「指定なし」を守る。IsRequired でも検索空選択肢は残ると実測）。既存データは ADR-0029 バックフィル済みで NULL ゼロ
- **請求書の状態ロック**: 下書き以外（発行済/一部入金/入金済/取消）は IsViewOnly＝閲覧専用。操作ボタン（下書きに戻す・取消・Excel/PDF）だけ個別解除（見積と同パターン）。遷移ハンドラは Submit 前に一時解除
- **明細の列幅調整**: Quote/SalesOrder/InvoiceLine の ListLayouts に Width 指定（数量70・単位70・単価110・金額120・税区分150・摘要=auto で残り全部）。**新知見: ListElement.Width（px・null=auto）で一覧列幅を固定できる**
- **入金予定の自動作成（ADR-0032）**: 発行済み化の全経路（手動発行/取消戻し/定期実行/SES）で未確定入金を自動作成し消込 ToDo 化。SumReceipts は消込済みのみ集計に変更・巻き戻し/取消時は予定を自動削除。発行→予定作成→巻き戻し→予定削除→再発行の一巡を実機検証。既存発行済み5件にバックフィル（temporary/backfill_pending_receipts.sql）。**⚠ docs/tests/11 の入金章は「未確定の行を開いて確定」への読み替えが必要（文書改訂は持ち越し）**
- **P/L のカテゴリ太罫線**: ProfitLoss.mod.cs 新設（行初期化で小計行/段階利益行にクラス付与）＋ app.css の tr:has() で太い下罫線・段階利益は太字＋薄背景（BankStatementLine の行スタイルパターンを転用）

### 同日・追加検討（ADR-0033: 自動生成請求書の発行取消と入金フローの一本化）

ユーザーの問いかけ（「定期生成の請求書の取り消しが赤黒しかないのは仕様としてどうか」「入金の手動作成・削除は不要では」）→回答→GO の手順で仕様確定・実装・実機検証済み:

- **「発行を取り消す」新設**: 定期/SES 生成請求書（recurring/recurring_annual/ses）は下書きに戻さず**生成物ごと削除**（関連仕訳＝年額なら前受計上＋全按分振替・入金予定・請求書本体）。冪等ガードが請求書存在判定のため削除＝未生成に戻り再実行で再生成可能。ガード=消込済み入金なし＋全仕訳の期間未締め（締め済みは赤黒訂正＋void を案内）。生成分は「下書きに戻す」を非表示化
- **按分振替に void 条件追加**: アンカー年額請求書が void なら按分をスキップ（締め済みで削除できない場合の停止手段。従来は void しても按分が止まらない穴があった）
- **入金の手動新規作成・未確定削除を廃止**: 入金一覧の新規作成撤去・新規URL直打ちは一覧へリダイレクト・未確定の削除ボタン撤去（スクリプトの削除能力は温存）
- **一部入金の確定時に残額分の入金予定を自動作成**: 不変条件「未回収の発行済み請求書には残額分の未確定予定が常に1件」が成立し未確定=消込ToDoが完全に。INV-26-002 に残額 820,000 円をバックフィル
- **データ対応（ユーザー依頼）**: INV-26-014（年額8月分）と仕訳 No.74/75 を SQL 削除（temporary/delete_inv014.sql。契約は確定済のまま＝定期実行で再生成される）
- **検証は DB バックアップ→復元方式**: 発行取消→全削除／再実行→再生成（年額3点セット＋予定）／年額 void→翌月按分スキップ（他契約は正常）／部分確定 165,000→100,000 で残額 65,000 予定の自動作成、を実機確認しデモデータ無汚染で復元
- **【7/26 追補】入金取消時の予定統合（ADR-0033 追補）**: 確定→即取消で未確定が2行に割れるとユーザー実測報告（確定は予定を作るが取消が畳まない非対称）。取消の最後に「他の未確定予定を削除＋本人の行を残額に更新（備考差替え・統合トースト）」を追加し、確定・取消をどの順で繰り返しても「未確定はちょうど1行＝残額」に収束させた。単一確定（22M取消→33M統合）・複数確定（10M+20M消込済+3M予定から10M取消→3M削除・13Mに統合・20Mとpartial不変）の両系を実機検証。既発生の INV-26-014 2行状態は SQL 統合（temporary/consolidate_inv014_pendings.sql）

## 2026-07-26 定期請求実行のプラン方式（ADR-0034）

ユーザー要望（対象月の生成対象と金額を実行前にリストで見たい・ただしプレビューと実行のロジック二重化は不可）
→ プラン方式を提案・GO → 実装・designcheck 緑・デプロイ・実機検証済み:

- **BuildPlan(対象月) に判定を一元化**: 期間・年払い周期・冪等（生成済み）・金額の判定は BuildPlan だけが行い、
  結果を `recurring_run_plan`（ddl/350・全行洗い替えの一時テーブル）へ書き出す。プレビュー一覧
  （RecurringRunPlan.mod.json・ListField＋明示 Reload）はテーブルを表示するだけ、
  実行は押下時に BuildPlan を再実行して planned 行を plan_kind（monthly/annual/defer）どおり機械的に消費
  ＝**プレビューと実行がズレる余地が構造的にない**
- **一覧の列**: 契約/取引先/部門/種別/請求額(税抜)/消費税/按分振替額/状態/内容/請求書番号。
  状態=生成予定・生成済み・対象外（下書き/無効化/起点月未実行/void 停止は理由文つき）。
  年払いは「按分振替のみ（n/12ヶ月目）」まで周期判定込みで正確表示
- **実行後の書き戻し**: プラン行を「生成済み」に更新し仕訳 No・請求書番号を表示（実行画面がそのまま結果一覧に）
- ボタン名を「対象月の請求書・仕訳を一括生成」へ変更（按分振替も作る実態に合わせた）
- 会計処理・冪等判定・入金予定の自動作成は ADR-0033 までの仕様と完全同一（判定の置き場所と UI だけ変更）
- **実機検証**（DB バックアップ→復元・デモデータ無汚染): 生成済み月（7月）/未生成月（9月）の表示・
  9月実行で請求書2＋仕訳4＋予定2生成と全行「生成済み」化・下書き/無効化契約の対象外表示、いずれも期待値どおり
- **⚠ docs/tests/11 の定期請求章は旧ボタン名・旧結果表示前提のため読み替えが必要（文書改訂は持ち越し）**

## 2026-07-26 取引先口座の全欄検証と口座削除（ユーザー仕様提案→GO）

各欄が一部空白でも登録できてしまう問題への対応。ユーザー提案の仕様（登録=全欄必須検証／削除=確認して全欄クリア）に
「検証基準の FB 共通化」「ボタン名は口座登録/口座削除」「支払予定チェックは注意書きのみ」の合意を加えて実装・実機検証済み:

- **口座登録（Primary）**: 口座5項目（銀行4桁/支店3桁/種別/口座番号7桁/受取人名カナ）を全銀基準で検証し、
  不備があればエラートーストで登録拒否。全欄空白も「口座情報が入力されていません」でエラー
- **口座削除（Danger）**: 確認ダイアログ（取引先自体は削除されない旨を明記）→ 全欄クリアして保存。
  未登録時は警告のみ。ボタン行の下に赤字の注意書き（未払いの支払予定がある取引先の口座削除は振込データ作成がエラーになる）
- **検証ロジックの共通化**: 正典を PartnerBank.mod.cs（AccountValidationError/IsDigitsLen/KanaError）に置き、
  FbExport は取引先ごとの検証を partner.AccountValidationError() 呼び出しに置換（自社側項目も new PartnerBank() 経由で同基準）。
  モジュール間スクリプトメソッド呼び出しが designcheck・ランタイムとも通ることを実測（Project.md 7/26 知見）
- **一覧に「口座種別」列を追加**（銀行/支店/口座種別/口座番号/受取人名の順）
- **実機検証**: 部分入力→登録拒否・カナ不正（小文字/全角）→拒否・全欄正常→保存・削除ダイアログ→クリア保存・
  空で登録→エラー・空で削除→警告・FB生成で全角カナ取引先が共通メッセージで除外、をブラウザで確認

## 2026-07-26 体験シナリオ 17（経費精算一巡）・18（購買一巡）を追加

ユーザー依頼（販売系 14〜16 と同形式で経費精算・購買の全機能を最初から最後まで体験できる詳細シナリオ）:

- **17_体験シナリオ_経費精算一巡**: 開発2部の 5 申請（立替/交際費/備品10万以上/事前申請→実費超過再承認/取引先へ支払）で
  申請→並列OR承認→却下・再申請→総務併載→経理の精算処理待ち→仕訳生成→支払→完了を一巡。AI読み取り・複製・キャンセル・
  検索・ロール別ボタン出し分けも網羅。仕訳目安 No.83〜92
- **18_体験シナリオ_購買一巡**: 既存デモデータ（受領2・未払計上済2）を物語に組み込み、受領登録→未払計上とその取消→
  支払予定表→取引先口座マスタの整備（口座登録の検証エラー体験・口座削除・空欄ガード。本日実装の新仕様を反映）→
  FB生成1回目の除外4件（未登録/小文字ｮ/全角カナ）→マスタ修正→2回目全件生成→支払登録→支払取消→複製・削除→検索を一巡
- 期待値は現在のデモDB（最終仕訳 No.82・買掛金 99,000）を SQL で実測して記載。アルタイルの受取人名に半角小文字「ｮ」
  （U+FF6E・全銀不可）が含まれる事実も実測で発見しシナリオの教材に組み込んだ

## 2026-07-27 相殺入金の本格対応（ADR-0035・ユーザー指摘→案B で GO）

「入金方法: 相殺」で確定しても D 普通預金/C 売掛金 が生成されるバグ（ユーザー発見）への本格対応:

- **借方科目を入金方法で切替**: bank→普通預金1020 / cash→現金1000（これも潜在バグだった）/ offset→買掛金2000
- **相殺は仕入請求の連動消込とワンセット**: receipts.offset_vendor_invoice_id（ddl/360）＋「相殺する仕入請求」Select
  （相殺選択時のみ表示・vendor_invoices.select_label をトリガー保守）。確定時に 未払計上済み（accrued）・同一取引先・
  税込額=入金額（全額消込）を検証し、消込仕訳 1 本（D 買掛金/C 売掛金）で仕入請求を paid に連動（payment_entry_id/paid_date）
- **取消経路は入金側に一本化**: 入金取消が仕入請求を accrued に巻き戻し（FK 参照解除→仕訳削除の順序）。
  買掛側の「支払を取り消す」は相殺消込を検知してブロック＋入金画面へ誘導（二重払い防止）
- **差額の振込手数料自動処理は bank のみ**に限定（現金・相殺の不足額は手数料ではない）
- 見送り: 仕入請求の一部相殺（AP 側に消込残高の概念が必要）・複数仕入請求の同時相殺（入金予定の分割で代替可）
- **実機検証**: 表示切替・金額自動セット・取引先不一致/金額不一致/未払計上前エラー・正常系（仕訳 D2000/C1100・
  仕入請求 paid・請求書 paid）・買掛側取消ブロック・入金取消での完全巻き戻し、をブラウザ＋SQL で確認

## 2026-07-27 SES 精算・請求のプラン方式化（ADR-0036・ユーザー要望）

## 2026-08-02 夜 ログイン後ポータル着地とホーム動線の一元化（ADR-0042・ユーザー指摘→GO）

「ログイン直後は自分が見られる業務へのリンクが並ぶ姿を期待する／階層の抽象度が混在して気持ち悪い／
『ほかの業務』が分かりにくい」というユーザー指摘に回答→方針承認（折衷案なし）で実装:

- **RoleDispatch → PortalHome に改名**（rename-module CLI）し、権限別自動転送を廃止。アクセスできる部品の
  タイル（7部品＋システム管理・Primary青・ADR-0027 準拠）を表示、クリックで変種フレームを解決して遷移
- **全13フレームのサイドバー先頭に「ホーム」**（→ Main/PortalHome）。既存の部品トップ「ホーム」は「トップ」に改名。
  「ほかの業務/○○」リンク計 45 本と ExpenseHome/SalesHome の切替ボタン群を全撤去（Python スクリプトで一括 JSON 手術）
- **実測知見（docs/12 に FB 起票）**: FB-035 表示専用モジュールのボタンが pointer-events:none でクリック不能
  （IsViewOnly=false で解除）／FB-036 rename-module がレガシー TopPageModule を追従しない（手修正）／
  FB-037 非表示フィールドの固定幅カラムが空のまま残る（app.css の :has() で空カラムを畳んで解消）
- **検証**: designcheck 0件・check_navigation.py エラー0・デプロイ＋サーバ再起動後、
  admin（システム管理のみ）/soumu_bucho（7タイル→会計）/kaihatsu1_ippan（2タイル→経費）/
  kaihatsu1_bucho（3タイル→経営管理=Approver変種）/eigyo_ippan（3タイル→営業）と
  「ホーム」での往復をブラウザ実機確認
- 文書追随: ADR-0042 新規・docs/10 §2/§5/§6/§7・docs/tests/19 読み替え表（「ほかの業務」→ポータル経由）
- 運用知見: サーバは**BusinessApp.Server プロジェクトディレクトリを CWD にして起動**しないと appsettings が
  読めず全ログインが 500 になる（Content root は起動時 CWD）

## 2026-08-03 未明 レイアウト崩れの全画面スイープと修正（ユーザー指示: UX最優先・驚き最小）

ユーザーから「使いやすさをゴールに・驚き最小の原則」の恒久指針と「折返しレイアウト崩れの修正」指示。
全 13 フレーム×全サイドバーページ＋主要詳細ページを、広幅（2352px）/1514px/1344px の 3 段で
JS スイープ（ボタン/ラベルの折返し検出 = rect.height≥lineHeight×1.85・main の横はみ出し検出）:

- **折返し修正（幅不足の固定列）**: ExpenseRequest 添付ラベル2件（140→160/240）／通知一覧「未読に戻す」列
  （110→130）／固定資産「除却・売却日」（100→120）／JournalImport 下書きチェック（400→460）／
  H2 センタータイトル 3 件（MatchingRule 360・InvoiceTransitionRate 460・ApprovalFlowTemplate 340。
  3 分割 auto 列は 1/3 幅しか取れず狭幅で折れるため Width 明示）
- **検索欄の見切れ解消**: 全 48 モジュールの検索行を `IsWrap: true` に一括変更（1344〜1514px で請求書等の
  検索欄が右に見切れ→折返しに。新規約として Project.md に記録）
- **検証**: designcheck 0件・デプロイ後、修正 7 画面すべて折返し/はみ出し 0 を実機再測定
- **残課題（報告）**: 販売系詳細の明細テーブルは 1344px で横スクロール（→ユーザーが自分で調整すると裁定・
  そのまま）／ApprovalFlowTemplateMember の「承認者の決め方」ラベルが 1344px で折返し（admin 専用画面・軽微）

## 2026-08-03 意味論的な驚き最小レビュー第1弾（起票系フォームと状態表示）

全画面ツアー（レイアウトスイープと兼行）に続き、起票系フローを実操作で精査（データ無汚染・Submit なし）:

- **確定済み仕訳の詳細に案内を追加**: 従来は操作ボタンも状態表示も無く「なぜ編集できないのか」が画面から
  読めなかった → 「確定済みのため編集・削除不可。訂正は反対仕訳（赤黒訂正）で」の説明ラベルを
  確定時のみ表示（JournalEntry.PostedNote。ADR-0026 の赤黒方針を画面上で説明）
- **「押せるのにエラーになるボタン」の非表示化**: 新規（未保存）画面に出ていた「この申請を複製」
  （ExpenseRequest）「この請求書を複製」（VendorInvoice）を未保存時は非表示に（ガードは防御として温存）。
  Quote/SalesOrder/Acceptance の状態遷移ボタンは既に新規時非表示であることを確認
- **問題なしと確認**: 請求書詳細の「発行を取り消す/取消にする」併存（ADR-0033 承認済み仕様）・
  検索フォーム・ポータル動線
- **軽微・保留**: sticky Logout が短い画面で最下段メニューに一時的に被る（スクロールで到達可）／
  検索行折返し時にラベルと入力欄が別行に割れるケース（1344px・構造上ラベルと入力が別カラムのため）
- 検証: designcheck 0件・デプロイ＋再起動後に 3 修正すべて実機確認

定期請求（ADR-0034）と同じプラン方式へ載せ替え:

- ses_run_plan（ddl/370）＋ SesRunPlan 行モジュール新設（recurring_run_plan への相乗りは洗い替え衝突のため不可）
- 対象月変更で自動再構築（「精算を計算」ボタン廃止）。状態列（生成予定=黄色/生成済み/対象外+理由）で
  旧実装が黙って skip していた案件（無効・契約条件未設定・請求額0以下・void）も理由つきで可視化
- 「対象月の請求書・仕訳を一括生成」は押下時に BuildPlan 再実行→planned 行を機械消費（判定はプランのみ・冪等）
- row-planned の黄色ハイライト CSS を SesBilling にもスコープ追加（app.css）
- **実機検証**: 7月=3件生成済み表示・8月=生成予定2+対象外1（黄色2行）・一括生成で請求書2+仕訳（D1100税込/C4010/C2200）+
  入金予定2・再実行で重複なし「生成対象はありませんでした」、をブラウザ＋SQL で確認
- **⚠ docs/tests/11 の SES 章も定期請求章と同様にプラン方式への読み替えが必要（文書改訂は持ち越し）**

## 2026-07-29 BusinessApp ソリューション移行と CLB 1.3.16 対応（ADR-0037・ユーザー指示）

ユーザー新規作成の BusinessApp ソリューション（CLB 1.3.16／Extras 0.4.0／ApexCharts 0.25.3）へ全面入れ替え:

- **デザイン**: `Designer/Designer/`→`Designer/Design/` 改名（新 claude-workspace 規約）。1.3.16 で読込不能だった
  ExpenseRequest（AITextAnalyzerFieldDesign の型が Extras.Designs へ移動）を修正、新検証で顕在化した PageFrame の
  無効 SortFieldVariable 3 種を整理（承認ルート判定は Priority 昇順に正した）。designcheck 0 件
- **サーバ**: Server.Shared 廃止・Extras.Server 0.4.0 NuGet 化の新テンプレートへカスタム再統合。
  AI は Mock/Claude=in-project・AzureOpenAI=Extras 標準へ委譲（AppAISettings で Provider 切替）。
  BankAi/InvoiceCheck/NtaInvoiceSettings/appsettings（AccountingSQLite・active_app_users）/UserSecretsId 引継ぎ
- **ワークスペース**: ClaudeCodeForDesigner/ は ai-refresh 生成物として Git 追跡外化。ルート .claude/settings.json を
  新 exe（BusinessApp.Designer Release）＋新 CLI 群（rename-*/ai-refresh/field-catalog 等）で更新
- **E2E スモーク（admin/経理 2 ロール）**: 仕訳 CRUD・試算表/BS 貸借一致/月次推移・資金繰りチャート・元帳・
  請求/入金/仕入請求/支払予定・SES/定期請求プラン・銀行取込プレビュー・経費申請＋**AI 領収書読み取り実稼働**・
  インボイス照合・ユーザー管理・hot-reload（zip 検知→無操作反映）を確認。Excel/PDF ダウンロードのみ未検証（持ち越し）
- 旧資産（AccountingApp.slnx・AccountingApp/・Designer/*.old）を削除。経緯・将来の罠は ADR-0037

## 2026-07-31 データソース・DB ファイル改名（ADR-0039・ユーザー提案）

- `AccountingSQLite`→**`BusinessAppSQLite`**・`accounting_v1.db`→**`business-app_v1.db`**（Git 追跡外含め 154 ファイル一括置換＋DB 本体リネーム。ADR-0001 と本台帳の過去ログ・`LocalData/backup/` の旧名バックアップは歴史的記録として温存）
- 検証: designcheck 0 件・sql CLI 疎通（仕訳 79 件）・deploy→サーバ起動→HTTP ログイン E2E（antiforgery＋admin＋current_user）まで実機確認
- ADR インデックス（decisions/README.md）の記載漏れ 0031〜0038 を補完

## 2026-08-02 部品アーキテクチャ再編＋権限モデル刷新（ADR-0040/0041・上司レビュー起点・全 8 フェーズ完遂）

上司指摘「テンプレート再利用の単位＝PageFrame。部品別×役職別に再編」を起点に、ユーザーとの設計議論で
「ロール概念の廃止（部門メンバーシップ×管理者フラグ）」まで確定させ、一気通貫で実装・実機検証:

- **権限基盤**: department_managers→department_members（「一般」追加）・departments に部門種別・
  app_users に is_sysadmin＋権限キャッシュ列3つ（DB トリガーが経路非依存で転記・ddl/380/385）。
  旧 role 列は全検証通過後に廃止（ddl/390・並走方式）
- **PageFrame**: ロール別 4 枚を廃止し部品×対象者の 14 枚へ（Main＋13。全リンクは旧フレームから deepcopy 生成）。
  sysadmin の業務同乗を全廃（職務分掌）・会計マスタ書込を経理へ移管。切替導線＝経理系 7 枚の静的相互リンク＋
  RoleDispatch「マイページ」＋ホームの動的ボタン
- **条件スイープ**: 96 条件（82 モジュール）＋スクリプト 14 ファイルを Role→キャッシュ列へ機械変換。
  営業部品に営業∨経理の読取ゲートを新設（従来は無条件＝開発一般にも見えていた）
- **Home 分割**: 全部品参照ハブの旧 Home を廃止し ExpenseHome/SalesHome/AccountingHome へ
  （各ホームの参照は自部品内のみ・HomeKpiData は AccountingKpiData として会計へ）
- **実機検証**: 5 権限パターンの着地・全切替導線・権限外直 URL 拒否・経費申請→課長 2 名 OR ルート生成→
  受信箱→キャンセル→削除・部門メンバー UI 編集→キャッシュ転記往復・role 廃止後ログイン。
  **削除可能性テスト**: 経営管理=0 件／購買=Receipt 相殺 4 件のみ／工数=SesBilling 1 件のみ（予測と完全一致）
- **文書**: docs/10_部品アーキテクチャ.md（部品定義・依存マップ・外し方・カスタムポイントの正典）新設、
  docs/tests/19 読み替え表（旧ロール・旧 URL→新体制）、FB-034・DOC-016 起票
- **持ち越し**: docs/tests/11・14〜18・docs/09 の本文は旧前提のまま（19 の読み替え表で吸収。全面改訂は次回）。
  SalesHome の KPI 拡充（受注残・売掛サマリ）も後続候補

## 2026-08-05 3軸分離・部課階層・ポータル本格化（ADR-0043/0044/0045・ユーザー設計議論→GO・24h自律）

ユーザーとの設計議論（ポータルのあるべき姿→権限モデルの根本疑問→組織階層）で 3 つの根本改造を確定し、
ドキュメント→実装→検証まで一気通貫（Phase 0〜4・コミット 326f96d/805eb9b/30b8a2d/0ad53de）:

- **ADR-0043 機能権限の直接付与化（3軸分離）**: 「画面権限／組織所属／承認構造」を別構造に分離。
  has_sales_access/has_accounting_access をキャッシュから直接付与の真実へ昇格（列名不変=96条件無修正）、
  can_use_expense/can_use_timesheet 新設（既定ON・adminはOFF=isAdmin特例全廃）、dept_type 廃止、
  is_approver のみ導出キャッシュ（課長/部長行∨テンプレ個人指名。ADR-0041既知課題も解消）。
  ユーザー管理画面は権限チェックボックス化（承認者はview-only＋自動判定注記・削除不可の案内文）
- **ADR-0044 部課階層と walk-up 承認**: departments を自己参照2階層に（課はオプション・node_type導出）。
  役職解決を「所属ノードから親方向に最寄りの該当役職」に一般化——課長決裁は自課の課長のみ・
  課長空席は部長へ自動繰上げ・自己承認回避は特例から一般則へ・重複段の圧縮。伝票部門は部に固定
  （NodeType='dept' 候補絞り16モジュール・CurrentUser.所属部=business_department_id キャッシュ）。
  シードは5部×第一/第二課へ再編。**境界値7シナリオ実機全PASS**（自課のみ/空席繰上げ/経理代替/
  課長本人/部長本人/同一課OR/圧縮=DB段数1確認。検証はDBバックアップ→復元方式）
- **ADR-0045 ポータル本格化**: 業務導線を Main 左サイドバー（PortalSidebar・権限出し分け・変種解決・
  ログアウト自前）に一本化し、PortalHome を「自分あて→経理キュー→アラート→KPI」の宣言的合成に全面改装
  （契約 SQL 4本=PortalTodoData/QueueData/AlertData/KpiData。0件項目は行ごと非表示）。
  全13フレームのトップ廃止（着地=一番使う業務画面・セグメントStart・ActiveModuleSegments）、
  ExpenseHome/SalesHome/AccountingHome/AdminHome/AccountingKpiData を廃止。
  通知を Shell へ昇格（Send() 一元化・論理リンクの閲覧時フレーム解決・通知一覧は /Main/Notification）。
  PAY_DUE_SOON_DAYS を SystemThreshold へマスタ化（支払予定表と共用）
- **検証**: designcheck 0件・check_navigation 0件（新遷移規約対応に改修）・
  営業部長/経理部長/admin/開発一般の4視点で実機確認（サイドバー出し分け・着地・件数一致・
  アラート・クリック遷移・通知の閲覧時解決・ログアウト・直URL拒否/復帰・UI編集の永続化）
- **文書**: docs/10 全面改訂・docs/13 新設（ポータル契約の正典）・tests/19 追補・Project.md 知見3件・
  ADR-0041/0042 に supersede 注記・ADRインデックス補完
- **持ち越し**: E2E文書（tests/11・14〜18・docs/09）の本文は旧 /Top・課長OR前提のまま（19の追補で吸収。
  全面改訂は次回）。ポータル「未読の通知」0件時の空テーブル表示の見栄え改善は将来課題

## 2026-08-06 レビュー第9弾（ユーザー実機レビュー→全項目GO・ADR-0046）

前日の大改造をユーザーが実機確認（「ものすごくよくなってる」）した上での改善9項目＋DB設計議論を実装:

- **ポータル/サイドバー**: ログアウトを他フレーム同様に左下固定（CSS）。サイドバーに「通知 (N)」
  （未読件数バッジ）を追加し、通知一覧を /Main/Notification（NotificationCenter=カスタム一覧）に一本化
  （経費フレームの通知メニュー廃止）。既定は未読のみ表示・「未読に戻す」は青（ADR-0027準拠に是正）・
  「既読の通知を全て削除」（赤＋確認ダイアログ・実削除2件を実機確認）
- **経費**: 申請一覧に申請者検索＋既定=自分（経理は既定なし）。申請中は my_application_view
  （ddl/440・expense_request JOIN）化で 件名・金額列を追加し、表示を進行中＋却下に限定
  （却下は申請者のアクション待ちのため残す=ユーザー合意）。承認待ちの無意味な状態検索を廃止
- **予実対比**: 開いた時点で現在年度＋（非経理は）自部門で検索済みに（BudgetVsActual.mod.cs 新設。
  ポータルからの遷移に ?initialize_search=true 付与——空一覧の原因はこのフラグ欠落だった）。
  ポータルの予算警告を部門スコープ化: 経理=全部門／非経理の承認者=自部門のみ
  「⚠予算警告: あなたの部門（開発1部）」（PortalAlertData に警告部門IDリスト列）
- **会計年度表示**: fiscal_years.label（「第18期（2026年度）」・トリガー保守=ddl/430）を新設し、
  年度参照 SelectField 14 箇所を一括切替（4月開始→開始年=年度）
- **案件の部長開放（ADR-0046）**: is_director キャッシュ（ddl/450・部長行から導出）を追加し、
  Project の書込条件を「経理∨部長」に。SES 精算条件（単価・精算幅）は非経理に読取専用。
  工数フレームの案件一覧を ProjectView（廃止）→ Project 本体に差し替え（一般=閲覧・部長/経理=編集）。
  「三段論法の各段を正しい家に置く」（事実=キャッシュ／ポリシー=UserWriteCondition）設計はユーザーと合意
- **検証**: designcheck/check_navigation 0件・経理/開発1部長/開発一般の3視点で全項目実機確認。
  発見バグ1件（CurrentUser の SelectField DisplayText が候補未ロードで空→部門マスタ検索で解決）
- **文書**: ADR-0046・docs/10/13・tests/19 追補・ADRインデックス

## 2026-08-09 全ドキュメント現行化＋体験シナリオ・ライブラリ新設＋改称（ユーザー指示・並行作業）

ユーザーがレイアウト調整（Design 配下）を行う裏で、Fable がドキュメント側を全面的に作り直した回。
**役割分担を明示して並行**（ユーザー=Design のレイアウトのみ／Fable=docs のみ）し、
ユーザーのレイアウト作業が終わった時点で分担を解除して Design にも着手した。

- **現状棚卸し**: Design 実体（14 frm.json・96 mod.json/cs・Query.sql・ddl）から
  「現在の挙動の真実」を 6 ドメイン分抽出し、作業ブリーフとしてファイル化。
  以後のエージェントはこれを読むだけで済むようになり、トークン消費が桁で下がった
  （**教訓: 巨大な調査結果を fork で引き回すと上限に当たる。ファイルに落として渡す**）
- **docs 本体の現行化**: 01/02/03/04/05/07/08/09/README・Designer/Project.md・issues 3件・12/README。
  とくに **09 デモ台本は権限モデルの説明が現行と正反対**だった（「部門メンバー行だけで権限が決まる」）ため全面改訂
- **体験シナリオ・ライブラリ新設（`docs/tests/20_体験シナリオ/`・全 51 本）**: 11 ドメイン。
  1 ファイル＝実務の一本道、ライブラリ全体で条件分岐を網羅する方針。11_E2E の分岐 ID を参照タグとして各ステップに割当。
  旧 14〜18 は後継への案内スタブに置換して廃止
- **11_E2E の全面現行化**: README・00〜09 の全章と 06_利用シナリオ。
  旧 `/XXX/Top`・「ほかの業務」・部門種別からの権限導出・「課長 2 名並列」を一掃（**金額・仕訳番号は不変**）
- **ADR-0047 アプリ名を「よろず」へ改称**: 会計用語「帳合」では射程が狭くなったため。
  サイドバーのブランドを 2 行（`よろず` 18px ＋ `統合業務基盤アプリ` 12px）に。designcheck/check_navigation 0件・デプロイ済み
- **ADR-0048 承認ルートを積み上げ方式へ**: 金額が上がると承認者が入れ替わるのではなく段が積み上がる
  （課長 → 部長 → 総務）。テンプレートを「〜まで／＋総務」の命名規約に統一し `ddl/460` で移行。
  **ADR-0044 の「重複段の圧縮」がこれで初めて機能する**（課長空席・課長本人の申請が特例なしに部長 1 段へ畳まれる）
- **ISSUE-0004 修正**: ポータルの定期請求キューが課金サイクルを `'annual'` と比較していた（実値は `'yearly'`）ため
  年額契約を毎月カウントしていた。1 語修正で当月の誤カウント 1→0 を実測確認
- **改善候補の集約**: `docs/issues/2026-08-09_体験シナリオ執筆中に見つかった改善候補.md`（約30件）。
  A 章が要判断（分割検収の請求額上書き／**未確定の入金予定が「入金済」と数えられ売掛残高と資金繰り予測が壊れる**／
  件名が必須でない／手入力税額の無検算）
- **デモ DB の掃除**: テスト残骸のうち案件 `a`・ユーザー `test-test` を参照ゼロ確認のうえ削除（8/9）。
  定期請求契約 id=5 は請求書 3,300 万円と確定仕訳 4 本が生えていたため一旦保留し、
  **ユーザー承認のうえ 8/11 に削除**（請求書 INV-26-014・仕訳 No.77/78/80/81 と明細 9 行・入金 2 件・
  `recurring_run_plan` の該当行。**伝票番号 77/78/80/81 は欠番**＝アプリの「発行を取り消す」と同じ物理削除）。
  削除後 Σ借方＝Σ貸方＝**25,813,055** で貸借一致。現預金 700 万円台・定期請求の将来入金 275,000 円/月と等身大に戻り、
  資金繰り予測とポータルの金額が実データとして読めるようになった。
  引っかかり: 契約本体だけ消えず、**プラン用の一時テーブル `recurring_run_plan` の FK が原因**だった
  （対象月ごとに再構築される作業表なので該当行を削除して解決）。
  これに伴い体験シナリオ 5 本の数字を実測で取り直し（資金繰り・翌期繰越の期首残高・除外理由の材料）
- **運用ルールの追加**: ユーザーから「**確認してから作業を**」（**ユーザー自身が行ったレイアウト調整を
  Fable が勝手にコミットした**ことへの指摘）。疑問形の依頼は相談として扱う。
  レイアウト規約（取引先=440px・案件=640px）を `Designer/Project.md` に明文化
  - 〔2026-08-12 訂正〕当初この指摘を「コミットは常に事前宣言＋了承が必須」と記録したが、
    ユーザーから「**それは過剰。自分の作業は途中途中で自由にコミットしてよい**（むしろ全部終わるまで
    溜めるのは適切なコミットを妨げる）。禁止なのは**私がやった作業を勝手にコミットすること**だけ」と訂正された

## 2026-08-12 検収の明細化と税額計算の是正（ADR-0049/0050・改善候補 A-1 起点・ユーザー設計提案→GO）

改善候補 A-1（分割検収から作った請求書の金額が受注全額に化ける）を、
ユーザーの提案「**そもそも検収が明細行を持つべきでは**」を受けて構造ごと直した回。

- **ADR-0049 検収に明細行（`acceptance_lines`）**: 受注明細のコピーで、摘要・数量・単位・単価・受注金額・税区分は
  読み取り専用、**編集できるのは「検収金額」だけ**。各行は `sales_order_line_id` で受注明細に紐づき、
  行ごとの残額自動セット・行単位の超過判定ができる。検収額・消費税額は明細合計からの導出に変わり手入力を廃止。
  請求書生成は検収明細をそのままコピーするだけになり、**明細合計＝検収額が構造的に成立**する。
  行の追加・削除ボタンは出さない（`CanCreate/CanDelete: false`。**スクリプトからの `AddRows()` は妨げられない**ことを実機確認）
- **A-1 のもう一段深い真因**: 明細コピーを入れても金額が化けたため追ったところ、
  `Invoice.Lines_OnDataChanged` が **`金額 = 数量 × 単価` を無条件に再計算**しており、書き込んだ検収額をその場で潰していた。
  `Amount` への代入自体が同ハンドラを再帰的に呼ぶため、代入した値が同じイベント内で消える構造。
  これは検収由来に限らず**利用者が請求明細の金額を手で直しても消える**状態だった。
  自動計算を「検収に紐づかない行」だけに限定して解決
- **行単位のリアルタイム赤字警告**: 請求明細が元の検収明細の金額を超えたら、発行時ではなく**入力したその場で**
  行・超過額つきで警告する（ユーザー指定）。**ブロックはしない**（正当な増額を機械が判定できないため人に委ねる）
- **ADR-0050 税額計算を明細の税区分ベースへ**: 従来は明細の税区分を無視して合計に一律 10% を掛けていた
  （8%・非課税・免税・不課税の行にも 10% が乗る不具合）。**税率ごとに集計してから 1 回だけ端数処理**する
  インボイス制度の要求に合わせた。**税区分は必須**にし（見積・受注・請求の各明細）、
  既定値は**コードに埋めず `tax_categories.default_for` マスタ列**で決める（ユーザー指摘: 「10% が普通」は 2026 年だけの話）。
  `SALES_10` のハードコードは `RecurringRun` / `SesBilling` / 資金繰り予測 / ポータル警告からも一掃
- **移行**: `ddl/470`（既定税区分列）・`ddl/471`（`acceptance_lines` 新設＋既存 9 件の復元＋`invoice_lines.acceptance_line_id` 紐付け）。
  分割検収 1 件（A-26-009）は最終行で差額調整。**帳簿は 1 円も動かない**（売上仕訳は検収額合計で計上済みのため）
- **実機検証**: `soumu_bucho` で 10 項目を通した（残額自動セット／明細編集の自動再計算／確定仕訳／
  請求書の金額が化けない・開き直しても化けない／行単位の赤字警告／税区分 8% への追随／マスタ既定の自動セット／
  8%+10% 混在の端数処理 89,333 円／移行済み INV-26-017 への警告）。詳細は ADR-0049 末尾の表。
  検証で作った検収 2 件・請求書 2 件・仕訳 1 本は削除済みで、貸借一致・孤児レコード 0 件を確認
- **ドキュメント追随**: 体験シナリオ `02_営業_受託/03` を「明細で分ける分割検収」へ改題・全面書き換え
  （罠の実演 → 罠が無いことの確認＋赤字警告の体験）、11_E2E の RCV-09 改訂＋RCV-09b 新設、
  02_販売 2-4 / 07_特殊取引 の値引き解説を明細ベースに、改善候補 A-1 を対応済みに更新

## 2026-08-12 未確定入金の除外と手入力消費税の検算（ADR-0051・改善候補 A-2/A-3/A-4・ユーザー合意済み仕様で実装）

改善候補の A-2〜A-4 を、着手前にユーザーと仕様を詰めたうえでまとめて修正した回。

- **A-2 帳票の入金集計を「消込済みだけ」に**: 請求書の発行時に税込全額の入金予定が自動作成される（ADR-0032）ため、
  `receipts` の単純合計が請求額と同額になり、**売掛残高は全件「入金済・残額 0」・資金繰り予測の入金予定は構造的に 0 円**
  だった。`ReceivableBalance` / `CashFlowForecastData` / `PortalAlertData`（2 か所）の集計に
  「消込仕訳がある入金だけ」の EXISTS 条件を追加。`ReceiptList` と `Receipt.mod.cs` は元からこの流儀で、
  3 帳票だけが取り残されていた。`journal_entries(source_type, source_id)` のインデックスも追加（ddl/480）
  - 実測: 売掛残高の既定検索が **0 件 → 9 件**（未回収 8,432,000 円）、資金繰りの入金予定が
    **0 円 → 2026-08 に 5,957,000 円 / 2026-09 に 2,475,000 円**
  - 「入金予定」列は足さない判断（売掛残高＝帳簿の売掛金と一致させる。予定は入金一覧の未確定で見る）
- **A-3 経費申請の件名を必須に**: 空件名が通知本文・仕訳摘要・固定資産名にそのまま流れていた。
  見積・請求書は元から必須で経費だけが例外だったので揃えただけ。既存 14 件に空件名は無くデータ移行不要
- **A-4 手入力「うち消費税」の検算**: 税率からの計算値と **±10%** を超えて乖離したら**警告して通す**
  （レシート記載額を尊重する設計は壊さない）。負値・税込金額以上は**エラーで止める**。
  不課税・非課税の費目では**欄を隠し入力値もクリア**（クリアは通知）。費目未選択のうちは欄を出す。
  判定は入力時／金額変更時／AI 読み取り完了時と、申請時・実費確定時。閾値はマスタ化せずスクリプト内定数
- **実機検証**: `soumu_bucho` で売掛残高・資金繰り予測・ポータルのアラート・経費申請の 8 項目を確認
  （詳細は ADR-0051 末尾の表）。**検証用の経費申請は保存せずに離脱したので DB は 1 行も増えていない**

### 2026-08-12 夜 — 税区分未設定の廃止と消費税集計表の作り直し（ADR-0052 / 改善候補 B-5・B-6）

ユーザーと B-6 の内容・修正方針を詰めたうえで着手（相談 → 案 2「借方・貸方を分けて出す」で合意 →
`NOT NULL` 化も実施、検証行は不要と判断して取り下げ、科目別内訳と期間指定を次タスクに）。

- **段階 0 リサーチ**: インボイス経過措置は令和8年度改正で 80→70→50→30→0% の 5 段階・2031-09 まで
  （`ddl/010` の 4 レコードと日付まで一致）。課税売上割合＝(課税＋免税)÷(課税＋免税＋非課税)、
  不課税・対象外は分母に含めない。BS 科目の税区分は市販ソフトでも原則「対象外」。**マスタとの矛盾なし**
- **B-5 の根＝ NULL と `OUT_OF_SCOPE` の二重表現**を廃止（`ddl/490` で 128 行移行 → `IsRequired`
  3 か所 → `ddl/500` で `journal_lines.tax_category_id` を `NOT NULL` 化）。
  テーブル再構築は新旧 DB を `ATTACH` して全列 `EXCEPT` 双方向で差分 0 行を確認してから確定
- **全 15 経路の棚卸しで、相手勘定の行に税区分が無いことが判明** → `JournalEntry` に正典メソッドを置き
  全経路が `Submit()` 直前に呼ぶ規約に。**当初「科目マスタの既定で埋める」としたのは誤りで、実測で是正**
  （減価償却の貸方に `PUR_10` が付き課税仕入が 114,375 円過少／按分振替の貸方に `SALES_10` が付き
  課税売上を二重計上）。**明示されなかった行は「対象外」**へ変更し、入出金起票だけは相手科目に
  科目既定を明示的に入れる（受取利息＝非課税売上の取りこぼし防止）
- **消費税集計表を全面改訂**: 符号付き差引（B-6 の解決）・戻し（本体/税）・税込対価・
  経過措置の期間分割と控除率/控除対象税額・最終行に課税売上割合と控除方式の判定。
  判定閾値は `ddl/510` で `system_thresholds` へ（`unit` 列を追加）
- **実測（第18期）**: 課税仕入 10% 本体 1,632,922 → **1,508,547**、課税売上 10% 本体
  13,065,000 → **12,865,000**、経過措置 税 800 × 80% = 控除対象税額 **640**、課税売上割合 **99.99%**
- **実機検証**: `soumu_bucho` で集計表の 8 行＋判定行を確認。入出金起票で 2 件起票して税区分の
  自動付与を確認（会議費→課税仕入10%／普通預金→対象外、受取利息→非課税売上／普通預金→対象外）。
  **検証用の 2 伝票は削除済みで、仕訳は 201 行・貸借とも 27,302,555 で開始時と同じ**
- 新たに見つけた改善候補 3 件を issues に追記（B-7 入出金起票の課税科目／定型仕訳の法定福利費の
  税区分不整合／`JournalImport` の科目コードのハードコード）
- **次**: 消費税集計表の期間指定 → 科目別税区分表の新設（誤設定の発見手段。B-6 調査で
  「未設定」より「誤設定」のほうが実務リスクが高いと判明したため）

### 2026-08-12 夜（続き） — 消費税集計表の期間指定と、科目別税区分表の新設（ADR-0052 決定 4）

主タスク（B-5/B-6）完了後、ユーザー依頼の次タスクとして実施。

- **期間指定**: 消費税集計表に 日付（自）／（至）を追加。対象年度の内側を絞る形（未指定なら年度まるごと）。
  最終行を「課税売上割合（表示期間）」に改め、月次に絞ると申告用の年間割合とは別物であることを画面で示す。
  実測（2026-07 に絞る）: 課税売上 12,865,000 → 11,415,000 / 課税仕入 1,508,547 → 799,447
- **科目別税区分表（`TaxDetailByAccount`）を新設**。科目 × 税区分の内訳。
  **未設定（構造で塞げる）より誤設定（塞げない）のほうが実務リスクが高い**という B-6 調査の結論への対応で、
  税区分に「対象外」「不課税」を指定して損益科目が混ざっていないかを点検するのが定石の使い方。
  会計フレームの財務帳票に導線を追加（財務帳票を持つフレームは Accounting のみと確認）
- **実測で E2E の検算が取れた**: 会議費／課税仕入 10% が本体 6,000・戻し 5,000 で、
  07_特殊取引 の赤黒訂正（誤 5,000 → 赤 5,000 → 黒 6,000）の期待値と一致
- **次**: docs/tests/20 の体験シナリオを使ったユーザーの実機 UX レビュー → 出てきた改善点の実装。
  改善候補は B-1〜B-4・B-7・C-1〜C-7・D 群が未着手

### 2026-08-13 — 改善候補 B-1〜B-4・B-7 と、ADR-0052 の原因訂正（ADR-0053）

ユーザーから B-1〜B-4 は対処案どおり実装の承認、B-7 は「案 A（税行を生成する）＋網羅的な再チェック」の指示。

- **⚠ ADR-0052 決定 2 の原因分析が誤っていたことが判明し、訂正した**（ADR-0053 決定 1）。
  減価償却の貸方に課税仕入 10% が付いていたのは `ddl/490` の穴埋めではなく、
  **`Lines_OnDataChanged` → `ApplyLineDefaults()` がスクリプト生成の伝票にも発火して
  科目の既定を入れていた**ため（移行前の NULL 91 行に工具器具備品が無いことで確認）。
  つまり「明示しなければ対象外になる」という前提が成り立たず、**520 でデータを直しても
  新しい償却仕訳で再発する**状態だった → `MarkAllLinesOutOfScope()` で明示的に上書きする方式へ。
  実機検証: 検証用資産の償却仕訳を生成 → 減価償却費・工具器具備品とも「対象外」
- **B-7（案 A）**: 入出金起票で相手科目が課税区分なら内税として扱い、消費税行まで生成する。
  **表示の問題ではなく、画面が作られたときから帳簿が誤っていた**（費用が税込で計上され
  仮払消費税が立たない＝納税額が過大）。実測: 会議費 3,300 → 会議費 3,000 ＋ 仮払消費税 300 ／ 普通預金 3,300。
  網羅チェックで、定型仕訳は下書き経由なので同じ問題を持たないこと・外税では貸借が崩れるので
  内税のみ使うこと・`RegenerateTaxLines` は 1 回だけ呼ぶこと、を確認
- **B-1〜B-4**: 締め済みメッセージの統一／経費トーストに勘定科目名／外税伝票の差額注記／
  領収書未添付の警告文。いずれも対処案どおり
- **DB はクリーン**（検証用の伝票・固定資産は削除済み。仕訳 201 行・76 伝票・資産 2 件）

#### テストシナリオへの追随（同日・後追い）

実装コミット（`0dca601`・`de2dab9`）の時点では ADR と issues しか更新していなかったため、
**`docs/tests/` 配下 28 ファイルを現行化した**。

- **ADR-0053（入出金起票の税行）**: 体験シナリオ 06-04 のステップ 6〜10 を書き直し
  （会議費 1,100 → 本体 1,000 ＋ 仮払消費税 100 の 3 行・結果ラベルの「うち消費税」・
  **出納帳の相手科目が「諸口」に変わる**・相殺仕訳も税行込みの鏡像になる）。
  E2E は CSH-03 の期待仕訳と 4-6/4-7 の解説、04_会計コア README の税行仕様を更新。
  **本流 No.16〜24 の金額・仕訳番号は不変**（4-6 の相手科目=普通預金は対象外、4-7 の受取利息は非課税）
- **B-1〜B-4**: 締め済み系メッセージを全章で差し替え（振替伝票「取引日の期間は締め済みです」・
  入出金起票「日付の—」・固定資産「対象年度は締め済みです」ほか）。
  経費トーストは「消耗品費（費目: 印刷製本費）」形式に、外税の差額注記と領収書警告文も現行化
- **ADR-0052 の取りこぼしも回収**: 8/12 の実装時に更新されていなかった記述を是正——
  「税区分が空の行には税行が立たない」（現在は NOT NULL・対象外を使う）、
  「集計表は符号を持たない単純合計」（現在は差引＋戻し列）、消費税集計表の列構成
- **⚠ 残: E2E 9-7（消費税集計表）の数表は旧仕様の値**。列構成も集計方法も変わっており、
  正しい期待値は**クリーン DB で E2E を再完走しないと確定できない**ため、
  当該ステップに「要再取得」の注記と再取得時の確認事項（赤黒の相殺・内部振替の対象外化・
  インポート税行の計上・入出金起票の税分解）を書いて残した。**9-7 以外の帳票の期待値は不変**
- **次**: docs/tests/20 の体験シナリオを使った実機 UX レビュー。
  未着手の改善候補は **B-5/B-6 を除く C-1〜C-7・D 群**

### 2026-08-14 — 改善候補 C-2: 定型仕訳の「有効」を「一覧への表示」フラグへ（ADR-0054）

ユーザーからの問い「そもそも有効／無効フラグに意味があるのか」を起点に、対処案そのものを見直した。

- **調査**: `Designer/Design/` を全走査したところ、`JournalTemplate` を指す参照は
  自モジュール・子明細・サイドバーのメニュー項目の 3 か所だけで、**参照フィールドの候補として
  引かれている箇所はゼロ**。他マスタ（勘定科目・取引先）の `IsActive` が
  `SearchTargetVariable: "IsActive.Value"` で「選べなくする」効き方をしているのとは別物で、
  定型仕訳には「候補から外す」効き方が構造的に存在しない＝**名前が実態と食い違っていた**
- **決定（ADR-0054）**: 起票をガードするのではなく、`IsActive` → `ShowInList`
  （DB 列 `show_in_list`）へ改名し、表示名も「有効」→「表示」に統一。
  一覧に検索（定型名・表示）を新設して**既定は表示中のみ**、非表示の定型を開いたときは
  「この定型は一覧に表示されない設定です（この画面からの起票はできます）。」と案内する。
  **起票はガードしない**——廃止した契約の取消仕訳を当時の定型で起こす等の例外を残せる。
  もう使わせたくない定型は削除する、が正しい運用（弥生会計の「非表示」も同じ効き方）
- **ついでの実バグ修正**: 新規作成時のチェックボックス初期値が未チェックだったため
  `is_active = 0` の定型が作られていた（デモ DB の T01 が実際に 0 のまま残っていた）。
  旧仕様では挙動に出ないので誰も気づけなかったが、新仕様なら**作った直後に一覧から消える**。
  `Detail_OnAfterInit` で新規は `true` を入れる。既存行も 1 に寄せた
- **実機検証**（soumu_bucho）: 既定で 3件→2件に絞られる／クリア＋検索で非表示も出る（表示列「—」）／
  案内はチェックを外したその場で出る／非表示の定型からも起票できる（下書きは検証後に削除）／
  新規は既定 ON。**DB はクリーン**（max entry 118・テンプレは 3 件とも表示 ON）
- **既知の挙動**: `OnSearchInitialization` は `?initialize_search=true` 付きの遷移でしか発火しない
  （CLB 仕様）。サイドバー経由なら既定が効くが、URL 直打ちでは全件出る。通知一覧の「未読のみ」既定と同性質
- **次**: 未着手の改善候補は **C-1・C-3〜C-7・D 群**

### 2026-08-14 — 入出金起票の作り直しと部門の必須化を決定（ADR-0055 / ADR-0056・実装前）

ユーザーの問い「入出金起票はどういう目的の機能か／市販ソフトにもあるか」を起点に、
この画面と市販ソフト（弥生・freee・マネーフォワード・勘定奉行）の出納帳入力を比べ、
差分を 3 つに切り分けたうえで対話しながら方針を固めた。**実装はこれから。**

- **調査で分かったこと（実データ）**:
  - 部門別 P/L が**ほとんど機能していない**——費用 38 行中 29 行・収益 18 行中 15 行が部門なし。
    部門が入っているのは経費精算由来だけで、銀行取込 32/32・仕入先請求書 9/9・SES 9/9・定型 8/8・
    入出金 6/6・振替伝票 34/37 が空。「穴が 1 つ」ではなく売りの機能が動いていない状態
  - **部門マスタに「全社共通」（code `00`）が既にある**。共通費の受け皿は新設不要で、使われていなかっただけ
  - 入出金起票の**現預金科目の候補が JSON にハードコード**（`現金,1000`/`小口現金,1010`/`普通預金,1020`）。
    口座科目を増やしてもこの画面に出ない＝CLAUDE.md「マスタ化してハードコードしない」に反していた
  - `journal_entries` の `updated_at` が動いているのは **76 伝票中 5 本だけ**で、5 本とも
    「振替伝票を下書き保存 → あとで確定」。自動起票は一度も更新されていない
    ＝ `updated_at` は既に「人が手で介入した印」として機能している
- **ADR-0055（入出金起票を下書き方式へ）**: 銀行明細取込（ADR-0012）と同じステージング方式に作り替える。
  下書き 1 行 = 仕訳 1 本・採番は一括起票時・**下書き行は作成者を持ち自分の行だけ見える**
  （打ちかけを他人が確定する事故を構造的に防ぐ。銀行明細は「会社の共有物」なので扱いが違う）・
  残高は「帳簿残高（基準時刻付き）」と「この下書きを起票した後の見込み残高」の 2 段。
  あわせて現預金科目をマスタのフラグで決め、相手科目からは現預金科目を除く。
  **確定後の取消（仕訳削除）はスコープ外**——下書き方式にすれば必要性が激減するため、必要になってから
- **ADR-0056（部門の必須化）**: 規約は「**損益科目の行には必ず部門・BS は任意・部レベル・
  紐づかない費用は全社共通**」。決め方は三段（人が画面にいる経路はエラーで止める／機械経路は自動解決／
  最後の保険が全社共通）で、これは税区分の
  「経路が明示 → 科目マスタの既定 → `MarkRemainingLinesOutOfScope()`」と同じ型。
  `journal_lines.department_id` を **NOT NULL** 化し既存を全社共通へ移行する
  （条件付き NOT NULL は SQLite の CHECK がサブクエリ不可のため書けない。ADR-0052 の税区分と同じ形を採る）。
  **確定済み伝票でも部門・案件は編集可**にする（締め済み期間を除く・経理のみ）——
  財務諸表が 1 円も動かない管理会計の軸だから許され、かつ**これが無いと部門必須化が運用に耐えない**。
  摘要は法定帳簿の記載事項なので対象外
- **実装時に確認すること（ADR に明記済み）**: ①明細だけ変更したとき親の `UPDATE` が走るか（走らないと
  `updated_at` が動かず監査証跡の前提が崩れる）②確定済み伝票でフィールド単位に編集可否を分けられるか
  ③`ListField` の `CanCreate` で行追加できるか ④売上行に部門が載っていない原因
- **次**: ADR-0055 / 0056 の実装（DDL・モジュール・移行 SQL）。着手はユーザーの指示待ち

### 2026-08-14 深夜 — ADR-0055 / ADR-0056 の実装・検証（自律実行）

ユーザーから「宿題・実装・テスト完了まで自律自走」の指示。宿題 4 件 → 実装 → 実機検証 → ドキュメント追随まで通した。

- **宿題の答え**（詳細は各 ADR の「実装で分かったこと」）:
  ①明細だけ保存しても親行は `UPDATE` されない → 親の `UpdatedAt`/`Updater` を明示代入してから `Submit()`（実測で `updated_at` が動くことを確認）
  ②列単位の編集可否は `ListLayout` の `IsViewOnly` で可。ただし**レイアウトはスクリプトから切り替えられない**ので専用画面にした
  ③`ListField.CanCreate` は使わず入力欄＋ボタン方式（ユーザーの想定とも一致）
  ④売上に部門が無かった原因は「コードの穴（銀行・SES・定型・仕入先請求書ほか）」と「元データ由来（7/10 の E2E データ。現在は解消）」の 2 つ
- **ADR-0055（入出金起票の下書き方式）**: `cash_entry_drafts` を新設（ddl/530）。`CashEntryDraft` モジュール＋
  `CashBalance` クエリモジュール（現預金の帳簿残高）を追加し、`CashEntry` を「入力欄＋残高表＋下書きグリッド＋
  3 ボタン」に作り替え。**下書きは作成者ごと**（`Variable: "CurrentUser.Id.Value"` 一発で絞れる）。
  勘定科目マスタに `is_cash_equivalent` を足して現預金科目の JSON 直書きを廃止
- **⚠ ADR-0055 決定 4 を実装中に修正**: 「相手科目から現預金科目を全部除く」は**誤り**だった——
  小口現金の補充（普通預金→小口現金）が作れなくなる。**同じ科目どうしだけを検証で弾く**形に変更
- **ADR-0056（部門の必須化）**: `departments.is_common`（全社共通）を追加し、`journal_lines.department_id` を
  NOT NULL 化（ddl/540。既存 173 行を全社共通へ移行）。`JournalEntry.FillMissingDepartments()` を
  **15 経路すべての `Submit()` 直前**に挿入（税行は親行から継承）。人が画面にいる 4 経路は
  損益科目の行が空ならエラー。銀行明細・仕訳ルール・仕入先請求書・定型仕訳の明細に部門欄を追加し、
  SES 精算の売上行にも部門を刻むよう修正
- **【仕訳】部門・案件の修正**を新設（`JournalLineDepartment`）。伝票番号で明細を出し、
  部門・案件だけ編集可（他列は `IsViewOnly`）。締め済み期間は保存不可・経理のみ
- **CLB 知見 3 件を記録**（すべて静かな失敗。FB-039〜041）: `ref`/`out` が伝わらない／
  `ListField` の行削除が DB に届かない／日付列を `TEXT` で作ると書式が変わって SQL の日付関数が壊れる
- **実機検証**: designcheck 0 件。入出金起票の入力ガード・連続入力・グリッド編集・行削除・残高 2 段表示・
  一括起票（うち消費税の表示を含む）・生成仕訳の部門の付き方、振替伝票の部門ガード（否定/肯定の両方）、
  定型からの部門引き継ぎ、部門・案件の修正画面と親の `updated_at`——いずれも期待どおり。
  **検証で作った伝票・下書きは削除済み**（仕訳 202 行・76 伝票・部門なし 0 行・下書き 0 件）
- **ドキュメント追随**: ADR-0055/0056 に実測を追記、体験シナリオ 06-04 のステップ 6〜10 を全面書き直し、
  E2E の CSH-01〜04・4-6/4-7・04 章 README、会計ドメイン設計（§3 の列定義・§8 の不変条件）、
  Project.md（知見 4 件）、docs/12（FB-039〜041）
- **次**: 未着手の改善候補は **C-1・C-3〜C-7・D 群**。
  既存 173 行の「全社共通」は移行の結果であって正しい部門ではないので、必要に応じて新画面で直す

### 2026-08-14 深夜（続き） — 部門・案件の修正を「振替伝票のサブ画面」にする

ユーザー指摘 2 件への対応。①画面の説明文に「（ADR-0056）」と内部参照が漏れていた（利用者に見せる情報ではない）
②修正は**振替伝票から**できるのが最善で、別画面に飛ばすのは筋が悪い。

- **文言**: 説明文から ADR 番号を削り、利用者の言葉に直した。デザイン全体を grep して
  利用者に見える文字列（Text/Label/Placeholder/DisplayName）に内部参照が無いことを確認
- **導線をユーザー指定の仕様に作り替え**: ①修正画面の見た目を振替伝票とほぼ同じに（ヘッダの並び・
  明細の列順を合わせ、直せない列は編集不可で見せる）②入口は**振替伝票詳細の
  「部門・プロジェクトを修正する」ボタンだけ**（確定済みのときだけ出る。対象は `?entry=` で渡す）
  ③「保存して伝票に戻る」で元の伝票へ戻る ④**サイドメニューからは行けない**
- **実測 2 件（どちらも静かな失敗）**:
  - `this.IsViewOnly = true`（モジュール全体）は**ボタンまで `pointer-events: none`** にする（FB-035 の追記）。
    確定済みのロックを**項目ごとの `IsViewOnly`** に変更（`LockPostedFields()`）
  - サイドバーのリンクを消すと**モジュールが真っ白**になる（FB-042）。
    `OtherPageModuleDesigns` に登録して「メニューに出さないが URL では開く」を実現
- **ADR に書いたのに未実装だった「最終更新の表示」も入れた**（伝票詳細に
  「最終更新: 2026/08/14 07:15 総務部 部長 山田太郎」。作成と同時刻なら出さない）
- **実機検証**: 確定済み伝票 → ボタン → サブ画面（振替伝票と同じ見た目）→ 部門変更 → 保存 →
  **伝票詳細に戻り明細が更新されている**、を往復で確認。確定済み伝票は個別ロック後も入力要素 0 個。
  検証で変えたデモデータは元に戻した（仕訳 202 行・部門なし 0 行）
- **ボタン色の是正（ユーザー指摘・2026-08-15）**: 「保存せずに戻る」を赤にしていたが **ADR-0027 違反**
  （赤は削除・取消＝元に戻しにくい操作の色。保存せずに離れるのは何も壊さない）。
  ボタンごと廃して**振替伝票と同じ左上の戻る矢印**（`BackAnchor`・`HistoryBack`・FontSize 30）に統一し、
  画面のボタンは青の「保存して伝票に戻る」1 つだけにした。
  「主ボタン＝青／副ボタン＝別色」は ADR-0027 が却下済みの発想で、自分で規約を破っていた

### 2026-08-14 — 改善候補 C-1 / C-8 / C-9: 見積の下流ガードと定期請求契約のライフサイクル（ADR-0057）

ユーザーとの設計議論（C-1 の相談から C-8・C-9 が派生）→ GO を受けて実装・実機検証まで完了。

- **C-1（見積の二重変換）**: 「1 つの見積の下流は 1 つだけ」を不変条件にし、`Quote.mod.cs` に
  `FindDownstreamLabel()` を新設して変換系・巻き戻し系の 3 ハンドラを対称化。「下書きに戻す」の
  `accepted` 限定は廃止（旧ガードをすり抜けたデータが実在したため状態を見ない）
- **C-8（削除導線）**: `RecurringBilling` に削除ボタンを新設（**下書き かつ 請求書 0 件**・
  権限は**作成者本人＋経理**）。「下書きに戻す」は**void でない請求書があれば禁止**。
  判定を分けた理由は ADR-0057 に記載（削除は FK という現実の制約・巻き戻しは RecurringRun の流儀）
- **C-9（検索と終了状態）**: 検索条件 7 つを新設（開始月・終了月・件名・状態・取引先・案件・部門）。
  `status` に `ended` を追加し「終了にする」（終了月を必ず埋める）「終了を解除」（過去の終了月は警告）を実装。
  既定検索は「下書き＋確定済」
- **既存バグ 1 件を発見・是正**: `PortalAlertData` / `CashFlowForecastData` が `recurring_billings` を
  `is_active = 1` だけで JOIN しており、**確定前の下書き契約の金額が資金繰り予測とポータル警告に載っていた**
  （`PortalQueueData` だけが `status = 'confirmed'` を見ていた）
- **実装前に実測で方針を決めた**: `AllowEmptySearch` が「範囲検索に NULL 行を含める」フラグなら
  状態の追加は不要になるため、終了月が 過去 / 未来 / NULL の 3 件で検証 → **効果なし**（FB-043 / DOC-017）。
  この結果を受けて `ended` 状態を持たせる案を採用した
- **実機検証（全ケース Pass）**: 請求実績ありの契約で「下書きに戻す」がブロック／「終了にする」で
  終了月が自動設定され一覧の既定から消える／「終了を解除」で確定済に戻り、終了月が過去のときだけ警告が出る／
  下書き契約の削除が通り、請求実績のある下書き（旧データを SQL で再現）では止まる／
  下書きに戻した見積で「受注にする」がブロックされ受注が 0 件のまま
- **デモ DB 是正**: `quotes.id = 13`（Q-26-009）を `draft` → `accepted`。検証で触った契約 4 は元に戻した
- **ドキュメント**: ADR-0057・改善候補文書に C-1/C-8/C-9 の対応記録・`Designer/Project.md` に実測知見 4 件・
  `docs/12_CLB改善提案/` に FB-043 / DOC-017

### 2026-08-15 — CLB 1.3.18 への更新（ADR-0058・ユーザー指示）

ユーザーが素の 1.3.18 ソリューション（`../BusinessApp-1-3-18/`）を生成 → そこから必要分を取り込み。
プロジェクト構成の変化はゼロで、差分はパッケージ参照とテンプレート由来 7 ファイルのみだった。

- **パッケージ**: CLB / CLB.Designer 1.3.16→1.3.18、Extras（+ .Designer / .Server）0.4.0→0.5.0、
  Designer.Standard 0.5.0→0.6.0。ApexCharts 0.25.3・DbAccess 0.1.0・Licensing 1.3.0 は据置
- **取り込んだテンプレート差分**: ①CSRF トークンの発行がミドルウェア便乗から専用エンドポイント
  `GET /api/account/antiforgery` へ、②スクリプト用の一括ファイル入出力 API（`BulkFileReader` /
  `BulkFileTransferService`）に対応するサーバ 3 エンドポイントと `BulkAddThreshold`／`BulkAddAsync` override
- **維持したカスタム**: `Server/Program.cs`（AI/NTA 設定・htmlNoCache）・`SystemConfig`・
  `AITextAnalyzeController`・`index.html` の日本語化・`appsettings*`
- **検証**: build 0 警告 0 エラー・出力 DLL のバージョン確認・designcheck **0 件**・`ai-refresh` で
  `ClaudeCodeForDesigner/` を 1.3.18 版に再生成
- **実機**: ユーザーがデザイナをビルド → プロジェクトを開く → デプロイして動作確認（正常）。
  その際デザイナが 5 モジュールの `.mod.json` を 1.3.18 のシリアライザで書き直した
  （新プロパティの既定値出力・`Padding` の null 省略・順序。意味的変更なし・designcheck 0 件）

### 2026-08-16 — ユーザー指摘 12 件の一括対応（ADR-0059〜0062）

ユーザーがアプリを触って挙げた指摘（会計・営業・システム管理・その他）を、実バグ／UX 改善／
設計判断の 3 つに仕分けてまとめて実施。**設計判断はユーザーの明示指示に従った**
（税込列は案B→実測で不可となり実列＋トリガーへ／開発 DB のポリシーは緩く／プラン方式は Fable 判断）。

**実バグ（5 件）**
- **検収の導出値が新規時に編集できた**（ADR-0062）: `IsUpdateProtected` は更新時だけの保護で、
  新規では検収額・消費税額を手入力でき、しかも `OnDataChanged` が無いため消費税は再計算されず、
  明細合計と一致しない検収が保存できた。レイアウト要素を `IsViewOnly:true` にして導出専用にした
- **ポータルの未生成件数が画面と合わなかった**（ADR-0060）: 「3 件」の内訳は定期請求 0＋SES 3 で、
  リンク先は定期請求だけ。SES 画面は 2 件しか出しておらず件数自体もズレていた。行を分け、
  判定条件を `BuildPlan` と厳密に揃え、突合の検証 SQL（`docs/tests/portal_billing_count_check.sql`）を新設
- **SES が完了・中断した案件を毎月の生成予定に残していた**（ADR-0060）: `is_active` しか見ていなかった
- **SES が工数実績 0 分の月でも請求額を出していた**（ADR-0060）: 精算幅つき契約は実績 0h だと
  下限まるごと控除で金額が出る。原因はほぼ常に工数の入力漏れで、そのまま実行すると誤請求になる。
  対象外にしたうえで、消えた分を「▶ SES の当月工数が未入力: N 件」としてポータルに可視化した
- **`date('now')` が UTC だった**（ADR-0060）: 全 Query SQL 15 ファイルを `date('now','localtime')` へ。
  毎月 1 日の 0:00〜9:00 JST は前月を数えていた（実測: UTC 8/15 / ローカル 8/16）

**UX 改善（5 件）**
- 振替伝票の「確定」に確認ダイアログ（ADR-0062）。あわせて `_OnClick` 49 本を棚卸しし、
  **「(A) 取り消す導線が無い」「(B) 一括」のときだけ出す**という規約を決めて 10 操作に追加
- 検収に「請求書を開く」ボタン（経理のみ・直接請求→合算先の順に解決）
- 請求書一覧に「請求額（税込）」列（ADR-0061）。**生成列は CLB から見えなかった**ので実列＋トリガー
- 固定資産台帳の検索に「資産計上科目」「部門」を追加（2 行目・検索行 3 組までの規約に従う）
- ポータルの定期請求／SES を別行にし、それぞれ正しいモジュールへ遷移

**設計判断（3 件）**
- **パスワードポリシーのマスタ化**（ADR-0059）: 検証は一切無く 1 文字でも通っていた。
  `password_policies`（1 行固定・ddl/560）を新設し、判定はサーバの `PasswordPolicyService` 1 か所
  （`CustomizedModuleDataIO` に差し込み、CLB 経由の全パスワード書き込みが通る点で判定）。
  開発 DB は 4 文字・種類制限なし・ID 同一可で seed（ユーザー指示）
- **自己パスワード変更**（ADR-0059）: DB に紐づかない表示専用モジュール＋`/api/password/change`。
  更新対象は Claim の本人に固定。ビュー（`active_app_users`）への `INSTEAD OF UPDATE` トリガーを追加（ddl/570）
- **プラン方式の件数**（ADR-0060）: プランテーブルは共有の一時領域で「画面を開いたときにしか作られない」ため
  ポータルから数える案は採らず、**SQL 側を BuildPlan と揃えて「対で保守する契約」として明示**した

**検証**: designcheck 0 件／`dotnet build` 0 警告 0 エラー／実機で
ポータル件数の一致・SES プランの理由表示・請求書一覧の税込列・検収の読み取り専用と「請求書を開く」遷移・
振替伝票の確認ダイアログ・固定資産の科目検索（2 件に絞り込み）・パスワード条件の即時表示と
サーバ側の拒否トースト・ポリシーを 12 文字/3 種類に変えた場合の検証 API の応答を確認。
突合 SQL も一致（定期請求 0=0・SES 0=0）。**検証で触ったデータはすべて元に戻した**
（ポリシー値・請求書の金額・一時付与した sysadmin フラグ）

**未検証（ユーザーに依頼したいもの）**: 自己パスワード変更の**成功パス**。
現在パスワードの照合失敗・不一致・条件違反はすべて確認したが、実際に変更が成功する経路は
本人のパスワードが必要なため未実施（部品はすべて個別に検証済み: VerifyHash・ポリシー判定・
ビュー越しの UPDATE トリガー）

**別セッションへ切り出したタスク**: `docs/tasks/01_市販ソフト比較レビュー.md`・
`docs/tasks/02_ドキュメントルール整備.md`（そのまま渡せる作業指示書として作成）

### 2026-08-16（追補）— 「設定」フレームの新設と、固定資産科目フラグ（ADR-0063）

ユーザー指摘 2 件への対応。

- **「設定」フレーム**: サイドバーのトップ階層に「パスワードの変更」が業務メニューと同じ高さで並ぶのは
  怖い、という指摘。`Settings.frm.json`（ゲートなし＝全員）を 1 枚かませ、
  サイドバーは「設定」→ `/Settings`、フレーム内は「アカウント/パスワードの変更」にした。
  Main の `OtherPageModuleDesigns` からは外し、同じ画面に URL を 2 本持たせない。
  着地は ADR-0045 の方針どおり部品トップを作らず主画面へ直接。
- **固定資産科目フラグ（ADR-0063）**: 「資産計上科目」の候補が資産科目 18 件全部で、
  現金や売掛金を選べた。この科目は**償却仕訳の貸方**になるため、誤選択すると
  「借 減価償却費 / 貸 現金」となり現金が毎年静かに減る（内部振替なので消費税集計表にも出ない）。
  `accounts.is_fixed_asset_account` を新設（ddl/580）し、候補条件と**償却仕訳生成の関門**の
  2 か所で効かせた。表示区分で絞らなかった理由は ADR-0063 に記載
  （区分＝財務諸表の並び順であって、台帳の対象とは一致しない）。

**検証**: designcheck 0 件／check_navigation errors=0（frames 15）／実機で
①サイドバー「設定」→ `/Settings/Start` 着地 →「アカウント」配下から `/Settings/PasswordChange`
②固定資産の候補が 18→4 件（建物附属設備・工具器具備品・ソフトウェア・ソフトウェア仮勘定）に
  絞られること（登録画面・検索画面の両方）
③**否定テスト**: 1520 のフラグを一時的に外して償却仕訳生成 → 赤トーストで停止・仕訳 0 件増（76 のまま）
④**肯定テスト**: フラグを戻して同じ操作 → 関門を通過して確認ダイアログが出ることを確認（キャンセルで終了）
⑤既存レコードの科目表示が消えないこと（CLB は候補外でも現在値を選択肢に足す・実測）
**検証で触ったデータは全て復旧済み**（1520 のフラグ）。DB は 76 伝票・202 明細・資産 2 件で変化なし。
