---
title: 失わないためのルール
status: current
scope: 全体
audience: [開発]
updated: 2026-09-21
supersedes: []
related: [30_作業のルール.md, 31_検証のルール.md, decisions/0044-削除はごみ箱送りに一本化しrmを機械で止める.md, decisions/0046-稼働DBの退避と復元を戻せる道具に閉じる.md]
---
# 失わないためのルール

> **消すとき・稼働 DB を戻すとき・保護対象を上書きするときの判断**（§1）と、**開発用 DB を Claude がどう管理するか**（§2）を持つ。
> **「保護対象」とは、消えたり上書きされたりすると戻せないので、機械が経路を塞いで守っているファイルのこと**である
> ——稼働 DB・追跡外の環境設定・`.git` など。**一覧と、載せる基準は [`tools/claude/protected_paths.json`](../tools/claude/protected_paths.json)。**
> **2026-09-15 に [30_作業のルール](30_作業のルール.md) の §10 から出した**——
> 30 は毎タスク読む文書なので、**工程のときにだけ要るものを分けた**（[00 §5](00_ドキュメント規約/README.md)）。
> **コマンドの書式と道具ごとの使い方は [tools/README](../tools/README.md) が持つ。ここは判断だけ。**

## 1. 消すときは `trash`、稼働 DB を戻すときは `db_snapshot`。保護対象を控えなしに上書きしない

**ごみ箱送り（`trash.ps1`）も、稼働 DB の退避と復元（`db_snapshot.ps1`）も、
開発者の確認を待たずに実行してよい**（開発者の指示。2026-09-07 と 2026-09-08）。
**確認プロンプトで自律作業が止まるのを避けるためであり、許せるのは「戻せる」からである**——
ごみ箱は復元でき、`db_snapshot` は何も消さず、戻す前に必ず現状を退避する
（経緯は [ADR-0044](decisions/0044-削除はごみ箱送りに一本化しrmを機械で止める.md)・[ADR-0046](decisions/0046-稼働DBの退避と復元を戻せる道具に閉じる.md)）。
**この 2 つは、[34](34_開発者とClaudeの分担.md) の境界（開発者の担当に当たらないことは Claude が行う）の実例である**（[34 §2-1](34_開発者とClaudeの分担.md)）。

**稼働 DB は、退避を取ってから壊す。**

- **マイグレーションを稼働 DB へ当てる前と、壊す実験の前に `-Save` を打つ**（[31 §1](31_検証のルール.md) の検証ループ）
- **`cp` で写しを取らない**（使用中の SQLite を複製すると壊れた写しができる。
  **一次情報と機序は [ADR-0046](decisions/0046-稼働DBの退避と復元を戻せる道具に閉じる.md) の理由**）。**関門は `cp` を直に打つ形を今までどおり拒む**
- **`db_snapshot.ps1 -Restore` は、保護対象（稼働 DB）を無確認で上書きする経路として認めた唯一のものである**
  （`Edit` は確認つきで通る。下。**機械の側にはまだ穴②が残っている**ので「機械的に唯一」ではない）。
  §1 の見出しが「**控えなしに**上書きしない」と言っているのはそのため——**`cp` を直に打つやり方は拒み、戻せる道具に閉じた 1 本だけを通す**。
  **2 本目はできたが、この行は変わらない**——[`worktree_db.ps1`](../tools/clb/worktree_db.ps1)（2026-09-16）が
  書くのはワークツリーの写しで、[protected_paths.json](../tools/claude/protected_paths.json) に行が無い。
  **「作り直せるから」ではない**——ワークツリー側で入れた帳簿データは作り直せない。
  **あの道具が退避して残すので消えない**からである（載せる基準そのものは同ファイルの `_README_criteria`）。
  **ただし機械の側は対称ではない**——`guard_delete.py` はワークツリーの中から走ると根を 2 つ持つので、
  **ワークツリーの中の Claude から見ると守られ、本体側から打つこの道具からは守られない。**
  **行き先を許す条件の正典は、あの道具の `.DESCRIPTION`** である（ここに写さない）
- **溜まった退避もごみ箱の中身も、開発者が消す。** Claude は触らない。
  **ただし守り方が違う**——**退避（`LocalData/backup/`）は保護対象なので機械が消させない**（[34 §2-2](34_開発者とClaudeの分担.md)）。
  **ごみ箱の中身を空にする経路は、拒む仕組みが無い**——`trash.ps1` の引数に空にする道は無く、
  `Clear-RecycleBin` は `guard_delete.py` の語彙にも `settings.json` の `deny` にも無い
  （**2026-09-19 にその 3 つを読んで確かめた。打って試してはいない**）。
  **`allow` にも無いので、打てば確認は出る**——**拒まれはしないが、素通りもしない。**
  （**`LocalData/temp/` は守らない**——
  いつ消えてもいいものの置き場。開発者の指示。2026-09-10。守るのは正典に載せた行だけである）

**機械が何を拒むか。語彙の正典は `tools/claude/guard_delete.py` の正規表現**であって、この箇条書きではない。
**「語彙」とは、守りが探す語の一覧のこと**である——**そこに無い綴りは素通りする。**

**守りは呼び出し全体を拒む。だから呼び出しを分ける。** 保護対象の名前と書き込み（`>`・`sed -i`・
python の書き込みなど）が**同じ 1 つのコマンド文字列に同居**すると、`&&` で繋いだ無害な `git add` まで
道連れに、**何も実行されない**。**守りはコマンド文字列の字面しか見ないので、引用の中でも設定値でも同じ**
——保護対象の名前を含む**文章**をヒアドキュメントで書き足そうとしても拒まれる。
**書き込みはファイルに逃がして `python <ファイル>` で実行し、`git add` は単独の呼び出しにする。**

- **削除にあたるコマンドは、当たり先によらず拒まれる**（`rm`・`Remove-Item`・`shutil.rmtree`・
  `[IO.File]::Delete` など）。**拒むときは代わりのコマンドが理由文で示される**
- **コミットしていない変更を消す git（`clean`・`reset --hard`・`restore`・`stash drop`・`checkout -- ` など）も拒まれる**
  ——**「Git で戻せないものを守る」と決めた以上、git 自身の破壊コマンドを外す理由が無いから**である。
  **副作用がある**（`git clean -n` の空打ちや、1 ファイルだけの `git checkout -- ` も止まる）。
  **逆に、`--` を書かない `git checkout <パス>` は通る**ので、
  **コミットしていない作業ごと捨てられる経路がここに残っている**（[31 §1](31_検証のルール.md) の控えの話）。
  **塞げない**（Claude の整理。2026-09-18）——**ブランチの切り替えと字が同じ**なので、
  正規表現でも控えでも `git checkout main` まで巻き込む。
  **要るときは開発者に声をかける**（開発者の指示。2026-09-07）
- **作業コピーを戻したいときは `git restore` ではなく `git stash` を使う**
  （**開発者の決定。2026-09-18**。**理由は「`restore` の射程が『編集中のファイル』ではなく打ったパスだから」**——
  `git restore .` は誰が編集したかに関係なく配下の未コミットの変更を全部捨てる）。
  **以下は Claude の整理。** **捨てる形は拒み、退避する形を通す**——`stash` は中身を残し、
  `git stash list` に出るので、**戻し損ねたことに気づける**。ごみ箱・`db_snapshot` と同じ形である。
  **溜まった stash は開発者が消す**（`drop`・`clear` は拒まれる。退避・ごみ箱と同じ扱い。上）——
  **`git stash pop` は通る**ので、適用して消す経路は塞がっていない。
  **拒みすぎる形の一覧は [`guard_delete.py`](../tools/claude/guard_delete.py) の「限界」が持つ**（ここに写さない）
- **ブランチを消すときは `git branch -d`（`--delete`）を使う。強制する指定は拒まれる**
  （**開発者の指示。2026-09-17**）。**ブランチや reflog を強制的に動かす git**——
  `update-ref`・`reflog expire`／`delete`／`drop`・`gc --prune`——**も同じく拒まれる**
  （**開発者の指示。2026-09-18**。**「Claude がこれらを実行することはないと考えて対策なしでいい。
  一瞬で完了するなら足していい」**）。
  **以下は Claude の整理。** **`-d` は git 自身がマージを検査する**
  （いま checkout しているブランチ、上流があれば上流）ので、**失うものが無い**。
  **強制で動かしたブランチはコミット済みなので git で戻せる**——ここだけは「Git で戻せない」に当たらない。
  **拒むのは、載せる基準の 2 句目「戻し損ねたことに気づけるか」に当たるから**である
  （基準の正典は [`protected_paths.json`](../tools/claude/protected_paths.json) の `_README_criteria`。
  **ファイルの基準を git の側にも当てた**）。
  ごみ箱は開けば元の名前で中身が見え、`db_snapshot` は書く前に必ず退避するが、
  **reflog は何を失ったかを既に知っている人しか掘れない**——
  **`reflog expire` と `gc --prune` はその reflog そのものを消す**ので、いちばん戻らない
- **保護対象への `Write`（全上書き）と、保護対象の名前が出る上書きのコマンド**（`sed -i`・`Copy-Item`・
  `> ファイル` など）**も拒まれる。上書きは削除より強い**——ごみ箱は前の中身を残すが、上書きは残さない。
  しかも `Write` は中身を照合しないので、**想定と違うファイルでも黙って通る**。
  **`Write(...)` の形の権限規則は参照されない**ので、`.claude/settings.json` に**同じ形の控えは置けない**
  （[ADR-0044](decisions/0044-削除はごみ箱送りに一本化しrmを機械で止める.md)）。**控えは `ask` の側が兼ねており、
  フックが起動しなければ上書きは拒否ではなく確認に落ちる**
- **保護対象を直すときは `Edit` を使う**（拒まれないが、**確認が入る**）。
  `Edit` は `old_string` の照合があるので**取り違えでは壊れない**。
  ただし**照合を通す字は読めば手に入るので、意図すれば空にもできる**——だから確認を挟む
- **保護対象を新しく作る素直な経路は、どれも塞がっている。開発者に頼む**
  （**これは開発者の担当だからではなく、機械が塞いでいるからである**——[34 §2-2](34_開発者とClaudeの分担.md)。
  `Write` はフックが拒み、`Edit` は既存のファイルにしか当たらない。
  **`.sample` からの複製 4 通りを 2026-09-08 に打ったところ、4 つとも行き先の名前で拒まれた**）。
  **ただし「できない」と決めつけない**——`guard_delete.py` が見ている語は網羅ではないので、
  **語彙に無い書き方は通りうる**（`guard_delete.py` の「限界」）。
  新環境の手順は [README](../README.md) の「セットアップ（新環境）」にあり、**これは開発者の作業**である
- **消せないものがある**——**一覧も、載せる基準（消えたとき、戻す操作が現実的か・戻し損ねたことに気づけるか）も、
  正典は [`tools/claude/protected_paths.json`](../tools/claude/protected_paths.json) の 1 ファイル**で、
  フックと `trash` の両方がそれを読む。**消す必要が出たら開発者に頼む**
- **保護対象を 1 つでも内側に含むフォルダは、丸ごとは消せない**（`trash` が拒む）。
  リポジトリの根・`Designer`・`LocalData` などが該当する
- 追跡ファイルを消すのは怖がらなくてよい——ただし**コミット済みであれば**である。
  **コミットしていない変更は git でも戻せない**

**「戻せる」には穴が 2 つある**（**どちらも埋めないまま進むと開発者が決めた**。2026-09-08。
**材料は「該当する保護対象がまだ 1 つも無い」と「絞ると正当な作業まで止まる」**——
[ADR-0044](decisions/0044-削除はごみ箱送りに一本化しrmを機械で止める.md) の決定 12）。
①**ごみ箱の容量を超えるものは完全削除になるとされ、`trash` は拒めない**
（**未確認**。実測も一次情報も取っていない）。
②**削除の語が字面に出ない形**（変数に入れる・パスを組み立てる）**と、保護対象の名前が出ない上書き**は、
開発者への確認ではなくそのまま実行になる（`.claude/settings.json` に白紙の許可があるため）。
**削除は、語さえ出ていれば当たり先によらず拒まれる**——名前の有無で決まるのは上書きだけである。
**`Write` ツールの上書きも違う**（フックが落ちれば確認に落ちる）。
**だから「機械が止めてくれる」に寄りかからない。**

**道具を許可リストに足すと、その道具は語彙の外から来る。**
`iconv` と uv を `.claude/settings.json` の allow に足したのは**開発者の決定**（2026-09-16。
**自走中に許可の確認で止まらないため**——制度を調べる回は 1 回で何本も原文を取る）。
**足した当日の自己レビューで、2 つとも語彙に無いことが実測で分かった**——
`uv venv --clear` は**削除の語を 1 つも書かずにディレクトリを丸ごと消し**、
`iconv -o` は**上書きの語を 1 つも書かずに保護対象へ書く**。
**どちらも語彙に入れて検体で縛り、uv の許可は `uv run` の形だけに絞った**
（`Bash(uv:*)` は前方一致で `uvx` まで覆う。実測）。

**型は「許可を広げる側にだけ計器が無い」**だった。**2026-09-16 に機械へ寄せた**——
`guard_delete.py --selftest` は `deny` を語彙と双方向に突き合わせ、`ask` を正典から生成して形まで見るのに、
**`allow` は形しか見ていなかった**（権限判定が参照しない書き方が無いか、だけ）。
いまは **`.claude/settings.json` の `allow` の 1 行 1 行を、棚卸しと双方向に結ぶ**。

- **`Bash(…)` / `PowerShell(…)` の行は `ALLOWED_TOOLS` と、道具そのものの名前は
  `ALLOWED_TOOL_NAMES` と結ぶ**（後者は「消せるか」を持たない）。
  **道具を allow へ足したら、棚卸しにも足す**——足さないと `--selftest` が赤くなる。
  **逆に、allow から外したのに棚卸しに残っていても赤くなる。**
- **何を書くかの正典は、`ALLOWED_TOOLS` の上のコメントである**（ここに写さない）。
- **機械が見るのは 3 つまで**である——①字が書いてあるか ②名指しした守りが実在するか
  ③**その道具で拒まれる検体が 1 つ以上あるか**。
  **その道具の書ける形を数え切ったかは見ない。**
  だから**字を書かずに広げることはできない**が、**書いた字が当たっているかは人が確かめる**
  （`--help` の全文を読み、`decide()` に検体を当てる）。
- **「消せない」と書いた行に、機械が要求するのは字があることだけ**である
  （語彙に載っている道具を「消せない」と書いたときだけ鳴る）。**分類の誤りは人しか見つけられない。**
- **いまも「見ない」で通っている道具がある**（一覧と理由は `ALLOWED_TOOLS`）。
  **追うのは [README の保留リスト](README.md)。**

**棚卸しを作った回に、守りの穴が 10 か所見つかった**（2026-09-16 の自己レビュー。すべて実測）。

- **語彙に無かった 4 つ**——`zip`・`Compress-Archive`・`uniq`・`awk -i inplace`
- **綴りを 1 つしか見ていなかった 5 つ**——`sed -i`（`sed -E -i` と `--in-place` が素通り）・
  `curl -o`（`--output` ほか 4 つ）・`iconv -o`（`--output`）・`sort -o`（`--output`）・
  `-OutFile`（PowerShell は `-outf` でも届く）
- **「読むだけ」に分類していた 2 つ**——`Get-Item` / `Get-ChildItem` が返す `FileInfo` は
  `CopyTo`・`MoveTo`・`OpenWrite`・`AppendText` を持つ
- **いちばん重いのは `git`** である。**`clean` と `reset --hard` しか見ておらず、
  `git restore .` は素通りだった**——この守りが据える「戻せないのはコミットしていない変更」の、
  正面の道具が抜けていた

**見ているのは `.claude/settings.json` だけである。** 追跡外の `settings.local.json` と
`Designer/.claude/` の側は結んでいない（[README の保留リスト](README.md)）。

**操作制限の実体（`tools/claude/`・`.claude/settings.json`）は保護対象に載せない**
（開発者の判断。2026-09-08。逐語は「**これらは Git 追跡されていますよね？ よしとしましょう**」——
**Git 追跡下にあり、壊しても戻せる**ので載せる基準に当たらない。載せると `trash` まで拒み、
正当な作業が止まる。[ADR-0046](decisions/0046-稼働DBの退避と復元を戻せる道具に閉じる.md) の決定 8）。
**つまり Claude は操作制限を無確認で書き換えられる。**
**`deny` の控えを広げるのも Claude が行った**（開発者の指示。2026-09-18。**「今回は Claude が行ってください」**——
**一回限りの委任であって、担当が移ったわけではない**。**正典は `guard_delete.py` の `GIT_DENY_FALLBACK`** で、
`settings.json` に手で足さない）。**ただし操作制限を緩めるのは別で、開発者が決める**（[34 §1](34_開発者とClaudeの分担.md) の 8 と [34 §1-3](34_開発者とClaudeの分担.md)。2026-09-19）——
`allow` に道具を足すと、**その道具を使うときの確認が出なくなる**。**フックは `allow` と関わりなく毎回走る**が、
**その道具の消し方・上書きの仕方が語彙に無ければ、確認もなく通る**（**上の `iconv`・uv がその実例**）。
直したら `--selftest` を通し、
**「壊したら鳴るか」まで確かめる**（[31 §1](31_検証のルール.md)）。

## 2. 開発用 DB は、Claude が管理する

**開発用 DB——稼働 DB と、その退避・ワークツリーの写し——は、Claude が開発者の確認なしに管理する**
（**開発者の決定。2026-09-17**。**退避を消すことだけは除く**——下の 6）。
**理由も開発者の言葉である**——**DB の管理は全面的に Claude に任せる。あくまで開発用の DB であり、
その内容はそこまで重要ではなく、万が一作業に失敗しても影響は小さいから**（逐語はコミット 553899f4 までの git log が持つ）。
**[34 §1](34_開発者とClaudeの分担.md) の 6（影響重大 かつ 不可逆か高リスク）に当たらない**——退避を取れば戻せ、開発用のデータだからである
（判定は [34 §1-2](34_開発者とClaudeの分担.md)。**「開発用 DB は重要な情報ではない」は開発者の字である**）。

**以下の表と作法は Claude の敷衍である**（開発者が述べたのは上の 1 文と理由まで。2 と 3 の機械は [ADR-0064](decisions/0064-稼働DBのスキーマはmigrateだけで動かし適用の記録はコミットごとに突き合わせる.md) で開発者が採った）。

| 操作 | 道具 | 前に取る退避 |
|---|---|---|
| マイグレーションの適用 | `migrate.ps1 -Apply` | 要る（§1） |
| `schema_migrations` の記録の訂正 | `sql.ps1` で `UPDATE` 1 文（条件と手順は [migrations の README](../Designer/migrations/README.md)） | 要る |
| 正典からの作り直し | `sql.ps1 -AllowDdlOnce -Reason "正典からの作り直し"` で印を置いて `DROP` を流し、`ddl/`（`-File Designer/ddl/<ファイル>` を番号順に。追跡済み・未変更のものだけ通る）＋ `seed/` を流して `-Adopt`（作り直す方針は [ADR-0020 §6](decisions/0020-スキーマは現在形の正典で持ち変更は差分で配る.md)。**帳簿データが要るなら退避から戻す**） | 要る |
| 退避からの復元 | `db_snapshot.ps1 -Restore`（サーバを止めてから。§1） | 道具が取る |
| 検査・投入・掃除（`SELECT`・`INSERT`・`UPDATE`・`DELETE`） | `sql.ps1` | 壊す実験なら要る |

**守る作法。** 機械が止めるものと、字でしか守っていないものを分けて書く。

1. **壊す前に退避を取る**（§1。**機械は止めない**）
2. **スキーマは `migrate.ps1` だけで動かす。`sql.ps1` で DDL（`CREATE`・`DROP`・`ALTER`）を流さない**——
   記録（`schema_migrations`）が残らず、後から「この DB に何を当てたか」が追えなくなる
   （2026-09-16 に適用済みのトリガを手で作り直し、記録が残らなかった——[qa/04](qa/04_実機操作テスト/2026-09.md) の 2026-09-17）。
   **機械が止める**——`sql.ps1` が DDL を拒む（[ADR-0064](decisions/0064-稼働DBのスキーマはmigrateだけで動かし適用の記録はコミットごとに突き合わせる.md)）。通るのは `-File Designer/ddl/<ファイル>`（正典を流し直すとき。追跡済み・未変更のものだけ）と、
   `sql.ps1 -AllowDdlOnce -Reason "<理由>"` で印を置いたあとの 1 回だけ（印は読んだら消える。`.git/` への書き込みは字面では拒まれるので道具が書く）
3. **適用したマイグレーションは、コミット前でも編集しない**（[migrations の README](../Designer/migrations/README.md)）。
   **機械が止める**——`migrate.ps1 -Verify` が記録とファイルのチェックサムを突き合わせ、コミット前フックが毎回流す（[ADR-0064](decisions/0064-稼働DBのスキーマはmigrateだけで動かし適用の記録はコミットごとに突き合わせる.md)）。
   適用してからコミットするまでの窓は `check_frozen.py` の外だったが、これで閉じた
4. **DB を触った操作は [qa/04](qa/04_実機操作テスト.md) に残す**——何を・いつ・どの退避を取ったか。
   **1 文の `UPDATE` でも書く**（記録が無いと、翌日の自分が追えない。**機械は止めない**）。
   置き場が qa/04 なのは、稼働 DB を触った記録が以前からそこにあるからである（2026-09-07 の 0008 の訂正）
5. **本番相当のデータを入れない**（**機械は止めない**。「重要な情報ではない」はデータの性質に立っている——
   利用者の実データが入った瞬間に §2 の前提が崩れる）
6. **溜まった退避を消すのは開発者**（§1）——**理由と、同じ形のものの一覧は [34 §2-2](34_開発者とClaudeの分担.md)**
