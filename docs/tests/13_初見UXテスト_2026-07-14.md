# 初見ユーザーUXテスト（2026-07-14）

> 注: 本書は実施時点（旧ロール別フレーム）の記録。2026-08-02 の部品再編（ADR-0040/0041）後の画面・権限は docs/10 と tests/19 の読み替え表を参照。

> **手法**: アプリの実装知識を持たない AI エージェントに「初めて社内システムを触る社員」のペルソナを与え、業務ミッションだけを渡してブラウザで実際に操作させる。マニュアル無し・画面の表示だけが頼り。エージェントが詰まった箇所・迷った箇所・質問した内容＝**画面の説明が足りない箇所**として収集する（ユーザー発案 7/13）。
> **制約と読み方**: エージェントは人間より文字を丁寧に読む傾向があるため、「エージェントが詰まった＝人間も高確率で詰まる」は成り立つが、逆は保証されない（改善ポイントの検出器であって証明ではない）。
> 対応状況の凡例: ✅=本セッションで対応済み ／ 📋=バックログ（要ユーザー判断） ／ 💬=仕様どおり（説明改善のみ検討）

## テスト構成

| Phase | ペルソナ | ミッション | 状態 |
|---|---|---|---|
| 1 | 開発2部 一般 松本結衣（新入社員） | 経費申請（立替・事前）・工数入力・通知確認・取引先確認 | 完了 |
| 2 | 開発2部 第一課長 佐々木綾（システム初見の管理職） | 承認処理・部下の工数確認・予実確認・自分の申請（自己承認回避の観察） | 実施中 |
| 3〜 | 経理 山田太郎 ほか | 経理処理・販売/購買/会計/財務帳票/予算/案件/マスタの全フレーム | 予定 |

---

## Phase 1: 一般社員（松本結衣・経費/工数/通知/取引先）

**結果**: 5タスク全完了（ただし日付入力で人間なら停止級の詰まりが1件）。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U1-1 | **日付欄の手入力が壊れる**: 「2026/07/10」とキー入力すると年欄に「202607」と6桁入り、通常のキー操作では復旧できない。エージェントはスクリプト実行で回避したが「実際の新入社員ならここで完全に手が止まる」。※親エージェント（Fable）も同日の自律テストで同じ問題を踏んでJS入力に切り替えた実績あり | 高→低 | 💬 **対応しない（2026-07-16 ユーザー判断）**: ブラウザ標準 date input の仕様であり、人間のユーザーは日付ピッカーを使う想定のため。（なお自動テスト時は JS で値をセットする＝運用知見として記録済み） |
| U1-2 | **経費申請一覧に全社員の申請が出る＋他人の行にも削除ボタンが見える**: 「他人の金額が見えるのもどうなのか」「誤クリックが怖くて一覧操作に緊張した」 | 高 | ✅（2026-07-16）閲覧範囲はユーザー判断で現状維持（部門で絞れるよう申請一覧に**部門列＋部門検索**を追加＝部門スナップショット方式）。削除は**一覧の削除ボタンを全面撤去**し、詳細画面に「自分が起案 かつ 精算=下書き」のときだけ出る削除ボタンを新設（実機検証済み。API直叩き対策は今回はドキュメント記録に留める=ユーザー合意） |
| U1-3 | **「見込み額」と「金額（税込）」の使い分けが説明ゼロ**: 事前申請選択で突然出現し、両方に同額を入れてしまった（実害はないが正しい運用は見込み額のみ） | 中 | ✅ プレースホルダを「見込み額を入力（金額（税込）欄は空欄のまま＝承認後の実費確定で入ります）」に変更 |
| U1-4 | 「うち消費税」空欄時の挙動（内税10%自動計算）が画面から読めない | 中 | ✅ プレースホルダを「レシート記載の消費税額（記載がなければ空欄＝内税10%で自動計算）」に変更 |
| U1-5 | 工数の**分単位入力**: 「3時間」を180に自分で換算。既定値480が8時間だと初見では分からない | 中 | 📋 時間単位入力の併用は ADR-0009（分単位 INTEGER）の設計変更を伴うため見送り。ラベルは ✅「時間（分・60分=1時間）」に変更（TimeEntry/TimeEntryAdmin） |
| U1-6 | 工数の新規保存で「**更新しました**」トースト: 新規なのに更新と言われ不安になる | 低 | 📋 CLB 標準トーストの文言（新規/更新の区別なし）。FB 候補 |
| U1-7 | 通知一覧が「経費精算」フレーム配下にあるのが直感に反する（全業務の通知では？） | 低 | 💬 現状の通知発火源は経費承認のみなので実害なし。通知源が増えたら再配置を検討 |
| U1-8 | ホームの「進行中のあなたの申請: 3件」をクリックで一覧に飛びたい | 低 | 📋 ラベルのリンク化（AnchorTag化）は可能。次回UI改善の候補 |
| U1-9 | 検索条件の「部分一致/一致」ドロップダウンが全項目に付いていて初見では意味不明 | 低 | 💬 CLB 標準の検索比較UI。一般ユーザーには冗長だが機能としては有用 |

### 良かった点（エージェントの自発的評価）
ログアウト確認ダイアログ／担当者・日付・精算対象者の自動セット／各画面冒頭の説明文（レビュー第3弾の成果）／申請後に即「申請中」が見える／取引先一覧の「閲覧専用。登録・変更は経理に依頼」の案内と○—表記。

### 教育係への質問（=画面が答えられなかったこと）
1. 事前申請で金額と見込み額の両方に入れてしまったが正しいか → U1-3 で対応
2. レシートに税額記載がない場合の「うち消費税」 → U1-4 で対応

---

## Phase 2: 承認者（佐々木綾・承認/予実/自己申請）

**結果**: 5タスク全完了。ホーム通知→開く→承認の動線は「迷わず到達できた」と高評価。**自己承認防止の実機観察**: 課長本人の申請（8,800円）は同格の第二課長・山口健太に自動で回り、本人には承認ボタンが出ない＝設計どおりを初見ユーザーが確認。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U2-1 | **日付入力の6桁問題を承認者も再現**（工数検索・経費申請の2画面で計5回試行）。Phase 1 と合わせ**全員が踏んでいる** | 高 | 📋 U1-1 と同一（CLB DateField / ブラウザ標準挙動。FB 候補に格上げ） |
| U2-2 | **フロー表示の記号 ✓▶— に凡例が無い**。「(▶山口,▶佐々木)」が AND か OR かは承認後に「—」を見て初めて分かる | 中 | ✅（2026-07-16）フロー表示の下に凡例「✓=承認済　▶=承認待ち（カッコ内の複数名は並列＝1人の承認で先へ進みます）　—=スキップ」を常設（実機確認済み） |
| U2-3 | **「⚠超過警告」の文言**: 消化率83%（未超過）でも「超過警告」と出るため「予算を超えた」と誤読する | 中 | ✅ 「⚠予算警告」に文言変更（閾値80%は従来どおり制度閾値） |
| U2-4 | **承認済のその後が見えない**: 「承認済」が支払済みか経理処理待ちか画面から読めない。「部下にいつ振り込まれるか聞かれても答えられない」 | 中 | ✅（2026-07-16）「申請」ページの説明文に精算ステータスの遷移（下書き→申請中→承認済→精算済。承認済=経理の振込待ち）を明記 |
| U2-5 | 一覧の「状態」（フロー）と「精算」（精算ステータス）の2列の違いが初見で分からない | 低 | ✅（2026-07-16）U2-4 の説明文で「状態=承認フロー／精算=経理処理」の違いも同時に説明 |
| U2-6 | **領収書未添付のまま承認まで通る**: 「承認者が証憑チェックの責任を負わされて怖い」 | 中 | ✅（2026-07-16）ユーザー判断=(a)警告方式: 未添付のまま申請すると警告トースト「領収書が添付されていません。後から添付するか、紙の原本を保管してください」を表示（申請自体は通る。実機で警告表示を確認） |
| U2-7 | 承認画面に**その費目の部門予算残高が出ない**（予実対比と未連動） | 中 | 📋 承認画面への予算サマリ差し込みは価値の高い機能追加候補 |
| U2-8 | **部下の申請の所在が見えない**: 松本の45,000円事前申請が部長ルートで進行中だが、課長からは「今誰の所で止まっているか」を見る画面が無い（申請中ビューは自分専用） | 中 | 📋 「部門の申請一覧（役職者向け・閲覧専用）」の新設候補 |
| U2-9 | 毎日数十件になると1件ずつ詳細を開く承認は辛い（一括承認・サマリ付きリスト） | 低 | 📋 一括承認は内部統制との兼ね合いがあるため保留 |
| U2-10 | 検索0件時に「データが無い」のか「条件が悪い」のか分からない（第19期予算未登録で数分迷った） | 低 | 💬 CLB 標準の0件表示。FB 候補 |
| U2-11 | 担当者選択ダイアログが**ダブルクリック**で確定と気づきにくい | 低 | 💬 CLB 標準のピッカー挙動。FB 候補 |

### 教育係への質問
- 第19期の予算が未登録なのは想定どおりか → 想定どおり（繰越直後の当期予算は未策定という自然な状態）。「0件の理由が画面から読めない」点を U2-10 として採用。

---

## Phase 3: 経理（山田太郎・経費精算処理/購買/振替伝票/帳簿・帳票）

**結果**: 5タスク全完了。生成仕訳（No.56〜59）はすべて複式簿記として妥当（費用計上=利用日付・支払=処理日付・内税分離が「正しい」と経理ペルソナが自発的に評価）。試算表の貸借検算 14,577,695=14,577,695 一致。経費の「仕訳を生成→精算済→完了」のボタン誘導、購買の一気通貫、振替伝票の差額リアルタイム表示、ホーム経営サマリは「市販ソフトより優れる」と明記された。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U3-1 | **日付入力の破綻を経理も再現**（仕訳帳の期間絞り込み・4回試行）。「経理は日付をテンキーで打つ職種。最優先で修正を」。**全3ペルソナが同一箇所で詰まった** | 高 | 📋 CLB の DateField（ブラウザ標準 date input）の問題として **FB 起票が妥当**（アプリ側で回避不能）。docs/12 へ |
| U3-2 | **仕訳生成後に伝票番号が画面に出ない**: 「仕訳を生成」「未払計上」後、何番の伝票ができたか分からず仕訳帳で自力捜索 | 中 | ✅（+📋）**調査の結果、経費・購買・検収・入金・償却・入出金など主要な生成箇所は既にトーストへ伝票No.を表示済み**（エージェントはトーストの自動消滅で見逃した）。唯一未表示だった**銀行の一括起票にNo.一覧を追加**（トーストと結果ラベル両方）。「トーストが消えると追えない」問題自体は残るため、トースト履歴/常設結果表示は FB 候補 |
| U3-3 | **経理向け「精算処理待ち」受信箱が無い**: 承認済＝経理の作業対象を申請一覧から目視で探す必要 | 中 | ✅（2026-07-16）経費精算フレームに経理専用「精算処理待ち」ビューを新設（承認済/経理処理中/精算済で絞り込み・件名/金額/利用日/申請者/部門/精算列＋「開く」で詳細へ。読取=経理+sysadmin。実機検証済み） |
| U3-4 | 試算表・仕訳帳の期間既定が「全期間」: 「当月既定＋月選択が定石」 | 中 | ✅ 試算表・仕訳帳・総勘定元帳・出納帳の検索初期値に**当年度の期首〜期末**を自動セット（Search_OnInitialization。URL 指定があれば尊重）。試算表で繰越残高 5,000,000・普通預金残高 +4,737,120 の正常表示を実機確認（U3-8 も同時に根治） |
| U3-5 | 費用科目ドロップダウンに**全勘定科目**（現金・資本金まで）が出る（仕入先請求書） | 中 | 📋 費用系科目への候補絞り込み（SearchCondition に account_type 条件を足すだけ）。次回改善候補 |
| U3-6 | 「印刷製本費」等の一般的な科目が無く経理判断で消耗品費を選択 | 低 | 💬 科目マスタは sysadmin が追加できる設計（デモの科目表は72科目に絞っている）。運用で解決可能 |
| U3-7 | 伝票一覧の並びが伝票No.順でない（取引日降順のため 51,41,40…と見える） | 低 | 💬 列ヘッダクリックでNo.順ソート可能。既定ソートの是非は好みの範囲 |
| U3-8 | **普通預金残高が -970,620 円に見える**（現金も -25,300 円に見えた）: 「経理なら即調査する数字」 | 中 | 💬→📋 **調査の結果、データ異常ではなく表示仕様**: 期首残高マスタには 現金100,000／小口50,000／普通預金5,000,000 が正しく存在し（ホームKPIの現預金 4,238,680 とも整合）、**試算表を期間未指定（初期表示）で開くと期首繰越列が 0 になり、年度内の動きだけでマイナスに見える**のが真因。「初見の経理が実在しない異常値に驚く」実害があるため、**U3-4（期間既定値を当年度にする）と同一改修で解消するのが正**。改修候補に統合 |

### 教育係への質問（=画面・データが答えられなかったこと）
1. 小口現金の補充は「小口現金」勘定か「現金」勘定か（過去伝票No.20は小口現金/普通預金、今回のNo.59は指示の文言どおり現金/普通預金で起票）→ 社内ルールとしては小口現金が正。**ミッション指示文の曖昧さ由来なのでUX問題ではない**が、「同じ摘要の過去伝票と科目が揺れてもシステムは何も言わない」のは摘要ベースの科目サジェスト（仕訳ルールの振替伝票版）という将来アイデアにつながる
2. 普通預金のマイナスは既知か → U3-8 として記録

---

## Phase 4: 経理（山田太郎・販売チェーン一気通貫）

**結果**: 見積 Q-26-004 → 受注 SO-26-004 → 検収 A-26-004 確定（売上仕訳 No.60）→ 請求書 → 入金消込（No.61）→ 売掛残0 → SES精算プレビュー（PRJ-005 控除 700,000／PRJ-003 超過 845,000）まで**7タスク完遂**。「メニューが業務フロー順」「検収確定トーストに仕訳番号・金額」「入金候補が未消込請求書のみ」「番号体系・採番」は市販ソフト比でも高評価。

### 「請求書を作成が無反応」の調査結果（最重要事項）

エージェントは検収画面の「請求書を作成」が3回無反応と報告し、請求書を手作成して迂回した。**親エージェントが実機で再現調査した結果、アプリは正常**:
- ボタンは disabled=false（有効）。JS クリックで INV-26-010 が正常作成され、請求書詳細へ遷移・トーストも表示
- 既請求ガード（「この検収の請求書は既に作成済みです」）も正常動作
- 真因は**ブラウザ自動操作の座標クリックが Blazor ボタンで空振りする既知現象**（Project.md 記録済み）。人間のマウス操作では起きない
- 事後処理: 診断で作った INV-26-010 は削除し、手作成の INV-26-009 を検収 A-26-004 に紐付けて（SQL）チェーンを整合させた。二重請求ガードの効きも確認済み

ただしこの誤認の背景にある指摘は正当: **確定済み検収の主要アクション「請求書を作成」がグレー（Secondary）で「無効に見える」**こと（下表 U4-1）。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U4-1 | **ボタンの色と重要度・危険度が不一致**: 確定後の本命アクション「請求書を作成」がグレー（無効に見える）。逆に SES 画面では安全な「精算を計算」がグレーで**売上仕訳込みの「請求書を一括生成」が最も目立つ青** | 中 | ✅（2026-07-16）規約化して適用: 検収「請求書を作成」=Primary、SES「精算を計算」=Primary・「請求書を一括生成（売上仕訳込み）」=Danger（赤=重い副作用の明示。実機確認済み） |
| U4-2 | **手作成請求書を検収に後から紐付けられない**（請求書フォームの「検収」はラベルのみ）。紐付け欠落中は検収側の既請求ガードが効かず**二重請求リスク** | 中 | ✅（2026-07-16）請求書フォームの検収参照を編集可能化し、候補を確定済(confirmed)検収に絞り込み（表示は U4-3 のラベルで「番号 取引先 件名」。実機確認済み） |
| U4-3 | 参照ドロップダウンが**番号のみ表示**（検収の受注選択・入金の請求書選択）。「実務では受注一覧を別画面で開いて突合することになる」 | 中 | ✅（2026-07-16）見積/受注/検収/請求書に select_label 列（「番号 取引先名 件名」）を追加し、参照ドロップダウン4箇所の表示を統一。保守は**DBトリガー**（手動/検収/SES/定期の全INSERT経路と取引先名変更に自動追随=ddl/280）。実機確認済み |
| U4-4 | 見積・請求の明細**税区分に仕入系が混在**（11択） | 中 | ✅（2026-07-16）見積/受注/請求の明細税区分を売上系6候補（課税売上10%/8%軽減/非課税/免税/不課税/対象外）に絞り込み（taxation_type の課税仕入・免税仕入を除外。実機確認済み） |
| U4-5 | **請求書の新規作成の初期状態が「発行済」**（見積は「下書き」始まりで非対称） | 中 | ✅（2026-07-16）ユーザー承認のうえ**下書き(draft)始まりに変更**: 新規=下書き・保存後に「発行する」ボタンで発行済へ（明細なし発行はガード）。下書きは売掛残高・入金候補から除外。実機で draft→発行→売掛対象の1周を検証済み |
| U4-6 | 入金画面に**入金先口座の選択が無い**（普通預金固定） | 低 | 📋 複数口座対応は機能拡張。現ペルソナ（メインバンク1口座）では実害小 |
| U4-7 | 入金確定後の**関連仕訳へのリンクが無い**（摘要検索で No.61 を自力捜索） | 中 | 📋 U3-2（伝票番号のトースト表示）と統合して対応候補 |
| U4-8 | 案件ドロップダウンに**種別・顧客名が出ない**ため「社内業務改善」を顧客請求に使ってよいか判断不能→空欄で処理。案件の新規登録導線も無い | 中 | 📋 案件名に種別プレフィックスを含める運用 or 表示列合成。登録導線は「マスタ管理（経理）＞案件」への案内文で緩和可能 |

### 教育係への質問と回答
1. 「請求書を作成」は経理で使える仕様か → 使える（上記のとおり正常動作。誤認は自動操作起因）
2. 検収と手作成請求書の紐付け欠落 → 親が SQL で修復済み（今後の実運用では U4-2 の改修が必要）
3. 案件空欄の判断 → 案件別損益で管理したい売上なら受託案件を新規に起こすのが正。今回のようなスポット案件を全社共通売上とするのも実務上は許容（判断基準を案件マスタの説明文に書く改善余地あり）

---

## Phase 5: 経理（山田太郎・銀行明細取込フルコース）

**結果**: CSV貼り付け→前任者のプレビュー残置に遭遇（正しく判断して合流登録）→AI科目推定（地代家賃・消耗品費を正答）→一括起票（伝票No.62〜65）→残高照合→仕訳ルール追加（ID12・ﾎﾟｽﾀｰ→広告宣伝費）→資金繰り予測まで**8タスク完走**。「一連の流れが1メニュー配下でマニュアル無しで完走できた。市販ソフトと比べても優秀」「推定元列（rule/ai）は監査的に非常に良い」と高評価。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U5-1 | **前任者のプレビュー残置が無警告で合流**: 画面を開いた時点で他人の未登録プレビューが1件あり、「消えるのか合流するのか」画面から読めない。「誤登録・二重登録の温床」 | 中 | ✅（2026-07-16）取込画面を開いた時点で未登録プレビューが残っていれば警告バナー「⚠ 登録されていないプレビューが N 件残っています…確定するか破棄してから取込を始めてください」を表示（実機でプレビュー残置を再現して確認） |
| U5-2 | ヘッダ行が「**解析不能行 1 行**」とカウントされ「データが壊れていたか」と焦る（説明文には「ヘッダ行は自動スキップ」とある） | 低 | ✅ 文言を「スキップ行（ヘッダ・明細以外）」に変更 |
| U5-3 | 一括起票の完了メッセージが件数のみで**伝票番号が出ない** | 中 | ✅ トーストと結果ラベルに「（伝票 No.66）」形式で番号一覧を表示（実機で取込→ルール適用→起票の1周を再検証。エージェント作の「ﾎﾟｽﾀｰ」ルールが実運用で発動することも確認） |
| U5-4 | 明細一覧の「仕訳ID」と振替伝票の「伝票No」の**二重番号**でどちらが正式か迷う | 低 | ✅（2026-07-16）明細一覧の列を「伝票No」表示に変更（journal_entries への参照を LinkField 化し journal_no を表示。実機確認済み） |
| U5-5 | **仕訳ルール新規作成時に「有効」が初期OFF**（見落とすと無効ルールを作る罠） | 中 | ✅ MatchingRule の新規時に有効=ON を既定化。**同じ罠が本日新設の承認ルート判定にもあったため同時に修正**（Detail_OnAfterInit 追加） |
| U5-6 | 状態列が列幅で見切れる（「previ…」「未起…」） | 低 | ✅ 真因は Status 候補に preview が未定義で**生の英語値が表示**されていたこと。「プレビュー,preview」を候補に追加（未起票・起票済は元から日本語） |
| U5-7 | 残高照合で 7/15 以降 約131〜132万円の恒常差異（帳簿＞明細）＋「未起票（累計）」列の意味が説明なし | 低 | 💬 **差異はテストデータ由来**: 7/13 の全状態データ投入は仕訳側だけを投入し銀行明細側を投入していないため、以降の照合が構造的に不一致（機能は正常で、7/1 の一致がそれを示す）。デモで残高照合を見せる際はバックアップ（E2E直後状態）を使う運用。「未起票（累計）」の説明文追加は改善候補 |

### 教育係への質問と回答
- 131万円差異の原因調査をすべきか → 不要（上記のとおりテストデータの構造的差異。実運用なら即調査すべきという判断は正しい）

---

## Phase 6: 経理（山田太郎・予算/案件損益/人件費配賦/工数代理修正）

**結果**: 5タスク完走。予算の12ヶ月均等登録（第19期 開発2部 旅費交通費 600,000）→予実対比で確認、案件損益レビュー（最大粗利 PRJ-003 396,887円／最大赤字 PRJ-002 -895,000円を数字根拠つきで特定）、工数の代理修正（180→240→180分の往復・原状回復済み）。「新規12・上書き0の内訳表示は経理として大変安心」「案件損益⇔配賦明細が1円単位で突合一致するのは信頼できる」と高評価。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U6-1 | **「月No」に暦月の併記がない**: 月No4=2026年7月と読み取れる情報が皆無で、逆算に試行錯誤（最大の迷い）。年度にも期間（2026/04〜2027/03）の併記がない | 中 | ✅（2026-07-16）配賦の月No選択を「4（7月）」形式の候補に変更、人件費コストの月No入力にプレースホルダ「1〜12（1=4月・4=7月・12=3月）」を追加（実機確認済み） |
| U6-2 | **検索が「2回押し」でヒットする**（1回目0件→同条件2回目で1件）: 「0件を信じて誤判断するリスク」 | 中 | 💬 過去の実測で「検索セレクトはJS由来の値セットでイベント不達がありうる（実キーボードなら確実）」と判明している事象と同型＝**自動操作起因の可能性が高い**。ただし人間でも選択直後の即クリックで起こり得るため、人間での再現確認をバックログに |
| U6-3 | **案件損益に集計期間の表示がなく、月次フィルタもない**（年度のみ） | 中 | ✅（2026-07-16）画面説明文に「数字は選択した会計年度の累計です。」を明記（実機確認済み）。月次レビュー用途の月フィルタ追加は引き続き機能拡張候補 📋 |
| U6-4 | **【本質的指摘】未配賦人件費が画面のどこにも出ない**: 人件費コスト計5,220,000 vs 配賦計4,739,996 で**差額480,004円**。工数未入力者（営業部 伊藤さくら 480,000円）のコストが静かに配賦から漏れる＋円未満切捨てで月4円消失。「月次締めで見逃す危険」 | **高** | ✅（2026-07-16）配賦一覧に **⚠未配賦行**（工数未入力者を個人別に表示）・**（端数）行**（円未満切捨ての調整額）・**【合計】行**（配賦＋未配賦＋端数=人件費コスト合計）を追加。実機で 480,000（伊藤さくら）/ 4円 / 5,220,000 の検算一致を確認。画面説明文にも検算式を明記 |
| U6-5 | 配賦比率が整数%丸めで「0%」なのに配賦額4,716円が付き矛盾に見える | 低 | ✅ 比率を小数1桁に変更（クエリ ROUND(,1)＋DbType decimal＋Format #,0.0。実機確認: 7.1／42.9／16.2%） |
| U6-6 | 工数フォームの新ラベル「時間（分・60分=1時間）」が縦折り返しで読みづらい（U1-5対応の副作用） | 低 | ✅ ラベルは「時間（分）」に戻し、入力欄の右に補足ラベル「※分で入力（60分=1時間。例: 480=8時間）」を追加する方式に変更（TimeEntry/TimeEntryAdmin） |

### 教育係への質問と回答
1. 配賦の未配賦480,004円は仕様か不具合か → **計算仕様としては正しい**（工数未入力者は配賦されない・ADR-0009）が、「差額がどこにも表示されない」のは月次締めリスクという指摘を採用し U6-4 として最優先バックログ化
2. 案件損益の集計単位 → 年度累計が正式（月次の正式手順は現状なし。月フィルタは拡張候補）

---

## Phase 7: sysadmin（情シス引き継ぎ初日・制度マスタ管理）

**結果**: 5タスク完走。①ユーザー登録（kaihatsu2_shinjin 高橋健・開発2部・一般＝ID 27）→②実ログイン確認（ホームのロール出し分けが正しいことを新規ユーザーで実証: 入口3つ・SC2つ・経営サマリ非表示）→③費目「印刷製本費」追加（PRT・消耗品費・課税仕入10%＝Phase 3 の指摘 U3-6 を管理者ロールプレイで解消。経費フォームの候補に反映確認済み）→④**承認ルート判定の読み解き: 画面説明だけで first-match・境界含む・上限なし・優先度の隙間設計まで完全に正しく説明できた**（ADR-0023 の画面設計の検証成功）→⑤消費税12%改正の手順案（税率追加→税区分追加→費目付替えの正しい手順を導出）。

### 発見した問題（重要度順）

| # | 事象 | 重要度 | 対応 |
|---|---|---|---|
| U7-1 | **ユーザーの無効化（在籍フラグ）が無い**: 退職処理が「削除」しか見えず監査証跡上怖い。最終ログイン日時も無い | 中 | ✅（2026-07-16）ユーザー承認のうえ実装: app_users.is_active＋認証を active_app_users ビュー経由に変更（無効ユーザーはログイン拒否）。ユーザー管理画面に「有効」チェック（新規は有効既定）、承認者解決（役職/経理代替/固定指定）からも無効ユーザーを除外。実機で無効化→Login failed→再有効化→ログイン成功を検証。新規DB時の admin seed は INSTEAD OF トリガーで担保。※最終ログイン日時は未対応（CLB 認証基盤の範囲） |
| U7-2 | パスワードポリシー無し（識別名と同一でも通る）・初回変更強制なし | 低 | 💬 CLB 認証基盤の範囲。デモ用途では現状維持、実導入時の要件として記録 |
| U7-3 | 承認ルート判定の画面から読み取れなかった4点: 判定額の税込/税抜・複数課長の扱い・自己承認の扱い・一致ルール無し時の挙動 | 中 | ✅ HeaderTitle に「税込（事前申請は見込み額）」「一致なしはエラーでブロック」「課長・部長は部門の役職者全員に並列・1人の承認で先へ」を追記（自己承認回避は説明過多になるため見送り） |
| U7-4 | 経過措置控除割合の説明文「（80%→50%）」が実データ（80/70/50/30 の4段階）と不一致で、どちらが正か確信が持てない | 中 | ✅ **データが正**（2026年度改正で4段階化＝docs/research の Web リサーチ済み事実。ADR-0003 にも記録）。画面説明文を「2026年度改正で 80%→70%→50%→30% の4段階」に更新（説明文の更新漏れだった） |
| U7-5 | 経費申請の「内税10%で自動計算」プレースホルダが**税率固定を示唆**（改正時にコード改修が要ると誤解させる） | 中 | ✅ 実装確認の結果**税率マスタ完全連動**（費目→税区分→税率の RatePercent で内税計算）。文言を「費目の税区分の税率で内税自動計算」に修正（本日 U1-4 で書いた文言の自己修正） |
| U7-6 | テンプレート編集画面の「承認者の決め方」空欄＋個人指名の関係が読めない | 低 | ✅（2026-07-16）「承認者の決め方」を明示候補（指定ユーザー/申請者の部門の課長/同・部長）にし、既存データの空欄も 'user' に正規化（ddl/090 seed も修正。実機確認済み） |
| U7-7 | 費目一覧に「表示順」列が無く採番慣例が見えない | 低 | ✅ 一覧に「表示順」列を追加 |
| U7-8 | Logout（英語）とログアウトしますか？（日本語）の表記ゆれ | 低 | ✅（2026-07-16）サイドバーの Logout を「ログアウト」に日本語化（index.html の MutationObserver 方式=CLB 内部文言のため。blazor-error-ui も同時に日本語化）。CLB 本体へのローカライズ API 要望は FB-027 として起票 |

### 教育係への質問と回答
1. 内税自動計算は税率マスタ参照か → **参照している**（改正はマスタ変更のみで完結。U7-5 で文言修正済み）
2. 複数課長の解決仕様 → 部門の役職者**全員に並列**で回り**1人の承認で完了**（OR承認・ADR-0016。U7-3 で画面に明記）
3. 退職者のアカウント処理 → 現状は想定運用なし（U7-1 としてユーザー相談事項に）
4. 経過措置 70/30 は意図した設定か → **意図どおり**（2026年度改正の4段階化。画面説明文が古かった＝U7-4 で修正）

---

## 総括（7フェーズ・35タスク）

- **全35タスク中34完了・1迂回**（唯一の「詰まり」も調査の結果アプリ正常＝自動操作起因）。初見・マニュアル無しで経費申請→承認→経理処理→販売チェーン→銀行取込→予算/管理会計→制度マスタ管理まで完走できた。**「画面冒頭の説明文」（レビュー第3弾の成果）が全ペルソナから繰り返し高評価**
- 発見事項 33件。同夜の改善ラウンドを3回実施し、**✅即時対応 16件**（プレースホルダ/文言系・有効フラグ既定ON・帳票の当年度既定値・一括起票の伝票No.・費目表示順列・配賦比率小数化・状態列日本語化 ほか）。📋バックログと💬仕様確認は各表のとおり
- **バックログの優先上位**（ユーザー判断待ちを含む）: ①日付入力の6桁問題（全員が再現・CLB への FB が妥当） ②未配賦人件費の可視化（U6-4・月次締めリスク） ③仕訳生成系トーストへの伝票番号表示（U3-2/U4-7/U5-3） ④帳票の期間既定値（U3-4/U3-8） ⑤経費一覧の閲覧範囲と削除ボタンの出し分け（U1-2・プライバシー方針の判断要） ⑥領収書未添付の警告（U2-6・業務ルールの判断要） ⑦ユーザー無効化（U7-1）
- テストが生んだデータ（そのまま残置・ユーザー確認用）: 経費申請 #28/#29（松本・申請中）・技術書申請（佐々木→山口承認待ち）・販売チェーン Q/SO/A-26-004＋INV-26-009（入金済・仕訳 No.60/61）・仕訳 No.56〜65・銀行明細4件起票済み・仕訳ルール ID12・第19期予算（開発2部 旅費 600,000）・費目「印刷製本費」・ユーザー「高橋健」
