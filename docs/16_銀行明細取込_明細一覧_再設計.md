# 銀行明細取込・明細一覧の再設計（仕様検討）

> 作成: 2026-07-21 ／ **改訂 v2: 2026-07-21（ユーザー提案の3モジュール分割を採用。§2.3〜2.6 を再構成）**。ユーザー指摘（2026-07-21 夜）を受けた「一から作り直すつもりの」仕様検討。
> 対象: 【会計】銀行 > 明細取込（BankImport）・明細一覧（BankStatementLine）と、その関係先（仕訳ルール MatchingRule・残高照合 BankReconciliation・仕訳 JournalEntry）。
> 状態: **検討ドキュメント（実装はまだ行わない）**。ユーザーレビュー後に実装する。
> 根拠: BankImport.mod.cs / BankImport.mod.json / BankStatementLine.mod.json / Accounting.frm.json を精読（2026-07-21 時点のコード）。

---

## 1. 現状の問題

### 1.1 データと画面の全体像（前提）

- 明細は 1 テーブル `bank_statement_lines` に集約され、`status` 列で区別される:
  `preview`（プレビュー）→ `pending`（未起票）→ `journalized`（起票済）／`excluded`（対象外）
- 「明細取込」画面は表示専用モジュール（BankImport）で、同テーブルを status で絞った 2 つのリスト（プレビュー / 未起票）を持つ。
- **ユーザーの理解どおり、プレビューリストは「DB に status='preview' で登録済みのレコード」の表示**である（取込ボタンの時点で DB に INSERT されている。重複キーで再取込を防ぐための設計）。

### 1.2 不具合（実測＋コード根拠）

| # | 症状 | 根本原因 |
|---|---|---|
| B-1 | プレビュー/未起票リストの行削除が保存されない（保存を押すと復活） | 行削除はメモリ上の行リストから消えるだけ。保存処理 `SaveListEdits()` は「いまメモリにある行」を1行ずつ Submit する方式なので、**消えた行を DELETE する者がいない**。リロードで DB から復活する |
| B-2 | プレビューで削除・編集しても「この内容で登録」に反映されない（削除した行が登録され、編集前の値で登録される） | `ConfirmImport_OnClick` は**画面のリストを見ずに DB を直接検索**（status='preview' 全行）して pending に遷移させる。メモリ上の編集・削除は完全に無視される |
| B-3 | プレビューリストの編集を保存する手段がそもそも無い | `SaveListEdits()` は未起票リスト（PendingLines）だけを対象にしており、**プレビューリスト（PreviewLines）用の保存経路が存在しない** |
| B-4 | 「AIで相手科目を推定」を押すと保存ボタンなしで DB が更新される | 推定結果を行に代入して**その場で `Submit()`** している。ユーザーが確認・修正してから確定する導線（「AI は下書き、確定は人間」）と矛盾 |
| B-5 | 状態セレクトが自由編集できる | 一覧の「状態」列が編集可能なドロップダウン（候補は preview/pending/journalized/excluded の全4値）。**手で「起票済」にすると仕訳が無いのに起票済になる**など、状態機械を UI から破壊できる。ADR-0026「状態遷移はボタンに一元化・状態フィールドは表示専用」の全アプリ規約に、この画面だけ違反している |
| B-6 | 明細の原本列まで編集できる | 一覧レイアウトで**日付・摘要・出金・入金・残高も編集可能**。明細は銀行データの写し（原本）であり、書き換えると残高照合の意味が崩壊する |
| B-7 | 「対象外」にすると行き止まり | 対象外にした行は取込画面の未起票リスト（status='pending' 絞り）から消える。明細一覧には出るが、**明細一覧は削除ボタンしか無い**（CanUpdate: false / 詳細画面なし）。誤って対象外にした明細を未起票に戻す正規の手段が無い |
| B-8 | 明細一覧の物理削除が無条件 | **起票済の明細も削除できる**。削除しても生成済み仕訳は残るため、①明細→仕訳のトレーサビリティが切れる ②残高照合が「帳簿にはあるが明細に無い」状態になり差異が出る ③重複キーが解放され同じ明細を再取込→再起票（二重仕訳）できてしまう |
| B-9 | ボタンの配置が操作対象と対応していない | 「この内容で登録」等が CSV 欄の下にまとまっており、どのリストに効くボタンか読み取れない（ユーザー指摘） |

一言でまとめると、**「リスト上の編集・削除（メモリ）と DB の同期規律が設計されておらず、状態遷移と原本保護のガードも無い」**。個別修正ではなく保存セマンティクスから決め直す。

---

## 2. あるべき仕様

### 2.1 設計原則（3つ）

1. **保存は明示・一括**: リスト上の編集・削除・対象外化はすべてメモリ上の変化とし、**保存系ボタンを押した瞬間にまとめて DB に反映**する。行削除の即時 DB 反映はしない（ユーザー決定 2026-07-21）。
2. **状態遷移はボタンだけが行う**（ADR-0026 準拠）: 状態列は表示専用。ユーザーが起こせる遷移は「未起票 ⇄ 対象外」のみ。preview→pending（登録）と pending→journalized（起票）はシステムの遷移。
3. **明細は原本**: 日付・摘要・金額・残高・口座・重複キーは取込後は読み取り専用。ユーザーが編集できるのは**相手科目（候補）だけ**。

### 2.2 状態機械（唯一の正）

```
（CSV取込）──→ preview ──「この内容で登録」──→ pending ──「候補科目で一括起票」──→ journalized
                  │                              │  ↑
                  │「全てのプレビューを取り消す」    │  │「未起票に戻す」（明細一覧の詳細から）
                  │ または行削除＋保存             ↓  │
                  └──→ （物理削除・キー解放）      excluded
                                                 pending の行削除＋保存 → 物理削除・キー解放
```

| 遷移 | 誰が | どこで（§2.3 の3モジュール） | 備考 |
|---|---|---|---|
| → preview | システム | 【明細取込】取込ボタン | 重複キーで既存スキップ |
| preview → pending | システム | 【明細取込】「この内容で登録」 | 暗黙保存を含む（§2.4） |
| preview → 削除 | ユーザー | 【明細取込】行削除＋保存 / 全て取り消す | 重複キー解放＝貼り直し可 |
| pending → excluded | ユーザー | 【未起票明細一覧】行の「対象外」チェック＋保存 | 唯一の日常的な状態操作 |
| excluded → pending | ユーザー | 【未起票明細一覧】詳細画面「未起票に戻す」 | 誤操作の差し戻し（B-7 解消） |
| pending → journalized | システム | 【一括起票】「候補科目で一括起票」 | 仕訳リンク付与 |
| pending → 削除 | ユーザー | 【未起票明細一覧】詳細画面「削除」 | キー解放。取込ミスの後始末用 |
| journalized → （不変） | — | — | 削除・編集とも不可。訂正は仕訳側の赤黒で行い、明細はそのまま残す（照合の証跡） |

- 「状態」列はどの画面でも**表示専用**にする（B-5 解消）。
- excluded の削除は許可（対象外＝帳簿対象外の記録なので消す意味は薄いが、ゴミ掃除は妨げない）。ただし残高照合は「明細が存在する日」を突合するため、**対象外行も残高照合の材料である**ことを削除確認ダイアログで注意喚起する。

### 2.3 モジュール構成（v2: 3モジュールへ分割。ユーザー提案 2026-07-21）

1画面に2リスト＋役割の違うボタン群が同居していることが分かりにくさの根源、という指摘を受け、**「1画面 = 1リスト = 1つの仕事」に分割**する:

```
サイドバー【会計/銀行】: 明細取込 ／ 未起票明細一覧 ／ 一括起票 ／ 残高照合 ／ 仕訳ルール

【明細取込】 = CSV の受入検査場。プレビューを整えて登録したら仕事完了
  口座選択 ／ 明細CSV貼り付け欄
    └─ [明細を取り込む]
  ── プレビュー（未登録）リスト ──────────────
  編集可: 相手科目（候補）＋原本列（日付・摘要・金額。登録前に限り文字化け等を修正可）
  行削除: ○（保存で確定）
    └─ [プレビューの変更を保存] [AIで相手科目を推定] [この内容で登録] [全てのプレビューを取り消す]

【未起票明細一覧】 = 起票前の作業台帳（現在の「明細一覧」を改組）
  検索: 口座・状態（既定=未起票）・日付
  ── 明細リスト ──────────────────────
  編集可: 相手科目（候補）・「対象外」チェック　　原本列・状態: 表示専用　行削除: ×
    └─ [変更を保存]
  行クリック → 詳細画面: 状態に応じた例外操作（対象外にする／未起票に戻す／この明細を削除）

【一括起票】 = 実行専用。常に保存済みの DB の姿を表示し、見たまま起票する
  起票対象（相手科目候補が設定済みの未起票）の確認リスト（表示専用）
  ＋「候補未設定 n 件（未起票明細一覧で設定してください）」の案内
    └─ [候補科目で一括起票]
  結果表示（起票件数・伝票 No・スキップ理由）
```

- **AI推定・候補の整備はプレビュー段階で済ませる**（ユーザー提案）: ルール適用は取込時、AI 推定はプレビューのボタン。**未起票に上がった時点で「起票できる状態」が原則**になり、未起票明細一覧は例外処理（候補の手直し・対象外化）だけの画面になる。
- この分割の副次効果として、**「未保存の編集があるまま起票してしまう」ハザードが構造的に消える**（一括起票画面は編集機能を持たず、常に保存済みデータを表示するため。旧案 §2.4 の「起票前の暗黙保存」が不要になる）。
- 未起票リストの行削除は置かない。「削除」と「対象外」の使い分けの混乱を避け、登録済み明細の削除（取込ミスの後始末）は詳細画面の例外操作に寄せる。プレビューは未登録の作業中データなので行削除を残す。
- 「対象外」チェック列（BooleanField）＋保存で excluded へ遷移（状態セレクト直編集の代替。ADR-0026 に整合）。

### 2.4 保存セマンティクス（各ボタンの DB への作用）

原則: **DB を書くのは「保存」「登録」「起票」「取り消す」の4系統だけ**。参照・推定系はメモリのみ。

| 画面 | ボタン | メモリ→DB 同期 | 内容 |
|---|---|---|---|
| 明細取込 | 明細を取り込む | INSERT のみ | 新規行を preview で作成（既存の編集には触れない）。**未保存のプレビュー変更がある場合は警告**（§2.7） |
| 明細取込 | プレビューの変更を保存 | 差分同期 | プレビューリストの編集反映＋削除行の DELETE |
| 明細取込 | AIで相手科目を推定 | **なし** | 候補未設定のプレビュー行（メモリ上の現在値で判定）に推定値をメモリ代入するだけ。保存するまで DB は変わらない（B-4 解消）。破棄したければ保存せずリロード |
| 明細取込 | この内容で登録 | 差分同期＋遷移 | **暗黙に「変更を保存」を実行してから** preview→pending。画面で消した行・直した科目・AI候補がそのまま登録される（B-2 解消） |
| 明細取込 | 全てのプレビューを取り消す | DELETE | preview 全行を物理削除（キー解放）。確認ダイアログ付き |
| 未起票明細一覧 | 変更を保存 | 差分同期＋遷移 | 科目候補修正の反映＋「対象外」チェック行を excluded へ |
| 未起票明細一覧（詳細） | 対象外にする／未起票に戻す／この明細を削除 | 即時 | 詳細画面の単発操作は確認ダイアログ＋即 DB 反映（一覧の一括編集と違い、対象が1件で明確なため） |
| 一括起票 | 候補科目で一括起票 | 遷移のみ | この画面に編集機能は無く**常に保存済みデータを表示**するため、暗黙保存が不要。確認ダイアログ→起票→仕訳リンク付与 |

### 2.5 差分同期の実装方式（B-1 の恒久対策）

「削除ボタンを押したらリストから消え、保存で初めて DB に反映」は次の方式で実現する（実現可能・既存知見の組み合わせ）:

1. リストをリロードするたびに、**ロード済み行の ID 集合をスクリプト変数に記録**する（スナップショット）。
2. 保存時に「スナップショットにあるが、いまメモリに無い ID」= ユーザーが削除した行 → 1行ずつ `Delete()`（明細は子なしモジュールなので物理削除が確実に効く。Project.md 2026-07-19 知見）。
3. メモリに残っている行は 1 行ずつ `Submit()`（未変更行は送信データなしで無害）。
4. 同期後にリロードし、スナップショットを取り直す。

- **DB 全行との突き合わせにしない**ことが重要（リストに LimitCount があるため、「DB にあるが画面に無い＝削除」と誤判定して**表示されていないページの行を消す**事故になる）。スナップショット方式なら「一度画面に出た行」しか削除対象にならない。
- 補助ガードとして、リストの LimitCount は現行 200 → **null（全件）**にする。月次の明細件数（数十〜数百）なら実用上問題なく、「201件目が画面に出ず起票されない」系の取りこぼしも同時に消える。

### 2.6 未起票明細一覧（BankStatementLine 改組）の設計

役割は「**起票前の作業台帳（主）＋全状態の監査ビュー（従）**」の1画面2役とする:

- **既定の検索条件を 状態=未起票 にする**（開いた瞬間は作業台帳。日常業務はこの絞り込みのまま完結）。状態フィルタを変えれば起票済・対象外・プレビューも閲覧でき、従来の「明細一覧」（監査ビュー・仕訳リンク確認・CSVダウンロード）を兼ねる。→ 兼用でよいか **Q2**
- 一覧の編集可能列は**相手科目（候補）と「対象外」チェックのみ**（＋[変更を保存]）。原本列・状態は表示専用。**一覧の削除ボタンは廃止**（B-8 解消）。
- **詳細画面を新設**（現行 DetailLayouts は空）: 行クリックで遷移し、状態に応じたボタンを出す（ADR-0026 パターン）:
  - pending: 「対象外にする」（赤）／「この明細を削除」（赤・確認ダイアログ＋キー解放の注意書き）
  - excluded: 「未起票に戻す」（青）／「この明細を削除」（赤）
  - journalized: 操作ボタンなし。仕訳番号を表示し「訂正は仕訳側で赤黒訂正」の案内ラベル
  - preview: 「取込画面で作業中の明細です」の案内のみ（操作は取込画面で）

### 2.6b 一括起票（新設モジュール）の設計

- 表示専用モジュール。リストは「相手科目候補が設定済みの未起票明細」（読み取り専用・日付昇順=伝票番号順）。
- ヘッダに件数サマリ: 「起票対象 n 件 ／ 候補未設定 m 件（未起票明細一覧で設定してください）／ 対象外 k 件」。
- [候補科目で一括起票] → 確認ダイアログ（件数明示）→ 実行 → 結果表示（起票件数・伝票 No 一覧・締め済みスキップ・失敗）。
- 締め済み期間ガード・二重起票ガード・税行方式の仕訳生成は現行ロジックを踏襲。

### 2.7 細部仕様

- **未保存変更の警告**: 未保存の編集・削除がある状態で「明細を取り込む」「AIで相手科目を推定」以外の画面遷移をしたら変更は破棄される（CLB のリスト再読込の標準動作）。少なくとも「明細を取り込む」実行時はプレビューの未保存変更を検知して確認ダイアログを出す（変更検知が CLB API で困難な場合は、取込時に必ず暗黙保存する案に倒す。実装時に実測して決める）。
- **プレビュー放置警告**（他担当者の作業中プレビューの検知）: 現行仕様を維持。
- **AI推定・ルールの対象判定**: 「候補が未設定」はメモリ上の現在値で判定（保存前の手入力を上書きしない）。AI の対象は**プレビュー行**（v2）。未起票に上がってから候補が消えた/直したい行は、未起票明細一覧のドロップダウンで手動設定する（ルールを育てれば次回から自動化される。AIボタンを一括起票画面にも置くかは **Q4**）。
- **原本列の修正はプレビュー段階のみ可**（v2 で Q1 を解決）: 文字化け・摘要の乱れは受入検査（プレビュー）で直し、「この内容で登録」以降は原本として凍結する。修正すると重複キーも再計算する（キーは原本列から導出されるため）。
- **推定元（rule/ai）表示**: 現行維持。手修正したら推定元を「手入力」に変える（新値 manual を候補に追加）。
- **重複キー**: 現行方式を維持（日付|摘要|出金|入金|連番）。削除＝キー解放＝再取込可能、を各削除ダイアログに明記する。
- **権限**: 現行どおり経理・sysadmin のみ（UserWriteCondition 設定済み）。
- **残高照合・明細一覧の CSV ダウンロード・冪等スキップ**: 仕様変更なし（B-8 の削除制限により、照合の穴と二重起票経路がむしろ塞がる）。

---

## 3. 実装方針（レビュー後に着手）

| 対象 | 変更 |
|---|---|
| BankImport.mod.cs / .mod.json | **取込専用に縮小**: 未起票リスト・一括起票を撤去。プレビューリスト＋4ボタン（保存/AI/登録/全取消）。スナップショット差分同期・AI のメモリ化・原本列のプレビュー時編集＋キー再計算・LimitCount null |
| BankStatementLine.mod.json | 「未起票明細一覧」へ改組: 既定検索=未起票／一覧の削除廃止・候補と対象外チェックのみ編集可＋[変更を保存]／詳細遷移有効化・詳細レイアウト新設 |
| BankStatementLine.mod.cs（新設） | 一覧の保存（差分同期＋対象外遷移）／詳細画面の例外操作（対象外にする・未起票に戻す・削除） |
| BankPosting.mod.json / .mod.cs（新設） | 一括起票モジュール: 起票対象リスト（表示専用）＋件数サマリ＋実行。仕訳生成ロジックは現行 PostAll を移設 |
| Accounting.frm.json / Sysadmin.frm.json | 銀行グループのリンク再編: 明細取込／未起票明細一覧／一括起票／残高照合／仕訳ルール |
| docs/11/05_銀行・docs/06 S-13 | 画面構成・ボタン名・AI挙動・状態操作の期待値を全面改訂 |
| DB | スキーマ変更なし（status 値も現行のまま。SuggestionSource に manual を追加する場合も列は TEXT のため DDL 変更不要） |

検証: E2E（05_銀行 の CSV を使った取込→プレビュー編集・削除→AI→登録→対象外→一括起票→照合の一巡＋差し戻し・削除の分岐）をブラウザ実機で回す。

---

## 4. 未決事項（レビューで決めたい点）

- ~~Q1: 原本列の扱い~~ → **v2 で解決**: プレビュー段階のみ編集可（受入検査）、登録後は凍結。
- **Q2**: 未起票明細一覧は「既定フィルタ=未起票の全状態ビュー」（1画面2役・モジュール数を抑える案）でよいか。それとも未起票専用にして、全状態の監査ビューを別画面として残すか。
- **Q3**: ユーザー提案にあった「**伝票Noの入力**はプレビューの時点で」の意図の確認。伝票 No は一括起票時に年度内連番で自動採番される（入力欄は存在しない）ため、(a) 相手科目候補など起票に必要な情報の整備をプレビューで済ませる、という趣旨か、(b) 伝票番号を手で指定したいという新要望か。(a) なら v2 で反映済み。
- **Q4**: AI推定ボタンをプレビュー（明細取込）だけに置くか、一括起票画面にも「候補未設定の残り n 件」用に置くか。v2 案は前者（未起票での候補付けは手動＋ルール育成）。
- **Q5**: 「全てのプレビューを取り消す」の名称はユーザー案どおり採用予定（現行「プレビューを取り消す」から変更）。
