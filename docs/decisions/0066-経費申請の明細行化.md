---
title: ADR-0066: 経費申請に明細行を持たせる
status: current
scope: 経費精算
audience: [開発]
updated: 2026-08-17
supersedes: []
related: []
---
# ADR-0066: 経費申請に明細行を持たせる

- 日付: 2026-08-16（GO: 2026-08-17）
- 状態: **採用**（実装着手前。実機検証の結果は末尾に追記する）
- 起点: `docs/14_市販ソフト比較レビュー.md` §5 の改善候補 E-1 / 作業指示書 `docs/tasks/03_経費申請の明細行化.md`
- 関係: ADR-0023（承認ルート判定のマスタ化）・ADR-0048（承認ルートの積み上げ方式）・ADR-0044（部課階層と walk-up 承認）・
  ADR-0049（検収に明細行を持たせる）・ADR-0050（税額計算を明細の税区分ベースへ）・ADR-0062（導出値の読み取り専用化）

## 背景

`ExpenseRequest` は **1 申請 = 1 金額**で、明細の子表を持たない。金額・費目・利用日・案件・うち消費税が
それぞれ 1 つずつしかないため、**レシート 1 枚につき申請 1 件**になる。

月末にレシートを 10 枚出す社員は申請を 10 件作り、**承認者は 10 回承認する**。
ADR-0048 で承認を積み上げ方式（課長 → 部長）にしたため、3 万円以上では押下回数がさらに倍加する。
ペルソナ（`docs/02` §4）は承認者 20 名・一般社員 50 名で、負担が集中する側が少数派である。

市販の経費精算（MF クラウド経費・弥生経費 Next・freee 経費精算）はいずれも
**1 申請に複数の経費明細をぶら下げる**のが標準で、ここは明確な見劣りである。

「この申請を複製」ボタンは既にあるが、これが軽くするのは**申請者の入力**だけで、
**承認回数は 1 件も減らない**。本課題の痛点は承認側にあるため、複製は代替にならない。

## 決定

### 1. 明細行を持たせる。最初から行ごとに費目を持つ（段階分けしない）

`expense_request_lines` を新設し、費目・金額・利用日・案件・税区分を**行が持つ**。

作業指示書 §3-4 は「第 1 段階は費目をヘッダ固定にする」案を挙げているが、これは採らない。

- **実需に合わない。** 月末のレシートは交通費・会議費・消耗品費が混ざっているのが普通で、
  費目をヘッダ固定にすると結局「交通費の申請」「会議費の申請」と分けることになる。
  承認 10 回が 3 回に減るだけで、「1 申請」というゴールには届かない
- **総コストが増える。** 第 2 段階で承認ルート判定・仕訳生成・少額資産判定・交際費必須判定が作り直しになり、
  **体験シナリオ 11 本を 2 回書き換える**ことになる。本作業の重心はドキュメントなので、段階分けは損になる
- 段階分けが有効なのは「第 1 段階で止まりうる」ときだが、費目混在は経費精算の常態であり、止まらない

### 2. 承認ルートは「金額＝合計・費目＝行ごと」で解決し、**段を合成**する

最大の論点。次の 2 段構えとする。

#### 2-1. 判定額は申請合計

`GetJudgeAmount()` は明細の合計（税込）を返す。事前申請は見込み合計、実費確定後は実費合計。

「1 万円 × 25 行」が「25 万円 1 行」と同じルートになるが、これは**正しい挙動**である。
承認は会社が支出する総額に対する統制であり、合計で見なければ申請を分割して上位承認を回避できてしまう。

#### 2-2. 費目は行ごとにルールを解決し、得られたテンプレートの段を合成する

各明細行について `(申請合計, その行の費目)` で `approval_route_rules` を評価し、
行ごとのテンプレートを得る。**得られたテンプレート集合の段を 1 本の段列に合成する。**

合成規則:

1. 各テンプレートの `approval_flow_template_order` を、**承認者キー**でまとめる
   （キー = `manager` / `director` / 固定ユーザーの `approver_user_id`）
2. 同一キーの段は 1 つに畳む
3. 並び順は「そのキーが登場したテンプレート内の `order_no` の最大値」の昇順。
   同値のときは `manager` → `director` → 固定ユーザー（id 昇順）

実際のマスタ（ADR-0048 の 4 テンプレート）での動作:

| 明細の内容（合計 6 万円） | 行ごとの解決 | 合成結果 |
|---|---|---|
| 消耗品 5 万＋交際費 1 万 | `経費_部長まで`[課長,部長] ＋ `経費_部長まで＋総務`[課長,部長,総務] | 課長 → 部長 → 総務 |
| 消耗品 5 万＋会議費 1 万 | どちらも `経費_部長まで` | 課長 → 部長 |
| （合計 1 万）消耗品＋交際費 | `経費_課長まで`[課長] ＋ `経費_課長まで＋総務`[課長,総務] | 課長 → 総務 |

**なぜこの方式か。**

- ADR-0048 の積み上げ方式は「必要な段が重なっていく」モデルなので、
  「行ごとに必要な段を求めて重ねる」が同じ思想の素直な延長になる
- 作業指示書 §3-1 の案 (a)「最も強いルールを採用」と案 (b)「行ごとに解決して和集合」は対立していない。
  **(b) の手続きを実行すると結果として (a) になる。** 強さを表す新しい列をマスタに足す必要がない
- 案 (c)「ヘッダに代表費目を持つ」は採らない。**交際費が 1 行混ざっているのに代表費目が消耗品費だと
  総務承認を素通りする**という統制上の穴が空く。最小の変更ではなく、壊れた変更である
- **明細が 1 行のときは現行と完全に同じテンプレート・同じ段列に帰着する。**
  移行後も既存 14 件と体験シナリオの期待値が変わらない（後述の移行方針が成立する根拠）

walk-up による繰り上げと重複段の圧縮（ADR-0044）は、**合成後の段列に対して**従来どおり適用する。

#### 2-3. 契約メソッドの変更

`ExpenseRequest.SelectTemplateName()`（単一の名前を返す）を
`SelectTemplateIds()`（テンプレート ID のリストを返す）に置き換え、
`ApprovalFlow.LoadFromTemplate()` を複数テンプレートの合成に対応させる。

`approval_flow.template_id`（単一列）には**代表テンプレート**（合成に最も多くの段を寄与したもの）を記録し、
合成に使った全テンプレート名を申請履歴（`ApprovalHistory.Comment`）に残して監査できるようにする。
承認段の実体は申請時に Orders/Members へコピーされる（ADR-0023 の性質）ため、`template_id` は記録目的のみ。

### 3. ヘッダの `amount` / `tax_amount` を「明細合計の集計列」に転用する

新しい合計列を作らず、**既存列の意味を「導出値」に変える**。

理由: 精算処理待ちキュー・ポータル・資金繰り予測・通知文言（`TargetSummary`）・支払仕訳生成が
すべて `expense_request.amount` を参照している。ここを集計列にすれば**下流が無修正で動く**。

- `amount` = 明細の金額（税込）合計。**画面では読み取り専用**（ADR-0062）
- `tax_amount` = 明細のうち消費税の合計。同じく読み取り専用
- 都度計算ではなく列に持つ（`docs/14` A-2 と同じ考え方。参照側が多く、検索・集計にも使うため）

### 4. ヘッダに残すもの / 明細へ移すもの

| ヘッダに残す | 明細（`expense_request_lines`）へ移す |
|---|---|
| 件名・目的・申請区分・支払先区分・支払先（社員／取引先） | 費目・金額（税込）・うち消費税・税区分 |
| 部門（申請時スナップショット）・承認状態・精算ステータス | 利用日・利用先・摘要・案件 |
| **計上日**（`expense_date` を意味替え）・合計金額・合計消費税 | 交際費 3 項目・固定資産フラグ・資産管理番号・領収書 |
| 見込み額（事前申請。ヘッダの 1 つのまま） | |

**`expense_date` は「利用日」から「計上日（仕訳日付）」へ意味を変える。**
明細ごとに利用日が違うため、仕訳の日付をヘッダで 1 つ決める必要がある。既定値は**明細の最も遅い利用日**。
月をまたぐレシートが混ざった場合は遅い方の月に計上されるが、これは重要性の原則として許容する
（締め済み期間への計上を当月へ倒す既存のフォールバックと同じ考え方）。

**見込み額をヘッダのままにする理由**: 事前申請は「これから使う」段階で明細が確定していないことが多く、
行ごとの見込みを求めるのは実務に合わない。実費は行で入れる。

### 5. 仕訳は 1 申請 = 1 伝票。借方は行ごと、税行は借方行ごと、貸方は 1 行

```
D 旅費交通費   3,000  （税区分 課税仕入10%）  案件: A     ← 明細1
D 仮払消費税     300  （行1の税行）
D 会議費       9,091  （税区分 課税仕入10%）  案件: なし  ← 明細2
D 仮払消費税     909  （行3の税行）
C 未払金      13,300                                      ← 合計 1 行
```

- **借方は明細 1 行につき 1 行**（同一科目でも畳まない）。総勘定元帳で明細が見えるほうが実務で追える
- **税行は借方行ごと**に立てる。ADR-0050 は「税率ごとに 1 回だけ端数処理」だが、あれは
  **自社が発行する請求書の税額**の話。経費は**レシート記載の税額が正**であり、
  1 枚ごとの税額をそのまま記帳するのが正しい。この違いを明示的に区別する
- **貸方（未払金）は 1 行**。支払先はヘッダで 1 つなので分ける理由がない
- **案件は行が持つ値を借方行・税行に引き継ぐ**（貸方行は複数案件が混ざるため案件なし）。
  部門はヘッダのスナップショットを全行に引き継ぐ（現行どおり。`FillMissingDepartments()` も従来どおり）
- 固定資産計上対象の行は、その行だけ借方科目を工具器具備品(1520)に振り替える

### 6. 行単位になる判定・ヘッダ単位のままの判定

| 仕組み | 単位 | 備考 |
|---|---|---|
| 少額資産の自動判定（`IsAssetCandidate` × 閾値） | **行** | 1 申請に資産行と経費行が混在しうる。固定資産台帳への登録も行ごと（`EXP-{申請id}-{行no}`） |
| 交際費の必須 3 項目 | **行** | その行の費目が交際費のときだけ必須 |
| 「うち消費税」の検算（±10% 警告／税込以上エラー） | **行** | 現行ロジックを行に適用 |
| AI 領収書読み取り | **1 枚 → 1 行追加** | 読み取り結果で新しい明細行を作る |
| 事前申請の実費確定（110% 判定・ルート跨ぎ判定） | **合計** | 承認は総額に対する統制なので合計で判定する |
| 領収書の未添付警告 | **行** | 添付の無い行があれば警告（申請はブロックしない。現行の方針を維持） |

### 7. 既存データの移行は「1 申請 → 1 明細」

2026-08-16 時点の実測（`sql` CLI）:

| 精算ステータス | 件数 | うち固定資産 | うち案件付き | うち事前申請 |
|---|---|---|---|---|
| draft | 1 | 0 | 0 | 0 |
| applying | 6 | 1 | 0 | 1 |
| settled | 1 | 0 | 1 | 0 |
| completed | 6 | 1 | 1 | 1 |
| **計** | **14** | 2 | 2 | 2 |

ヘッダの費目・利用日・利用先・案件・うち消費税・交際費 3 項目・固定資産フラグ・領収書を
そのまま明細 1 行へ写す。`amount` / `tax_amount` は単一行なので合計と一致し、**値は 1 円も動かない**。

**決定 2-2 により、明細 1 行の申請は現行とまったく同じ承認ルートに帰着する**ため、
進行中の 6 件（applying）についても解決結果は変わらない。
仕訳・固定資産台帳・承認履歴はいずれも移行で変化しない（検算で確認する）。

## 実装前に確認が必要なこと（未解決）

- **ListField の行に FileField（領収書添付）を置けるか。** 検収明細（ADR-0049）には添付が無いため前例がない。
  グリッド内でアップロードできない場合の代替は、明細行の詳細レイアウトを開いて添付する形にする。
  **実装の最初に実機で確かめる。**

## トレードオフ・リスク

- **影響範囲が広い。** 承認ルート判定（ADR-0023/0048 の改訂）・仕訳生成・固定資産登録・AI 読み取り・
  入力ガードのすべてが変わる。作業量の大半は**体験シナリオ 11 本の書き換え**になる見込み
- **1 行だけの申請がわずかに面倒になる。** 明細を 1 行足す操作が増える。
  新規作成時に空行を 1 行用意しておくことで、体感の悪化を抑える
- **承認ルートの解決がマスタ設定に敏感になる。** 費目ごとのルールを増やすほど段が積み上がる。
  合成結果は申請画面で「このルートで承認されます」と申請前に見せて、驚きを消す

## 実装順序

1. DDL（`Designer/ddl/600_expense_lines.sql`）— テーブル新設 ＋ 14 件の移行 ＋ 検算
2. `ExpenseRequestLine` モジュール新設・`ExpenseRequest` の改修（ヘッダ列の意味替え・明細 ListField）
3. `ApprovalFlow.LoadFromTemplate()` の合成対応と契約メソッドの差し替え
4. 仕訳生成・固定資産登録・AI 読み取り・入力ガードの行対応
5. `designcheck` findingCount 0 → `deploy.ps1` → **サーバ再起動**（`*.mod.cs` を触るため必須）
6. 実機検証（体験シナリオ `01_経費精算/` 11 本を通す）
7. ドキュメント現行化（体験シナリオ本文・`docs/07`・`docs/14` §5.3・`docs/issues/` の E-1）

## テスト設計

**この変更は不具合が入り込みやすい。**承認ルート・仕訳・税額・資産判定という「間違っても画面上は正常に見える」
仕組みを同時に触るため、`designcheck` では 1 件も拾えない。以下を網羅的に実施する（ユーザー指示 2026-08-17）。

**実機で確認するもの / SQL で検算するもの**の切り分け:

- **実機必須** … 承認ルートの解決結果（画面遷移・ユーザー切替が要る）／入力ガードの発火／
  出し分け（費目による欄の表示）／AI 読み取り／状態による編集可否
- **SQL 検算** … 移行の同一性／仕訳の貸借一致と行構成／税額の端数／固定資産台帳
- **併用** … 仕訳生成は画面から実行し、出来上がりを SQL で検算する

### A. 承認ルートの合成（最重要）

判定の境界は現行ルール（ADR-0048）の 29,999 / 30,000 / 199,999 / 200,000。

| # | 明細 | 合計 | 期待ルート | 意図 |
|---|---|---|---|---|
| A-01 | 消耗品 29,999 × 1 行 | 29,999 | 課長 | **1 行なら現行と同一**（境界下） |
| A-02 | 消耗品 30,000 × 1 行 | 30,000 | 課長 → 部長 | 同上（境界上） |
| A-03 | 消耗品 199,999 × 1 行 | 199,999 | 課長 → 部長 | 同上 |
| A-04 | 消耗品 200,000 × 1 行 | 200,000 | 課長 → 部長 → 総務 | 同上 |
| A-05 | 交際費 29,999 × 1 行 | 29,999 | 課長 → 総務 | 費目ルールの境界下 |
| A-06 | 交際費 30,000 × 1 行 | 30,000 | 課長 → 部長 → 総務 | 費目ルールの境界上 |
| A-07 | 消耗品 20,000 ＋ 会議費 15,000 | 35,000 | 課長 → 部長 | **合計で判定**（各行は 3 万未満） |
| A-08 | 消耗品 15,000 ＋ 会議費 14,999 | 29,999 | 課長 | 合計の境界下 |
| A-09 | 消耗品 50,000 ＋ 交際費 10,000 | 60,000 | 課長 → 部長 → 総務 | **費目混在の合成** |
| A-10 | 消耗品 5,000 ＋ 交際費 5,000 | 10,000 | 課長 → 総務 | 合成しても部長は入らない |
| A-11 | A-09 の行順を入れ替え | 60,000 | 課長 → 部長 → 総務 | **行順に依存しない** |
| A-12 | 会議費 ×3 行 | 任意 | 段が重複しない | **同一キーの段が畳まれる** |
| A-13 | 課長空席の課から A-02 | 30,000 | 部長のみ | walk-up ＋圧縮（ADR-0044）が合成後に効く |
| A-14 | 課長本人が A-02 を申請 | 30,000 | 部長のみ | 自己承認回避（ADR-0010） |
| A-15 | 明細 0 行／全行 0 円 | 0 | 申請ブロック | 静かに通さない |
| A-16 | どのルールにも一致しない費目 | — | 明示エラー | ルート未解決を黙って通さない |

### B. 金額・税額の集計（行単位）

| # | 条件 | 期待 |
|---|---|---|
| B-01 | 明細の追加・金額変更・削除 | ヘッダ合計が即座に追随し、読み取り専用である |
| B-02 | 手入力の消費税がある行と無い行の混在 | 合計＝手入力額＋自動計算額 |
| B-03 | 不課税費目（諸会費 `NON_TAXABLE`）の行 | 税額 0・その行の「うち消費税」欄が出ない |
| B-04 | 行の消費税 ≧ その行の税込金額 | **エラーで止まる** |
| B-05 | 行の消費税が理論値と ±10% 超の乖離 | 警告のみで通る |
| B-06 | 行の消費税に負値 | エラー |
| B-07 | 3,000 円（10%）の行 | 税額 272 円（切り捨て）。**行ごとに端数処理** |
| B-08 | 課税 8% と 10% の行が混在 | 各行の税区分で計算され合算される |

### C. 仕訳生成

| # | 明細 | 期待する仕訳 |
|---|---|---|
| C-01 | 課税 2 行 | 借方 2 ＋ 税行 2 ＋ 貸方 1 ＝ 5 行・貸借一致 |
| C-02 | 課税 1 行 ＋ 不課税 1 行 | 借方 2 ＋ 税行 1 ＋ 貸方 1 ＝ 4 行 |
| C-03 | 全行不課税 | 税行なし（借方 n ＋ 貸方 1） |
| C-04 | 案件が行ごとに異なる | 借方行・税行はその行の案件、**貸方行は案件なし** |
| C-05 | 任意 | 全行の部門＝ヘッダのスナップショット（NOT NULL・ADR-0056） |
| C-06 | 生成済みの申請で再実行 | 二重生成ガードで止まる |
| C-07 | 計上日が締め済み期間 | 当日へフォールバックし摘要に元の日付 |
| C-08 | 固定資産行を含む | **その行だけ** 1520、他行は費目の既定科目 |
| C-09 | 任意 | 貸方 1 行の金額＝ヘッダ合計 |
| C-10 | 精算（支払仕訳） | D 未払金／C 普通預金がヘッダ合計で 1 組 |

### D. 少額資産の判定（行単位）

| # | 条件 | 期待 |
|---|---|---|
| D-01 | 資産性費目 99,999 円の行（閾値 100,000） | 自動 ON にならない |
| D-02 | 資産性費目 100,000 円の行 | 自動 ON（境界上） |
| D-03 | 資産行 ＋ 経費行の混在 | 台帳に登録されるのは資産行だけ |
| D-04 | 資産行が 2 行 | 台帳 2 件・資産コードが衝突しない |
| D-05 | 自動 ON を手で OFF | OFF のまま維持され、台帳に載らない |

### E. 交際費の必須 3 項目（行単位）

| # | 条件 | 期待 |
|---|---|---|
| E-01 | 交際費行 ＋ 会議費行 | 交際費行だけ 3 項目必須。会議費行が空でも申請できる |
| E-02 | 交際費行の 1 つが未入力 | 申請ブロック・**どの行か**が分かるメッセージ |
| E-03 | 交際費 → 会議費 に変更 | 必須が外れ、欄が消える |

### F. 事前申請

| # | 条件 | 期待 |
|---|---|---|
| F-01 | 見込み 100,000 → 実費合計 105,000 | 再承認なし（110% 以内） |
| F-02 | 見込み 100,000 → 実費合計 120,000 | 自動再承認 |
| F-03 | 見込み 29,000 → 実費合計 31,000 | **区分跨ぎ**で再承認（率は 110% 以内でも） |
| F-04 | 実費確定後 | 判定額が**明細合計**になっている |

### G. 移行（全件 SQL で検算）

| # | 検算 |
|---|---|
| G-01 | 14 件すべてが明細 1 行を持つ（孤児・欠落 0） |
| G-02 | `amount` / `tax_amount` が移行前後で全件一致 |
| G-03 | `journal_entries` / `journal_lines` が 1 行も変化しない |
| G-04 | 固定資産台帳が変化しない |
| G-05 | 承認フロー・承認履歴が変化しない |
| G-06 | 進行中 6 件（applying）を承認完了 → 仕訳生成まで進めて正常に動く |

### H. 状態・UI・既存機能の回帰

| # | 条件 | 期待 |
|---|---|---|
| H-01 | 申請後（applying 以降） | 明細が編集不可 |
| H-02 | 却下 → 下書きに戻る | 明細が再び編集可 |
| H-03 | 「この申請を複製」 | 明細もコピーされる |
| H-04 | 下書きの削除 | 明細も消える（孤児が残らない） |
| H-05 | 領収書の添付 | 行ごとに添付できる（実装方式は要検証項目の結論による） |
| H-06 | AI 読み取り | 1 枚読むと **1 行追加**される |
| H-07 | 精算処理待ちキュー・ポータル・資金繰り予測 | 金額が合計で正しく出る（`amount` 転用の確認） |

### I. CLB の既知の罠に対する回帰

| # | 罠 | 確認 |
|---|---|---|
| I-01 | `OnDataChanged` はスクリプト生成にも発火する（ADR-0053） | 明細を生成するスクリプトで値が上書きされない |
| I-02 | 明細の `tax_category_id` は NOT NULL（ADR-0052） | 生成・手追加のどちらでも埋まる |
| I-03 | `OnValidateInput` が無言で保存を止める | 明細の検証で無言の失敗が起きない |
| I-04 | Boolean は新規時の初期値が未チェック | 固定資産フラグの初期状態が意図どおり |

### J. 体験シナリオの通し

`docs/tests/20_体験シナリオ/01_経費精算/` の **11 本を実機で全通し**する。
書き換えた期待値どおりに動くことをもって完了とする。

**実施結果は本 ADR に「実機検証」節を追加して記録する**（ADR-0049 と同じ形式）。

## 並行セッションとの衝突回避

本作業が触るのは `Modules/Expense/*`・`Modules/MasterSystem/ApprovalRouteRule.*`。
`ddl/` は 590 まで使用済みなので **600 番台**を使う（採番前に再確認する）。

---

## 実装中に判明した設計上の追補（2026-08-17）

### 1. 事前申請も明細を持つ。見込み額は申請時のスナップショットへ

当初の決定 4 は「見込み額はヘッダの手入力のまま」としていたが、これは**統制の穴になる**。
費目は承認ルートの判定条件（`approval_route_rules`）なので、明細を持たない事前申請は
交際費であっても「全費目」ルールにしか当たらず、**総務段が積み上がらないまま承認が通ってしまう**。

そこで事前申請も申請時に明細（費目と見込み金額）を入れる形にし、
`estimated_amount` を「申請したときの明細合計のスナップショット」へ意味を変えた（手入力をやめ、
画面では読み取り専用）。判定額は立替・事前を問わず**常に明細合計**に一本化され、
`GetJudgeAmount()` から区分の分岐が消えた。

副作用として「実費が確定したか」を `amount > 0` で判別できなくなったため、
`expense_request.actual_confirmed`（`ddl/601`）を追加した。
`settlement_status` に新しい値を足す案は採らない——ポータル件数・精算処理待ちの SQL が
`'approved'/'accounting'/'settled'` を直接列挙しており、「下流を無修正で保つ」方針に反するため。

### 2. 実費確定の再承認判定は「段構成の突き合わせ」にする

当初は金額区分を跨いだかで判定するつもりだったが、明細を編集できる期間ができたことで
**実費確定のあいだに交際費の行を足す**ようなケースを取りこぼす。

そこで「いま必要な承認者の段構成」と「実際に承認された段構成」を文字列化して突き合わせる方式にした
（`ApprovalFlow.IsRouteChanged`）。金額区分の跨ぎも費目の追加も同じ 1 つの判定で捕まえられ、
承認フローに列を足す必要もない。

### 3. AI 読み取りの添付は明細行へ移せない（CLB の制限）

> **この項は後に撤回した。** 下の「UI の作り直し」を参照。
> (a) `FileField` は `GetMemoryStream()` / `SetFile()` でスクリプトからファイルを移せる（触れないのは内部表現だけ）。
> (b) そもそも現行 UI では**明細行そのものが AI 読み取りと領収書を持つ**ので、移す必要が無くなった。

CLB のスクリプトから触れる `FileField` のメンバーは `FileName` だけで、
実体を指す `FileGuid`・`FileSize` は読み書きできない（`designcheck` で実測）。
そのため「AI に読ませた 1 枚をその行の領収書にする」ことができない。

現行は **行ごとの領収書は明細グリッドの領収書欄に添付**し、
AI に読ませた 1 枚はヘッダの「AI に読ませた領収書」欄に残る形にした。
CLB への改善提案として `docs/12_CLB改善提案/` に記録した。

なお **`ListField` の行に `FileField` を置くとファイル選択ボタンとして描画される**ことは実機で確認済み
（要検証項目として挙げていた点。グリッド内アップロードは成立する）。

---

## 実機検証（2026-08-17）

`http://localhost:5085` で `kaihatsu1_ippan` → `kaihatsu1_kacho1` / `kaihatsu1_buchodairi` →
`soumu_bucho` を切り替えて実施。承認ルート・入力ガード・出し分け・AI 読み取り・状態による編集可否は
すべて実機で操作し、移行の同一性・仕訳の構成は `sql` CLI で検算した。

### A. 承認ルートの合成（すべて実機で申請ボタンを押して確認）

| # | 明細 | 合計 | 実際の承認ルート | 判定 |
|---|---|---|---|---|
| A-01 | 消耗品 29,999 × 1 行 | 29,999 | 課長 山本由美 | ✅ 現行と同一 |
| A-02 | 消耗品 30,000 × 1 行 | 30,000 | 課長 →（部長代理 渡辺剛, 部長 田中宏） | ✅ |
| A-04 | 消耗品 200,000 × 1 行 | 200,000 | 課長 → 部長2名 → 総務 石川真理 | ✅ |
| A-05 | 交際費 29,999 × 1 行 | 29,999 | 課長 → 総務（部長なし） | ✅ |
| A-07 | 消耗品 20,000 ＋ 会議費 15,000 | 35,000 | 課長 → 部長2名 | ✅ **各行は 3 万未満でも合計で判定** |
| A-08 | 消耗品 15,000 ＋ 会議費 14,999 | 29,999 | 課長のみ | ✅ 合計の境界下 |
| A-09 | 消耗品 50,000 ＋ 交際費 10,000 | 60,000 | 課長 → 部長2名 → 総務 | ✅ **費目混在の合成（本命）** |
| A-10 | 消耗品 5,000 ＋ 交際費 5,000 | 10,000 | 課長 → 総務 | ✅ 合成しても部長は入らない |
| A-11 | A-09 の行順を入れ替え | 60,000 | 課長 → 部長2名 → 総務 | ✅ 行順に依存しない |
| A-12 | 会議費 10,000 × 3 行 | 30,000 | 課長 → 部長2名 | ✅ 同一費目の段が重複しない |
| A-14 | 課長本人が消耗品 30,000 を申請 | 30,000 | 部長2名のみ（1 段） | ✅ walk-up ＋圧縮が合成後も効く |
| A-15 | 明細 0 行 | 0 | 「明細を 1 行以上入力してください」で下書きのまま | ✅ |
| A-16 | ルールを一時的に無効化して申請 | 10,000 | 「一致するルールがありません（費目 消耗品費 / 判定額 10,000 円）」で停止 | ✅ 静かに通さない |

DB 側でも確認: A-09 は `approval_flow_order` が 3 段、申請履歴のコメントが
「承認ルート（合成）: 経費_部長まで ＋ 経費_部長まで＋総務」。A-14 は 1 段。
ブロックした 3 件はテンプレート未設定・段 0・履歴なしで、**中途半端な状態が残らない**。

### B. 合計の追随と税額（新規作成フォームで実機操作）

| # | 操作 | 結果 |
|---|---|---|
| B-01 | 行を足す → 金額を変える → 行を消す | 合計 99,999 → 100,000 → 105,000 → 100,000 と即時追随。行削除に確認ダイアログは出ない（ADR-0062） |
| B-02 | 手入力の消費税がある行 | ヘッダのうち消費税に手入力額が反映（8,000 が保存される） |
| B-04 | 消費税 100,000 ＝ 税込 100,000 | 「1 行目: …金額（税込）100,000 円以上です」でブロック |
| B-05 | 消費税 8,000（理論値 9,090・許容 909） | 警告のみで**申請は通る** |
| B-06 | 消費税 −100 | 「1 行目: 「うち消費税」に負の金額は入力できません」でブロック |
| B-07 | 99,999 円（10%） | 9,090（切り捨て）。5,000 円を足すと 9,544 ＝ 9,090 ＋ 454 で**行ごとに端数処理** |

**更新経路の確認（重要）**: 却下 → 明細を 29,999 → 50,000 に修正 → 再申請 で、
ヘッダ合計が DB でも 50,000 に更新され、ルートも 課長 → 部長へ再解決された。
`IsUpdateProtected` を付けなかった判断が正しいことの確認（付けていれば
「明細を直したのに合計が保存されない」静かな失敗になっていた）。

### C. 仕訳生成（画面で実行 → SQL で検算）

明細 5 行（消耗品 20,000 案件あり／会議費 15,000／諸会費 10,000 不課税／
備品 120,000 固定資産・案件あり／旅費 10,800 税額手入力 800）＝合計 175,800 の申請で:

| 行 | 貸借 | 科目 | 金額 | 確認点 |
|---|---|---|---|---|
| 1 | D | 6140 消耗品費 | 18,182 | 行の案件を引き継ぐ |
| 2 | D | 1900 仮払消費税 | 1,818 | 親行 1・行の案件 |
| 3 | D | 6120 会議費 | 13,637 | 案件なし |
| 4 | D | 1900 | 1,363 | 親行 3 |
| 5 | D | 6260 諸会費 | 10,000 | **不課税なので税行が立たない** |
| 6 | D | **1520 工具器具備品** | 109,091 | **固定資産の行だけ振り替わる** |
| 7 | D | 1900 | 10,909 | 親行 6 |
| 8 | D | 6100 旅費交通費 | 10,000 | **手入力 800 が計算値 981 に優先** |
| 9 | D | 1900 | 800 | 親行 8 |
| 10 | C | 2020 未払金 | 175,800 | **1 行にまとめ・案件なし・税区分は対象外** |

借方合計 175,800 ＝ 貸方（差額 0）。全行の部門は申請者の所属部（開発1部）。
固定資産台帳には**その行だけ** 1 件（取得価額 109,091 ＝ 税抜本体、取得日は**行の利用日**、
名前は行の摘要）が登録された。支払仕訳（D 未払金 / C 普通預金 175,800）も生成。

| # | 追加確認 | 結果 |
|---|---|---|
| C-06 | 生成後 | 「仕訳を生成」ボタンが消え、再実行できない |
| C-07 | 計上日 2026/06/15（締め済み） | 本日 2026/08/17 へフォールバックし Info で通知。摘要に「（計上日 2026/06/15）」が残る |

### D・E. 少額資産と交際費（行単位）

| # | 条件 | 結果 |
|---|---|---|
| D-01 | 備品 99,999 円（閾値 100,000） | 固定資産チェックは OFF のまま・トーストなし |
| D-02 | 備品 100,000 円 | 自動 ON ＋ Info トースト。資産管理番号欄が出る |
| D-03 | 資産行と経費行の混在 | 台帳に載るのは資産行だけ |
| E-01 | 交際費行 ＋ 消耗品行 | 交際費行だけ 3 項目必須。消耗品行は空でも申請できる（A-09/A-10 で確認） |
| E-02 | 交際費行の 3 項目が空 | 「**1 行目**: 交際費は相手先・参加人数・目的の入力が必須です」 |

### F. 事前申請

| # | 見込み → 実費 | 結果 |
|---|---|---|
| — | 申請時 | 見込み額が空 → 申請後に **20,000（明細合計）** が記録される |
| F-01 | 20,000 → 21,000（105%） | 「実費 21,000 円を確定しました」・**再承認なし**・承認済のまま |
| F-02 | 20,000 → 25,000（125%） | 再承認へ（進行中に戻り履歴に「再申請」） |
| F-03 | 29,000 → 31,000（106.9%・**110% 以内**） | **区分跨ぎを段構成の比較で検出**して再承認。新ルートは 課長 → 部長の 2 段 |
| F-04 | 実費確定後の仕訳 | 21,000（見込み 20,000 ではない）で計上 |

承認後・実費確定前だけ明細が編集可能になり、確定すると編集不可に戻ることも確認。
見込み額は再承認でも上書きされない（比較の基準が動かない）。

### G. 移行（全件 SQL で検算）

移行の前後で 9 指標が**完全一致**（`expense_request` 14 件・合計 737,900・
`journal_entries` 76・`journal_lines` 202（うち経費由来 35）・`fixed_assets` 2・
`approval_flow` 14・order 17・member 31・history 30）。14 件すべてが明細 1 行を持ち、
費目・税区分・利用日に欠けなし。移行済みの申請を承認 → 仕訳生成 → 精算 → 完了まで通しても正常。

### H. 状態・UI・既存機能

| # | 確認 | 結果 |
|---|---|---|
| H-01 | 完了済み申請 | 明細の入力がすべて `disabled` |
| H-02 | 却下 → 下書きに戻る | 明細が再び編集可・「再申請」ボタンが出る |
| H-03 | 「この申請を複製」 | **明細 5 行がコピー**され下書きになる |
| H-04 | 下書きの削除 | 申請・明細ともに消え、**孤児明細 0 件**。ダイアログ文言も「明細ごと削除しますか？」 |
| H-05 | 領収書 | **明細グリッドの中にファイル選択ボタンが描画される**（要検証項目の結論） |
| H-06 | AI 読み取り | 1 回で**明細が 1 行増える**。金額 12,800・うち消費税 1,163・費目 交際費・利用先が入る |
| H-07 | ポータル・精算処理待ち | 件数が SQL と一致（`amount`／`settlement_status` の転用で下流は無修正） |

### 途中で見つけて直した不具合

1. **AI 読み取りで金額だけ 0 になる。** `Lines.AddRow()` が**その場で** `Lines_OnDataChanged` を
   発火させ、まだ空の行で合計が 0 に書き戻されるため、受け皿から行へ写すときには値が消えていた。
   受け皿の値を `AddRow()` より前に退避して解決（ADR-0053 の実例）。
2. **合成時の代表テンプレートが実態とずれる。** 3 段のルートができたのに `template_id` に
   2 段のテンプレートが入っていた。段数が最大のものを記録するよう変更。
3. **移行でヘッダの「うち消費税」が NULL のまま残る。** 新設した不変条件 D05 が 11 件検出。
   `ddl/602` で明細から埋め直した。

### 回帰

- `designcheck` findingCount **0**
- `Designer/tools/lint_design.py` **error 0 / warn 37**（着手前と同数）
- `docs/qa/20_不変条件SQL/run_all.ps1` **32 合格 / 7 違反** — 違反 7 本は着手前の基準値と
  完全に同一（A09=1・A11=13・B03=10・B04=1・C05=1・C08=1・C10=1）。**改修による増加はゼロ**。
  新設した D05 は合格
- 検証で作ったデータ（申請 21 件・仕訳 6 本・固定資産 1 件・通知）はすべて削除し、
  DB の指紋が移行直後と一致することを確認（差分は `ddl/602` の税額埋め直しのみ）

---

## UI の作り直し（2026-08-17 夜・ユーザー指摘を受けて）

### 何がダメだったか

最初の実装は「明細グリッドに行を足して、その行の中で全部入力する」形にした。ユーザーから 3 点の指摘:

1. **新規作成画面が複雑で、何をすればいいか分からない。** 1 行あたりの情報が多すぎる（14 列）
2. **AI 読み取りがほぼ機能していない。** 2 枚目のレシートを読ませると 1 枚目が消え、明細が全部上書きされる
3. **AI 入力しかないように見える。** 手で入れる導線がはっきりしない

2 の原因は**こちらの前提が誤っていた**こと。「AI はヘッダの受け皿にしか書かない」と考えて
受け皿→行のコピーを組んだが、実際には `AITextAnalyzerField` は **`Lines` コレクションにも
非決定的に書き込む**。結果として 1 回の読み取りで行が二重に増え（合計が 2 倍）、
2 回目の読み取りで既存行が上書きされていた。**レシート 1 枚しか試しておらず、
2 枚目の挙動を検証しないまま「2 行目が足される」と体験シナリオに書いてしまった**のも失敗。

### 決定: 4 ブロック構成にし、「明細 1 件分の入力フォーム」を明細行そのものにする

ユーザー提案の 4 ブロックを採用する。

```
（見出し無し）        件名・目的・計上日・申請区分・支払先
レシート 1 枚ぶんを入力  ← 明細 1 件分のフォーム（AI 読み取り＋領収書＋全項目）＋「この内容で明細に追加」
この申請の明細          ← 積み上がったリスト（9 列）＋ 合計金額・うち消費税・見込み額
承認                    精算ステータス・経理ボタン・承認フロー
```

見出しに番号は振らない。**枠（カード）で 4 つに区切る**ので、番号が無くても切れ目が分かる。
先頭のカードは件名・計上日を見れば何のブロックか自明なので見出しを置かない
（編集中は「レシート 1 枚ぶんを入力」→「この明細を編集」に変わる）。

要点は **入力欄のカードが「フォーム」ではなく「明細テーブルの行そのもの」**であること。
`ExpenseRequestLine` を `ModuleFieldDesign`（`DbColumn: editing_line_id`・`LayoutName: "entry"`）で
親に埋め込み、その行が **AI 読み取りフィールドと領収書 FileField を自分で持つ**。

これで、ユーザーが「最悪」と言った 2 つの状態をどちらも回避できる:

- **入力欄で読ませて、リストにもう一度アップロードし直す** → 起きない。
  1 回のアップロードで抽出値とファイルが同じ行に載る
- **領収書が 1 枚しか添付できない** → 起きない。明細 1 件ごとに 1 枚持てる

**1 枚のレシートを複数明細に分けたい**場合（高価な機器 2 台を別々に固定資産登録する等）は、
AI に読ませて 1 件目を追加 → 空になったフォームに 2 件目を手入力して追加、で成立する。
2 件目に領収書は付かないが、これは例外的な操作であり、毎回の負担にはならない。

### 入力中の行の扱い（データモデル）

| 状態 | `expense_request_id` | `line_no` | リスト | 合計・仕訳・検証 |
|---|---|---|---|---|
| 入力中（入力欄のカード） | NULL | NULL | 出ない | 入らない |
| 確定済み（明細一覧） | 親の id | 1..N | 出る | 入る |

親は `expense_request.editing_line_id`（新設・`ddl/603`）で入力中の行を指す。
明細リストは親 FK で絞るので、**入力中の行は自動的にリストにも合計にも出てこない**。
「この内容で明細に追加」で両方が入り、その瞬間に明細になる。

行の削除では `RenumberLines()` が `line_no` を 1..N に詰め直す。

### CLB の実測知見（この設計を成立させるために必要だったもの）

いずれも `docs/12_CLB改善提案/` にも記録した。

1. **`ModuleField` は保存のたびに、自分が抱えている子の Id を DB 列へ書き戻す。**
   同じ列を指す `NumberField`（`EditingLineIdRaw`）に別の行の Id を入れても**上書きされる**。
   → 別の行を開くときは、**画面の外**（`ModuleSearcher` で取り直した別インスタンス）で列を
   張り替えてから `this.Reload()` する（`PointEditingLineTo`）
2. **`ModuleField` の FK を `null` にすると、次の保存で新しい空の子が作られる。**
   これを「入力欄を空に戻す」手段として使っている（`CommitEntry` の 2 回目の Submit）
3. **`NavigationService.NavigateTo` は、同じモジュールの別クエリでもページを作り直さない。**
   `?line=..&t=..` のように URL を変えても `ModuleField` の子は入れ替わらなかった。
   **`this.Reload()` が正解**（DB の列を張り替えたうえで読み直せば、その行が載る）
4. **未保存（`IsNewData`）のモジュールで `this.Reload()` を呼ぶと例外**
   （`@temporary:...` を数値に変換できない）。新規入力の書きかけを捨てる操作は
   Reload ではなく**フィールドを 1 つずつ空にする**（`ClearEntryFields`）
5. **`FileField` はスクリプトからファイルを移せる。** 補追 3 の「移せない」は**誤り**だった。
   `GetMemoryStream()` / `SetFile(fileName, content)` / `ClearFile()` がある
   （触れないのは `FileGuid`・`FileSize` という**内部表現**だけ）。
   ただし現行設計ではファイルを移す必要がそもそも無いので使っていない
6. **`ApprovalFlow.Initialize` の初期状態を `Pending` → `Draft` に変更。**
   明細を 1 件足した時点で申請が下書きとして保存されるようになったため、`Pending` のままだと
   「申請していないのに進行中」の行が DB に残り、**申請ボタンが消える**（実機で踏んだ）

### 入力欄（「レシート 1 枚ぶんを入力」）の操作

| 操作 | 動き |
|---|---|
| AI で読み取る | 抽出値とファイルがその行に載る。費目から税区分の既定を補完し、少額基準以上の資産性支出は固定資産計上を自動 ON |
| この内容で明細に追加 | 検証 → 行番号を採番 → 親にひもづけて保存 → 入力欄を空に戻す → 合計を取り直す |
| （リストの行をクリック） | その明細を入力欄に読み込む（見出しが「この明細を編集」に変わる） |
| この明細を更新 | 読み込んだ行を上書き保存し、入力欄を空に戻す |
| 入力内容をクリア / 編集をやめる | 入力中の内容を捨てる。確定済みの明細には触らない |

書きかけを残したまま **申請・実費確定はできない**（`HasPendingEntry()` が止める）。
黙って捨てないための関門。

### 不変条件の追加

`docs/qa/20_不変条件SQL/D06_経費_明細の行番号と入力欄の行が壊れていない.sql` を新設。
「親を持つ行は必ず行番号を持つ」「行番号は 1..N の連番」「`editing_line_id` は実在し
他申請の明細を指さない」を見る。実データで合格。

---

## 実機検証（2026-08-17 夜・UI 作り直し分）

`http://localhost:5085`。`soumu_bucho` → `soumu_buchodairi` → `soumu_kacho1`、
`kaihatsu1_ippan` → `kaihatsu1_kacho1` → `kaihatsu1_bucho` を切り替えて実施。

| # | 確認したこと | 結果 |
|---|---|---|
| U-01 | 新規作成画面が 4 ブロックで出る。未保存でも入力フォームが編集できる | ✅ |
| U-02 | 手入力だけで 1 件追加 → リストに載り合計が付く（申請 id が採番され下書き保存される） | ✅ 4,180 / 税 380 |
| U-03 | 費目を選ぶと税区分の既定が入る | ✅ 課税仕入 10% |
| U-04 | **AI 読み取り 1 枚目**（`receipt_kasuga.png`）→ 利用日・金額・税額・利用先・摘要・費目が入り、**領収書もその行に付く** | ✅ 2026/07/03・4,180・380・文具のカスガ 神田店・文具・事務用品 |
| U-05 | **AI 読み取り 2 枚目**（`ファミマレシート.png`）→ **1 件目は無傷**、2 件目として別に追加される | ✅ **報告された不具合の解消を確認**。1,100・税 100・FamilyMart 高松上之町一丁目店 |
| U-06 | 2 件それぞれに別の領収書が付く | ✅ `receipt_kasuga.png` / `ファミマレシート.png` |
| U-07 | リストの行をクリック → その明細が入力欄に載る（領収書の画像ごと） | ✅ 見出しが「この明細を編集」に変わる |
| U-08 | 金額を直して「この明細を更新」→ 行と合計が追随、入力欄は空に戻る | ✅ 1,100 → 2,200／合計 6,380 |
| U-09 | 行を削除 → 合計が追随し、残った行の行番号が 1 に詰まる | ✅ SQL で `line_no=1` を確認 |
| U-10 | 入力欄に書きかけを残して申請 → 止まる | ✅ 「入力欄に、明細に追加していない内容が残っています」 |
| U-11 | 「入力内容をクリア」→ 入力欄だけ空になり、ヘッダ（件名・申請区分）は残る | ✅ |
| U-12 | **1 枚のレシートを 2 明細に分ける**（AI で 330,000 → 追加 → 手入力で 275,000 → 追加） | ✅ 2 行とも固定資産 ON・合計 605,000 / 税 55,000 |
| U-13 | U-12 を申請 → 承認（部長代理・課長）→ 仕訳生成 | ✅ ルート「経費_部長まで＋総務」 |
| U-14 | U-13 の仕訳（SQL で検算） | ✅ D 工具器具備品 300,000 / D 仮払消費税 30,000（親行1） / D 工具器具備品 250,000 / D 仮払消費税 25,000（親行3） / C 未払金 605,000。貸借一致 |
| U-15 | U-13 の固定資産台帳（SQL で検算） | ✅ `EXP-62-1` 300,000・`EXP-62-2` 250,000 の **2 件が別々に**登録される（税抜） |
| U-16 | 事前申請: 明細を入れて申請 → 見込み額がスナップされる | ✅ 48,000 |
| U-17 | 承認後、申請者に**入力欄のカードが再び現れる**（実費確定待ちのあいだだけ編集可） | ✅ ヘッダは編集不可のまま |
| U-18 | 行をクリック → 実費に直して更新 → 「実費を確定」 | ✅ 46,200 / 税 4,200・見込み 48,000 は保持・入力欄のカードが消える |
| U-19 | 承認者・経理の画面では入力欄のカードが出ない（申請後は明細を触れない） | ✅ |

### 途中で見つけて直した不具合（UI 作り直し分）

1. **申請ボタンが消える。** 明細追加で申請が保存されるようになったため、`ApprovalFlow.Initialize` が
   立てていた `Pending` が DB に残り「進行中」と判定された。初期状態を `Draft` に変更
2. **リストの行をクリックしても入力欄に載らない。** `ModuleField` が `editing_line_id` を
   自分の子の Id で上書きしていた。別インスタンス経由で張り替える方式に変更
3. **`NavigateTo` でページが作り直されない。** クエリを変えても子が入れ替わらないため
   `this.Reload()` に変更（URL も汚れなくなった）
4. **未保存の申請で「入力内容をクリア」が例外。** `Reload()` が `@temporary:` id で落ちる。
   新規入力のときはフィールドを個別に空にする方式へ
5. **費目を選んでも税区分の既定が入らない。** 明細モジュール側の `OnDataChanged` が
   JSON に配線されていなかった（スクリプトだけ書いて結線し忘れ）

### 回帰（UI 作り直し後）

- `designcheck` findingCount **0**
- `Designer/tools/lint_design.py` **error 0 / warn 37**（着手前と同数）
- `docs/qa/20_不変条件SQL/run_all.ps1` **33 合格 / 7 違反** — 違反 7 本は着手前と完全に同一
  （A09=1・A11=13・B03=10・B04=1・C05=1・C08=1・C10=1）。新設の D05・D06 はいずれも合格
- 検証で作ったデータ（申請 3 件・仕訳 1 本・固定資産 2 件・承認履歴・通知）はすべて削除し、
  申請 14 件 / 明細 14 行 / 入力欄の行 0 件 / 承認フロー 14 件 / 仕訳 76 本 / 固定資産 2 件で
  移行直後と一致することを確認

---

## 見出しと「レシート／領収書」の見直し（2026-08-17 夜・2 回目のユーザー指摘）

### 指摘

1. 「1. この申請について」のような**番号は不要**
2. **各タイトルがいまいち**。見た目でブロックが分かれていればタイトル自体不要かもしれない
3. **「レシートを読む」と「領収書」の違いが全く分からない**（初見のユーザーの目線で考えること）

### 決定 1: 番号をやめ、枠（カード）で分ける

各ブロックを `GridLayoutDesign.IsBordered: true` のカードにした。**枠があれば切れ目は見えるので番号は要らない。**

| カード | 見出し | 理由 |
|---|---|---|
| 申請ヘッダ | **無し** | 件名・計上日・支払先を見れば何のブロックか自明。見出しを置くと「1 行目に意味の無い言葉がある」だけになる |
| 明細の入力欄 | **レシート 1 枚ぶんを入力**<br>（編集中は「この明細を編集」） | ここだけは「何をする場所か」が自明でない。**1 枚ぶん**と言い切ることで、明細の粒度も同時に伝える |
| 明細の一覧 | **この申請の明細** | 「追加した明細」だと操作の履歴に聞こえる。**いま何が入っているか**を指す言い方にした |
| 承認 | **承認** | 1 語で足りる |

### 決定 2: 「レシートを読む」→「AI で読み取る」にし、関係を文で書く

**事実として、ファイルは 1 つしかない。** AI 読み取りフィールドの `FileField` は `Receipt` を指しているので、
どちらから入れても**同じ「領収書」に入る**。2 つのコントロールは「添付して読み取る」と「添付だけする」の
2 通りの入り口にすぎない。**これが初見で分からないことが問題**だった。

そこで (a) 順番を「読み取る → その結果としての領収書」にし、(b) 1 行ずつ説明を添えた。

```
AI で読み取る   [画像を選ぶ] [Text]
      レシートの画像を選ぶと、領収書として添付したうえで下の項目を自動で埋めます
      AI を使わないときは、下の「領収書」から直接添付します（添付は 1 明細につき 1 つ）
領収書          [ファイルを選ぶ]  receipt_kasuga.png  🗑
      （プレビュー）
```

- 「レシートを読む」は**何が起きるか**を言っていなかった。「AI で読み取る」なら**追加機能**だと分かり、
  使わない道があることも同時に伝わる
- 「領収書」は業務上の名前として正しいので変えない。**説明文で 2 つの関係を明示する**方が、
  ラベルをひねるより誤解が少ない
- 領収書はプレビューで縦に伸びるため、ラベルは上端揃え（`VerticalAlignment: Top`）にした。
  中央揃えのままだと、ラベルが入力欄から離れた位置に浮いて対応が読めなくなる

**AI 側だけに一本化しない理由**: AI プロバイダが止まっている・実 API のコストを掛けたくない・
単に証憑を付けたいだけ、のときに添付できなくなる。**添付は AI に依存させない。**

### 実機確認（2026-08-17 夜）

| # | 確認 | 結果 |
|---|---|---|
| V-01 | 4 枚のカードが枠で分かれ、番号が無い | ✅ |
| V-02 | 先頭カードに見出しが無くても意味が分かる | ✅ |
| V-03 | 「AI で読み取る」で読ませると、値が入り**領収書にも同じファイルが付く** | ✅ プレビュー表示 |
| V-04 | 2 つの説明文が読み取り欄と領収書欄のあいだに並ぶ | ✅ |
| V-05 | 追加 → 明細 1 件・合計 4,180 / 税 380 | ✅ DB で確認 |
| V-06 | 空の入力欄で「追加」→「金額（税込）を入力してください」 | ✅ |

`designcheck` 0 件 ／ `lint_design.py` error 0・warn 37 ／ 不変条件 33 合格・7 違反（着手前と同一）。
検証データは削除し、申請 14 件 / 明細 14 行 / 承認フロー 14 件の指紋に戻した。
