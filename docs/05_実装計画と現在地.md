---
title: 実装計画と現在地
status: current
scope: 全体
audience: [開発]
updated: 2026-08-27
supersedes: []
related: [03_機能スコープ.md, 04_会計ドメイン設計.md, 06_消費税設計.md]
---
# 実装計画と現在地

> **毎回の起点。** 再開するときは §1「次にやること」だけ読めばよい。
> 何を作るかは [03_機能スコープ](03_機能スコープ.md)、どう作るかは [04](04_会計ドメイン設計.md) / [06](06_消費税設計.md)。

**この文書の育て方**（前回プロジェクトでここが最大の文書に育った。同じ轍を踏まない）

- **節を増やさない。** §1〜§5 で固定する。増やしたくなったら、それは別の文書の仕事である
- **常に書き換える。追記しない。** 済んだことは消す。何をしたかは git log、なぜそうしたかは
  [decisions/](decisions/)、CLB の踏み抜きは [qa/](qa/01_CLB静かな失敗.md) と
  [11_CLB改善提案](11_CLB改善提案/README.md) が持つ（[00 §4-3](00_ドキュメント規約.md)）
- **進捗の粒度はフェーズと完了条件まで。** タスク単位の消化ログを書かない
- **250 行を超えたら、分割ではなく削る。** 分割すると溢れた先が育つ

## 1. 次にやること

**用語は [ADR-0017](decisions/0017-仕訳と伝票の呼び分け.md) に従う。** 入力の単位は「振替伝票」、帳簿は「仕訳帳」。画面で「仕訳」と単独で書かない。
**利用者に見せる文言は「です・ます」調で、内部表現を出さない**（[CLAUDE.md §2-7](../CLAUDE.md)）。

**取引先は DB・画面・保存の関門・計上時の焼き込みまで実装済み**（§2）。
設計は [ADR-0019](decisions/0019-マスタは会計が生んだ概念かで分ける.md)・[07_取引先設計](07_取引先設計.md)。
DB マイグレーションの仕組みも**実装済み**。スキーマを変える手順は
[migrations/README](../Designer/migrations/README.md)。

**着手順**（上から。済んだものは消す。何をしたかは git log が持つ）

1. **取引先部品の分割を実装する**（[ADR-0025](decisions/0025-取引先を部品として分ける.md) で
   分けると決定。2026-08-27。分割基準の改定は [ADR-0024](decisions/0024-部品の分割は束ねる相手抜きで使われる場面があるかで決める.md)）。
   C# ライブラリ（`BusinessApp.Partners` ほか）・PageFrame の 2 分割・品質の関門の持ち出しまで。
   受け入れ条件は ADR-0025 の帰結。**いまは開発者が決定の文書を読んで確認する段階**
   （開発者の指示。2026-08-27）。確認が済んだら実装に着手する
2. **優良な電子帳簿のチェックシートとの 1 対 1 の対応表**を書く。**フェーズ 2 の完了条件**
   （[電帳法リサーチ §13.1](research/2026-08-23_電子帳簿保存法.md) の A1〜G2 が素材）。
   このとき **`creator` / `updater` を伝票に持たせるか**（qa/02 R5-08）も決める
3. **「どこで実行するか・どの層に置くか」の設計文書を作る**（開発者の指示。2026-08-26）。
   いま規則が [ADR-0008](decisions/0008-CLBとCSharpライブラリの責務分担.md)・
   [ADR-0013](decisions/0013-機能単位のモジュール分割と依存方向.md)・
   [ADR-0016](decisions/0016-画面から呼ぶサーバ処理はWebAPIに置く.md)・
   [ADR-0022](decisions/0022-明細を並べる帳簿はクエリモジュールで作る.md) の 4 つの表に分かれており、
   **更新され続ける規則が判断ログに載っている**。現在形の正典を新しい設計文書に 1 つ作り、
   ADR は**なぜそう決めたか**だけを残す（ADR の表は消さず、注記で「当時の姿」と示す。qa/02 R13-05）。
   **フェーズ 3 の着手前**（サーバ完結の帳票を作るときに 4 表を同時に触るため）
4. **観点網羅の計器**——SQL・DDL テストの観点漏れを機械で炙り出す計器群（未着手）。
   設計・経緯・時期の判断は [qa/05](qa/05_観点網羅の計器.md) が持つ。着手時に ADR を起こす

**フェーズ 2 の帳簿・訂正・取消・取引先は実装済み**（§2）。**全文書の横断レビューも済んでいる**
（2026-08-26。採否は [qa/02](qa/02_自己レビュー記録.md) のラウンド 13）。

## 2. 現在地

| 項目 | 状態 |
|---|---|
| リポジトリ基盤（`.claude` / `tools` / 公開前チェック） | 完了 |
| ドキュメント基盤（規約・索引・ADR 台帳） | 完了 |
| 制度リサーチ（消費税・電子帳簿保存法） | 完了（[research/](research/)） |
| 会計ドメイン設計 | 完了（[04](04_会計ドメイン設計.md) / [06](06_消費税設計.md)） |
| C# ライブラリ | 骨格と不変条件（I-01・I-03〜I-06・I-13・I-17）＋経過措置のゴールデンテスト。機能単位のモジュール分割と型付き識別子（[ADR-0012](decisions/0012-テスト方針とカバレッジのゲート.md) / [ADR-0013](decisions/0013-機能単位のモジュール分割と依存方向.md) / [ADR-0014](decisions/0014-識別子は型を作る.md)） |
| DB スキーマ（DDL）と初期データ | 12 テーブル ＋ 初期データ（勘定科目 105 / 税区分 10 / 部門 6 / 第 18 期の月次 12）。[`Designer/ddl/`](../Designer/ddl/README.md) と [`Designer/seed/`](../Designer/seed/README.md)。**取引先の素性（種別・法人番号・名寄せの親・所在地）と適格請求書発行事業者の登録テーブルまで追加済み**（[07](07_取引先設計.md)。2026-08-25） |
| DB マイグレーション | 完了（[ADR-0020](decisions/0020-スキーマは現在形の正典で持ち変更は差分で配る.md)）。`tools/clb/migrate.ps1`（`-Adopt` / `-Apply` / `-Status` / `-Verify`）＋ [`Designer/migrations/`](../Designer/migrations/README.md)。稼働 DB は採用済み・正典と同値（手で当てたトリガ 3 本もずれ無し）。適用と記録は 1 トランザクション。失敗時の巻き戻し・チェックサムによる編集検知まで実機で確認済み（2026-08-25）。同値テストとコミット前フックの `-Verify` が二重の網 |
| テスト | `dotnet test BusinessApp.slnx` で **930 件緑**（AccountingCore 429 / AccountingCore.Server 240 / Schema 261）。`AccountingCore` と `AccountingCore.Server` はカバレッジ行・分岐とも 100%（[ADR-0012](decisions/0012-テスト方針とカバレッジのゲート.md)）。**2026-08-25 に、この計測が async とイテレータをまるごと外していたことが分かって直した**（[qa/03](qa/03_テストで漏らした実例.md) L-07）。サーバ側は本物の DDL と初期データを載せた使い捨て SQLite に本物の SQL を流して検査する。コミット前フックが秘密・文書・デザイン・テスト・稼働 DB の同値・ミューテーション・削除ガードの 7 段を自動で流す。**自動テストの緑は「実機で動く」を意味しない。** 台本は [qa/04](qa/04_実機操作テスト.md) |
| C# の記法規約 | 完了（[ADR-0021](decisions/0021-CSharpは読みやすさを損なわない範囲で最新の記法に揃える.md)。2026-08-26）。`.editorconfig` が規則 ID ごとにビルドを止め、`CSharpStyleConvention` が**構文木（Roslyn）で**禁止形（`is { }`・`== null`・`#if`・改行コード）を見る。あわせて**関門そのものの穴**——`.editorconfig` の**実効 severity**・`NoWarn` による無効化・csproj の宣言・ソリューションから漏れたプロジェクト・強制対象の件数・`.gitattributes`・CRLF——も検査する。**機械の関門は、足したら一度わざと壊して鳴ることを確かめる**。この規律と 3 回の自己レビューで、**「テストは緑・ビルドも通る・しかし何も守っていない」穴が 4 つ**出た（[qa/02 ラウンド 8](qa/02_自己レビュー記録.md) に全件）。ADR §4 |
| CLB デザイン | マスタ 5 画面 ＋ 会計設定 3 画面 ＋ 仕訳（振替伝票形式）。一覧・詳細・新規作成・更新をブラウザで確認済み。会計年度・会計期間は**参照のみ**（締めは操作なので画面から直接いじらせない） |
| 仕訳の入力 | 振替伝票形式。会計年度は計上日から自動、行番号・借貸・税区分（科目の既定）も自動で入る。計上済みの伝票は画面が閲覧専用になる（`DataWriteCondition` で `Status = draft` のときだけ書ける） |
| 取消・訂正 | 完了（実機で確認済み）。**画面から呼ぶ入口（`JournalAmendmentEndpoint`）も検査の中に入れた**（2026-08-26。qa/02 R4-03〜05）。識別子の解釈・トランザクション・結果への写像をコントローラから `AccountingCore.Server` へ出したので、カバレッジとミューテーションのゲートに載る。**「取消だけが残らない」——ADR-0016 が Web API を選んだ決め手——を、途中で落とす仕掛けで初めて検査した**。**種別は作成時に決まり、あとから変えられない**（変えられると種別ごとの関門が丸ごと外れる。[qa/03](qa/03_テストで漏らした実例.md) L-08）。計上済みの伝票の画面に「取り消す」「訂正する」を置き、サーバ側の Web API が処理する（[ADR-0016](decisions/0016-画面から呼ぶサーバ処理はWebAPIに置く.md)）。取消は**明細と摘要をサーバが原仕訳から作る**。訂正は取消を計上したうえで、**原仕訳を写した下書き**を開いて利用者に直させる（[ADR-0015](decisions/0015-訂正は取消と再計上の組で表し更新履歴方式を採らない.md)）。取引日は原仕訳と同じ、計上日だけが後ろにずれ、会計年度は計上日から引く。**原仕訳が取り消されていない再計上は計上できない**（通すと取引が二重に載る）。二重取消・二重訂正はアプリと DB の両方で止まる。未実装の種別（期首残高・決算振替・繰越）は関門が拒む |
| 下書きの破棄 | 一覧から削除でき、明細も一緒に消える。**計上済みの行には削除アイコンが出ない**（書き込み条件が効く） |
| 仕訳の一覧・検索 | 会計年度・伝票番号・取引日・計上日・状態・摘要の一覧。検索は取引日・計上日・伝票番号が**範囲指定**、会計年度・取引先・状態・摘要と**組み合わせ（AND）**できる。これは**入力のための一覧**であり、下書きも含む。帳簿としての検索は仕訳帳が担う |
| 仕訳帳（法定検索） | 完了（実機で確認済み）。**明細 1 行が 1 レコード**のクエリモジュール（[調査](research/2026-08-24_仕訳の取引金額での検索.md)）。計上済みだけを出す。取引年月日・取引金額・**伝票番号**を**範囲**で、会計年度（課税期間）・勘定科目・取引先・摘要と**組み合わせ**て引ける（記録項目は通達 8-14、範囲は 8-15、組み合わせは 8-16）。**空値検索**もある（通達 8-13）。**伝票番号の列はその伝票へのリンク**（規則 5 ⑤一ロ の相互関連性）。SQL と宣言のずれは `QueryModuleTests` が、振る舞いは `JournalBookQueryTests` が本物の DDL に本物の SQL を流して検査する |
| 総勘定元帳・補助元帳 | 完了（実機で確認済み。2026-08-26）。**1 画面**で、勘定科目・補助科目・取引先・部門で絞る（[ADR-0022](decisions/0022-明細を並べる帳簿はクエリモジュールで作る.md)）。**科目コード順にまとまり、科目ごとに累計が積み上がる**。**相手勘定科目**（法規 55 ②。2 科目以上なら「諸口」）を出し、累計の符号は[04 §6-1](04_会計ドメイン設計.md)の通常残高の側に合わせる（評価勘定は逆）。**期首残高（フェーズ 4）を含まない**ので列名は「期間内累計」。検索は仕訳帳と同じ範囲・組み合わせ・空値に加えて補助科目と部門。並べ替えさせない（並び順が帳簿の意味そのもの） |
| 取引先マスタ | 完了（実機で確認済み。2026-08-26）。素性（種別・法人番号・所在地・名寄せの親）を画面に出し、**`PartnerSubmitGate`** が検査用数字・個人事業者との矛盾・自己参照を利用者の語で止める（[07 §1-5](07_取引先設計.md)）。**検査用数字を見るのはこの関門だけ**（DB は桁と字種しか見ない）。種別は画面で必須・DB は NULL 可（未分類）。名寄せの**連鎖と循環はフェーズ 3**（解決の規則と一緒に作る） |
| 仕訳の計上 | 完了（実機で確認済み）。`CustomizedModuleDataIO.SubmitAsync` の関門が保存を挟んで二段で通す（[qa/01](qa/01_CLB静かな失敗.md) F-11〜F-13）。伝票番号の採番・入力年月日・計上日時・違反の差し戻し・巻き戻しを実機で確認済み。**計上した人（`posted_by`）も記録する**（qa/02 R2-05。保存経路と Web API 経路の両方で実機確認済み。2026-08-25）。認証が無い経路は NULL、識別子が壊れていれば計上ごと止める。画面・帳簿への表示はまだ（qa/02 R6-01） |
| 実機操作テスト | 台本と観測の作法を [qa/04](qa/04_実機操作テスト.md) が持つ。**2026-08-26 に 15 ケースを通しで流し、取引先の素性（M-03〜M-08）と帳簿（B-05〜B-08）の 10 ケースを足して流した**。**実機でしか出ない本番不具合が 2 件**出て直した（[qa/03](qa/03_テストで漏らした実例.md) L-10）。どちらも自動テストは緑のまま通っていた——**テストの土台が CLB の返り値を偽っていた**ため。**トーストは押す前に `MutationObserver` を張れば捉えられる**と分かり、前回プロジェクトからの積み残しが 1 つ消えた |

**デザインの状態**: `designcheck` は findings 0。金額列の右詰めは `app.css` ＋ 列の `ClassName` で効かせている（帳簿のみ。他の数値列は未適用）。DB の中身は `Designer/seed/` ＋ 実機確認で作った仕訳。

## 3. フェーズ

各フェーズは「**そのフェーズだけで検証できる**」単位に切ってある。

### フェーズ 1 — 会計コアの骨格（仕訳が正しく記録できる）

- C# プロジェクト 4 本の骨格とテスト基盤
- 勘定科目・部門・取引先・税区分のマスタ（DDL ＋ CLB モジュール）
- 会計年度・会計期間・事業所情報
- 仕訳（下書き → 計上）、伝票番号の採番、入力年月日の自動付与
- 不変条件 I-01・I-03〜I-06・I-13・I-17 の検証と自動テスト
  （**I-02（全体の貸借一致）はフェーズ 4** に送ってある。qa/02 R1-44）
- **完了条件**: 仕訳を入力して計上でき、計上済みが変更・削除できないことをテストと実機で確認できる

### フェーズ 2 — 訂正と帳簿（優良な電子帳簿の要件を満たす）

- 訂正・取消（反対仕訳）
- 仕訳帳・総勘定元帳・補助元帳
- 法定検索（3 項目・範囲・組み合わせ・Null 検索）
- 帳簿間の相互関連性（伝票番号でのドリルダウン）
- **完了条件**: 優良な電子帳簿のチェックシートの各項目に対して、
  「製品のどの機能が満たすか」を 1 対 1 で説明できる

### フェーズ 3 — 消費税

- 税区分・用途区分・制度ルール（有効期間つき）
- 税抜経理の消費税行の生成、税率ごとの端数処理
- 経過措置（控除割合の日付判定・相手方別上限）
- 課税売上割合・控除方式 3 種
- 税区分別集計表
- **完了条件**: 2026-10-01 をまたぐゴールデンテストが通り、税集計から仕訳へドリルダウンできる

### フェーズ 4 — 残高と決算

- 期首残高、試算表、月次推移、部門別試算表・部門別 P/L
- B/S・P/L・株主資本等変動計算書・個別注記表
- 月次締め・年度締め・再オープン
- 翌期繰越（暫定・確定・陳腐化検知）
- **完了条件**: 不変条件 **I-02**・I-07〜I-12 が自動テストで緑

### フェーズ 5 — 固定資産

- 固定資産台帳、減価償却（定額法・定率法・一括償却・少額減価償却資産）
- 償却仕訳の生成、除却・売却
- **完了条件**: 制度ルール（耐用年数・償却率）を版で切り替えても過去が再現できる

### フェーズ 6 — 入出力と統制

- CSV 取込（冪等性・Dry-run・行単位エラー）
- 完全なエクスポート（データ辞書つき）
- 監査ログ
- 他部品からの仕訳投入 API
- システム関係書類（概要書・操作説明書・事務手続書類のテンプレート）
- **完了条件**: 全データを出して読み戻せる。優良な電子帳簿の一般要件（システム関係書類）が揃う

### フェーズ 7 — 仕上げ

- デモデータ（第 17 期・第 18 期）
- 通しの体験シナリオと実機検証
- 性能確認（年間 3,000〜4,000 仕訳）

## 4. フェーズをまたぐ約束

- **各フェーズの終わりに `dotnet test` が緑・`designcheck` が 0・`lint_docs` と `lint_secrets` が緑**
- **制度に触れる実装の前に、必ず [research/](research/) の該当節を読む。** 記憶で書かない
- CLB で「静かな失敗」を踏んだら、その場で [qa/01_CLB静かな失敗](qa/01_CLB静かな失敗.md) と
  [11_CLB改善提案](11_CLB改善提案/README.md) に追記する
- フェーズの区切りでドキュメントの棚卸しをする（[00_ドキュメント規約](00_ドキュメント規約.md) §4-3）

## 5. 未決事項

判断が必要になった時点で ADR を起こす。現時点で分かっている論点。

| # | 論点 | いつ決めるか |
|---|---|---|
| 1 | 経過措置の相手方別上限の**超過部分の特定方法**（一次情報が未確認。論点の本体は [06 の保留リスト](06_消費税設計.md)） | フェーズ 3 の着手前 |
| 2 | **集計帳票の API の形**——条件の渡し方・結果の型・件数の分割・描画（CLB 標準の一覧か `ProCodeField` か）。[ADR-0016](decisions/0016-画面から呼ぶサーバ処理はWebAPIに置く.md) が決めた URL・認証・`status`・トランザクションの規約をそのまま広げるかを含む | フェーズ 3（最初のサーバ完結帳票＝税区分別集計表の着手前。[ADR-0022](decisions/0022-明細を並べる帳簿はクエリモジュールで作る.md)） |
| 3 | 監査ログの保存先と改ざん防止の方式。**取消理由（`reversal_reason`）を持つか**もここで決める（qa/02 R3-09） | フェーズ 6 |
| 4 | **制度ルールに編集 UI を作らない**という決定が、市販ソフトの実態のリサーチを経ていない（[ADR-0020](decisions/0020-スキーマは現在形の正典で持ち変更は差分で配る.md) の帰結が自ら「要確認」と書いている。CLAUDE.md §2-6） | フェーズ 3 の着手前 |
| 5 | **ポータル（玄関）PageFrame を作るか**。[ADR-0025 §3](decisions/0025-取引先を部品として分ける.md) は今は作らないと決めた。後から足すコストは今とほぼ変わらない | 機能実装が一段落したら UI を見て開発者が判断（2026-08-27） |

**番号で参照しない**（消すたびに振り直される。[00 §4-4](00_ドキュメント規約.md)）。他の文書から指すときは論点の名前を書く。

