---
title: 実機操作テスト
status: current
scope: 全体
audience: [開発]
updated: 2026-09-21
supersedes: []
related: [01_CLB静かな失敗.md, 02_自己レビュー記録.md, ../decisions/0012-テスト方針とカバレッジのゲート.md]
---
# 実機操作テスト

**サーバを起動してブラウザで操作し、画面の挙動を確かめる。**
[31 §2](../31_検証のルール.md) の「実機で操作していない機能は、できていないものとして扱う」を
実行するための台本である。

**`dotnet test` が緑でも、それは「C# のロジックが正しい」までしか言っていない。**
前回プロジェクトの実測で、**コード上は正しく見えてもブラウザで操作すると動かない**ことが
繰り返し起きた（[qa/01](01_CLB静かな失敗.md)）。CLB は静的検査がほとんど効かない層なので、
画面・保存・遷移は実際に触るまで分からない。

**この文書の育て方**: 本書は**規則（§1・§2）と台本（§3）だけ**を持ち、書き換える。**記録は §4。**
**記録の中で「何を見るか」に番号を振るなら、`M-01` のような字は使わない**——
§3 の台本と [qa/01](01_CLB静かな失敗.md) の記号に当たる（[81 §2-1](../81_略語と記号.md)）。
**「見るもの 1」のように書く。**

## 1. いつ流すか

**この表は 2026-09-21 に Claude が改めた。開発者の承認はまだ受けていない**（[05](../05_開発者への問い.md) の Q-49）。
**開発者が決めたのは「1 点だけの修正」の行までである**（2026-09-20。旧 Q-11）。

**「毎回の項目」とは、毎回必ず踏む 17 項目のことである**（一覧と選んだ根拠は §1-1）。
**「全件」は §3 の 145 項目**（149 項目から【再演不可】の 4 件を除いた数。§3 の前書き）。

**「毎回の項目」とは、毎回必ず踏む 17 項目のことである**（一覧と選んだ根拠は §1-1）。
**「全件」は §3 の 145 項目**（149 項目から【再演不可】の 4 件を除いた数。§3 の前書き）。

| 契機 | 流す範囲 |
|---|---|
| CLB のデザイン（`*.mod.json` / `*.mod.cs`）を変えた | 触った画面 ＋ **毎回の項目**（§1-1） |
| `AccountingCore` / `AccountingCore.Server` を変えた | **毎回の項目**（画面の裏は全部ここを通る） |
| `Partners` / `Partners.Server` / `ServerSupport` / `BusinessApp.Server` / `Client` / `Client.Shared` を変えた | **触った経路とその周辺**（取引先・保存の関門・認証など、その層が受け持つ画面） |
| DB スキーマを変えた | **毎回の項目** ＋ 変えたテーブルを使う画面 |
| **権限・認証・玄関**（＝ログイン後に着地する入口のフレーム。[ADR-0035](../decisions/0035-フレームは役割と部品の組で分け玄関を1枚置く.md)）**に触れた** | **毎回の項目 ＋ §3-6 の全件**（**章ごと踏む**——条件は章の中で互いに支え合っているので、1 項目だけ見ても閉じているとは言えない） |
| **保存の関門に触れた** | **毎回の項目 ＋ その関門が働く章の全件**（**一覧ではない**——`BusinessApp/**/Application/*Gate.cs` を数えると 6 つある。**その関門が働く章を当てる**——マスタなら §3-1、取引先なら §3-1、伝票なら §3-2 と §3-7、登録番号なら §3-5、自社情報なら §3-1） |
| フェーズの区切り | **§3 の全件** |
| **1 点だけの修正**（文言 1 か所や断りの出し方のように、**判定の中身・画面・スキーマを変えない**修正） | **触った経路と、その周辺だけ**。毎回の項目も全件も要らない |

**使うのをやめた語（引退した語）は `lint_docs.py` が機械で止める。**
**語は「全件」に統一した**（開発者の指示。2026-09-20。逐語は「**一度『通し』を全部消しても、いつの間にかまた使いそうなことです**」。**機械で止める形は Claude の設計**。それまで「§3 の通し」と「§3 の全件」の 2 語があり、 <!-- lint-docs:retired-ok 引退した語そのものを説明する行 -->
**違いが定義されていないまま同義で運用していた**）。**古い文書に「通し」とあれば「全件」と読む。**
**`lint_docs.py` が止めるのは「§3 / 台本 / 全件」の近くに「通し」が来る形である** <!-- lint-docs:retired-ok 止める形そのものを説明する行 -->
（`§3 の通し`・`台本を通し`・`全件通し` など。**「台本の通し用取引先」は検体の名前なので外れる**）。 <!-- lint-docs:retired-ok 止める形そのものを説明する行 -->
**過去の記録は、行ごとに `lint-docs:retired-ok` に理由を添えて外す**（**理由の無い印は効かない**）。
消しても、いつの間にかまた書くからである。 <!-- lint-docs:retired-ok 引退した語そのものを説明する行 -->

**この表が [30 §7-1](../30_作業のルール.md) の条件 3 の「ソースコード」の範囲の正典である。**
**開発者が決めたのは同 §7-1 の 6 条件で、範囲をこの表へ委ねたのは Claude の敷衍である**
（同 §7-1 の前書き）。
**ここに行の無いもの**（`tools/` の道具・`docs/` だけの変更・各 CLI）**は対象外**——
実機で確かめられることが何も無いからである。
**代わりに、その道具自身の `--selftest` と、壊して赤くなるかの確認を通す**（[31 §1](../31_検証のルール.md)）。
**「1 点だけの修正」の行は開発者の決定である**（2026-09-20。もとは Claude の判断
（[00 §4-11](../00_ドキュメント規約/本文.md)。旧 Q-11）——
**§3 の台本は 100 項目を超えるので、1 行の修正に全件を課すと規則のほうが先に折れる。**
**括弧の中の条件は 2026-09-16 に狭めた**——それまでは「不具合 1 件・文言 1 か所」で、
**判定の中身を変える不具合修正まで入って見えた**。判定はどの画面からも通るので、
**触った経路だけでは足りない**。**「スキーマ」も同日に足した**（前例が書いていたのは判定と画面まで）。

**「挙動を変えないリファクタリング」でも流す。** 変えないつもりだったかどうかは、
画面が動くかどうかと関係が無い。

**複数の行が同時に当たったときの優先**（**開発者が決めたのは旧 Q-11 の形まで。2026-09-20**。**2026-09-21 の書き換え——免除の対象を全件から 17 項目に変え、負ける行を 2 つ足した——は Claude で、未承認である**。もとは Claude の判断。2026-09-16。
[00 §4-11](../00_ドキュメント規約/本文.md)。旧 Q-11）

**同じ 2 行**（「`AccountingCore` を変えた」と「1 点だけの修正」）**が 2 回続けて当たった。**
**1 回目は自己レビューが「表に優先が無い」と指摘し**（[qa/02 のラウンド 118](02_自己レビュー記録/2026-09.md)）、
**2 回目は前例をそのまま踏襲した**（[記録の 2026-09-16](04_実機操作テスト/2026-09.md) の 2 節）。**その場の判断を字にする。**

**上から順に当てる。**

1. **「フェーズの区切り」が当たるなら、§3 は全件流す。**
   **ほかの行の「＋ …」の部分**（触った画面・変えたテーブルを使う画面）**は足す**——
   フェーズの行は台本の中しか言っていないので、**そのままでは他の行を含まない**。
   **全件は毎回の項目を含む**ので、毎回の項目を別に数えない。
2. **「1 点だけの修正」は、フェーズの区切り・「権限・認証・玄関」・「保存の関門」を除く他の行に勝つ**（**2026-09-21 の見直しは Claude**）**。**
   例外を置いた理由は**台本の項目数**だったが、**毎回の項目は 17 項目なのでその理由はもう効かない**（2026-09-21 に見直した）。
   **いまこの行が免除するのは毎回の項目である。**
   **権限と保存の関門の 2 行に負けるのは、「章ごと踏む」の理由が 1 行の修正でも変わらないからである。**
   **当たるかどうかは表の行の括弧が決める**（判定の中身・画面・スキーマを変えていないこと）。
3. **それ以外は、当たった行の和を採る。**「広いほう」では決まらない組がある——
   CLB のデザインの行と DB スキーマの行は、**どちらも他方を含まない**。
   **どの行に当たるかで迷うなら、流す側に倒す。**
4. **優先を使った回は、§4 が指す年月のファイルの記録にそう書く。**「1 点だけ」かどうかを決めるのは Claude なので、
   **後から読み直せる形で残す**（そう書いていない回は、範囲を狭めた理由が消える）。

**§3 の全件にかかる時間とトークンは、2026-09-20〜21 に実測した**
（**開発者の指示。2026-09-20**。逐語は「3の台本を全部流すとどれくらい時間とトークンがかかるのでしょうか？今度の自律作業中に実測してみてください」）。
**値は [記録の 2026-09-20〜21](04_実機操作テスト/2026-09.md) が持つ。**
**ここに引くのは、この節の規則を決めた根拠になる 2 つだけである**——**149 項目で実時間 2 時間 31 分**
（サブエージェント 3 人並列・4 波。**1 項目あたり延べ 113 秒**）。**残りは記録にしか無い。**

**この値が §1-1 の毎回の項目を置いた理由である。** **フェーズ 3 は回ごとに `AccountingCore` を触る**ので、
**全件を課すと回の数だけ 2 時間半が乗る**（開発者の問題提起。2026-09-20。逐語は
「**毎回毎回、台本の全てを行っていると時間がいくらあっても足りません**」）。

**ただし [30 §4-1](../30_作業のルール.md) は、この台本を「妥協せず 100%」の側に置いている**
——**会計ロジックの不具合の絶無を期すための仕組み**だからである。**「割に合わないからやめる」とは書かない。**
**§1-1 は、同じ範囲を別の手で守れることを示して絞っている**——
**実機でしか捕まらない型は毎回の項目が受け、それ以外はコミット前フックの段が毎回受ける**（§1-1 の末尾）。
**範囲を狭めるのは機械的関門を緩める操作だと読んだので、決めるのは開発者である**（[34 §1](../34_開発者とClaudeの分担.md) の 8。**ただし 34 §1-3 の定義に実機の台本は挙がっていない**——**当てはめは Claude の読みで、[05](../05_開発者への問い.md) の Q-49 の ③ で問うている**）。
**この節の形は Claude が設計したもので、開発者の承認はまだ受けていない**（[05](../05_開発者への問い.md) の Q-49）。

### 1-1. 毎回の項目——毎回必ず踏む 17 項目と、それで足りる理由

**毎回の項目とは、どの回でも必ず踏む 17 項目のことである。** **選ぶ範囲は「実機でしか捕まらない型」の再演に限る。** 選んだ根拠は
[qa/03](03_テストで漏らした実例.md) の**全 60 件を分類した結果**である（2026-09-21。
**分類の表は [記録の 2026-09-20〜21](04_実機操作テスト/2026-09.md) が持つ**）。

**分類は 2 人のサブエージェントが独立に行い、数が割れた**——**実機で見つかった漏れは 13〜14 件**、
うち**「実機でしか捕まらない型」は 12 件前後**で、**マスタは 1〜3 件・帳簿は 0〜1 件**と読みが分かれた
（割れたのは「**どちらとも言えない**」をどちらへ寄せるかである）。
**だから毎回の項目は件数では決めない**——**どちらかの分類で 1 件以上が出た経路を、全部踏む**ことを条件にした。
その経路は**計上・マスタ・取消訂正・登録番号・同時操作・帳簿**の 6 つである（**帳簿は 2 人目が 0 件と読んだ**が、条件は「どちらかで」なので入る）。
**複製と基盤は両方の分類で 0 件**、**権限も 0 件だが、別の理由で 1 項目だけ残す**（下の表の P-17）。
**下の表が 8 行あるのは、L-50 が伝票とマスタの両方で起きた**ため、**検索欄の前後の空白を 1 行に割った**からである。

**経路の語**——ここでいう**経路**は「**漏れが通った画面と機能のまとまり**」で、qa/03 の 60 件をこの回に分けたものである。
**基盤**は型・カバレッジ・道具・テストの土台など**画面を持たないもの**、**複製**は伝票の「複製する」の一連を指す。
**分類は qa/03 の側には無い**（この回に数えるために置いた区分である）。

| 経路 | 毎回の項目 | 再演する漏れ |
|---|---|---|
| 計上 | **J-03・J-10・J-11** | L-10（CLB の返り値は実機でしか観測できない）・**L-16**（検査を通る保存ボタンと通らない保存ボタンが並ぶ）・**L-23**（**空文字は本番の画面でしか作れない**） |
| マスタ | **M-13・M-26・M-28** | **L-29**（DB が何も拒まない型なので、関門の文言が画面に出るかは実機だけ）・L-28（定型文に落ちていないか・必須の印が出るか） |
| 取消・訂正 | **A-01 ＋ A-03**（**2 つで 1 組**） | **L-41**（`Amend(...)` を共有する 3 ボタンのうち**押さなかった 2 つが壊れていた**。`designcheck` は緑のままだった） |
| 同時操作 | **C-01**（**対照つき**）・**C-03** | **L-31**（**対照を入れないと「たまたま失敗しただけ」と区別できない**。本質は守りではなく**文言**である） |
| 帳簿 | **B-14・B-15** | L-49（検体が数で束縛していた間、**SQL は実機で 1 件も返していなかった**） |
| 検索欄の前後の空白 | **J-56・M-36** | **L-50**（`ShouldTrimAfterEdit` は詳細で効き検索で効かない——**「`true` と書いてあるか」を見る単体テストでは永久に緑**）。**L-50 が壊れていたのは伝票の摘要と取引先の 10 欄**なので、**伝票（J-56）とマスタ（M-36）の両方を踏む** |
| 登録番号 | **R-03・R-23** | **L-12**（CLB が日付に時刻を付けるので**本番だけ検査が外れる**。**保存された値を `sql` で覗く**のが要点） |
| 権限 | **P-17** | L-22。**型が 0 件なのは、まだ実機で踏んでいないからかもしれない**（L-49 の教訓）。**取消は不変で取り返しがつかない**ので残す |

**12 件のうち、17 項目が再演するのは 8 件である**（**L-30・L-43・L-53・L-58 の 4 件は、どの項目も再演していない**——**受け持ちは未整理で、次に台本を触る回の宿題である**）。
**表の `L-nn` は「その経路で実際に起きた漏れ」の例であり、12 件と 1 対 1 ではない**
——**L-22 と L-49 は自己レビューで見つかったもの**で、実機の側には入らない（2026-09-21 に数え直した）。
**毎回の項目が引き受けるのは経路であって件数ではない。**

**毎回の項目に入れなかったもの。**
**基盤**（qa/03 の 60 件中 **19 件で最多**）は**実機では 1 件も捕まらない**——持ち場は道具の `--selftest` と掃引である。
**複製**は型が 0 件で、**L-40 は「実機の台本も同じ読みで書いた」**と書く——**A-09 の手順が要件の読み違えをそのまま写していたので、踏んで緑になるのは当然だった**。**実機が効かなかった実例**である。

**毎回の項目は退行検知ではない。** **実機の台本は退行検知としては弱い**——**動いているものは、動いて見える**。
**L-47** は「画面では動いている。絞り込みは実機で触れば効くので、**『効かなくなったときに気づけるか』だけが抜けていた**」と書く。
**退行を捕まえるのはコミット前フックの段である**——`dotnet test`・カバレッジ・ミューテーション・
**行セットの差分で殺す掃引**（`-Mode Rows`）が**毎回流れる**（段の正典は [tools/README](../../tools/README.md)）。
**毎回ではない計器が 2 つある**——**制約ノックアウト**と **SQL ミューテーションの `-Mode Tests`** で、
**流す契機は [31 §6](../31_検証のルール.md) が別に持つ**。
**毎回の項目を絞れるのは、この 2 段構えが生きているからである**——**どちらかを緩めたら、この節の前提が崩れる。**

**毎回の項目の項目を消す・減らすときは、その項目が再演している L-nn をどこが受け持つかを書く**（[30 §9](../30_作業のルール.md)）。

## 2. 観測の作法（ここを間違えると半日溶ける）

| 見たいもの | 正しい見方 | やってはいけないこと |
|---|---|---|
| **書けたか・何が残ったか** | **DB を見る**（`sql` CLI） | 画面の表示だけで判断する |
| 画面の状態（入力値・活性/非活性） | **スクリーンショットを見る** | `get_page_text` で読む（`<input>` の value を拾わない。[qa/01](01_CLB静かな失敗.md) G-07。**この誤診で半日溶かした**） |
| トーストの文言 | **押す前に `MutationObserver` を仕込む**（下） | 押してから捉えに行く（消えている。[qa/01](01_CLB静かな失敗.md) G-05） |
| 一覧の件数 | 削除のあとは**再検索してから**読む | 削除の直後の件数を信じる（行だけ減って件数が古い。[qa/01](01_CLB静かな失敗.md) D-14） |
| 遷移したか | URL とスクリーンショット | |

**操作が Blazor に届かないことがある**（[qa/01](01_CLB静かな失敗.md) G-01〜G-04）。
入力は実クリック＋実タイピングで行い、**結果は必ず DB で確かめる**。

**出力は全部読む。確かめに行った 1 点だけを見て、残りを「確認済みの背景」として流さない**
（踏んだ形と処方は [qa/03 L-39](03_テストで漏らした実例.md)）。**見た文字列は §4 が指す年月のファイルの記録に写す**——
写すと読むことになる。

### 2-1. トーストを捉える

**押す前に**張っておけば取れる。押してから捉えに行くと間に合わない。

```js
window.__cap = [];
new MutationObserver(() => {
  const t = document.documentElement.innerText || '';
  const i = t.indexOf('計上できません');      // 探す文言
  if (i >= 0) window.__cap.push(t.slice(i, i + 400));
}).observe(document.documentElement, { childList: true, subtree: true, characterData: true });
```

押したあと `[...new Set(window.__cap)]` を読む。
**`document.body` ではなく `document.documentElement` に張る**（トーストは body の外に出ることがある）。

これで 2026-08-26 に
`計上できません（2 件）。①借方合計 1,000 円と貸方合計 900 円が一致していません。②行 1: …`
を実際に捉えた。**「トーストは開発者に頼むしかない」は解消した**（前回プロジェクトからの積み残し）。

### 2-2. ブラウザ操作の落とし穴（このハーネス固有）

| 症状 | 事実 | どうするか |
|---|---|---|
| クリックが効かない | **要素参照（ref）経由のクリックは Blazor に届かないことがある** | **スクリーンショットの座標で実クリックする** |
| 座標がずれる | スクリーンショットは幅 1568 に**縮尺されている**。CSS 座標とは別物 | `k = 1568 / window.innerWidth` を掛けて変換する。**2 回掛けない** |
| `<input type="date">` に入力できない | **CDP のキー送出も `insertText` も届かない**（フォーカスは当たる。値は空のまま） | ネイティブ setter ＋ `change` イベントで値を入れ、**検索ボタンは実クリックする**。値の入れ方だけが代替で、検索の経路は本物を通る |
| 文字の欄に `type` が届かない | ページを開いた直後や検索欄で、`type` の文字が入らないことがある（2026-09-10・11。フォーカスは当たる） | `value` を入れて `input`・`change` を発火させる（Blazor の束縛はこれで更新され、関門まで届く——2026-09-11 の S-03）。**日付の欄と同じ形** |
| スクリーンショットが `timed out` | 描画の途中に当たると落ちる | **もう一度撮る**（2 回目はほぼ通る）。バッチの末尾に置かず単独で撮ると安定する。**`timed out` の次の 1 枚が「拡大された一部」の絵になることがある**ので、**2 回で足りるとせず、絵が期待と違ったらもう一度撮る**（2026-09-20） |
| 描画途中を撮ってしまう | 件数やボタンが**まだ出ていない**状態が写る | 「無い」と判定する前にもう一度撮る |
| 台本のために変えたデータを戻せない | **`UPDATE` は変える前の値を返さない。** 一括で変えると、どれが元からその値だったのかが消える（2026-09-08 に取引先の `is_active` で踏んだ） | **変える前に `SELECT` で控える。** 控えを取らずに変えてしまったら、**画面のスクリーンショットが唯一の証拠**になる |

**並行して実機を踏む回は、[qa/01](01_CLB静かな失敗.md) の G-11〜G-13 を先に読む**
——**背面のタブ**（`document.visibilityState` が `hidden` のタブ）**では実クリックが 1 つも届かない**（例外も出ない）・**`navigate` の直後はスクリーンショットを 1 枚撮るまで届かない**・
**`ctrl+a` は分類器に止められる**。**2026-09-20 の全件実測で踏んだ**（G-11 は 2 人、G-12・G-13 は 1 人ずつ）。

## 3. 台本

前提: `pwsh -NoProfile -File tools/clb/deploy.ps1` → サーバ起動
（`dotnet run --project BusinessApp/BusinessApp.Server --launch-profile http`）→
`http://localhost:5085`。**`*.mod.cs` と DB スキーマの変更はサーバ再起動が要る。**

**「伝票 N」は、断りのない限り伝票番号を指す。行の id を指すときは「id N」と書く**
（2026-09-20 の全件で取り違えが起きた。記録も両方の意味で使っていた）。

**【再演不可】の印**——**規則より前に作られたデータ**や、**一方通行の遷移・一度きりの状態**を検体にしている項目は、**その検体を消費すると再演できない**
（**一方通行の関門が増えるほど、この型は増える**）。**全件からも外す。**
**持ち場は C# のフィクスチャつきテストである**——`BusinessApp.AccountingCore.Server.Tests` の `Fixtures/AccountingServer.cs` が
**「いまの製品では作れない形」**を作り、取消・訂正・関門のテストが使っている。
**印のある項目は、いまの開発機では踏めない**——**検体を `seed/`（初期投入の SQL）に含める回**
（[33 §2](../33_失わないためのルール.md) の「正典からの作り直し」）**でだけ踏める**。
**DB を作り直すだけでは足りない**（A-06 の検体は、いまの関門が新しく作らせない）。
**したがって「全件」は 149 項目のうち印の 4 件を除いた 145 項目である**（実測の 149 は、印を付ける前に踏んだ数である）。

### 3-1. マスタ（読み出しの経路）

**この表に未実装の行はもう無い**（2026-09-02。フェーズ 2.5 が終わったので「（フェーズ 2.5 で作る）」の印を外した）。
**次に未実装の行を足すときは、同じ形の印を付ける**——落ちたときに「退行」と読み違えないため。

| # | ねらい | 手順 | 期待 |
|---|---|---|---|
| M-01 | 一覧が出る | 勘定科目を開く | 105 件の科目が出る |
| M-02 | 詳細が開く | 科目を 1 つ開く | 値が入った状態で表示される |
| M-03 | 取引先の新しい列が出る | 取引先を開く | **一覧に種別・法人番号・所在地・名寄せの親が出る**（2026-09-20 の実測で直した。名寄せの親は詳細だけではない）。詳細では名寄せの親を候補から選べる |
| M-04 | **種別は画面で必須** | 種別が空のまま登録する | CLB が「必須項目です」で止める（DB は NULL 可のまま。[13 §1-2](../13_取引先設計.md)） |
| M-05 | **検査用数字の合わない法人番号を弾く** | `1700110005901` で登録する | 差し戻され、**打ち間違いとして**知らせる。DB は変わらない |
| M-06 | **個人事業者に法人番号は入れられない** | 法人番号が入った取引先の種別だけを個人事業者に変える | 差し戻される（**差分に法人番号が無くても**保存されている値で判定する） |
| M-07 | **自分自身を名寄せの親にできない** | 名寄せの親に自分を選んで登録する | 差し戻される。DB は変わらない |
| M-08 | 振替伝票の注意書き | 下書きの伝票を開く／計上済みの伝票を開く | 下書きには明細の上に注意書きが出て、**計上済みには出ない**（[13 §1-4](../13_取引先設計.md)） |
| M-09 | **無効な取引先も名寄せの親にできる** | 取引先を 1 件無効にし、別の取引先の名寄せの親にその取引先を選ぶ | **候補に出て、選べて、保存できる**（[13 §2-5](../13_取引先設計.md)） |
| M-10 | **個人事業者と法人系は互いに親にできない** | 法人の取引先を、個人事業者の取引先の名寄せの親にする | 差し戻される。**未分類・「その他」・法人 × 人格のない社団等は通る**（[13 §2-5](../13_取引先設計.md)） |
| M-11 | **名寄せの親は 2 段につながらない** | ①親を持つ取引先を、別の取引先の親にする ②子を持つ取引先に、親を付ける | **どちらも差し戻される**（DDL のトリガも同じものを拒むことを `sql` CLI で確かめる） |
| M-12 | **補助科目の候補が勘定科目で絞られる** | 明細を 2 行足し、行ごとに違う勘定科目を選んで補助科目の候補を開く | **その行の科目に属する補助科目だけ**が出る（行ごとに独立）。**科目を選び直すと、属さなくなった補助科目は空になる** |
| M-13 | **使用中の科目の科目区分は変えられない**（[ADR-0038](../decisions/0038-使用中のマスタは意味を変えられない.md)。qa/03 L-29 の再演） | 計上済みの明細がある科目（例: 通信費）を開き、科目区分を変えて登録する／名前だけ変えて登録する／税区分の課税区分を変えて登録する | 科目区分・課税区分は関門の文言（`MasterMeaningGateTests` が全文で固定している。計上済みの明細の件数入り）で差し戻され、DB は変わらない。**名前だけの変更は通る**。DDL のトリガも同じものを拒むことを `sql` CLI で確かめる |
| M-14 | **「補助科目を使う」の欄が改名後も出る**（`uses_sub_account`。[ADR-0038 §3](../decisions/0038-使用中のマスタは意味を変えられない.md)） | 勘定科目の詳細を開く／使用中の科目でその欄を切り替えて登録する | 欄が「補助科目を使う」として出る。使用中の科目では M-13 と同じ形で差し戻され、**文言に「補助科目を使う」が入る**。DB は変わらない |
| M-15 | **補助科目マスタの「勘定科目」の候補が、補助科目を使う科目だけになる**（[21 §1](../21_画面の原則.md) の「押す前に見せる」） | 補助科目の新規作成で「勘定科目」の候補を開く | **「補助科目を使う」がオンの科目だけ**が並ぶ |
| M-16 | **勘定科目の一覧に「補助科目を使う」の列が出る** | 勘定科目の一覧を開く | ○ / — の列が「有効」と「評価勘定」の間に出る |
| M-17 | **【再演不可】「取引先を要する」は、使用中の科目でもオンにできる**（[15 §1-2](../15_記帳の枠組み.md)。**一方通行の列**。**開発機の買掛金は 2026-09-16 にオンになったので、オフ → オンの遷移はもう踏めない**——2026-09-20 に未達） | 計上済みの明細がある科目（買掛金）を開き、「取引先を要する」をオンにして登録する | 「更新しました」。**M-13・M-14 と違って差し戻されない**——**厳しくする向きを止めると、規則を後から採り入れられなくなる**（[15 §1-2](../15_記帳の枠組み.md)）。一覧と候補ダイアログにも ○ / — の列が出る |
| M-18 | **同じ欄を、使用中の科目でオフにはできない**（同上。**止めないと二層の守りをまとめて外せる**） | M-17 の科目を開き、「取引先を要する」をオフにして登録する | 差し戻される。**文言は `MasterMeaningGateTests` が全文で固定している**（台本は写しを持たない）。**「変えられません」ではなく「オフにできません」**で、**代わりに何をすればよいか**まで言う。DB は変わらない |
| M-19 | **一方通行であることを、押す前に知らせている**（[21 §1](../21_画面の原則.md) の「制限は押す前に見せる」） | 勘定科目の詳細を開く | 「取引先を要する」の下に注記が出る——**オンにするのはいつでもでき、計上済みの明細がある間はオフに戻せない**ことが書いてある |
| M-21 | **書式の断りが条件ごとに分かれる**（[12 §2-1](../12_マスタ台帳.md)） | `sql` CLI で `INSERT INTO accounts … VALUES ('-A', …)` | 「「科目コード」の先頭に「-」「_」は置けない。」（文言の正典は DDL。`MasterCodeGuardTests` が 6 表 × 追加/更新 × 6 条件で固定） |
| M-22 | 同（連続） | `'A--B'` | 「「科目コード」の「-」「_」は続けて使えない。」 |
| M-23 | **「取引先を要する」の断りが関門と同じ言い方** | 計上済みの明細がある科目の `requires_partner` を `sql` CLI で 0 に | 「…勘定科目では、「取引先を要する」をオフにできない。」 |
| M-24 | 意味の凍結——「新しい勘定科目を作る」 | 計上済みの明細がある科目の `code` を `sql` CLI で変える | 「…勘定科目の意味は変更できない。新しい勘定科目を作る。」 |
| M-25 | 同（補助科目。列ラベルの写しが消えた） | 計上済みの明細が使う補助科目の `code` を変える | 「…補助科目の意味は変更できない。新しい補助科目を作る。」 |
| M-26 | **「全社共通」の断りが、いまなっている部門の名前とコードを言う**（[qa/02 R57-06](02_自己レビュー記録/2026-09.md)） | 部門の新規作成で「全社共通」をオンにして「登録」 | 「登録できません。「全社共通」の部門は 1 つだけです。いま「全社共通」になっているのは部門名「全社共通」（部門コード 00）です。そちらをオフにしてから、こちらをオンにしてください。」（＋標準ボタンの「更新に失敗しました」。[21 §2-6](../21_画面の原則.md)） |
| M-27 | **保存していない変更があると、離れる前に確かめる**（[21 §1](../21_画面の原則.md)） | ①部門の詳細で部門名を直し、サイドメニューの「税区分」を押す ②「入力を続ける」 ③もう一度押して「離れる」 ④**新規作成を開いて何も触らずに**「税区分」を押す | ①「画面を離れる確認」が出て遷移しない ②画面に残る ③税区分の一覧へ移る ④**確認は出ず**に移る（[qa/01 F-43](01_CLB静かな失敗.md)） |
| M-28 | **条件つき必須の印と凡例**（税区分・部門） | 税区分の詳細と部門の詳細を開く | 税区分: 「税率区分」に `*`、凡例に「「税率区分」は「課税区分」が「課税売上」「課税仕入」のときに必要で、それ以外では空にします。」。部門: 凡例に「「全社共通」にできる部門は 1 つだけです。」 |
| M-29 | **新規作成の登録が通った後の遷移では、確認が出ない** | 部門の新規作成でコードと名前を入れて「登録」→ 一覧へ戻る／サイドメニューを押す | **「更新しました」**の後、確認なしで移る（[qa/01 F-43](01_CLB静かな失敗.md) の「保存が通れば偽に戻る」を新規で確かめる）。**新規作成でも「登録しました」にはならない**——CLB の既定の文言で、**この台本は 2026-09-16 まで「登録しました」と書いていた**（記録の側は初めから「更新しました」だった） |
| M-30 | **離脱の確認は、本文が違う画面でも出る**（利用者・自社情報・登録番号） | ①利用者の新規作成を何も触らずに離れる ②自社情報を直して離れる ③取引先の詳細の「登録番号を追加する」で開いた画面で登録番号を入れて離れる | ①確認なし ②確認あり ③確認あり（[記録の 2026-09-10](04_実機操作テスト/2026-09.md)） |
| M-31 | **勘定科目・補助科目のカナに上限がある**（[12 §2-2](../12_マスタ台帳.md)。旧 Q-26 の決定） | 勘定科目を開き、カナの欄のプレースホルダを読む → 61 文字を貼って「登録」→ 60 文字に減らして「登録」 | プレースホルダは「60 文字以内」。61 文字は「**登録できません。**「カナ」は 60 文字以内です。いまは 61 文字あります。短くして入力し直してください。」で断られ、**60 文字は通る**（名前の上限 30 では断られない） |
| M-32 | **自社情報の 5 欄に上限がある**（同上） | 自社情報を開き、会社名・カナ・代表者名・住所・電話番号のプレースホルダを読む → 電話番号に 21 文字を入れて「登録」 | プレースホルダは 100／200／30／200／20 の「N 文字以内」。21 文字は「**保存できません。**「電話番号」は 20 文字以内です。いまは 21 文字あります。短くして入力し直してください。」で断られる（**接頭は押したボタンの札で決まる**——自社情報だけ「保存」なのでここだけ「保存できません。」になる） |
| M-33 | **郵便番号は書式で断る**（同上。長さではない） | 自社情報の郵便番号のプレースホルダを読む → 「1234567」で「登録」→ 「123-4567」に直して「登録」 | プレースホルダは「数字 3 桁 ＋ 「-」 ＋ 数字 4 桁（123-4567）」。「1234567」は「**保存できません。**「郵便番号」は数字 3 桁 ＋ 「-」 ＋ 数字 4 桁（123-4567）です。いまは 7 文字あります。入力し直してください。」で断られ、**「123-4567」は通る** |
| M-34 | **取引先のカナは 200 文字**（同上。2026-09-16 に 100 から動かした） | 取引先を開き、カナのプレースホルダを読む → 201 文字を貼って「登録」→ 200 文字に減らす | プレースホルダは「200 文字以内」。201 文字は「**登録できません。**「カナ」は 200 文字以内です。…」で断られ、**200 文字は通る** |
| M-35 | **郵便番号は、使えない字も位置と字を言って断る**（同上） | 自社情報の郵便番号に「12a-4567」を入れて「保存」 | 「**保存できません。**「郵便番号」の 3 文字目の「a」は使えません。数字 3 桁 ＋ 「-」 ＋ 数字 4 桁（123-4567）で入力し直してください。」——**同じ断りに欄の名前を 2 回出さない**（[21 §2](../21_画面の原則.md)） |
| M-36 | **マスタの検索欄が前後の空白を落とす**（[21 §0](../21_画面の原則.md)。[qa/01 E-09](01_CLB静かな失敗.md)） | 勘定科目・補助科目・部門・税区分・取引先の検索欄で、①**末尾に空白**を付けた語 ②**先頭に空白** ③**全角空白** ④**空白だけ** を入れて検索する | ①②③とも **空白なしの同じ語で検索したときと、同じ件数になる**（**件数で判定する**——末尾の空白は画面に写らないので、欄の字はスクリーンショットでは見分けられない。[G-07](01_CLB静かな失敗.md)）。④は**欄が空になり、その欄では絞らない**（＝全件が出る。[21 §0](../21_画面の原則.md) が受け入れると決めた） |

### 3-2. 振替伝票（入力と計上）

**J-06〜J-08 は[記録](04_実機操作テスト/2026-08.md)にしか無かった**（写しの焼き込み・改名・取消の引き継ぎ）。
2026-08-28 に台本へ戻した。番号は記録と同じものを使っている。

| # | ねらい | 手順 | 期待 |
|---|---|---|---|
| J-01 | 新規作成の初期値 | 新規作成 | 取引日・計上日＝今日、状態＝下書き、種別＝通常、会計年度＝計上日の年度 |
| J-02 | 明細の自動入力 | 明細を 2 行足す | 行番号が自動、貸借は**どの行も「借方」が既定**（振替伝票の作法。貸方にする行は利用者が変える）、科目を選ぶと既定税区分が入る |
| J-03 | **計上できる**（**新規作成から下書き保存を挟まずに**。[L-10](03_テストで漏らした実例.md) は「新規のまま計上」と「保存してから計上」が別経路だと書く） | 貸借一致で計上 | 伝票番号が付き、画面が閲覧専用になる。DB に `posted_at` / `posted_by` が入る |
| J-04 | **差し戻される** | 貸借不一致 ＋ もう 1 つ違反で計上 | 計上されず、**伝票も明細も採番も残らない**。画面は下書きのまま |
| J-05 | 下書きを破棄できる | 詳細画面の「削除」ボタンから削除（2026-08-31 に一覧から移した） | 明細ごと消える。**計上済みには削除アイコンが出ない** |
| J-06 | **計上すると相手方の写しが焼かれる** | 取引先を持つ伝票を計上し、`sql` で明細を見る | `partner_name_snapshot` と `registration_no_snapshot` が両行に入る（[ADR-0018](../decisions/0018-帳簿の記載事項は計上時に写して固定する.md)） |
| J-07 | **計上後に改名しても帳簿の記載は変わらない** | 計上してから取引先を改名し、帳簿を見る | 帳簿は計上時の名前のまま |
| J-08 | **取消は原仕訳の写しを引き継ぐ** | 計上 → 改名 → 取消 の順に触る | 原仕訳と取消が同じ名前。現在のマスタ名に引きずられない |
| J-09 | **空の明細のまま計上できない** | 明細を 1 行足し、何も入れずに「計上する」 | **CLB の入力検証が「必須項目です」で止める**（画面が経路の 1 本目）。**DB の言葉（`SQLite` / `constraint` / 列名）が画面に出ない**。DB に何も残らない。**サーバ側の関門（`JournalSubmitRequirements`）は画面を通らない経路のためのもの**で、この台本では踏めない |
| J-10 | **空の明細のまま下書き保存もできない** | 同じ状態で「下書き保存」 | CLB が止める——**欄には「必須項目です」、トーストには「不正な入力があります」**（2026-09-02 実測。計上のボタンはトーストを出さないので、**2 つのボタンで見え方が違う**）。**J-09 と J-10 は別経路である**——検査を通る保存ボタンと通らない保存ボタンを取り違えた事故が実際に起きた（[qa/03 L-16](03_テストで漏らした実例.md)） |
| J-11 | **一度選んだ参照欄を空に戻して保存できる** | 明細の補助科目を選んで下書き保存し、開き直して補助科目を × で消し、もう一度下書き保存する | 保存できる。`sql` で `sub_account_id` が **NULL**（空文字ではない）。**`SQLite Error 19` が画面に出ない**（[qa/01 F-33](01_CLB静かな失敗.md)・[qa/03 L-23](03_テストで漏らした実例.md)）。**空にできる欄を足したら、その欄でも踏む**——`ListField` の行かどうかで挙動が違うので、**明細の欄はとくに踏む** |
| J-12 | **計上済みの伝票に計上者が出る** | 計上済みの伝票を開く | 計上した利用者の**表示名**が出る（数値の識別子ではない）。入力年月日・計上日時も出て、書式は `yyyy/MM/dd HH:mm`（[21 §2-5](../21_画面の原則.md)）。**この列より前に計上された伝票では計上者が空欄になる**——それが正常である |
| J-13 | **計上済みの伝票の取引先が計上時の写し** | 取引先を持つ伝票を計上 → その取引先を改名 → 伝票を開く | 取引先の欄が**計上したときの名前**のまま動かない。**振替伝票の一覧の取引先の列も同じ名前**（[ADR-0037](../decisions/0037-計上済みの伝票は画面でも計上時の姿を見せる.md) §1・§3） |
| J-14 | **下書きの取引先は今までどおり選べる** | 下書きを開く | 取引先を選ぶ欄（候補ダイアログつき）が出る。**一覧の取引先の列は現在のマスタ名** |
| J-15 | **写しの欄と参照の欄が二重に出ない** | 下書きと計上済みを続けて開く | どちらの状態でも取引先の欄は **1 つだけ**。隠したほうの桝が空いて崩れていない（qa/01 D-08） |
| J-16 | **`creator` / `updater` に利用者の識別子が入る** | ①下書きを保存 ②開き直して直してもう一度保存 ③`sql` CLI で `select creator, updater, typeof(creator) from journal_entries` | **どちらも空でない**（②で `updater` が入る）。`app_users.id` と突き合わせて名前が引ける。**空なら `LinkFieldDesign` の判断が誤り**（qa/01 F-09 の訂正。**型を間違えると自動セットが効かない**——文字列でも比較は当たるので、見るのは値の有無である） |
| J-17 | **取消は伝票の写しも引き継ぐ** | 計上 → 改名 → 取消 の順に触る | 原仕訳と取消の**伝票の取引先が同じ名前**（明細の J-08 と同じ規則を伝票にも当てる） |
| J-18 | **訂正の再計上は焼き直す** | 計上 → 改名 → 訂正 → 再計上を計上 | **原仕訳と再計上で取引先の名前が違う**（利用者は取引先を直せるので、これが正しい）。取消は原仕訳と同じ名前のまま。**再計上の「作成者」は空欄**——サーバが作った下書きなので CLB の自動セットを通らない（2026-09-03 実測） |
| J-19 | **取引先の無い伝票** | 取引先を選ばずに計上して開く | 取引先の欄が**空欄**で出る。一覧の取引先の列も空 |
| J-20 | **写しが空の古い計上済み伝票** | この列より前に計上した伝票を開き、その取引先を改名する | **詳細も一覧も現在のマスタ名**に落ちる（写しが無いので。[ADR-0037](../decisions/0037-計上済みの伝票は画面でも計上時の姿を見せる.md) の帰結）。**空欄にならない** |
| J-21 | **計上済みでは取引先マスタへ飛べない** | 計上済みの伝票の取引先の欄を触る | 候補ダイアログもリンクも出ない（`TextField` になっている。ADR-0037 の帰結） |
| J-22 | **新規作成の画面の見え方** | 新規作成を開く | 入力年月日・作成者・計上した日時・計上者の 4 つが**空欄で、書けない欄として見える**（[21 §1](../21_画面の原則.md)）。ラベルが折り返したり切れたりしていない（「入力年月日」は 5 文字で、ラベルの列は 120px） |
| J-23 | **新しいトリガ 5 本が効く** | `sql` CLI で ①明細の付け替え ②`INSERT OR REPLACE` の伝票・明細 ③`UPDATE OR REPLACE` を流す | すべて拒まれ、**3 種類の日本語**が返る（**伝票の `REPLACE` 2 本と明細の `REPLACE` 2 本は、それぞれ同じ文である**（`Designer/ddl/005_journals.sql` の 4 トリガ。**実物で数えた**）。2026-09-20 の実測で「それぞれ違う日本語」から直した。M-11 と同じやり方。画面からは踏めない経路） |
| J-24 | **会計年度の当てはめが両端とも含む** | 新規作成で計上日を ①期首ちょうど ②期末ちょうど ③期首の 1 日前 ④期末の翌日 に動かす | ①②は年度が出て、③④は**空**（2026-09-04 実測。オフバイワンが無い） |
| J-25 | **損益科目の行に部門が要る**（I-13） | 部門を空のまま損益科目の行を作って計上 | **「計上できません。行 1: 勘定科目「通信費」には「部門」が必要です。「部門」を選んでください。」**（2026-09-20 の実測で直した。**正典は `JournalEntryValidator` と `JournalEntryValidatorTests`**）（画面の「行」列の値と科目名の両方が入る。「1 行目」ではない——2026-09-11 に変えた。[21 §3](../21_画面の原則.md)） |
| J-26 | **明細が 0 行だと計上できない** | 明細を全部消して計上 | 「計上できません。明細が 1 行もありません。」。**合計の行は 0 / 0 で釣り合って見える**ので、そこは当てにしない |
| J-27 | **「計上する」を素早く 2 回押しても伝票は 1 件** | 二重クリック | 伝票も伝票番号も 1 つしか増えない（DB で数える） |
| J-28 | **摘要の必須の印と、いつ要るかが最初から出る**（[10 §4-2-1](../10_会計ドメイン設計.md)） | 新規作成を開く | 摘要のラベルに `*`、欄のプレースホルダは「取引の内容（200 文字以内）」、凡例が **J-46 の「1 枚の伝票には 1 つの取引を記します。」から始まり**（2026-09-20 の実測で直した。「「*」は必須項目です」はその次）、**計上のときだけ必要になる欄がある**ことを続けて書いている（**文言は写さない**——変わるたびに台本が古くなる）。**タイミングをプレースホルダに書かない**——欄の意味と離れた文はどこに出ても迷わせる（[qa/01 D-31](01_CLB静かな失敗.md)。検索欄には出ないと 2026-09-11 に実測） |
| J-29 | **摘要が空だと計上できない** | 貸借の揃った明細を入れ、摘要を空のまま「計上する」 | 「計上できません。「摘要」が入っていません。何の取引かを書いてください。」**「内容」の語を使わない**（同じ画面に明細の「内容」欄がある）。**「計上できません」は見出しだけで、各文は繰り返さない**（[21 §2-6](../21_画面の原則.md)） |
| J-30 | **空白だけの摘要も計上できない** | 摘要に**全角空白と半角空白だけ**を入れて「計上する」 | J-29 と同じ断り。**画面は欄を離れた時点で前後の空白を落とす**（`ShouldTrimAfterEdit`）ので欄は空に戻る。**画面を通らない経路のためにトリガも同じ字を落とす**——そちらは `JournalDescriptionGuardTests` が見る |
| J-31 | **下書き保存は摘要が空でも通る**（開発者の決定。2026-09-05） | 同じ状態で「下書き保存」 | 保存できる。**打ちかけの伝票を止めない** |
| J-32 | **摘要の空値検索は、新しい伝票では当たらないのが正しい**（B-09 の摘要の分はこちらで見る） | 仕訳帳で「摘要」を空で検索 | 規則より前に計上された行だけが出る（**2026-09-04 に数えたときは伝票番号 1・12 の 2 件。2026-09-08 には数え直していない**）。**新しい DB では 0 件が正しい**ので、**効いていることは取引先か部門で見る**（0 件と「壊れて 0 件」を取り違えない） |
| J-33 | **値を入れて消すと NULL になる**（[qa/01 A-12](01_CLB静かな失敗.md)） | ①摘要を入れて下書き保存 ②開き直して摘要を消して下書き保存 ③`sql` CLI で `typeof(description)` | ②のあとは **`null`**。**空白だけを打った場合も `null` になる**（2026-09-20 の実測）。**ただし CLB の 2 つの設定が合成されるのではない**——**サーバ側の `BlankTextNormalizer` が空白だけの値を NULL に寄せている**（2026-09-09 に入った。[20 §7](../20_実装の原則.md)）。**CLB の側は [qa/01 A-12](01_CLB静かな失敗.md) のまま**である。**一度も触っていない欄がどちらになるかは未計測** |
| J-34 | **補助科目を使わない科目の明細に補助科目を付けたままでは計上できない**（[15 §1](../15_記帳の枠組み.md)。[L-27](03_テストで漏らした実例.md) の再演） | 「補助科目を使う」がオフの科目（現金）の明細に、その科目の補助科目を選んで「計上する」 | 断られる。**文言は `JournalEntryValidatorTests` が全文で固定している**（台本は写しを持たない。[qa/02 R45-21](02_自己レビュー記録/2026-09.md)）。**伝票も明細も残らない**（DB で数える）。**【再演不可】候補は「有効な」補助科目だけが出る**——`JournalLine.mod.json` の絞りは `IsActive = True AND Account = <その行の科目>` なので、**無効な補助科目は並ばない**。**開発機では現金に紐づく唯一の補助科目が 2026-09-04 に無効化されたため、2026-09-20 は踏めなかった**（有効へ戻す経路も `MasterSubmitGate` が塞ぐ——A-05 と同じ形）。**マスタ側で補助科目を作らせない関門は 2026-09-09 に入った（`MasterSubmitGate`）|
| J-35 | **補助科目を空にすれば計上できる** | J-34 の下書きで補助科目を × で消して「計上する」 | 計上でき、伝票番号が付く。DB の `sub_account_id` が NULL |
| J-37 | **選べる補助科目が無い科目では、案内が変わる**（[21 §2-3](../21_画面の原則.md) の「次の一手」） | 補助科目が 1 件も無い科目（定期預金）の明細を作って「計上する」 | 「…**選べる補助科目がありません。補助科目マスタに登録してから、「補助科目」を選んでください。**」（2026-09-20 の実測で直した）**「選んでください」では踏めない**（候補が 0 件で開くだけ）ので分けてある |
| J-38 | **取引先を要する科目の明細は、取引先が無いと計上できない**（[15 §1-2](../15_記帳の枠組み.md)） | 「取引先を要する」がオンの科目（買掛金）の明細を作り、**伝票も明細も**取引先を選ばずに「計上する」 | 断られる。**文言は `JournalEntryValidatorTests` が全文で固定している**（台本は写しを持たない）。**案内するのは「伝票か明細の「取引先」」**——2026-09-16 に明細の一覧へ列を足したので、どちらを選んでも実効値が埋まる（[ADR-0062](../decisions/0062-明細の取引先は行ごとに選べるようにし伝票の取引先は既定値にする.md)）。**伝票も明細も残らない** |
| J-39 | **伝票の取引先で足りる**（**実効値で見る**。明細が空なら伝票のものが帳簿に載る） | J-38 の下書きで、**伝票の**「取引先」を選んで「計上する」 | 計上でき、伝票番号が付く。**明細の `partner_id` は NULL のまま**（DB で見る） |
| J-40 | **選べる取引先が無いと、案内が変わる**（[21 §2-3](../21_画面の原則.md) の「次の一手」） | 取引先をすべて無効にしてから、買掛金の明細で「計上する」 | 「…**選べる取引先がありません。取引先マスタに登録するか、無効にした取引先を有効に戻してから、伝票の「取引先」か、その行の「取引先」を選んでください。**」（**その勘定科目を使っている行が 1 つのときの字**。2 行以上で使っていると「行 1・行 2 の」になる——`JournalEntryValidator` の `RowAt`。**2026-09-20 は `navigate` が分類器に拒まれて未達**なので、**字は実装から写した。実機では未確認**）——**0 件と全部無効を同じ文で断らない**（取引先は運用で無効にされるマスタなので、「登録してから」だけでは踏めないことがある）。**踏み終わったら、無効にした取引先を元に戻す**——**元の状態を先に SQL で控えてから触る**（§2-2 の落とし穴） |
| J-41 | **「元の伝票」は触れない**（[qa/03 L-30](03_テストで漏らした実例.md)） | 新規・下書き・訂正の下書き・取消・計上済みを開く | 通常の伝票（新規・下書き・計上済み）では**欄そのものが出ない**。取消・訂正では元の伝票番号が**閲覧専用**で見えるだけ（枠も候補ボタンも出ない） |
| J-42 | **訂正の下書きは、元の伝票を持ったまま保存できる** | 計上済みで「訂正する」→ 開いた下書きの摘要を直して「下書き保存」 | 「更新しました」。`original_entry_id` が残っている（**画面が触っていない欄を差分に載せない**ことの確認。載せると関門が断る） |
| J-43 | **無効にした取引先は新たな計上に使えない**（[15 §1-3](../15_記帳の枠組み.md)） | 下書きの取引先を `sql` CLI で無効な取引先に付け替え、開き直して「計上する」 | 断られ、下書きのまま。**文言は `JournalEntryValidatorTests` が全文で固定している**（台本は写しを持たない） |
| J-44 | **訂正・取消の後に、何が起きて何を開いているかを言う** | 計上済みで「訂正する」／「取り消す」 | 訂正は「取消を計上しました。いま開いているのは訂正の下書きです。内容を直して計上してください。」、取消は「取り消しました。いま開いているのは取消伝票です。」の緑のトースト。**文言の正典は `JournalEntry.mod.cs`** |
| J-45 | **訂正の下書きの削除は、元の伝票の取消が残ることと次の一手を言う**（[21 §1](../21_画面の原則.md)） | ①訂正の下書きで「削除」→ 文を読んで「いいえ」 ②通常の下書きで「削除」→ 文を読んで「いいえ」 | ①「この訂正の下書きを削除します。元に戻せません。元の伝票は取り消されたままになり、この取引は帳簿に載りません。記し直すときは、元の伝票を開いてもう一度「訂正する」を押してください。よろしいですか？」 ②「この下書きを削除します。元に戻せません。よろしいですか？」 |
| J-46 | **凡例が 1 枚に 1 取引と、明細の条件つき必須を言う** | 伝票を開く | 凡例が「1 枚の伝票には 1 つの取引を記します。」で始まり、**「明細の「取引先」が空のときは、伝票の「取引先」が使われます。」**と**「行ごとに相手方が違うときは、その行の「取引先」を選んでください。」**を含み、**末尾が「すべての行で選んだときは、伝票の「取引先」は空のままでも計上できます。」**。伝票の「取引先」に `*`（**条件つき必須の印は過大側に倒し、凡例の末尾の 1 文で打ち消す**。[21 §1](../21_画面の原則.md)） |
| J-47 | **離脱の確認**（伝票） | ①下書きの摘要を直してサイドメニューの「振替伝票」を押す ②「入力を続ける」→「下書き保存」→ もう一度「振替伝票」 ③新規作成を開いて何も触らずに「振替伝票」 | ①「画面を離れる確認」 ②保存が通ると確認は出ずに一覧へ ③確認は出ずに一覧へ |
| J-48 | **スクリプトが起こす遷移では、離脱の確認を重ねない**（複製・削除） | ①下書きの摘要を直してから「複製する」→「はい」 ②下書きの摘要を直してから「削除」→「はい」 | ①複製の確認 1 枚だけで、複製した下書きが開く（「画面を離れる確認」は出ない） ②削除の確認 1 枚だけで一覧へ（消えた伝票の画面に留まらない） |
| J-49 | **計上済みの伝票を開いて離れても確認は出ない**（読むだけの画面。取引先の写しを出す経路） | 取引先のある計上済みの伝票を開き、サイドメニューを押す | 確認なしで移る |
| J-50 | **ブラウザの再読込・タブを閉じるは止めない**（未対応の記録） | 下書きの摘要を直して F5 | 確認は出ず、変更は失われる（`OnLocationChanging` はアプリ内の遷移だけ。[21 §1](../21_画面の原則.md)） |
| J-51 | **明細の取引先を行ごとに選べる**（[ADR-0062](../decisions/0062-明細の取引先は行ごとに選べるようにし伝票の取引先は既定値にする.md)） | 下書きの明細で、1 行目の「取引先」を選び、2 行目は空のまま「下書き保存」→ 開き直す | **部門の次に「取引先」の列があり**、候補から選べる。保存して開き直しても 1 行目だけに残る（`sql` CLI で `journal_lines.partner_id` を見る。2 行目は NULL） |
| J-52 | **計上済みの明細は、計上時の写しの名前を見せる**（[ADR-0037](../decisions/0037-計上済みの伝票は画面でも計上時の姿を見せる.md) を明細に当てる。[ADR-0062](../decisions/0062-明細の取引先は行ごとに選べるようにし伝票の取引先は既定値にする.md)） | J-51 の下書きを計上 → その取引先をマスタで改名 → 伝票を開き直す | 明細の「取引先」に**改名前の名前**が出る（伝票の取引先と同じ作法。J-07 の明細版）。**下書きの行は写しを持たない**ので、下書きでは現在のマスタ名が出る（訂正の下書きも持たない——`JournalCorrection.Draft` が落とす）。**伝票にだけ取引先を選んだ伝票では、行で選んでいない列にも伝票の取引先の名前が出る**——写しに焼くのは実効値だからである |
| J-53 | **振替伝票の一覧に「入力年月日」が出る**（旧 Q-22 の決定。[ADR-0048](../decisions/0048-伝票の複製は取引の内容だけを写す.md) の帰結） | 一覧を開く。計上済みを「複製する」で 2 本作り、一覧に戻る | **末尾の列が「入力年月日」**で、**時分まで**出る（`yyyy/MM/dd HH:mm`。[21 §2-5](../21_画面の原則.md)）。同じ日に複製した 2 本が、この列で見分けられる |
| J-54 | **訂正の再計上の下書きは、計上時の写しを持ち込まない**（[ADR-0062](../decisions/0062-明細の取引先は行ごとに選べるようにし伝票の取引先は既定値にする.md) の帰結 9） | **先にその取引先をマスタで改名してから**（改名しないと、写しといまのマスタ名が同じ字になって画面では判別できない。J-07 と同じ作法）明細に取引先のある計上済みを「訂正する」→ 開いた再計上の下書きで、明細の「取引先」を別の相手に選び直して「下書き保存」→ 開き直す | **下書きを開いた直後は、改名後のいまのマスタ名**（写しなら改名前が出る）。選び直したあとは**選び直した相手の名前**。`sql` CLI で `journal_lines.partner_name_snapshot` が NULL（**持ち込むと、`partner_id` は新しい相手・画面の字は古い相手という食い違いになる**） |
| J-55 | **振替伝票の一覧の「取引先」は実効値**（[ADR-0062](../decisions/0062-明細の取引先は行ごとに選べるようにし伝票の取引先は既定値にする.md)） | ①**伝票の取引先が空で、明細だけに取引先のある伝票**を一覧で見る ②一覧の「取引先」でその相手を選んで検索 ③**行ごとに相手方の違う伝票**を一覧で見る | ①その相手の名前が出る（空欄にならない） ②その伝票が出る（**0 件にならない**） ③**「（複数）」**と出る（**括弧つき**。取引先の名前と見分けるため）。**取引先の無い行は数えない**ので、「1 相手 ＋ 取引先の無い行」は 1 相手の名前 |
| J-56 | **振替伝票の検索欄が前後の空白を落とす**（[21 §0](../21_画面の原則.md)。[qa/01 E-09](01_CLB静かな失敗.md)） | 振替伝票の一覧の「摘要」と、伝票そのものの検索の「摘要」で、①末尾に空白を付けて**欄から出てから**検索 ②**欄から出ずに検索ボタンを押す** ③**欄の中で Enter を打つ** | ①②③ とも **空白なしの同じ語で検索したときと、同じ件数になる**（**件数で判定する**——末尾の空白は画面に写らないので、欄の字はスクリーンショットでは見分けられない。[G-07](01_CLB静かな失敗.md)）。**②③ が要る**——落とすのは編集の確定の手なので、**確定と検索が 1 つの操作になる経路**では順番が入れ替わりうる。**この 2 つは別のモジュールの欄である**（一覧は `JournalEntryList`、検索は `JournalEntry`） |

### 3-3. 取消・訂正（Web API の経路）

| # | ねらい | 手順 | 期待 |
|---|---|---|---|
| A-01 | 取り消せる | 計上済みを開いて「取り消す」 | 反対仕訳が**計上済み**で作られ、その伝票が開く。貸借が入れ替わっている |
| A-02 | 二重取消は止まる | **同じ伝票を 2 つの画面で開き**、片方で取り消してからもう片方で取り消す | 差し戻される。**伝票も伝票番号も増えない**（ボタンは開いた時点の状態で描かれるので、古い画面には残っている。これが実際に起こる形） |
| A-03 | 訂正できる | 計上済みを開いて「訂正する」 | 取消が**計上済み**、再計上が**下書き**で作られ、**下書きのほう**が開く |
| A-04 | ボタンの出し分け | 取消済みの伝票を開く | 「取り消す」「訂正する」が出ない |
| A-05 | **訂正の再計上に補助科目を付けると止まる**（[ADR-0038 §3](../decisions/0038-使用中のマスタは意味を変えられない.md)。取消だけを外す） | 補助科目の無い伝票を「訂正する」→ 再計上の下書きで現金の行に補助科目を選んで「計上する」 | J-34 と同じ断り。**再計上の中身は利用者が決めるので、補助科目を空にすれば計上できる**（行き止まりにならない） |
| A-06 | **【再演不可】規則より前に計上された補助科目つきの伝票を取り消せる**（**開発機の検体は 2026-09-08 に消費済み**。新しい検体は J-34 の関門が作らせない——2026-09-20 に未達）（[ADR-0038 §3](../decisions/0038-使用中のマスタは意味を変えられない.md) の免除。**これが免除の根拠**） | 補助科目を使わない科目に補助科目が付いた計上済みの伝票（開発機の伝票 36）を開いて「取り消す」 | 計上でき、**取消にも補助科目が写る**（原仕訳の写しだから）。断りは出ない |
| A-07 | **【再演不可】規則より前に計上された、取引先の無い伝票を取り消せる**（**開発機の検体 6 件は全部取消済み**。`requires_partner` が一方通行なので新しい検体を作れない——2026-09-20 に未達）（[15 §1-2](../15_記帳の枠組み.md) の免除。**これが免除の根拠**） | 取引先を要する科目の明細に取引先が無い計上済みの伝票（開発機で「取引先が無い」を数えて選ぶ（SQL は[記録の 2026-09-08](04_実機操作テスト/2026-09.md)））を開いて「取り消す」 | 計上でき、**取消にも取引先が無いまま写る**（原仕訳の写しだから。空欄を埋めない）。断りは出ない |
| A-08 | **複製できる**（[ADR-0048](../decisions/0048-伝票の複製は取引の内容だけを写す.md)） | 計上済みを開いて「複製する」 | 同じ内容の**下書き**が開く。**種別は通常・元の伝票は空・伝票番号は空**、取引日は原仕訳のまま、**計上日は今日** |
| A-09 | **取消済み・訂正済みの原仕訳でも複製できる**（決定 6） | 取り消された（または訂正された）**原仕訳**（種別は通常。開発機の **id 42**（伝票番号 33））を開いて「複製する」 | **「複製する」だけが出ていて**、取消・訂正は出ない。押すと通り、同じ内容の下書きが開く。**取消伝票そのものは元にできない**（A-12） |
| A-10 | **複製した下書きはそのまま計上できる** | A-08 の下書きで「計上する」 | 計上され、**新しい伝票番号**が付く。今日の規則に反していれば断られる（それが正しい）。**取引日が今日より後の伝票の複製は、取引日を直すまで計上できない**（ADR-0048 の帰結） |
| A-11 | **編集中に複製すると確認が出る**（決定 7） | 下書きで摘要を直し、保存せずに「複製する」 | 「保存していない変更があります。複製に写るのは保存済みの内容で、変更は失われます。よろしいですか？」。**直していないときは確認が出ない** |
| A-12 | **元にできない種別は、ボタンが出ない**（決定 6） | ①**取消伝票**（開発機の **id 62**（伝票番号 48））を開く ②期首残高・決算振替・繰越の伝票を開く（**いまは作る経路が無いので `sql` CLI で作る**。**`entered_at` は画面と同じ書式（秒まで）で入れる**——秒を落とすと画面が「日時として読めない値が入っている」と断り、削除もできなくなる。2026-09-20 に踏んだ） | どちらも「複製する」が出ない（可否をサーバに聞いている）。①では合計の行に「（取消・訂正・複製はできません: 種別が「取消」の伝票は対象にできません。対象にできるのは通常の伝票と訂正だけです。）」——**ボタンが 1 つも無い画面では、理由が 3 操作を名乗る**。API を直接叩けば「複製できません。①種別が「決算振替」の伝票は対象にできません。対象にできるのは通常の伝票と訂正だけです。」（**実機では未観測**。テスト `決算振替は複製できない` が全文を表明している）——**取消・訂正が同じ種別を断る文と同じ** |
| A-13 | **訂正の下書きを消したあと、もう一度「訂正する」と再計上の下書きだけができる**（[ADR-0052](../decisions/0052-訂正のやり直しは「訂正する」が兼ね取消済みで訂正の無い原仕訳には再計上の下書きだけを起こす.md)） | ①計上済みを「訂正する」→ 開いた下書きを「削除」→「はい」 ②元の伝票を開く ③「訂正する」 ④開いた下書きで「削除」せずに一覧へ戻り、元の伝票を開く | ②「取り消す」は出ず「訂正する」が出る。③確認が「この伝票を訂正します。取消は既に計上されているので作らず、内容を写した訂正の下書きだけを開きます。よろしいですか？」→「はい」→「いま開いているのは訂正の下書きです。取消は増えていません。内容を直して計上してください。」で下書きが開く。取消は増えない（`sql` CLI で数える） ④「訂正する」は出ず、上の断りが「この伝票は取り消されています（取消伝票 N）。訂正の下書きがあるので、振替伝票の一覧で「状態」を「下書き」、「種別」を「訂正」に絞って開き、直してください。」 |
| A-14 | **「取り消す」の確認が、取消は記帳の誤りを正す操作だと言う**（**この字は 2026-09-20 に書き換えが決まっている**——新しい字は [10 §5](../10_会計ドメイン設計.md)。**実装の回まではこの期待値で流す**）（[10 §5](../10_会計ドメイン設計.md)。[21 §1](../21_画面の原則.md) の「押す前に見せる」） | 計上済みを開いて「取り消す」→ 文を読んで「いいえ」 | 「この伝票を取り消します。取消は、記帳の誤りを正すための操作です。正しい内容で記し直すなら、「訂正する」を押してください。返品・契約解除・値引があったときは、元の伝票を取り消さずに、新しい伝票で記してください。できた取消伝票は帳簿に残り、あとから消せません。よろしいですか？」。**押さなければ何も起きない** |

### 3-4. 帳簿

> **ここの B-nn は帳簿の台本である。** §4 の
> [2026-08-31（振替伝票の一覧を作り直した）](04_実機操作テスト/2026-08.md#2026-08-31フェーズ-25-の-c振替伝票の一覧を作り直した)の記録も
> B-01〜B-10 を使っているが、**あちらは入力の一覧の項目**で、別のものを指す。
> **番号は記録と同じものを使う**という規則（§3-5 の R-nn と同じ理由）を優先し、
> **どちらも振り直さない**——代わりに、他の文書から指すときは**どちらの B-nn かを書く**
> （2026-09-02 の判断。[qa/02 ラウンド 34](02_自己レビュー記録/2026-09.md)）。

| # | ねらい | 手順 | 期待 |
|---|---|---|---|
| B-01 | 仕訳の一覧・検索 | 会計年度・状態で絞る | 絞られる。下書きも出る |
| B-02 | 仕訳帳（法定検索） | 取引年月日の範囲で引く | **計上済みだけ**が明細 1 行＝1 レコードで出る |
| B-03 | 取引金額での検索 | 金額の範囲で引く | 範囲で絞れる |
| B-04 | **仕訳帳に取引先名の写しが出る** | 取引先を付けた伝票を計上して仕訳帳を見る | 計上時に焼いた `partner_name_snapshot` が出る（[ADR-0018](../decisions/0018-帳簿の記載事項は計上時に写して固定する.md)） |
| B-05 | **仕訳帳から伝票へ辿れる** | 仕訳帳の伝票番号をクリック | その伝票が開く（電帳規則 5 ⑤一ロ） |
| B-06 | **伝票番号で引ける** | 仕訳帳で伝票番号の範囲を指定 | 範囲で絞れる（電帳通達 8-14 (注)） |
| B-07 | **総勘定元帳が出る** | 総勘定元帳を開く | 科目コード順にまとまり、相手科目と期間内累計が出る。金額は右詰め |
| B-08 | **1 科目に絞れる（補助元帳の形）** | 勘定科目で絞る | その科目だけになり、**累計が絞った範囲で積み直される**。伝票番号から伝票へ辿れる |
| B-09 | **記録事項が無いものを探せる**（電帳通達 8-13） | 仕訳帳の「空で探す」で、取引先・部門・補助科目・摘要・内容をそれぞれ選ぶ | その項目が空の明細だけが出る。**空文字で保存された行も拾う**（[20 §7](../20_実装の原則.md)）。2026-08-26 の記録にしか無かったので 2026-09-03 に台本へ戻した |
| B-10 | **複合仕訳の相手科目が「諸口」になる**（法税規則 55 ②） | 3 行以上の仕訳を計上し、総勘定元帳を伝票番号で絞る | **反対側が 2 科目以上の行だけが「諸口」**、反対側が 1 科目の行はその科目名（2026-09-04 実測。伝票 39） |
| B-11 | **補助元帳の累計が合う** | 補助科目で絞り、`sql` CLI で出した値と全行突き合わせる | 1 円のずれも無い（2026-09-04 実測。普通預金／みずほ銀行 本店の 6 行） |
| B-12 | **帳簿は列見出しで並べ替わらない** | 見出しをクリックする | 並びが変わらない（[21 §3](../21_画面の原則.md)） |
| B-13 | **評価勘定の累計の符号が逆にならない** | 貸倒引当金（資産の評価勘定）へ貸方で計上し、総勘定元帳を見る | **累計が正の数**で出る（科目区分だけで決めると -500 になる。[15 §1](../15_記帳の枠組み.md)） |
| B-14 | **帳簿を取引先で絞れる**（別表二十四（四）（五）の「相手方別に」を満たす手立て。[40 §4-1](../40_優良な電子帳簿の対応表.md)・[40 §3-4](../40_優良な電子帳簿の対応表.md) の D1。[qa/03 L-49](03_テストで漏らした実例.md)） | ①仕訳帳の「取引先」で 1 社を選んで検索 ②総勘定元帳で科目＋取引先で絞る ③**明細だけに取引先のある伝票**（伝票の取引先は空）でも当たるか | **どちらも当たる**（**0 件にならない**）。**明細だけに取引先のある行も出る**（実効値で探す）。**検体が数で束縛していた間、SQL は実機で 1 件も返していなかった**（見つけたのは自己レビューと `sql` CLI の実測で、**ブラウザで踏むのはこの台本が初めて**である） |
| B-15 | **帳簿の検索欄が前後の空白を落とす**（[21 §0](../21_画面の原則.md)。[qa/01 E-09](01_CLB静かな失敗.md)） | 仕訳帳・総勘定元帳の「摘要・内容」で、①末尾に空白を付けた語 ②**空白を含む語**（摘要に空白のある伝票を 1 件用意する）の末尾に空白を付けたもの | ①② とも **空白なしの同じ語で検索したときと、同じ件数になる**（**件数で判定する**——末尾の空白は画面に写らないので、欄の字はスクリーンショットでは見分けられない。[G-07](01_CLB静かな失敗.md)）。②は**語の途中の空白が消えていない**ことを、その語でしか当たらない件数で見る |

### 3-5. 登録番号（インボイス）

**R-01〜R-04 は[記録の 2026-08-26](04_実機操作テスト/2026-08.md)にしか無かった。** 2026-08-28 に台本へ戻した（番号は記録と同じ）。

| # | ねらい | 手順 | 期待 |
|---|---|---|---|
| R-01 | 一覧が開く | 登録の一覧（サイドバーの「登録番号（インボイス）」）を開く | 取引先をまたいだ一覧が出て、検索条件と取引先へのリンクがある。**新規作成の導線はここに無い**——登録は取引先の詳細の「登録番号を追加する」から起こす（R-20・R-21） |
| R-02 | **壊れた登録番号を関門が止める** | `T` で始まらない番号などで登録する | 差し戻される。**DB に 1 行も残らない** |
| R-03 | **正しい登録番号は保存できる**（**保存された `valid_from` を `sql` CLI で覗き、CLB が付ける時刻まで見る**——[L-12](03_テストで漏らした実例.md) の再演） | `T` ＋ 数字 13 桁で登録する | 保存される。`source` は DB の既定で `manual` |
| R-04 | 差し戻しの文言に次の一手がある | R-02 の文言を読む | 「入力し直してください」まで書いてある（[21_画面の原則 §2-3](../21_画面の原則.md)） |
| R-05 | **取引先を後から付け替えられない** | 既存の登録の編集画面を開く | 取引先は**ラベル表示のみ**（入力欄も候補ボタンも出ない。2026-09-02 の作り直しから）。API を直に叩く経路は関門 `RejectRepoint` の単体テストが守る（[14 §4](../14_適格請求書発行事業者の登録.md)） |
| R-06 | **裸の「登録番号」が法人番号と紛れない** | 取引先の詳細を開き、法人番号の欄と登録の一覧を**同じ画面で**見る | どちらの番号かが読んで決まる。決まらなければ [14 §4](../14_適格請求書発行事業者の登録.md) の「文脈がある」の線を引き直す |
| R-20 | **詳細の一覧は閲覧専用で、追加はボタンから** | 取引先の詳細を開く | 履歴の一覧に入力欄が無い。「登録番号を追加する」ボタンと、行ごとの「編集」リンクがある |
| R-21 | **追加の画面に取引先が写っている** | 「登録番号を追加する」を押す | 編集画面が開き、その取引先のコードと名前が表示だけで出る。URL は `?partner={id}` |
| R-22 | **成功したら取引先の詳細へ戻る** | 正しい内容で「登録」 | 成功のトーストが出て、元の取引先の詳細に戻り、一覧に行が増えている |
| R-23 | **失敗したら何も起こらない** | 期間の重なる内容で「登録」 | エラーのトーストが出て、**画面に留まる**（DB に行が増えない） |
| R-24 | **期間の不変条件が言葉で断る** | 14 §5 の R-I1・R-I2・R-I4・R-I5 に反する入力 | 生の SQL エラーでなく、直し方の分かる文言（21 §2-3） |
| R-25 | **行の編集で「終わり」を記録できる** | 一覧の「編集」→ 取消・失効年月日と理由を入れて「登録」 | 戻った一覧の行に終わりの日付が出る。そのあと再登録の行を追加できる |
| R-26 | **URL 直叩きは言葉で断る** | `?partner` 無しで新規作成の URL を開く | 「取引先が指定されていません」が出て、「登録」ボタンが出ない |
| R-27 | **登録番号の検索欄が前後の空白を落とす**（[21 §0](../21_画面の原則.md)。[qa/01 E-09](01_CLB静かな失敗.md)） | 登録の一覧の「登録番号」に、末尾に空白を付けた番号を入れて検索する | **空白なしの同じ語で検索したときと、同じ件数になる**（**件数で判定する**——末尾の空白は画面に写らないので、欄の字はスクリーンショットでは見分けられない。[G-07](01_CLB静かな失敗.md)） |

**単体・DB・実機の分担**（テスト設計。2026-09-02）: 登録の不変条件（R-I1〜R-I9。[14 §5](../14_適格請求書発行事業者の登録.md)）は
**関門の単体テスト**（`PartnerRegistrationSubmitGateTests` の「期間の不変条件」
「取引先の実在と行の削除」ほかの節）と **DDL のテスト**（`PartnerSchemaTests`）が全分岐を持つ。
実機（R-20〜R-26）で見るのは**導線と文言**——画面がどの入力を関門に運び、断りがどう見えるか——だけである。
同じ判定を 3 層で全部なぞらない（実機は遅く、壊れやすいから）。
台本の番号は R-20 から振り直した——R-07〜R-13 は[記録の 2026-08-31](04_実機操作テスト/2026-08.md)が別の意味で使っており、
「番号は記録と同じ」の規則を守るため（2026-09-02）。

### 3-6. 権限（フェーズ 2.5 で作った。[ADR-0034](../decisions/0034-役割は部品ごとの直交な軸で持ち軸の中は階段にする.md)〜[ADR-0036](../decisions/0036-権限は到達と書き込みに書き分け守りは保存の関門に置く.md) ＋ [ADR-0033](../decisions/0033-読み取りはそのアプリの役割を持つ人だけに開く.md)）

**役割ごとにログインし直して通す。** **P-16 以外は 2026-09-02 に埋め終えた**
（P-16 は `Partner` を閉じるフェーズ 4 の項目）。
**P-01〜P-07 は[記録の 2026-08-27](04_実機操作テスト/2026-08.md)（部品分割の通し）が使っている**ので、ここは P-08 から振る。

| # | ねらい | 期待 |
|---|---|---|
| P-08 | 玄関に着地する | どの役割でログインしても、ルート URL は玄関のフレームに着く |
| P-09 | 権限の無いフレームへのリンクが**出ない** | 情報システム管理者に会計のリンクが見えない。経理担当に管理のリンクが見えない |
| P-10 | **URL を直打ちしても入れない** | 玄関モジュールとフレームの両方の条件が効く |
| P-11 | **要る候補は出る** | 経理担当で振替伝票を開き、**勘定科目・部門の候補が出る**（入力に要る候補は「上位専用」ではない。[ADR-0033](../decisions/0033-読み取りはそのアプリの役割を持つ人だけに開く.md)） |
| P-12 | **書き込みだけが閉じている** | 経理担当（`staff`）が勘定科目の更新を**API へ直に投げても拒まれ、DB は変わらない**。<br>**画面からは踏めない**——勘定科目は設定フレーム（`manager`）にあり、到達の段で止まる。「読めるが書けない」画面は `viewer` を効かせる**フェーズ 4** まで存在しない |
| P-13 | **「読めないが作れる」が無い** | 条件を閉じたモジュールで、新規作成の URL からも書けない（[qa/01 F-19](01_CLB静かな失敗.md)） |
| P-14 | **アプリ全体のアクセス条件が締め出す** | `can_access_app` を 0 にすると、どの画面も開かない（[qa/01 F-28](01_CLB静かな失敗.md)） |
| P-15 | **役割を持たないアプリのデータは読めない** | 取引先だけの利用者が、API から振替伝票を読めない（[ADR-0033](../decisions/0033-読み取りはそのアプリの役割を持つ人だけに開く.md)。**フェーズ 2.5 の E** で塞ぐ入力の 3 本） |
| P-16 | **読めない相手の候補欄が出ない** | `partner_role` を持たない経理担当の振替伝票に、取引先の欄が出ない（**フェーズ 4**。`Partner` を閉じるのと同じ回） |
| P-17 | **CLB の条件が届かない経路も閉じている** | 会計の役割を持たない利用者で `POST /api/journals/` の **4 本**（`reverse`・`correct`・`duplicate`・`availability`）を投げると、**`violations[0].code` が `E-NOT-AUTHORIZED`** で差し戻され、**伝票が 1 件も増えない**（2026-09-20 に 4 本とも踏んだ）。**鍵の字は小文字始まりの `originalEntryId`**（`JournalAmendmentEndpoint` の `JsonPropertyName`）。**`400` が返ったときは権限の判定に届いていない**ので、**「閉じている」と誤読しない**——**何が 400 を返すかは未確認**（2026-09-21 の自己レビューで、MVC の既定は大小を無視すると指摘があった。**測り直す**）（[qa/03 L-22](03_テストで漏らした実例.md)。取消は不変なので取り返しがつかない） |

### 3-7. 同時操作（2026-09-04 に足した。**`sql` CLI で相手を演じる**）

**2 つのブラウザは要らない。** 片方の利用者を `sql` CLI で演じれば、1 つの画面で通せる
（[qa/03 L-31](03_テストで漏らした実例.md)）。**対照（④）を必ず入れる**——
入れないと「たまたま失敗しただけ」と区別できない。

| # | ねらい | 手順と期待 |
|---|---|---|
| C-01 | **上書きの競合が止まる**（楽観ロック） | ①下書きを画面で開く ②CLI で `description` を変え `optimistic_locking` を +1 ③画面で摘要を直して保存 → **拒まれ、②の値が残る** ④**開き直して同じ操作をすると通る**（対照） |
| C-02 | **削除との競合が止まり、行が作り直されない** | ①下書きを画面で開く ②CLI でその行（と明細）を削除 ③画面で保存 → **拒まれ、伝票の総数も最大 id も動かない** |
| C-03 | **競合の文言が「別の人が変えた／消した」と言う**（[L-31](03_テストで漏らした実例.md)） | C-01・C-02 の断りが「別の人が変更しました」「別の人が削除しました」**を含み**（2026-09-20 の実測で「で始まり」から直した。**見出しは [21 §2-6](../21_画面の原則.md) の規則どおり「保存できません。」が先に来る**）、次の一手（開き直す／一覧へ戻る）まで言う。**文言は `JournalSubmitGateTests` が全文で固定している**（台本は写しを持たない）。**トーストは 2 枚**（「下書き保存」は標準の保存ボタンなので、CLB の「更新に失敗しました」が重なる。[qa/01 F-42](01_CLB静かな失敗.md)） |
| C-04 | **削除との競合が「削除」の言葉で止まる** | ①下書きを画面で開く ②CLI でその行（と明細）を削除 ③画面で「削除」→「はい」 | 「削除できません。…別の人が削除しました。振替伝票の一覧に戻ってください。」。**削除なのに「入力内容を確かめ…」と言わない**（素通しにすると CLB の削除が失敗してそう言う。[qa/01 F-41](01_CLB静かな失敗.md)） |
| C-05 | **消えた明細は件数で束ねて断る** | ①下書きを画面で開く ②CLI でその明細を消す ③画面で**その明細の**金額を直して「下書き保存」 | 断られる（件数入り。文言は `JournalSubmitGateTests` が固定）。**トーストは 2 枚**（標準の保存ボタン。[qa/01 F-42](01_CLB静かな失敗.md)） |
| C-06 | **保存の後で差し戻されても、その保存は残らない** | ①明細の無い下書きを開く ②摘要を変えて「計上する」 | 「計上できません…」の 1 枚（「計上する」はスクリプトの `Submit()` なので重ならない。[qa/01 F-42](01_CLB静かな失敗.md)）。DB の摘要と版は動いていない（`sql` CLI で見る） |

## 4. 記録

**流した日・結果・気づいたこと**を、**年月ごとのファイル**に残す（`04_実機操作テスト/YYYY-MM.md`。`growth: append`）。
本書には置かない（**開発者の決定。2026-09-18**。逐語は「追記するものとそうでないものは分けてください」。**単位を年月にしたのも同日**——Claude の案を開発者が採った。フェーズは境目が後から動くので採らなかった）。
**以下は Claude の敷衍**——`growth` はファイルに 1 つしか付かず、書き換える規則・台本と追記で育つ記録を 1 冊に同居させると、[00 §4-3](../00_ドキュメント規約/本文.md) の「追記が正しいのは append だけ」を当てられない。

| 年月 | ファイル |
|---|---|
| 2026-08 | [2026-08](04_実機操作テスト/2026-08.md) |
| 2026-09 | [2026-09](04_実機操作テスト/2026-09.md) |

- **1 回 1 節。** 見出しは `## YYYY-MM-DD（題）`。**新しい記録はその月のファイルの末尾に足す**（古い順）
- **流す前に「何を見るか」を節の冒頭に書く**（[31 §7](../31_検証のルール.md)）。探索的テストの要否と、§1 の優先を使ったかも 1 行ずつ
- **見た文字列はそのまま写す**——写すと読むことになる（[qa/03 L-39](03_テストで漏らした実例.md)）
- **月が変わったら同じ形でファイルを 1 本足し、上の表に 1 行足す**（フロントマターは前の月のファイルを写す）。**まだ踏んでいない**——2026-10 の最初の回で試す（qa/02 も同じ）
- **他の文書から記録を指すときは、その月のファイルへ張る**（`[qa/04](04_実機操作テスト/2026-09.md) の 2026-09-16`）。§1〜§3 を指すときは本書へ
- CLB の踏み抜きは [qa/01](01_CLB静かな失敗.md) へ、漏れの型は [qa/03](03_テストで漏らした実例.md) へ、レビューの採否は [qa/02](02_自己レビュー記録.md) へ移す。**記録の中の「分かった作法」は §2 へ移して初めて効く**——記録に置いたままでは次の回に読まれない
