---
title: 適格請求書発行事業者の登録
status: current
scope: 取引先
audience: [開発]
updated: 2026-09-24
supersedes: []
related: [13_取引先設計.md, 11_消費税設計.md, decisions/0063-登録番号の行は計上済みの明細が写していない行だけ消せる.md, decisions/0018-帳簿の記載事項は計上時に写して固定する.md, research/2026-08-25_取引先の識別番号と公表システム.md, research/2026-08-26_登録の効力を失う日.md, research/2026-08-28_市販会計ソフト比較_登録番号の呼び名.md]
---
# 適格請求書発行事業者の登録

> **[13_取引先設計](13_取引先設計.md) から 2026-09-13 に切り出した**（13 が行数の目安を大きく超えたため。
> 移したのは 13 の §3 と §4-1・§4-2 で、13 側には `削除` の見出しが残っている。
> **旧番号との対応**——3-1→1／3-2→2／3-2-1→2-1／3-3→3／3-4→4／3-5→5／4-1→6／4-2→7）。
> **切り出せるのは、登録が取引先そのものとは別の実体だから**である——**有効期間つきの別テーブル**で持ち、
> 独自の画面（§4）・関門（§7）・不変条件（§5）を持つ。
>
> **取引先部品の一部**であり、部品の構成の正典は [13 §0](13_取引先設計.md) である。
> 制度そのものの説明と出典は
> [取引先の識別番号と公表システム](research/2026-08-25_取引先の識別番号と公表システム.md) にある。
> 本書は**設計上の決定**だけを書く。

## 1. 有効期間つきの別テーブルで持つ

登録は**取消・失効・再登録**があり、1 列では「その時点でどうだったか」を表せない。
`partners` に列を足さず、`partner_invoice_registrations` を作る。

**弥生会計の本体は登録番号の入力欄そのものを持たず、摘要か仕訳メモに書くよう案内している**
（[調査](research/2026-08-28_市販会計ソフト比較_登録番号の呼び名.md)。**調べたのは弥生 1 社だけ**）。
**本プロジェクトは違う選択をする**——計上時に明細へ写す記録が要り
（[13 §4](13_取引先設計.md)・[ADR-0018](decisions/0018-帳簿の記載事項は計上時に写して固定する.md)）、
**摘要に書く運用では「どの登録に基づいて計上したか」が後から辿れない**からである。

**基準日は `journal_lines.tax_point`**（課税仕入れを行った日。無ければ取引日で代える——§6。返還等の専用の税区分の行は取引日に固定——[11 §5](11_消費税設計.md)）。計上日ではない。

## 2. 列（確定。2026-08-25）

公表システムの提供項目（[リサーチ §3-2](research/2026-08-25_取引先の識別番号と公表システム.md) で
リソース定義書の項目一覧を確認）に対応させて、次で確定した（`Designer/ddl/006_partner_registrations.sql`）。

| 列 | なぜ要るか・対応する提供項目 |
|---|---|
| `registration_no` | 登録番号（registratedNumber）。**書式は「T ＋ 数字 13 桁」（計 14 桁）で確認済み**（リサーチ §3-3）。DB は書式を検査せず、入力画面・取込を作るときに**アプリ側で検査する**（13 桁部分が法人番号と同一かは未確認のままなので、法人番号との突合には使わない） |
| `valid_from` | 登録年月日（registrationDate） |
| `ended_on` / `end_reason`（`expired` / `revoked`） | `expired` ＝ 失効年月日（expireDate）由来、`revoked` ＝ 取消年月日（disposalDate）由来。**日付 1 列＋理由 1 列**で持つ。公表システムの提供も**履歴 1 行につきいずれかの日付**なので、この形と一致する（リサーチ §5-6 で実質確認。2026-08-25）。**それぞれが指す法的事象——届出による取りやめ・職権・事業廃止など——の対応は未確認**。提供された日付をそのまま持ち、**「その日を含むか」は §6**（2026-09-19 に開発者が決めた——含めない。当初案の `valid_to` をやめたのは、±1 日の解釈をデータに焼き込まないため）。登録より前に終わる行は CHECK が拒む（**同日はいま許している。§6 の決定で有効期間が 0 日になるので、拒む側へ直す**——**いつ・どう直すかは保留リストが持つ**） |
| `source`（`manual` / `nta_api` / `nta_download`） | **手で入れた値を自動同期が黙って上書きしないため** |
| `confirmed_on` | この行を人が最後に確かめた日（監査用） |
| `nta_updated_on` | 公表システム側の更新年月日（updateDate）。差分取得の基準 |
| `published_name` | 氏名又は名称（name）の写し。**公表名とマスタ名はずれる**（略称で登録している等）。突合結果が残り、名寄せの手がかりにもなる。個人のダウンロード提供では削除される項目があるため（どの項目かは未確認）空がありうる |

**前後の空白は落として保存する**（`InvoiceRegistrationNumber.Normalize`。法人番号も同じ）。
貼り付けで紛れ込むだけで利用者の意図ではないからで、**落とさないと同じ番号が 2 通りの文字列で
保存されて突合が壊れる**。**小文字の `t` と全角数字は落とさずに拒む**——
そちらは利用者が入れたつもりのものだからである（[21 §0](21_画面の原則.md)）。

同じ登録の二重取込は `UNIQUE (partner_id, registration_no, valid_from)` が拒む
（**登録年月日そのものが誤りだった場合の訂正の配信のされ方は未確認**。取込の設計時に
実データで確かめる。リサーチ §5-7）。

### 2-1. 同じ登録番号を別の取引先が持つことは正常である（排除しない）

**開発者の判断**（2026-09-05）。**登録番号は人格に紐づくので、同じ法人を支店ごとに
別の取引先として登録すれば、登録番号は必ず同じになる。** 排除しえない。

- 実データがそうなっている——`T1234567890123` を `P001` と `S0001` が
  期間の重なる形で持っている（2026-09-05 の探索的テストで見つけた。[qa/04](qa/04_実機操作テスト/2026-09.md)）。
  この 2 件は法人番号も同じ（`8700110005901`）で、**同じ法人の別レコード**として筋が通っている
- **[13 §2-2](13_取引先設計.md) の法人番号と同じ扱いである。** 同じ値なら同一法人であって、束ねる操作は要らない
- `PartnerRegistrationSubmitGate` の検査がすべて取引先ごとなのは**正しい**。
  取引先をまたぐ重なりを止める関門は置かない

**ただしフェーズ 3 で数えるときに効く。** [11 §5](11_消費税設計.md) の経過措置の相手方別上限は
**「一の者ごと」**（名寄せ後。[ADR-0066](decisions/0066-経過措置の上限は行ごとの適用の指定と計上時の関門で守る.md) の決定 5）に掛かるので、**取引先レコード単位で数えると、支店ごとに分けた分だけ
上限が増えて控除が過大になる**。**名寄せで解決したあとの単位で数える**こと（[13 §2-2](13_取引先設計.md) の解決の順序）。

## 3. 登録状況は税額計算を支配しない——既定値・警告・計上時の写しに使う

**計算を支配するのは利用者が選んだ税区分である**（[ADR-0018](decisions/0018-帳簿の記載事項は計上時に写して固定する.md)）。
相手が適格請求書発行事業者でも、登録番号のない区分記載請求書等の保存があれば
**一律に経過措置が適用される**（選択制ではない。[消費税リサーチ §1-4](research/2026-08-23_消費税インボイス制度.md) の 3）。適用は書類の保存と帳簿への旨の記載で決まり、**アプリは書類を見ていない**（旨は「適用する」の欄そのもの——[11 §5](11_消費税設計.md)）。

登録状況の使い道は 3 つに限る（Claude の案を開発者が承認。2026-09-19。**理由——アプリは書類を見ておらず、制度が認める記帳を塞がないため**。[ADR-0066](decisions/0066-経過措置の上限は行ごとの適用の指定と計上時の関門で守る.md) の決定 7——2026-09-18 に一度「登録事業者の行には経過措置の『適用する』を立てられない」と関門にしたが、翌日に既定値と警告へ戻した）。

- **経過措置の「適用する」の既定値**（字は [11 §5](11_消費税設計.md)）
- **警告**——登録事業者の行に「適用する」を立てたとき、未登録の相手に全額控除の税区分を選んだとき。止めない（除外する類型は [11 §5-3](11_消費税設計.md)。**開発者の決定。2026-09-20**——[ADR-0066](decisions/0066-経過措置の上限は行ごとの適用の指定と計上時の関門で守る.md) の決定 13） 
- **計上時に明細へ写す記録**（[13 §4](13_取引先設計.md)。[ADR-0018](decisions/0018-帳簿の記載事項は計上時に写して固定する.md) の「判定の根拠の記録」）

**登録状況を見る基準日は §6**。

## 4. 手入力の画面（実装は 2026-08-26。呼び名と導線は 2026-08-31 に変えた）

**画面での呼び名**（開発者の決定。2026-08-29。**「登録番号」が法令（消税法 57 の 4 ①。
[消費税リサーチ §9](research/2026-08-23_消費税インボイス制度.md)）と国税庁の資料
（[リサーチ §3-2](research/2026-08-25_取引先の識別番号と公表システム.md)）の語だから**）。
**「インボイス番号」は使わない**（用例が見つからず、法令・国税庁の語ともずれるから）。

| 場所 | 呼び名 |
|---|---|
| メニュー・ページの題（**文脈が無い**） | **登録番号（インボイス）** |
| 取引先の画面の中のフィールド（**文脈がある**） | **登録番号** |

補い方の考え方は [21 §5](21_画面の原則.md)、用例の調査は
[市販会計ソフト比較 — 登録番号の呼び名](research/2026-08-28_市販会計ソフト比較_登録番号の呼び名.md)。

**このアプリは取引先に法人番号も持つ**（[13 §1-2](13_取引先設計.md)）。裸の「登録番号」を使うのは
**取引先の画面の中だけ**にとどめ、それ以外では必ず補う。

`PartnerInvoiceRegistration`。**最低限**である——
取引先・登録番号・登録年月日・取消・失効年月日・取消・失効の理由・公表名・最終確認日だけを持つ。

**入力は別画面で行い、取引先の詳細には確定した履歴だけを見せる**
（画面の骨格——「埋め込みの一覧を直接編集させ、編集したとたん本物になる仕様は無理がある。
編集は別画面で行い、『登録』の成功で戻り、失敗なら何も起こらない」——は**開発者の提案**（2026-08-31）。
「追加ボタン 1 つ ＋ 行ごとの編集リンク」の形は Claude の提案を**開発者が承認**（2026-09-02。
**履歴は行ごとに「終わり」をあとから記録する操作があり、対象の行を選ぶ入口が要るから**）。
それまでの埋め込み編集の経緯は git の本節の履歴にある）。

- **取引先の詳細には、閲覧専用の履歴一覧と「登録番号を追加する」ボタンを置く。**
  登録 → 取消 → 再登録という**履歴が複数行あることが、取引先の画面で見える**。
  一覧の**行ごとに編集リンク**を置く——失効の記録・公表名や確認日の更新・誤記の訂正は、
  その行を開いて行う
- **編集画面（`PartnerInvoiceRegistration` の詳細）では、取引先は表示のみ。**
  利用者が選ぶ欄は無い（新規は親から識別子を渡す）。**「登録」ボタンで確定**——
  関門に拒まれたらエラーを出して何も起こらない。成功したら取引先の詳細へ戻る
- **終わりの無い登録（「取消・失効年月日」が空の登録）がある取引先の追加画面には、ボタンの行の直前に案内を出す**——赤字の 1 文（どの日からの登録の「取消・失効年月日」が空か）と、
  箇条書き 3 項目（保存してある終わりの無い登録より後に始まる登録＝再登録は、先に保存してある登録の終わりを記録する／それより前に始まる登録は、入力している登録の終わりも入れる／
  「取消・失効年月日」には国税庁の公表サイトの取消年月日・失効年月日をそのまま入れる——**箇条書きに従って前の登録を閉じる人には関門の断りが出ない**ので、入れる日付は押す前に言う）。
  **「有効な登録」とは言わない**——アプリが知っているのは欄が空であることだけで、公表サイトとは照らしていない（下の「不存在を未登録と断定しない言い方」の逆向き）。
  **既存の行の編集画面には出さない。** 文言は `PartnerInvoiceRegistration.mod.json` の `NoticeGuideLabel` と、`.mod.cs` の `ShowOpenRegistrationNotice`（日付の入る 1 文）が持つ。
  **置き場を最下段にしたのも、ボタンの行の直前にしたのも Claude の当てはめ**である——[21 §2-8](21_画面の原則.md) の開発者の決定（最下段に箇条書き）は振替伝票の新規作成画面への答えで、
  他の画面へ広げるかは決まっていない（[05](05_開発者への問い.md) の Q-52 の「### ②」と「### ⑤」で諮っている）。
  **関門にしか無い規則を作らない**（[21 §1](21_画面の原則.md)）——終わりの無い登録のあとに足せないこと（R-I5）を、押す前に言う（2026-09-24）
- **既存の登録の取引先は変えられない**（関門 `RejectRepoint`。2026-08-31）。
  変えられると A 社の履歴に穴が空き、B 社に他人の登録番号が生え、
  **すでに計上済みの明細に焼き込まれた写し（[13 §4](13_取引先設計.md)）と食い違う**。
  関門は取引先を**型を問わず差分から読む**（API を直に叩く経路と取込（フェーズ 6）が同じ入口を通るため）
- **独立した一覧は「見るため」に残す**（取込の結果を一括で確かめるのに要る。フェーズ 6）。
  **この一覧はクエリモジュール（`PartnerRegistrationList`）である**——親 FK を `IdFieldDesign` に
  すると参照先の名前を出せなくなるため（[qa/01 D-17](qa/01_CLB静かな失敗.md)）。
  [ADR-0022](decisions/0022-明細を並べる帳簿はクエリモジュールで作る.md)・[ADR-0027](decisions/0027-入力の一覧はラップモジュールとクエリモジュールで作る.md) と同じ判断で、
  **一覧の中身が素の姿と違うならクエリモジュール**にする。取引先名は取引先の詳細へのリンクにし、
  **出所**（手入力か取込か）も列に出す
- **取引先と登録を同じ保存で新規作成する経路は塞いである**（2026-08-26 に先行して塞いだ）。
  取引先の識別子が仮のままでも、**送られてきた文字列のまま突き合わせる**ので二重登録を止められる
  （`PartnerRegistrationSubmitGate`）

- **出所（`source`）と公表システム側の更新年月日（`nta_updated_on`）は画面に出さない。**
  手入力の行は DB の既定（`source = 'manual'`）が入る。
  取込（フェーズ 6）を作るときに、区別を見せる必要が出てから足す
- **消せるのは、計上済みの明細が写していない行だけである**（規則。R-I8——[ADR-0063](decisions/0063-登録番号の行は計上済みの明細が写していない行だけ消せる.md)。**画面に消す手が付くのはフェーズ 6**で、いまは消せない）。
  取消・失効は「終わり」を記録して残すものであって、行ごと消すものではない

**行の不存在は「調べて未登録だった」と「まだ入力していない」を区別しない**（2026-08-25 の決定）。
区別する列（未登録と確認した日）は、警告の文言設計（フェーズ 3）で必要になったら考える。
それまで警告は「登録情報が入っていません」のように、**不存在を未登録と断定しない言い方**にする。

## 5. 登録の不変条件（確定。2026-09-02。R-I8 は 2026-09-16 に改めた）

**1 つの取引先の登録の履歴が守る規則。** 破ると、計上時の写し（[13 §4](13_取引先設計.md)）が「どれを写すか
決められない」で止まるか、誤った番号が永久に焼き込まれる。**テストの観点もこの表から導く。**

| # | 不変条件 | どこが守るか |
|---|---|---|
| R-I1 | 終わりには必ず理由がある（`ended_on` と `end_reason` は対） | DDL の CHECK ＋ 関門（言葉で断る） |
| R-I2 | 登録より前には終われない（`ended_on >= valid_from`。同日はいま許す——§2。§6 の決定で拒む側へ直す） | DDL の CHECK ＋ 関門。**CHECK は生の文字列比較のまま**（`date()` 包みへの作り直しはフェーズ 6 の取込設計と一緒に——保留リスト） |
| R-I3 | 同じ日から始まる登録は 1 件（qa/02 R26-20） | DDL の一意索引（`date()` 包み）＋関門 |
| R-I4 | **期間は重ならない。ただし隣接は許す**（下の定義） | 関門＋ DDL のトリガ（2026-09-02 に追加） |
| R-I5 | 終わりのない行（現に有効な登録）は**高々 1 件で、必ず最も新しい行**（R-I4 の帰結） | 同上 |
| R-I6 | 既存の行の取引先は変えられない（§4） | 関門 |
| R-I7 | 登録番号の書式は「T ＋ 数字 13 桁」（§7） | 関門 |
| R-I8 | **計上済みの明細が写している行は削除できない。** 取消・失効は「終わり」の記録で表す。**写していない行（帳簿のどこにも出ていない行。消す典型は誤入力——Claude の補足）は消せる**（**開発者の決定。2026-09-16**。[ADR-0063](decisions/0063-登録番号の行は計上済みの明細が写していない行だけ消せる.md)。公表システムの差分の**処理区分 99＝登録簿からの削除**（[リサーチ §3-3](research/2026-08-25_取引先の識別番号と公表システム.md)）をどう表すかはフェーズ 6 の論点——保留リスト） | **いまは関門が削除を全部止め、画面も `CanDelete = false`**（画面の形は守りではない——qa/01 F-24）。**条件つきの削除（関門 ＋ DDL のトリガ）はフェーズ 6 に作る**（Claude の予定。ADR-0063 の敷衍）——それまでは規則より狭い守りで、消せるはずの行も消せない |
| R-I9 | 新規の行の取引先は実在する（2026-09-02 追加） | 関門が言葉で断る。最後は DB の外部キー |

**R-I4 の定義**（登録年月日の早い行を A、遅い行を B とする）:

- A に終わりが無い（`ended_on` が NULL）のに B がある → **拒む**（有効な登録が続いているのに次が始まる）
- B の登録年月日が A の終わりより前 → **拒む**（真の重複）
- B の登録年月日が A の終わりと**同じ日 → 許す**（隣接。どちらの制度解釈でも決定的に引ける——下の説明。**再登録でこの形が起きうるかは調べていない**（[2026-08-26 リサーチ](research/2026-08-26_登録の効力を失う日.md) の「確認できなかったこと」））
- B の登録年月日が A の終わりより後 → 許す（間に未登録の期間がある）

**隣接を許すのは、いまのコード（`InvoiceRegistrationHistory.InEffectOn`）が「終わりの日を含み、同日に 2 件当たったら新しいほうを採る」としているから**である（§6 の決定で「含めない」に直すまでの経過的な扱い）。

**関門は「保存後にできあがる履歴」で判定し、DB（一意索引・トリガ）は文単位で判定する。**
このずれのため、**複数行の期間を 1 回の保存で同時に編集する操作は、最終状態が正しくても
文の順序によっては DB が途中状態で拒みうる**（SQLite に遅延制約は無い。2026-09-02 のレビュー指摘）。
**画面は 1 行ずつ保存するのでこの形は出ない。** 複数行を 1 保存で送る取込（フェーズ 6）の
設計時に、実際の保存の順序で確かめて倒し方を決める。
取消・失効の日を有効期間に「含むかどうか」の制度解釈は 2026-09-19 に決まり（含めない。§6）、コードの修正はフェーズ 3 なので、
隣接の 1 日はどちらの解釈でも決定的に引ける形にしてある。

**編集の許容**（保存後の行に対して）:

| 対象 | 可否 | 理由 |
|---|---|---|
| 取引先 | **不可** | R-I6 |
| 登録番号・登録年月日 | 可 | 誤記の訂正。**マスタを直しても計上済みの写しは動かない**（[ADR-0018](decisions/0018-帳簿の記載事項は計上時に写して固定する.md)）ので過去の帳簿は変わらない |
| 取消・失効年月日・理由 | 可（追記が本来の操作） | 登録の「終わり」は保存のあとに起きる出来事である |
| 公表名・最終確認日 | 可 | 公表システムで再確認するたび更新する |
| 行そのもの | **計上済みの明細が写していなければ可**（R-I8） | いまは消す手が無い（R-I8 の守りの欄）ので、それまでは誤って確定した余分な行を**内容を正しい行に編集して使う** |

## 6. 基準日をどう決めるか（実装。2026-08-26）

| 決めごと | どうするか | なぜ |
|---|---|---|
| 基準日 | `journal_lines.tax_point`。**入っていなければ伝票の取引日** | `tax_point` は「引き渡しが取引日と違う取引」のための上書きである。上書きが無いなら取引の日に行われたと読むのが素直。**いまの振替伝票の画面は `tax_point` を入力させない**ので、「無ければ写さない」にすると写しが永久に空になる（計上済みは不変なので後から埋め直せない。[qa/03 L-12](qa/03_テストで漏らした実例.md)） |
| 終わりの日そのもの | **含めない**——取消・失効年月日の当日から適格請求書発行事業者でなくなる（**開発者の決定。2026-09-19**。根拠は消税法 57 の 2 ⑩ 二（事業を廃止した日の翌日に効力を失う＝廃止した当日は有効）とインボイスQ&A 問 13（翌課税期間の初日に効力が失われる）。逐語は [登録の効力を失う日のリサーチ](research/2026-08-26_登録の効力を失う日.md)）。**いまのコード（`InvoiceRegistrationHistory.InEffectOn`）は含めている**（2026-08-26 に、根拠の記録であって判定ではないとして含める側に倒した）。**直すのはフェーズ 3で、経過措置の関門と一緒**（保留リスト）。§2 の「登録より前に終わる行は CHECK が拒む（同日は許している）」も、有効期間が 0 日になるので同日を拒む側へ直す。**留保が 2 つ**——開発者が示した条文と Q&A は失効（消税法 57 の 2 ⑩。**届出による取りやめは同項 一 の失効**で、インボイスQ&A 問 13 がそれを説明する——**開発者が引いた問 13 は令和 6 年 4 月改訂版で、現行版と同文かは未確認**）で、**税務署長による取消し（同 ⑥）の効力の日の条文は示されていない**（決定は取消・失効の両方を「当日から」とした）；**公表システムの取消年月日・失効年月日がその日そのものかは未確認**（フェーズ 6 の取込で実データを見る）。**直す前に計上した境界日の写しは残す**（根拠の記録であって判定ではない——[ADR-0018](decisions/0018-帳簿の記載事項は計上時に写して固定する.md)。同じ相手・同じ日で写しが 2 通りになりうるが、計上済みは直さない） |
| 同じ日に始まる登録が 2 件 | **止める**（計上させない） | どちらを写しても 5 割の確率で誤った番号が**永久に**残る。ただし**入力の時点で止める**のが本筋なので、登録の保存の関門が先に弾く（§4） |
| 前の登録が終わった日に次が始まる | **新しいほうを採る**（いまのコード。終わりの日を含めない実装に直せば境界日の候補は 1 件になり、この行は要らなくなる） | この形では境界日に候補が 2 件になる。**関門が許す形（R-I4 の隣接）なので、引くときに決めておく**（再登録で起きうるかは調べていない——§5） |
| 事後の変更（登録の行の追加・登録年月日の遡及） | **計上済みの行は遡らない** | 写しが根拠の記録だから（[13 §4](13_取引先設計.md)・[ADR-0018](decisions/0018-帳簿の記載事項は計上時に写して固定する.md)）。既定値と警告も計上時のデータで見る（[11 §5](11_消費税設計.md)。[ADR-0066](decisions/0066-経過措置の上限は行ごとの適用の指定と計上時の関門で守る.md) の決定 14） |

規則は `InvoiceRegistrationHistory.InEffectOn`（純粋関数）が 1 か所で持つ。
**SQL 側では絞り込まない**——境界の条件が 2 か所に分かれると、片方だけ直したときに
「画面では有効なのに写しが空」という形で静かにずれる。

## 7. 登録番号の書式は保存の関門が見る（実装。2026-08-26）

DB は書式を検査しない（§2 の決定）ので、**書式を守れるのは `PartnerRegistrationSubmitGate` だけ**である
（**止める範囲の正典は §5 の表**——この関門は R-I1〜R-I9 をすべて見る）。
この節が言うのは、そのうち **2 つをここで止める理由**である。

1. **書式**（「T」＋数字 13 桁。半角のみ・大文字のみ。R-I7）。壊れた番号を通すと、計上のときに
   そのまま明細へ焼き込まれ、計上済みは不変なので直せない
2. **同じ取引先に同じ日から始まる登録が 2 件できること**（R-I3）。§6 の表の 3 行目を、
   計上の場面ではなく**入力の場面で**止めるためである

**画面のスクリプトでは検査しない**（[ADR-0008](decisions/0008-CLBとCSharpライブラリの責務分担.md)）。
取込（フェーズ 6）も同じ入口を通るので、ここに置けば経路が増えても検査が外れない。

## 保留リスト

- 2026-09-24 **登録番号の行の削除の断りが、ADR-0063 と食い違って読める**——文は「登録の行は削除できません。取消・失効は、…その行の「編集」を開き、「取消・失効年月日」を入れて…」。
  関門はいま削除を全部断るので、文はいまの振る舞いとしては正しい。しかし規則を「行は消せない」と言い切って読め、**誤入力の行を消したい人を架空の取消・失効の記録へ誘う**（Claude の判断。[qa/02 のラウンド 154](qa/02_自己レビュー記録/2026-09.md)）。
  **保存の関門が複数の違反を 1 通の断り（「見出し（N 件）。①…②…」）にまとめて返すよう作り直す回**（[21 §2-6](21_画面の原則.md) の (b)。時期は [04 §3](04_実装計画と現在地.md) のフェーズ 3）**で直す**。
  **直す向きは §5 の「いまは内容を正しい行に編集して使う」まで**——消せる条件を言うのは、条件つき削除を作るフェーズ 6（[ADR-0063](decisions/0063-登録番号の行は計上済みの明細が写していない行だけ消せる.md) の帰結）
- 2026-09-24 **同じ保存で複数の行を送る経路（API・取込）では、R-I5 の断りの「入力している登録」で、どの行か区別できない**——2 行とも「入力している」に当たる（文には登録年月日が入るので、日付では指せている）。
  画面は 1 行ずつ保存する（§5）ので、画面からは起きない。
  **フェーズ 6 の取込設計と同じ回に、画面に依らない言い方（日付で指す）へ見直す**（`PartnerRegistrationSubmitGateTests` のテスト `同じ保存で足す二行の早い行に終わりが無ければ早い行を名指す` の注記）
- 2026-09-24 **API だけが出す付け替えの断り**（「登録の取引先は、保存したあとは変更できません。」）**の「取引先」を鉤括弧で括っていない**——同じ関門の画面から出る断りは括った（[21 §2-6](21_画面の原則.md)）。
  **上の、断りを 1 通にまとめる回で括る**

- 2026-09-16 **登録の行の条件つき削除を作る**（R-I8。[ADR-0063](decisions/0063-登録番号の行は計上済みの明細が写していない行だけ消せる.md)。**「写した」の見方もそのとき決める**——同 ADR の敷衍）。
  **フェーズ 6 の取込設計と同じ回**
- 2026-09-02 **公表システムの処理区分 99（登録簿からの削除）をどう表すか**。R-I8（写された行の削除禁止）は
  手入力に対する確定で、取込が「削除」の差分を受けたときの形（第 3 の終わり理由か・別扱いか・条件つき削除か）は
  未決（レビュー指摘）。**フェーズ 6 の取込設計のとき**（上の条件つき削除と同じ回）
- 2026-09-02 **R-I2 の CHECK を `date()` で包む作り直し**。時刻付きと素の日付が混ざると
  「同日に終わる行」が誤って拒まれる（qa/03 L-12 と同型。関門は正しく判定する）。
  テーブルの作り直しが要るので、**フェーズ 6 の取込設計と同じ回で**
- 2026-08-25 公表システムからの取込（フェーズ 6）の前に残る未確認は
  **登録年月日の訂正の配信のされ方**と**アプリケーション ID の申請要件**
  （[リサーチ §5](research/2026-08-25_取引先の識別番号と公表システム.md)）。**フェーズ 6 の着手前**
- 2026-09-18 **登録の行を後から足す・登録年月日を遡らせるとき、遡らせた期間に経過措置の「適用する」の計上済みの行があれば警告するか**（[ADR-0066](decisions/0066-経過措置の上限は行ごとの適用の指定と計上時の関門で守る.md) の当てはめ 2。判定は計上時のデータで、過去の行は遡らない）。**フェーズ 3 で経過措置の関門を作る回**
- 2026-08-26 **振替伝票の画面に `tax_point`（課税仕入れの時点）を持たせるか。**
  いまは空なので取引日で代替している（§6）。支払日で起票した未払金の決済や
  締め日基準の一括計上では、取引日と課税仕入れの日がずれる（[11 §5](11_消費税設計.md)）。
  **返還等の行では使わない**（[11 §5](11_消費税設計.md)）。残る場面は締め日一括計上だけで、ペルソナには無いと見ている。
  **`tax_point` という名前を「課税仕入れの日」に当たる名前に改めるか**（開発者の指摘。2026-09-19。「あまりよい名前に思えません」。列・C# の型・デザイン JSON・DDL の移行を伴う）も同じ回。
  **フェーズ 3 で経過措置の判定を作るときに一緒に決める**（自己レビュー R10-03）
- 2026-08-30 **`IsUpdateProtected` が API 経由の更新も止めるか。**
  実機で確かめたのは画面からの保存だけである（[qa/01 F-26](qa/01_CLB静かな失敗.md)）。
  いま登録を書く経路は画面しか無いので確かめようがない。**取込を作るフェーズ 6** に、その経路で確かめる
- 2026-09-20 **§2 の CHECK が同日を許しているのを、拒む側へ直す形は開発者が承認した**（旧 Q-46）。**直す作業そのものは下の行が持つ**（2026-09-19 の決定の一部）
- 2026-09-19 **登録の終わりの日を「含めない」に直す**（開発者の決定。§6）。`InvoiceRegistrationHistory.InEffectOn` の `date <= ended` を `date < ended` に、§2 の CHECK の同日許容を拒む側に、テストと実機（[qa/04](qa/04_実機操作テスト.md) の登録の台本）をやり直す。
  **§3 の既定値・警告と計上時の写しもこの日で動く**（[ADR-0066](decisions/0066-経過措置の上限は行ごとの適用の指定と計上時の関門で守る.md) の当てはめ 2）。`006_partner_registrations.sql` の「隣接を許す」注記の理由（終わりの日を含むから）も改める。**フェーズ 3 で経過措置の関門を作る回**
- 2026-08-26 **公表システムの取消年月日・失効年月日が、消税法 57 の 2 ⑥・⑩ の「効力を失う日」そのものか**（制度の解釈は 2026-09-19 に決めた——§6。残るのは実データとの対応）。
  **実データでの裏取りはフェーズ 6**（公表システムの取込で、取消・失効のある事業者を実際に引く。
  [リサーチ](research/2026-08-26_登録の効力を失う日.md)）
